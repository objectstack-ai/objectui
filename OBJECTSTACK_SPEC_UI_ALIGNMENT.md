# ObjectStack Spec UI Alignment Analysis

**文档版本**: 1.0  
**创建日期**: 2026-02-03  
**目标**: 评估 ObjectUI 与 @objectstack/spec v0.9.0 UI 协议的对齐现状，制定服务端上线开发计划

---

## 📊 执行摘要 (Executive Summary)

### 当前状态
- ✅ **基础协议对接**: ObjectUI 已实现 @objectstack/spec 核心 UI 类型的集成
- ⚠️ **版本差异**: Console 声明支持 v0.8.2，当前 spec 版本为 v0.9.0
- ✅ **数据适配器**: ObjectStackAdapter 提供完整的 CRUD 和元数据支持
- ⏳ **高级功能**: 条件显示、高级验证等功能在规划中但未实现

### 就绪程度评估
| 维度 | 状态 | 完成度 | 关键缺口 |
|------|------|--------|----------|
| **类型系统** | 🟢 良好 | 90% | 部分高级字段类型缺失 |
| **数据连接** | 🟢 良好 | 85% | 需要更强的错误恢复机制 |
| **表单渲染** | 🟡 中等 | 70% | 条件显示、跨字段验证未实现 |
| **网格视图** | 🟢 良好 | 80% | 高级过滤器需要增强 |
| **导航系统** | 🟢 良好 | 85% | 动态权限检查待完善 |
| **生产部署** | 🟡 中等 | 60% | 缺少完整的部署文档 |

**总体评估**: ObjectUI 已具备服务端上线的**核心能力**，需要补充**高级功能**和**生产优化**。

---

## 🏗️ 软件包现状详细分析

### 1. 核心软件包 (Core Packages)

#### 1.1 @object-ui/types (v0.3.1)
**职责**: 纯 TypeScript 类型定义 - 协议层

**当前状态**:
- ✅ 依赖 `@objectstack/spec: ^0.9.0`
- ✅ 提供 90+ 组件的完整 TypeScript 类型
- ✅ 导出 Zod 验证模式 (运行时校验)
- ✅ 遵循 "Protocol First" 设计原则

**与 Spec 对齐**:
```typescript
// 继承链: @objectstack/spec → BaseSchema → 具体组件
import { UIComponent } from '@objectstack/spec/ui';

interface BaseSchema extends UIComponent {
  // ObjectUI 扩展属性
  visibleOn?: string;    // ⏳ 类型已定义，渲染未实现
  hiddenOn?: string;     // ⏳ 类型已定义，渲染未实现
  className?: string;    // ✅ 已实现 (Tailwind)
}
```

**缺口**:
- ⏳ 部分高级字段类型 (Vector, Grid, Formula) 类型定义存在但无实现
- ⏳ 跨字段验证规则类型定义不完整

---

#### 1.2 @object-ui/core (v0.3.1)
**职责**: 核心逻辑、验证、注册表、表达式求值

**当前状态**:
- ✅ 依赖 `@objectstack/spec: ^0.9.0`
- ✅ 实现表达式系统 (用于 `${data.field}` 引用)
- ✅ 组件注册表支持命名空间
- ✅ 基于 Zod 的模式验证

**与 Spec 对齐**:
```typescript
// 表达式求值引擎
evaluateExpression("${user.age > 18}", context);  // ✅ 已实现

// 条件显示逻辑
evaluateCondition(schema.visibleOn, context);     // ⏳ 待实现
```

**缺口**:
- ❌ 条件显示逻辑 (`visibleOn`, `hiddenOn`) 未实现
- ❌ 跨字段依赖 (`dependsOn`) 未实现
- ⏳ 高级验证规则 (async validation, cross-field) 部分实现

---

#### 1.3 @object-ui/react (v0.3.1)
**职责**: React 绑定和 SchemaRenderer

**当前状态**:
- ✅ 核心 `<SchemaRenderer>` 组件
- ✅ FormRenderer 直接使用 `FormView` from `@objectstack/spec/ui`
- ✅ FieldFactory 支持 `FormField` 规范
- ✅ 支持数据源 (DataSource) 接口

**与 Spec 对齐**:
```typescript
import { FormView, FormField } from '@objectstack/spec/ui';

// ✅ FormRenderer 完全遵循 spec
<FormRenderer schema={formViewSchema} data={data} />

// ✅ 字段渲染器工厂
<FieldFactory field={formField} value={value} onChange={onChange} />
```

**缺口**:
- ⏳ 部分字段类型渲染器缺失 (RichText WYSIWYG, Grid Editor)
- ⏳ 条件字段显示需要在渲染层实现

---

### 2. UI 组件软件包

#### 2.1 @object-ui/components (v0.3.1)
**职责**: 标准 UI 组件库 (Shadcn + Tailwind)

**当前状态**:
- ✅ 50+ 生产级组件 (Button, Input, Card, Table 等)
- ✅ Tailwind CSS 原生支持
- ✅ 亮暗主题切换
- ✅ WCAG 2.1 AA 无障碍标准

**缺口**:
- ⏳ 部分高级组件待完善 (RichTextEditor, MapPicker)

---

#### 2.2 @object-ui/fields (v0.3.1)
**职责**: 字段渲染器和注册表

**当前状态**:
- ✅ 基础字段类型完整 (text, number, select, checkbox 等)
- ✅ 惰性字段注册 (按需加载，减少包体积)
- ✅ 支持 `FieldWidgetProps` 接口

**与 Spec 对齐**:
```typescript
// 字段类型映射到 spec
const fieldTypeMap = {
  'text': TextFieldWidget,      // ✅ spec.FieldType.Text
  'number': NumberFieldWidget,  // ✅ spec.FieldType.Number
  'select': SelectFieldWidget,  // ✅ spec.FieldType.Select
  // ...
};
```

**缺口**:
- ⏳ 高级 Select 功能 (异步搜索、自定义渲染)
- ⏳ 文件上传预览和进度条
- ⏳ 富文本编辑器 (当前仅 textarea)
- ⏳ 位置字段地图可视化

---

#### 2.3 @object-ui/layout (v0.1.1)
**职责**: 页面结构组件 (Header, Sidebar, AppShell)

**当前状态**:
- ✅ 集成 React Router
- ✅ 响应式布局
- ✅ 支持嵌套导航

**缺口**:
- 文档较少 (无独立 README)

---

### 3. 插件软件包 (Plugin Packages)

| 插件 | 版本 | 状态 | 与 Spec 对齐 | 关键依赖 |
|------|------|------|--------------|----------|
| **plugin-view** | 0.3.1 | ✅ 核心 | ObjectQL 视图集成 | @objectstack/objectql |
| **plugin-grid** | 0.3.1 | ✅ 稳定 | 数据网格 CRUD | TanStack Table |
| **plugin-form** | 0.3.1 | ✅ 稳定 | 表单渲染 | React Hook Form |
| **plugin-kanban** | 0.3.1 | ✅ 稳定 | 看板视图 | dnd-kit |
| **plugin-aggrid** | 0.4.1 | ✅ 高级 | 企业级网格 | AG Grid (150KB) |
| **plugin-charts** | 0.3.1 | ✅ 稳定 | 图表可视化 | Recharts |
| **plugin-editor** | 0.3.1 | ✅ 高级 | 代码编辑器 | Monaco (120KB) |
| **plugin-calendar** | 0.3.1 | ✅ 稳定 | 日历视图 | - |
| **plugin-dashboard** | 0.1.1 | ⏳ Beta | 仪表板布局 | - |
| **plugin-markdown** | 0.3.1 | ✅ 稳定 | Markdown 渲染 | react-markdown |
| **plugin-timeline** | 0.3.1 | ✅ 稳定 | 时间线 | - |
| **plugin-gantt** | 0.3.1 | ✅ 稳定 | 甘特图 | - |
| **plugin-map** | 0.3.1 | ⏳ 基础 | 地图可视化 | Leaflet (需完善) |
| **plugin-chatbot** | 0.3.1 | ✅ 稳定 | 聊天界面 | - |

**总体状态**: 插件生态丰富，大部分处于稳定状态，支持企业级应用场景。

---

### 4. 数据适配器

#### 4.1 @object-ui/data-objectstack (v0.3.1)
**职责**: ObjectStack 服务端数据适配器

**当前状态**:
- ✅ 完整 CRUD 操作 (find, findOne, create, update, delete)
- ✅ 批量操作 (bulk create/update/delete)
- ✅ 元数据获取 (getObjectSchema)
- ✅ LRU 缓存 + TTL 过期机制
- ✅ 全面错误处理 (6种错误类型)

**与 Spec 对齐**:
```typescript
// ✅ 实现 @objectstack/client 的完整封装
import { createObjectStackAdapter } from '@object-ui/data-objectstack';

const dataSource = createObjectStackAdapter({
  baseUrl: 'https://api.example.com',
  token: 'auth-token'
});

// ✅ 查询参数转换
const result = await dataSource.find('users', {
  filter: { age: { $gt: 18 } },     // ✅ ObjectQL 过滤器
  sort: { createdAt: -1 },          // ✅ 排序
  skip: 0,
  limit: 20
});
```

**缺口**:
- ⏳ 连接状态监控和自动重连
- ⏳ 批量操作进度事件
- ⏳ 高级过滤器转换 (joins, subqueries)

---

### 5. 应用层

#### 5.1 @object-ui/console (v0.1.0)
**职责**: ObjectStack 控制台 - 标准运行时 UI

**当前状态**:
- ⚠️ 声明支持 **ObjectStack Spec v0.8.2** (需升级到 v0.9.0)
- ✅ 多应用支持 (App switching)
- ✅ 动态 UI 渲染 (Dashboard, Grid, Form)
- ✅ 权限感知导航
- ✅ 应用品牌定制 (logo, colors)

**与 Spec 对齐**:
```typescript
// ✅ AppSchema 支持
import { App } from '@objectstack/spec/ui';

const app = App.create({
  name: 'my_app',
  label: 'My App',
  icon: 'users',
  branding: {
    logo: '/logo.png',
    primaryColor: '#10B981'
  },
  navigation: [...]  // ✅ 支持 object/dashboard/page/url/group
});
```

**缺口**:
- ❌ 版本落后 (v0.8.2 → v0.9.0)
- ⏳ Dashboard 导航类型为 "planned"
- ⏳ Page 导航类型为 "planned"
- ⏳ 动态权限检查需要完善

---

#### 5.2 示例应用 (Examples)

| 示例 | 版本 | 用途 | 集成程度 |
|------|------|------|----------|
| **example-todo** | 0.6.1 | Todo 应用 | ✅ 完整 objectstack.config.ts |
| **example-crm** | 0.1.0 | CRM 应用 | ✅ 完整 metadata + 自定义服务器 |
| **example-msw-todo** | 0.6.1 | MSW Mock 集成 | ✅ 客户端 CRUD + MSW |
| **example-kitchen-sink** | 0.1.0 | 组件展示 | ✅ 所有 UI 能力演示 |

**价值**: 提供清晰的参考实现，展示 ObjectUI 与 ObjectStack 的集成模式。

---

### 6. 工具链

#### 6.1 @object-ui/cli (v0.3.1)
- ✅ 从 JSON schema 构建应用
- ✅ 开发服务器
- ✅ 项目脚手架

#### 6.2 @object-ui/runner (v0.3.1)
- ✅ 通用应用运行器
- ✅ Schema 测试工具

#### 6.3 @object-ui/create-plugin (v0.3.1)
- ✅ 插件脚手架工具

#### 6.4 object-ui (VSCode Extension, v0.3.1)
- ✅ IntelliSense
- ✅ Schema 验证
- ✅ 实时预览

---

## 🔍 与 @objectstack/spec v0.9.0 UI 协议对齐详情

### UI 类型继承链

```
@objectstack/spec/ui (v0.9.0)
├── UIComponent (基础接口)
│   ├── type: string
│   ├── id?: string
│   ├── className?: string
│   └── ...
├── FormView
│   ├── fields: FormField[]
│   ├── sections?: FormSection[]
│   └── layout?: 'vertical' | 'horizontal'
├── FormField
│   ├── name: string
│   ├── label?: string
│   ├── type: FieldType
│   ├── required?: boolean
│   ├── validation?: ValidationRule[]
│   └── ...
└── App
    ├── name: string
    ├── label: string
    ├── icon?: string
    ├── navigation: NavigationItem[]
    └── branding?: AppBranding

ObjectUI Extensions (@object-ui/types)
├── BaseSchema extends UIComponent
│   ├── visibleOn?: string        // ⏳ 条件显示表达式
│   ├── hiddenOn?: string         // ⏳ 条件隐藏表达式
│   ├── dependsOn?: string[]      // ⏳ 字段依赖
│   └── className: string         // ✅ Tailwind classes
└── ...50+ 具体组件
```

### 已实现的 Spec 功能

| Spec 功能 | ObjectUI 实现 | 使用位置 | 状态 |
|-----------|---------------|----------|------|
| **FormView** | FormRenderer | @object-ui/react | ✅ 完整 |
| **FormField** | FieldFactory | @object-ui/fields | ✅ 完整 |
| **FormSection** | FormRenderer sections | @object-ui/react | ✅ 完整 |
| **App Schema** | Console app config | @object-ui/console | ✅ 完整 |
| **NavigationItem** | Navigation rendering | @object-ui/layout | ✅ 完整 |
| **AppBranding** | Theme customization | @object-ui/console | ✅ 完整 |
| **FieldType (basic)** | Field widgets | @object-ui/fields | ✅ 完整 |
| **FieldType (advanced)** | Advanced widgets | @object-ui/fields | ⏳ 部分 |
| **ValidationRule** | Form validation | @object-ui/react | ⏳ 部分 |

### 未实现或部分实现的 Spec 功能

| Spec 功能 | 计划状态 | 缺失原因 | 优先级 |
|-----------|----------|----------|--------|
| **条件显示** (`visibleOn`, `hiddenOn`) | 📋 Roadmap | 需要表达式引擎集成 | 🔴 高 |
| **字段依赖** (`dependsOn`) | 📋 Roadmap | 跨字段通信机制 | 🔴 高 |
| **异步验证** | ⏳ 部分 | 需要后端集成 | 🟡 中 |
| **跨字段验证** | ❌ 未实现 | 验证器架构限制 | 🟡 中 |
| **高级 Select** (搜索/异步) | ⏳ 部分 | 组件功能不完整 | 🟡 中 |
| **富文本编辑** (WYSIWYG) | ⏳ 计划 | 仅有 textarea | 🟢 低 |
| **文件预览** | ⏳ 计划 | 仅有基础上传 | 🟢 低 |
| **地图可视化** | ⏳ 计划 | 仅有坐标输入 | 🟢 低 |
| **Grid 编辑器** | ❌ 占位符 | 复杂度高 | 🟢 低 |

---

## 🚀 服务端上线关键路径

### 关键能力就绪评估

#### ✅ 已就绪
1. **类型系统**: 完整的 TypeScript + Zod 验证
2. **数据连接**: ObjectStackAdapter 提供 CRUD + 元数据
3. **UI 渲染**: 50+ 组件覆盖主要场景
4. **路由导航**: React Router 集成完整
5. **主题系统**: 亮暗模式 + 品牌定制
6. **插件生态**: 14+ 插件支持高级场景

#### ⏳ 需要补充
1. **条件逻辑**: `visibleOn`/`hiddenOn`/`dependsOn` 实现
2. **高级验证**: 异步验证、跨字段验证
3. **错误恢复**: 连接监控、自动重连
4. **生产优化**: 性能监控、日志、部署文档

#### ❌ 缺失但非阻塞
1. 高级字段组件 (富文本 WYSIWYG、地图可视化)
2. Dashboard/Page 导航类型 (Console 标记为 planned)
3. Grid 编辑器 (占位符状态)

---

## 📈 开发计划时间表

### 第一阶段: 协议对齐 (1-2 周)
**目标**: 补齐核心 Spec 功能缺口

1. **Console 升级 v0.8.2 → v0.9.0** (2天)
   - 审查 spec 变更日志
   - 更新依赖和类型
   - 回归测试

2. **实现条件显示逻辑** (3天)
   - `visibleOn`/`hiddenOn` 表达式求值
   - FieldFactory 集成
   - FormRenderer 集成
   - 单元测试 + 示例

3. **实现字段依赖** (`dependsOn`) (2天)
   - 字段变更事件系统
   - 依赖链解析
   - 测试用例

4. **增强验证系统** (3天)
   - 异步验证支持
   - 跨字段验证规则
   - 错误消息优化

### 第二阶段: 服务端集成强化 (1-2 周)
**目标**: 提升生产环境稳定性

1. **错误处理增强** (2天)
   - 连接状态监控
   - 自动重连机制
   - 友好错误提示

2. **高级查询支持** (3天)
   - 复杂过滤器转换 (joins, subqueries)
   - 排序和分页优化
   - 查询缓存策略

3. **批量操作优化** (2天)
   - 进度事件反馈
   - 部分失败处理
   - 性能优化

4. **元数据管理** (2天)
   - Schema 版本控制
   - 缓存失效策略
   - 元数据更新通知

### 第三阶段: 生产部署准备 (1 周)
**目标**: 支持生产环境上线

1. **构建优化** (2天)
   - Tree shaking 优化
   - 代码分割策略
   - Bundle 体积分析

2. **配置管理** (1天)
   - 环境变量方案
   - 多环境配置
   - 密钥管理

3. **监控与日志** (2天)
   - 性能埋点
   - 错误追踪
   - 用户行为分析

4. **部署文档** (2天)
   - Docker 镜像
   - Kubernetes 配置
   - CI/CD 流程

### 第四阶段: 测试与验证 (1 周)
**目标**: 确保质量和稳定性

1. **集成测试** (2天)
   - 服务端场景覆盖
   - 错误场景测试

2. **端到端测试** (2天)
   - 关键用户流程
   - 浏览器兼容性

3. **性能测试** (1天)
   - 负载测试
   - 基准测试

4. **安全扫描** (1天)
   - 依赖漏洞检查
   - CodeQL 扫描

5. **UAT 准备** (1天)
   - 测试数据准备
   - 用户手册

---

## 📝 下一步行动项 (Immediate Action Items)

### 高优先级 (本周完成)
1. ✅ **完成本文档** - 建立现状共识
2. 🔴 **Console v0.9.0 升级** - 消除版本差异
3. 🔴 **条件显示 POC** - 验证技术方案可行性
4. 🔴 **部署文档初稿** - 明确上线流程

### 中优先级 (下周完成)
1. 🟡 **集成测试补充** - 覆盖服务端场景
2. 🟡 **错误处理增强** - 提升用户体验
3. 🟡 **性能基准建立** - 监控质量目标

### 低优先级 (月内完成)
1. 🟢 **高级字段组件** - 提升组件完整性
2. 🟢 **Dashboard 导航** - 解除 "planned" 状态
3. 🟢 **监控埋点** - 生产环境观测

---

## 📚 参考文档

1. **@objectstack/spec v0.9.0** - https://github.com/objectstack-ai/spec
2. **ObjectUI 主文档** - [/README.md](./README.md)
3. **贡献指南** - [/CONTRIBUTING.md](./CONTRIBUTING.md)
4. **Types 包文档** - [/packages/types/README.md](./packages/types/README.md)
5. **Data Adapter 文档** - [/packages/data-objectstack/README.md](./packages/data-objectstack/README.md)
6. **Console 文档** - [/apps/console/README.md](./apps/console/README.md)

---

## 🤝 团队协作

### 角色分工建议
- **前端工程师**: 条件显示逻辑、高级字段组件
- **后端工程师**: 服务端集成、错误处理、部署配置
- **测试工程师**: 集成测试、E2E 测试、性能测试
- **DevOps 工程师**: CI/CD、监控、部署流程
- **技术写作**: API 文档、部署指南、用户手册

---

**文档维护**: 请在每个阶段完成后更新本文档状态，保持与实际进度同步。
