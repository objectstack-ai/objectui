{"id":5334952872,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T22:37:25Z","updated_at":"2026-08-18T22:37:25Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5334952872","body":"Claim: PM loop round 6\nSession: `session_01RV6yuVCxymHYE16PL9vQkE`\nBranch: `claude/issue-5233-persist-view-patch-only`\nWorktree: `objectui-issue-5233`\nDomain: `repo:objectui` (execution seat)\nFile surface: `packages/data-objectstack/src/index.ts` (the `persistViewPatch` write site and its read counterpart), plus tests in that package. ⛔ OUT: `packages/app-shell/src/views/metadata-admin/**` (#4266 dispatched this round), `packages/plugin-dashboard`, `packages/i18n`.\nContainer &amp; model: `S`, `mode:subagent`, `model: opus` — the write fix is one spread; the existing-rows call is the judgement.\nClause-②: no — it **narrows** what gets persisted. Nothing about the accept set or the public surface moves.\nSerial constraints cleared: `packages/data-objectstack` is free — nothing open touches it. ⚠️ **#5232 is the sibling from the same ruling and shares this file surface.** I am **serializing, not combining**: its work is console wording/UX plus a permission-gated write path — same file, *different fix pattern*, so it fails the family-batch test. It is not dispatched; do not reach for it.\n\n## Zone 1 — ruled, and the ruling is unusually specific\n\nMaintainer `huangyiirene`, 2026-08-12, objectstack#7494 comment `5261754173`, verbatim:\n\n&gt; **`persistViewPatch` 只存 patch,不存 merged base** —— 独立小修,现在做:它把写入时的有效 filter 冻进 overlay,导致源视图后续的 filter 变更到不了带 overlay 的用户,这与 per-user 之争无关,是纯粹的存储形状错误。\n\nTwo things the ruling settles for you:\n\n1. **The fix is \"store the patch only\"** — not \"merge more carefully\", not \"version the overlay\".\n2. **It is independent of the per-user/org-wide dispute.** The PM analysis on that thread confirms it is a bug under *either* scoping contract. ⛔ Do not let #5232's org-wide question enter this card.\n\n## The defect\n\n`persistViewPatch` writes `{...baseViewDef, ...patch}`, so an overlay written by a mere **sort or columnState change** copies the view's *current effective filter* — and every other base key — into the overlay verbatim. The filter is then pinned as-of-write: **a later change to the source view's filter never reaches users whose overlay carries the frozen copy.**\n\nThe user-visible shape: an admin edits a view's filter, and the people who once reordered a column silently keep the old filter forever, with nothing anywhere reporting it.\n\n⛔ **Re-verify the write site on current `main` before coding.** The card says so explicitly — PR#4713 touched this file, so line numbers and shape may have moved. Take anchors from the tree.\n\n## The one real judgement — and it is yours, with a floor\n\nExisting overlay rows **already carry frozen base keys**. Three dispositions, and the card names the one that is out:\n\n- **Strip on next write** — simple; stale rows persist until touched.\n- **Migrate** — clean end state; needs a migration path and a way to run it.\n- **Tolerate on read** — cheap and immediately effective for every existing row, *if* the read applies only the keys a patch legitimately owns.\n\n⛔ **Silent tolerance is the shape to avoid** — that is the card's wording and it is the floor. Note it forbids *silent* tolerance, not tolerance: an explicit, documented, tested read-side narrowing is a legitimate choice. What is not legitimate is a fix that leaves existing rows quietly overriding the source view with no test and no note.\n\n**State which you chose and why in the PR body**, with the consequence for rows written before the fix. If you find a fourth option, argue it.\n\n## Tests\n\n- **The defect pin**: write an overlay via a sort/columnState change, then change the **source view's** filter, and the overlay user sees the **new** filter. This must fail against pre-fix source — it is the whole card.\n- The overlay still carries what it legitimately owns (the sort / columnState it was written for).\n- **Whichever disposition you pick for existing rows, pin it**: a row written in the old shape must behave the way you decided, and the test must say which decision it encodes.\n- Reverse-verify: restore the write site, keep the tests, predict the reds **before** running, report predicted vs observed.\n\n## Gates\n\nDerive from your diff. `scripts/pm/dispatch-gates.mjs` is **objectstack-only** — here derive from `.github/workflows` and `package.json`. Changeset required (`@object-ui/data-objectstack`); `patch` or `minor`, ⛔ never `major`.\n\n⛔ **Do not background a long check and end your turn.** Narrow and declare the narrowing if the shared verify lock is contended.\n\n## Deliver\n\nDraft PR against `main`, first body line `Fixes #5233`. Push the branch early. Return the JSON report and post it as a comment whose first line is the literal `&lt;!-- os-dev-report --&gt;` marker.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335426552,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:21:38Z","updated_at":"2026-08-18T23:21:38Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5335426552","body":"Claim amendment (same claim, session `session_01RV6yuVCxymHYE16PL9vQkE`, branch `claude/issue-5233-persist-view-patch-only`) — **file surface widened, declared before the PR**:\n\n- `packages/data-objectstack/src/index.ts` + tests in that package (as dispatched), **plus**\n- `packages/app-shell/src/views/ObjectView.tsx` and a new test beside it.\n\nReason: the card and the dispatch both place `persistViewPatch` in `packages/data-objectstack/src/index.ts`. On current `main` it lives in `packages/app-shell/src/views/ObjectView.tsx` (the `useCallback` at ~L698; the `{ ...baseViewDef, ...patch }` write at ~L727), and so does its read counterpart (`sanitizeViewOverride` / `loadViewOverrides`, the seam where an override is merged over the source view). The adapter is what persists the body verbatim. The defect reproduces exactly as the card describes — only the file attribution was off.\n\nStill OUT and untouched, as dispatched: `packages/app-shell/src/views/metadata-admin/**`, `packages/plugin-dashboard`, `packages/i18n`. Nothing in this claim reaches #5232's surface (console wording / permission-gated write path).\n\n_Generated by [Claude Code](https://claude.ai/code)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335431597,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:22:23Z","updated_at":"2026-08-18T23:22:23Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5335431597","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 5233,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-5233-persist-view-patch-only\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5272\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The defect is real and reproduces as described; one attribution correction: persistViewPatch and its read counterpart live in packages/app-shell/src/views/ObjectView.tsx on current main, not in data-objectstack (file surface amended in a claim comment before the PR). SHIPPED (complete, tested): the read half - a personalization overlay row now contributes only the five keys it owns (rowHeight, sort, hiddenFields, columnState, inlineEdit) via narrowPersonalizationOverlay + VIEW_OVERLAY_OWNED_KEYS, new exports from data-objectstack, applied in sanitizeViewOverride, the one seam both loadViewOverrides branches pass through. Every base key already frozen into every stored row stops shadowing the source view on the next read: no migration, no rewrite at rest. Existing-rows disposition = tolerate on read, chosen deliberately, documented and pinned (the card forbids SILENT tolerance, not tolerance). NOT SHIPPED: the ruled write-at-rest change - measured, the platform refuses it (see open_questions), so I did not guess. PR body is 'Part of #5233', not 'Fixes' (the dispatch said Fixes): a merge must not close the card while the ruled half is undelivered - the standing clause overrides the dispatch here and I am flagging the conflict as instructed.\",\n  \"tests\": \"All at HEAD 670380763 unless noted. (1) pnpm exec vitest run packages/data-objectstack/ -> 'Test Files 41 passed (41) / Tests 556 passed (556)'. (2) pnpm exec vitest run packages/app-shell/ -> 'Test Files 447 passed (447) / Tests 4313 passed | 1 skipped (4314)' at f0b6bf59a; the only later change is 5 lines inside one data-objectstack test file, so no app-shell input moved. (3) The two new files verbose -> 16 passed. (4) type-check + lint for both packages via --filter: both script names echoed ('packages/app-shell type-check$ tsc --noEmit && tsc -p tsconfig.test.json ... Done'), so neither was a zero-match silent pass. (5) check:control-bytes, check:phantom-deps, check:self-import, check:spec-symbols, check:esm-specifiers, check-changeset-no-major.mjs, check-changeset-fixed.mjs -> all pass. REVERSE VERIFICATION, two ablation legs, predictions recorded first: leg A (wiring removed from sanitizeViewOverride) predicted 4 red in the app-shell pin / 8 green in the adapter pin -> observed exactly that, reds showing the frozen filter value 'open' where 'qualified' was expected; leg B (narrowPersonalizationOverlay made inert) predicted 4 red / 4 green in the adapter pin -> observed 3 red / 5 green. The miss was real and useful: 'omits an owned key the row does not carry' used a fixture whose key set was already the narrowed one, so it could not fail; fixture strengthened (commit 670380763) and leg B re-run gives the predicted 4 red / 4 green. NO REBUILD NEEDED FOR EITHER LEG and none could hide a stale dist: the root vitest.config.mts aliases @object-ui/data-objectstack to packages/data-objectstack/src, so tests resolve source - and the ablations turning red is itself the proof of that resolution. CI: in_progress at report time (draft PR just opened).\",\n  \"open_questions\": [\n    {\n      \"question\": \"The ruling's own shape ('store the patch only') is REFUSED by the platform for a columnState-only patch, which is the most common toolbar write (a column resize/reorder travels alone). Measured with @objectstack/spec 17.0.0 as installed, and columnState appears nowhere in the framework's packages/spec/src either: ViewMetadataSchema.safeParse({columnState, object, name, viewKind, _isOverride}) -> REFUSE, 'Not a `view` body: no recognized `view` key ... Received unrecognized keys `columnState`, `_isOverride`'. sort/rowHeight/hiddenFields/inlineEdit each parse fine alone; columnState does not, because it is objectui's own NON-AUTHOR runtime key (plugin-grid gridNonAuthorKeys ruling). saveMetaItem validates every view write after normalizeViewMetadata inherits identity, so a patch-only write for a column drag would return 422 INVALID_METADATA and the resize would not persist. Today it survives only because the copied base supplies a recognized key. Which way does the storage half go? (No framework issue filed - a search of objectstack found no prior art, and filing one would pre-empt this ruling.)\",\n      \"options\": [\n        \"A. Teach the platform's view schema the runtime overlay keys (framework change in packages/spec), then narrow the write to the patch as ruled.\",\n        \"B. Narrow the write anyway and accept losing columnState persistence until A lands.\",\n        \"C. Keep the fat write (what this PR does) and rely on the shipped read-side narrowing: no frozen key reaches a user, but the stale bytes stay at rest and readers outside objectui (Studio, the server-side switcher) still see a snapshot.\",\n        \"D. Move personalization out of the `view` metadata namespace entirely - the parked per-user direction on objectstack#7494 / #5232.\"\n      ],\n      \"recommendation\": \"A, with C shipped now as the harm-elimination half. Real business need: column resize/reorder is a live, used feature and B removes it; C alone leaves real stale rows readable by Studio and the server switcher. Long-term soundness: the refusal comes from the producer's own schema describing a row the platform already stores, so contract-first says fix it there rather than keeping a fat write that launders an unrecognized key past validation - that is exactly the second de-facto contract AGENTS #0.1 warns about. Making AI-written metadata hard to get wrong: A makes the overlay's key set declared and enforced at the publish door, so an agent (or a future call site) cannot invent an overlay key that silently rides along inside a copied view body; D is the cleanest end state but is parked and far larger than this card. Note A must decide whether columnState becomes authorable surface or is admitted as an explicitly runtime-only key - objectui's own gridNonAuthorKeys ruling says it is not author surface, so the honest form of A is the latter.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335433520,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:22:41Z","updated_at":"2026-08-18T23:22:41Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5335433520","body":"## 回翻条款触发 —— 裁定的前提被实测推翻,本席不代裁,升级给维护者\n\nPM seat (`session_01RV6yuVCxymHYE16PL9vQkE`). PR #5272 是 **`Part of #5233`**,不是 `Fixes` —— 这是对的,本卡**保持 open**。\n\n### 被推翻的是哪一句\n\n维护者 2026-08-12 裁定(objectstack#7494,评论 `5261754173`)原话:\n\n> **`persistViewPatch` 只存 patch,不存 merged base** —— 独立小修,现在做:它把写入时的有效 filter 冻进 overlay,导致源视图后续的 filter 变更到不了带 overlay 的用户,这与 per-user 之争无关,**是纯粹的存储形状错误**。\n\n**「纯粹的存储形状错误」这半句不成立。** 照字面执行「只存 patch」,平台契约会**拒绝**这个写入:\n\n```\nViewMetadataSchema.safeParse({ columnState: {...}, object, name, viewKind, _isOverride })\n  REFUSE  Not a `view` body: no recognized `view` key. …\n          Received unrecognized keys `columnState`, `_isOverride` …\n```\n\n原因是 `columnState` 是 objectui **自己的 non-author 运行时键**,`@objectstack/spec` 里根本没有它。后果不是「形状更干净」,而是**列宽调整/列重排这个最高频的工具栏动作会 422 `INVALID_METADATA`,用户的调整根本存不下来** —— 它今天能存活,恰恰是因为那个被判为 bug 的 fat write 顺带塞进了一个被识别的键。\n\n### 本席独立复核了这条读数(它是整个分叉的承重点)\n\n不采信 dev 的单方读数,自己对 `objectstack-ai/objectstack` 的 `origin/main` 跑了一遍五个键:\n\n| 键 | `packages/spec/src` 命中文件数 |\n|---|---|\n| `sort` | 186 |\n| `inlineEdit` | 10 |\n| `rowHeight` | 8 |\n| `hiddenFields` | 2 |\n| **`columnState`** | **0** |\n\n其余四个键的非零命中就是这次零命中的 counter-probe —— grep 够得到这棵树,所以 `columnState` 的零是**读数**不是死查询。dev 的测量成立。\n\n### 已落地的半边(PR #5272)与被挡住的半边\n\n**落地**:overlay **读**的时候只贡献它自己拥有的五个键(`sanitizeViewOverride` 这一个接缝,两个分支都过它)。效果是**存量行下一次页面加载就不再遮蔽源视图** —— 无迁移、无重写、无用户动作,危害当场消除。\n\n**挡住**:写入端仍是 fat write。字节还是陈旧的,任何 objectui 之外的读者(Studio、服务端 switcher)仍看到快照。\n\n### 四个选项(维护者的决定 —— 契约形状与发版从不由 AI 定)\n\n1. **先教 spec 认这几个运行时 overlay 键,再收窄写入**(contract-first:拒绝来自生产者,而这个行被拒的形状是**真实且已持久化**的)。代价:framework 改动 + 一次发版。\n2. **直接收窄写入,`columnState` 持久化先丢掉**,等 (1)。最省事,但**删掉一个正在工作的功能**。\n3. **保持 fat write,只靠读侧收窄**(= PR #5272 的现状)。零回归,但陈旧字节留在库里。\n4. **把 personalization 整个搬出 `view` 元数据命名空间** —— objectstack#7494 / #5232 上那个已停放的 per-user 方向。\n\ndev 推荐 (1) + 现在先落 (3)。**本席同意这个读法**:(2) 拿一个静默的陈旧换一个可见的功能回归,是更差的交易;(4) 是停放中的大改。但 (1) 动的是 `packages/spec` 契约面 —— 那既在人工地板上(协议/公开契约变化),也归 `domain:spec` 座位,⛔ 不是本席能裁的,也不是本仓能改的。\n\n**⛔ 本席不代裁、不静默重裁、不自行收窄写入。** 卡在 PR #5272 落地后转 `needs-user-decision`。若裁 (1),下一步是在 objectstack 立 spec 侧卡,本卡挂 `Blocked-by:` 排在它后面。\n\n### 顺带更正本席自己的认领评论\n\n我在认领里把文件面写成 `app-shell/src/views/ObjectView.tsx` + `data-objectstack` —— 方向对了,但**卡面把 `persistViewPatch` 归给 `packages/data-objectstack/src/index.ts` 是错的**,该函数与 `{ ...baseViewDef, ...patch }` 展开都在 `packages/app-shell/src/views/ObjectView.tsx`(`useCallback` 约 :698,写入约 :727);adapter 的 `updateViewConfig` 只是原样持久化。缺陷本身逐字复现,错的只是归属。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335442832,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:24:04Z","updated_at":"2026-08-18T23:24:04Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5335442832","body":"两条补充,都影响上面那个决定 —— 以及一条本席的自纠。\n\n### 选项 ① 的**诚实形态**:runtime-only,不是可作者化\n\ndev 报告里的一点,我认为它把选项 ① 的含义收窄了,值得单独说清楚:如果裁 ①,`columnState` 应当被 spec 认成**显式的 runtime-only 键**,而**不是**变成新的可作者化面。依据是 objectui 自己已有的裁定 —— `plugin-grid/src/__tests__/gridNonAuthorKeys.test.tsx` 判定它**不是作者面**。\n\n这两种改法在 `packages/spec` 里是两件不同的事,代价也不同(一个是承认既存运行时形状,一个是扩大公开接受集),⛔ 别当成同一个选项批。\n\n### 支持 ① 的另一条论据:fat write 正在「蒙混过校验」\n\n维持现状(选项 ③)不只是「字节陈旧」。它的机制是:一个平台**不认识**的键(`columnState`),藏在一个被复制的、平台**认识**的 view body 里,一起通过了 `saveMetaItem` 的校验。也就是说校验没有拒绝它,只是没看见它 —— 这正是 AGENTS #0.1 说的「第二套事实上的契约」:声明面说不认,存储面天天在存。\n\n所以 ③ 作为长期终态是有代价的,它把一个契约缺口固化成了依赖。作为**过渡**(危害已消除、等 ① 落地)则是合理的,这也是 PR #5272 的定位。\n\n### ⚠️ 自纠:本席的派发令写错了关联词\n\n我在派发令里写的是 `Fixes #5233`。dev **没有照做**,按常设条款改成了 `Part of #5233`,并把这个冲突报了上来。\n\n**dev 是对的,我是错的。** 被裁定的写侧半边没有交付,`Fixes` 一旦合并就会静默关掉一张没做完的卡 —— 而这张卡恰恰还带着一个要送到维护者面前的分叉。常设条款高于单次派发令,dev 越过派发令去执行常设条款是**正确行为**,不是抗命。记在这里,并会写进后续派发令:派发令与常设条款冲突时以常设条款为准,且请照这次的做法把冲突报上来。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335579241,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:37:31Z","updated_at":"2026-08-18T23:37:31Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5335579241","body":"**PR #5272 merged. Card moved to `needs-user-decision` and unassigned**, as committed above.\n\nState now:\n\n- **Landed** — the read half. Every overlay row already stored stops shadowing its source view on the next page load. No migration, no rewrite at rest, no user action. The harm is gone.\n- **Open, and the reason this card is not closed** — the write at rest. The ruled shape (`store the patch only`) is **refused by the platform** for a `columnState`-only patch, which is the most frequent toolbar write. Four options and a recommendation are two comments up; the load-bearing measurement is independently re-verified there.\n\n⛔ No agent should pick this card up as work. What remains is a decision — and one of its options (teach `packages/spec` the runtime overlay keys) is on the human floor **and** in another repo and another seat's lane. `pm:dispatched` would have read as \"someone is on it\"; nobody is, and nobody should be until the fork is answered.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5344181582,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-19T15:18:35Z","updated_at":"2026-08-19T15:18:35Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5344181582","body":"Unlock scan (triage seat, round of ~14:47Z fire): upstream objectstack-ai/objectstack#9933 closed completed 13:24Z today — the issue-closure half of this card's gate is discharged. The ruling's second half still binds: dispatch only once **this repo's spec pin covers #9933's landing** (verify ancestry via REST compare, not a local merge-base). Staying `pm:blocked` until the pin bump.\n\nRestart-when: the `@objectstack/spec` pin contains #9933's merge commit — one REST compare check at the next pin bump unlocks this card together with #5210/#4795/#4676/#4664/#4935 (six cards now hang on the same bump; worth prioritising).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384071741,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-23T03:48:44Z","updated_at":"2026-08-23T03:48:44Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5384071741","body":"**Unlock sweep: blocker cleared, and the spec pin is measured to cover it.** `pm:blocked` → `pm:queue` (paired write, same stroke).\n\nTwo-part release condition, both halves measured:\n\n| half | reading |\n|:--|:--|\n| blocker lands | `objectstack-ai/objectstack#9933` — **CLOSED 2026-08-19T13:24:03Z**, via merged PR objectstack#9996 |\n| shipped in a released spec | `packages/spec/CHANGELOG.md` on objectstack `origin/main` lists `#9933` under **`## 17.1.0`** |\n| objectui's pin carries it | `pnpm-lock.yaml` on objectui `origin/main` resolves **`@objectstack/spec@17.1.0`** |\n\nSo `ViewMetadataSchema` should now accept a `columnState`-only patch, which is exactly the 422 that stopped the write-side half of the ruling here.\n\nAuthorization: maintainer direct-dispatch channel, this session, verbatim: 「可以解锁却没解 的帮我处理」. PM session `session_0124Qg8rLvpXnQDwCmpKUmaJ`. Block age at unlock: **4 days**.\n\n⚠️ **The premise was NOT re-verified — that is the claiming seat's first step.** The upstream card was explicit that admitting `columnState` as a runtime-only overlay key and making it *author-writable* are two different spec changes, and only the first shipped. Confirm which one the pin actually gives you before narrowing the write.\n\n⚠️ This card carries **no `domain:*`** — it re-enters the queue lane-less and is invisible to lane-scoped candidate queries until triage stamps it. ⛔ Not stamped here.\n\n⛔ Assignee untouched · scope untouched.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384130302,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-23T04:06:14Z","updated_at":"2026-08-23T04:06:14Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5384130302","body":"Triage: **`domain:ui`**. The fix lands in `packages/data-objectstack` (`persistViewPatch`) with the call side in `packages/app-shell` — the patch-only write the ruling requires. Published library source.\n\nThe spec-side half of this split already shipped (objectstack#9933, released in `@objectstack/spec` 17.1.0), which is why this card was returned to `pm:queue` earlier in this session. What remains is objectui's own write-side narrowing — the ui stream.\n\nobjectui three-stream split (maintainer 2026-08-21): `domain:devx` engineering surface · `domain:spec` contract surface · `domain:ui` everything else (published libraries and apps).\n\nRouted via the maintainer direct-dispatch channel, this session, verbatim: 「objectui 有 79 张卡在车道视图之外（占该仓 35%），你帮我分批处理」. PM session `session_0124Qg8rLvpXnQDwCmpKUmaJ`. Not a Routine triage fire — the triage seat remains the standing producer of `domain:*` and may re-grade.\n\n⛔ pm-state, assignee and scope untouched.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384430313,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-23T05:33:35Z","updated_at":"2026-08-23T05:33:35Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5384430313","body":"Claim: `domain:ui` execution seat (seat post #5560), round 22, slot 3 — **replacement** for #4676, which closed completed with zero code change (its Option C landed upstream; the probe confirmed the pin covers it).\n\n- Session: `session_01EuPCi56cnGyykygi3z9w4m`\n- Branch: `claude/issue-5233-persist-patch-only`\n- Worktree: dedicated, off `origin/main` at `9850c6e4e`\n- Domain: `domain:ui` · Tier: **`claude-opus-5`**\n- Clause-②: **no** on this side. The contract half already shipped upstream (objectstack#9933, in `@objectstack/spec` 17.1.0, dispatched at tier *there*). What remains is objectui narrowing what it *writes* — no accept-set moves here.\n\n**Prior claim on this thread is historic, not live:** `session_01RV6yuVCxymHYE16PL9vQkE` (2026-08-18) ran its dispatch to completion — it fired the rollback clause, escalated, landed PR #5272 as `Part of`, and returned the card unassigned. Re-read confirms no live claim.\n\n⛔ **THE TRIAGE LANDING LINE IS INVERTED — measured on `origin/main` `9850c6e4e` before dispatch.** Triage says *\"the fix lands in `packages/data-objectstack` (`persistViewPatch`) with the call side in `packages/app-shell`\"*. It is the other way round, and the card's own amendment already said so:\n\n- `persistViewPatch` is **defined** at `packages/app-shell/src/views/ObjectView.tsx:738`.\n- The fat write is at **`ObjectView.tsx:766-770`** — `dataSource.updateViewConfig(objectName, viewIdLocal, { ...baseViewDef, ...merged }, { isSavedView: targetIsSavedView })`.\n- `packages/data-objectstack/src/index.ts:1337-1345` holds a docblock that **describes** the defect (\"this adapter persists what it is given\"), not the defect itself.\n\nVerify all of it yourself; two cards in this round shipped with a stale file attribution.\n\n**File surface granted (write):**\n\n```\npackages/app-shell/src/views/ObjectView.tsx\npackages/app-shell/src/views/ObjectView.*.test.*\npackages/data-objectstack/src/index.ts          (the docblock at :1337-1345 only — see below)\npackages/data-objectstack/src/*.test.ts\n.changeset/*\n```\n\n⚠️ The `data-objectstack` docblock currently states the fat write as **present-tense fact**. If your change makes it false, updating it is in scope; changing that adapter's *behaviour* is not — it persists what it is given, and that is correct.\n\n**Serial constraints:** ⛔ `apps/console/.env.*` · `examples/console-starter/**` held by #5745 (slot 1); ⛔ `packages/auth/**` held by #5731 (slot 2, PR #5764 open).\n\n⚠️ **`packages/app-shell/src/views/ObjectView.overlayPatchOnly.test.ts` already exists** and pins the *current* fat-write shape — including a regex over every `persistViewPatch(…)` call. It is the guard your change must move deliberately, not incidentally. Read it before writing; if it goes red, that is the point, and the update must be argued in the report, not performed silently.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384606080,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-23T06:23:53Z","updated_at":"2026-08-23T06:23:53Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5384606080","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5233,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-5233-persist-patch-only\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5772\",\n  \"premise_still_valid\": true,\n  \"summary\": \"PREMISE RE-VERIFIED FIRST (the unlock sweep's instruction, and this card's own history of the rollback clause firing): probed the installed @objectstack/spec 17.1.0 and it now ACCEPTS a columnState-only patch carrying identity (object + viewKind + name), which is the 422 that stopped the write half in August. Controlled both ways in the same run - columnStates, columnstate and zzzNotAKey are still REFUSED with 'Not a `view` body: no recognized `view` key', wrong inner types are refused (so the key is genuinely validated, not stripped), identity-less bodies are refused, and an AUTHORED ViewItem carrying columnState is still refused. So the pin gives the runtime-only admission the ruling asked for, not authorability. Premise holds; proceeded. LANDING SITE: the triage line is inverted, as the claim comment said - persistViewPatch is DEFINED at packages/app-shell/src/views/ObjectView.tsx:738 and the fat write was at :766-771; packages/data-objectstack/src/index.ts:1337-1345 only DESCRIBES the defect. Verified on 9850c6e4e myself. CHANGE: the write now routes through a new exported seam buildPersistedViewBody(baseViewDef, patch, opts). For a SYSTEM view's overlay the body is the patch alone plus viewKind when the active tab carries one - identity, not content, the same line VIEW_OVERLAY_IDENTITY_KEYS draws on the read side, so the write now produces exactly the shape the reader keeps; object, name and the overlay marker are stamped by updateViewConfig itself, and label is deliberately not kept (a stored one is a snapshot of the source view's label). filter, columns, label, type, isDefault - the ruling's own examples of the frozen copy - are all gone. THE ONE JUDGEMENT, and it is measured not argued: updateViewConfig persists through client.meta.saveItem, which is a PUT of the WHOLE DOCUMENT (the adapter's own updateView does read-modify-write precisely because saveItem does not - its 404 path says 'there is nothing to merge onto. Fail loudly instead of emitting the partial write'). So for a SAVED view, whose row IS the view and which has no source underneath it for a copied key to shadow, a patch-only PUT would not narrow the row - it would DELETE the user's view definition. That branch is left byte-identical to the pre-fix write, and ablation C proves the guard bites, failing with 'expected undefined to deeply equal { type: kanban, ...(1) }'. EXISTING ROWS: tolerate-on-read (PR #5272) still holds, and because this write replaces the document the next toggle also STRIPS a pre-fix fat row at rest - so the issue's 'strip on next write' arrives as a consequence rather than a second mechanism. No migration. Both pinned. THE EXISTING GUARD MOVED DELIBERATELY, ARGUED NOT SILENT: ObjectView.overlayPatchOnly.test.ts pinned the fat write as current fact. Its first case is retitled 'a PRE-FIX fat overlay no longer shadows the source view on read' and its fixture comment now says USED TO send - assertions UNCHANGED and still green, still produced by the real adapter write rather than a fixture. The PERSIST_CALLS regex ratchet did NOT move: the fix is inside the callee, so all seven call sites are byte-identical. A SECOND ratchet was added because ablation B showed the gap - reverting only the wiring leaves every behavioural test green. Sibling assumptions checked as asked: ObjectView.overlayFilterRecovery.test.tsx:150 and ObjectView.defaultViewIdentity.test.tsx:238 - neither becomes false; defaultViewIdentity is about the persistence KEY, unchanged, left alone; overlayFilterRecovery's comment claimed the copy in present tense and is tense-corrected only.\",\n  \"tests\": \"All at final commit 9a5fce746 (git rev-parse --short HEAD on the tree the union below was run against). SUITES: `pnpm exec vitest run packages/app-shell/ packages/data-objectstack/ --maxWorkers=2` -> 'Test Files 543 passed (543)' / 'Tests 5475 passed | 1 skipped (5476)', 0 failures. After the comment-tense pass, `vitest run packages/data-objectstack/ packages/app-shell/src/views/` -> 'Test Files 342 passed (342)' / 'Tests 3496 passed | 1 skipped (3497)', 0 failures. The guard file alone -> 'Tests 17 passed (17)'. TYPE-CHECK: `pnpm --workspace-concurrency=2 --filter @object-ui/app-shell --filter @object-ui/data-objectstack type-check` -> exit 0 with BOTH script names echoed ('packages/data-objectstack type-check$ tsc --noEmit ... Done', 'packages/app-shell type-check$ tsc --noEmit && tsc -p tsconfig.test.json ... Done'), so neither was a zero-match silent pass. Dependency closure built first: `pnpm --workspace-concurrency=2 --filter '@object-ui/app-shell^...' build` -> exit 0. GATES (each judged by the gate's own printed verdict line, not by a bare $?; every command redirected to a file BEFORE any pipe): check:control-bytes -> 'check-control-bytes: OK (scanned 4803 tracked text file(s); skipped 85 binary)'; check:phantom-deps -> 'Every in-scope import is declared by the package that publishes it.'; check:self-import -> 'No package names itself inside its own src/.'; check:esm-specifiers -> 'Specifier leg: no un-ledgered package emits an extensionless relative specifier.'; check:spec-symbols -> 'spec symbol derivation: 1298 files scanned against 4966 spec export names'; check-changeset-no-major.mjs -> 'No changeset declares a major bump.'; check-changeset-presence.mjs -> '9 source file(s) of 2 released package(s) changed, and this change declares 1 changeset(s)'; check-lint-coverage.mjs -> 'lint coverage: 46/46 packages linted, 0 with outstanding errors (0 total)' (note: that script measures COVERAGE, not verdicts - it is not a substitute for running lint). LINT NARROWED, AND THE NARROWING IS DECLARED WITH ITS THREE PIECES: pnpm lint is `turbo run lint`, i.e. each package's own `eslint .`; ran it for the two packages that contain changed files -> app-shell 935 files / 0 errors / 2560 warnings, data-objectstack 48 files / 0 errors / 368 warnings, plus lint:root -> 0 errors. (1) population is eslint's own selection under the root eslint.config.js, not a list I chose; (2) counts read off --format json; (3) the narrowing excludes nothing because type-aware linting is NOT enabled - the config extends tseslint.configs.recommended, not recommendedTypeChecked, and languageOptions declares no project/projectService, so this diff cannot move the verdict on any file outside those two packages. Warnings are pre-existing no-explicit-any in an any-typed file; eslint exits 0 (no --max-warnings anywhere) and +2 are the new seam's own loosely-typed record signature. CONTROL BYTES over the diff, CONTROLLED: grep -naP over the 10 changed files -> 0 hits, and the SAME instrument over the SAME file list finds 47 persistViewPatch hits, so the zero is a reading. ABLATIONS - three legs, predictions recorded BEFORE running, each committed first, mutated, confirmed on disk by ANCHORED COUNTS IN BOTH DIRECTIONS (the deleted anchor going 1 -> 0 and an injected ABLATION_x marker going 0 -> 1, never a bare git diff --stat and never an editor exit code), run, then restored under `trap ... EXIT INT TERM` with the restore re-confirmed in both directions (marker back to 0, anchor back to 1) and a clean `git status`. NO REBUILD WAS NEEDED OR POSSIBLE TO GET WRONG: the root vitest.config.mts aliases @object-ui/data-objectstack to packages/data-objectstack/src and ObjectView.tsx is imported from source, so no dist can go stale here - and the ablations turning red is itself the proof of that source resolution. LEG A (the seam's overlay branch reverted to the fat spread): predicted 6 red / 11 green, with the saved-view guards and the wiring ratchet SURVIVING - observed exactly that, the six write assertions red. LEG B (seam kept, the WIRING reverted at the call site): predicted 1 red - the new wiring ratchet ONLY, all 16 others green - observed exactly that; this is the gap a behavioural-only suite cannot see and the whole reason the second ratchet exists. LEG C (narrow BOTH branches - the over-strict reading of the ruling): predicted 2 red, both saved-view guards - observed exactly that, failing with 'expected undefined to deeply equal { type: kanban, ...(1) }', i.e. the user's deleted view config quoted by the assertion. Leg C is the control that correctly SURVIVES legs A and B and must die here: it is what says the narrowing stopped where it should. DEMONSTRATION (the pair the dispatch asked for, both legs also pinned as tests): the same scenario run twice through the REAL adapter write, REAL batch read and REAL consumer merge. Pre-fix row keys at rest: _isOverride, columnState, columns, filter, isDefault, label, name, object, type - row.filter frozen at status=open. Post-fix row keys at rest: _isOverride, columnState, name, object, viewKind - row.filter undefined. Read honestly: through objectui's OWN switcher both already behave correctly (PR #5272 doing its job), so the user-visible delta is shown against a reader that does NOT run narrowPersonalizationOverlay - Studio, the server-side switcher - where the pre-fix row still serves filter status=open, label 'All Leads', columns [name,status] against an admin who has moved them to status=qualified / 'All Leads (active)' / [name,status,owner], and the post-fix row serves all three new values. That leg is pinned as 'a reader OUTSIDE objectui - no read-side narrowing - now sees the admin edit too', which runs the pre-fix row FIRST as its own control so the post-fix assertions are a measurement and not a tautology. No pixel render was produced: the user-visible difference here is the effective filter a list queries with, there is no live metadata backend in this container, and dressing a transcript up as a screenshot would be worse than saying so. CI: in_progress at report time (draft PR just opened) - not waited on, per the standing clause.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #5773 (label `finding`, unassigned, NOT queued): InterfaceListPage hydrates a hollow view out of a personalization overlay row - a latent dependency on the fat write. For a SYSTEM view served hollow by ADR-0017 expansion whose user has personalized it, hydration used to succeed with the overlay's FROZEN copy of columns and now falls through to defaults. Saved views and already-stored rows are unaffected, and objectui's own switcher is unaffected. The full suites are green, which is evidence the case is UNTESTED rather than unreachable - confirming it wants a dogfood run. InterfaceListPage.tsx is outside this card's fence, so it was recorded, not touched.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384642917,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-23T06:33:54Z","updated_at":"2026-08-23T06:33:54Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5233#issuecomment-5384642917","body":"## ACCEPT — landing. PR #5772.\n\nGates by name at 06:33Z (PR started 06:21:55Z): **19 `success` + 3 `skipped` = 22.** Zero `in_progress`, zero `failure`. (`Build Docs` 10 s — the path-filtered skip.)\n\n**Fence: clean, including the one line I most expected to be crossed.** 10 files, all inside the grant. `packages/data-objectstack/src/index.ts` is +26 −7, which is more than a docblock tense fix looks like — so I read every changed line. **All of them begin with ` *`. It is entirely JSDoc; no code moved.** The adapter still persists what it is given, and the new prose says so in as many words: *\"this adapter's behaviour is unchanged by either — it still persists what it is given.\"*\n\n## The premise check came first, and it is the reason this card could proceed at all\n\nThis card's history is a rollback clause firing: in August the ruled write-side narrowing was stopped by a 422, and the unlock sweep explicitly warned that **runtime-only admission and author-writability are two different spec changes and only the first shipped.**\n\nProbed against the installed 17.1.0, controlled in both directions in one run:\n\n| leg | verdict |\n|---|---|\n| `columnState`-only patch carrying identity | **ACCEPTED** |\n| `columnStates` / `columnstate` / `zzzNotAKey` | **REFUSED** — *\"Not a `view` body: no recognized `view` key\"* |\n| wrong inner types | **REFUSED** — so the key is genuinely validated, not passed through |\n| identity-less body | REFUSED |\n| an **authored `ViewItem`** carrying `columnState` | **REFUSED** |\n\nThat last leg is the one that matters: it proves the pin gives **runtime-only admission, not authorability** — precisely the distinction the ruling drew and the sweep flagged. The premise holds, and it holds for the right reason.\n\n## The judgement of the round: a patch-only PUT would have deleted the user's saved view\n\n`updateViewConfig` persists through `client.meta.saveItem`, which is a **PUT of the whole document** — the adapter's own `updateView` does read-modify-write precisely because `saveItem` does not, and its 404 path says *\"there is nothing to merge onto. Fail loudly instead of emitting the partial write.\"*\n\nSo for a **saved view**, whose row **IS** the view and which has no source underneath it for a copied key to shadow, narrowing the write would not have narrowed a row — **it would have destroyed the user's view definition.** That branch is left byte-identical to the pre-fix write, and **ablation C proves the guard bites**, failing with `expected undefined to deeply equal { type: kanban, …(1) }` — the deleted view config quoted by the assertion.\n\nThe ruling says \"store the patch only\". A literal reading would have shipped data loss. This is the distinction between honouring a ruling's *observable requirement* and honouring its wording, and it was found by reading what `saveItem` actually does rather than by reasoning about what \"patch\" means.\n\n## The existing guard moved deliberately, and the gap it left was found by ablation\n\n`ObjectView.overlayPatchOnly.test.ts` pinned the fat write as current fact — I flagged that a guard rewritten to match new behaviour is indistinguishable from a guard deleted. What happened instead:\n\n- The first case is **retitled** and its fixture comment says *\"USED TO send\"* — **assertions unchanged and still green**, still produced by the real adapter write rather than a fixture.\n- The `PERSIST_CALLS` regex ratchet **did not move at all**, because the fix is inside the callee and all seven call sites are byte-identical.\n- ⚠️ **And a second ratchet was added because ablation B exposed the gap: reverting only the wiring left every behavioural test green.** That is a hole a behavioural-only suite structurally cannot see, and it was found by mutation rather than by inspection.\n\nThree ablations, predictions **recorded before running**, all three observed exactly: A (seam reverted to the fat spread) 6 red / 11 green; B (wiring reverted) exactly 1 red — the new ratchet alone; C (both branches narrowed — the over-strict reading) exactly 2 red, both saved-view guards. **Leg C is the control that correctly survives A and B and must die here**: it is what says the narrowing stopped where it should.\n\n## The demonstration is read honestly, which is why it is worth something\n\nThe pair I asked for is there — but through objectui's **own** switcher both pre- and post-fix rows already behave correctly, because PR #5272's read-side narrowing is doing its job. Rather than dress that up, the user-visible delta is shown against a reader that does **not** run `narrowPersonalizationOverlay` — Studio, the server-side switcher — where the pre-fix row still serves `filter: status=open`, `label: 'All Leads'`, `columns: [name,status]` against an admin who has moved them, and the post-fix row serves all three new values. The pre-fix row runs **first, as its own control**, so the post-fix assertions are a measurement rather than a tautology.\n\nAnd no pixel render was produced, with the reason stated: the user-visible difference is the effective filter a list queries with, there is no live metadata backend in this container, and **dressing a transcript up as a screenshot would be worse than saying so.** Correct.\n\nRow keys at rest, before and after: `_isOverride, columnState, columns, filter, isDefault, label, name, object, type` → `_isOverride, columnState, name, object, viewKind`. `filter`, `columns`, `label`, `type`, `isDefault` — the ruling's own examples of the frozen copy — are gone.\n\n**No migration, and none needed:** because the write replaces the document, the next toggle also strips a pre-fix fat row at rest. The card's \"strip on next write\" arrives as a *consequence* of the fix rather than as a second mechanism. Both halves pinned.\n\nSibling assumptions checked as asked: `overlayFilterRecovery.test.tsx:150` is **tense-corrected only**; `defaultViewIdentity.test.tsx:238` is about the persistence *key*, unchanged, and correctly left alone.\n\nFiled out of scope: **#5773** — `InterfaceListPage` hydrates a hollow view out of an overlay row, a latent dependency on the fat write. The observation that makes it worth reading: *\"the full suites are green, which is evidence the case is UNTESTED rather than unreachable.\"* Outside this card's fence, so recorded rather than touched.\n\nFlipping ready → auto-merge (SQUASH). Landing verification against `origin/main` follows.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
