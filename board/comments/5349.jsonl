{"id":5362756754,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-20T22:24:20Z","updated_at":"2026-08-20T22:24:20Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5349#issuecomment-5362756754","body":"Claim: PM loop round 21\nSession: `session_01RV6yuVCxymHYE16PL9vQkE`\nBranch: `claude/issue-5349-objectgrid-silent-no-columns`\nWorktree: `objectui-issue-5349`\nDomain: `repo:objectui`\nFile surface: `packages/plugin-grid/src/**` (stop on breach; explain in the report)\nContainer & model: M, `mode:subagent`\nClause-②: no — additive diagnostics, no accept-set change\nSerial constraints cleared: none — #5447 (plugin-grid/index.tsx) is MERGED; rebase on current main\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5363217320,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-20T23:16:01Z","updated_at":"2026-08-20T23:16:01Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5349#issuecomment-5363217320","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5349,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-5349-objectgrid-silent-no-columns\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5456\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Implemented option 2 (the diagnostic). An authored object-grid column that can never resolve now emits one console.warn naming the address: which block (object-grid / the view:grid alias), which object and label, which columns[i], the keys that entry actually carries, and the rewrite that works — built from what was authored ('Write `{ field: \\\"amount\\\", label: \\\"Amount\\\" }` instead.'), not a template. It reuses ObjectGrid's existing 'you declared it, the renderer dropped it' channel (the export-format useEffect + `[ObjectUI] ObjectGrid <topic>:` prefix) rather than adding a second differently-shaped one, as the card required. The predicate reads the columns INPUT and nothing else — it never asks whether the grid found rows, so no 'needs columns' guess reaches a shared path; all five row-side escape hatches (bare data array, provider:'value', legacy staticData, bind, host `data` React prop) are honoured and pinned in both directions, as are the column-side ones (string[], absent columns, empty columns, hidden:true). The drop decision moved into a shared resolvesToDataColumn so the reporter cannot drift from the filter that drops the column. Rendering is unchanged in every measured case. File surface held: all source changes under packages/plugin-grid/src/**, plus the required changeset. No new user-facing copy, hence no locale-pack changes (the ten packs live in packages/i18n, outside the surface) — the diagnostic is developer-facing English on the console per commandment #-1.\",\n  \"tests\": \"All on final commit 57cda96a8. `pnpm exec vitest run packages/plugin-grid/` -> 'Test Files 84 passed (84) / Tests 786 passed (786)'. `pnpm exec vitest run packages/plugin-list/` (the ListView host) -> '43 passed (43) / 644 passed (644)'. `pnpm --filter @object-ui/plugin-grid type-check` -> clean (tsc --noEmit && tsc -p tsconfig.test.json; first attempt failed with TS2307 across @object-ui/* until `pnpm --filter '@object-ui/plugin-grid^...' build` built the dependency closure). `pnpm --filter @object-ui/plugin-grid lint` -> '655 problems (0 errors, 655 warnings)' (repo baseline; the gate is errors-only by design). Gates run green: check:control-bytes, check:spec-symbols, check:phantom-deps, check:self-import, check:i18n-keys, check:i18n-drift, check:i18n-dead-keys, changeset:check, scripts/check-changeset-presence.mjs. 39 tests added, 1 updated, none skipped/disabled. MATRIX (measured before and after via a throwaway probe, deleted before commit; headers are literal getAllByRole('columnheader') output): field-only ['#','Name','Amount'] both, 0 diag -> 0; accessorKey-only ['#'] + no data cells both, 0 diag -> 1 naming columns[0] and columns[1]; mixed field-first ['#','Name'] both, 0 -> 1 naming columns[1] only; mixed accessorKey-first ['#','Name'] both, 0 -> 1 naming columns[0] only; string[] ['#','Name','Amount'] 0 -> 0; all-hidden ['#'] 0 -> 0; no columns key ['#','Id','Name','Amount'] 0 -> 0; empty columns [] ['#','Id','Name','Amount'] 0 -> 0. Rendering identical in all eight legs; the diagnostic is additive. REVERSE VERIFICATION, direction predicted before running, both restored with `git status --porcelain` empty afterwards. Ablations are directly effective and no rebuild applies: the tests import ../ObjectGrid and ../columnSpellingDiagnostics as source and vitest aliases every @object-ui/* specifier to packages/*/src, so no dist is on the path — and each ablation flipped results, which is itself the proof it reached the code under test. LEG 1 (remove the emitter): predicted the pure-module file stays fully green and only diagnostic assertions go red — 9 of 13 in the render file plus 1 of 6 in the baseline file; observed 'Tests 10 failed | 35 passed (45)', exactly those ten test-for-test. LEG 2 (remove the hidden carve-out): predicted the direction is MORE diagnostics, not fewer — a false positive over authored intent — 3 red in the unit file and 1 in the render file; observed 'Tests 4 failed | 41 passed (45)' with 'AssertionError: expected [ Array(1) ] to deeply equal []' on the hidden case. Leg 2 also found a real defect in my own message: describeEntry's tail was the '`field` is an empty string' sentence with no length check under it, so with the carve-out gone it told a well-formed column its non-empty field was empty. Fixed explicitly in commit 57cda96a8 — a message must never assert something it did not check.\",\n  \"open_questions\": [\n    {\n      \"question\": \"NON-BLOCKING, flagged because it departs from the card's wording: option 2 says 'dev-only console warning', but this ships WITHOUT a NODE_ENV gate. Reason: the card also forbids a second differently-shaped warning channel in this area, and ObjectGrid's existing 'you declared it, the renderer dropped it' channel (the export-format warning) has no NODE_ENV branch — adding one here would itself be the second shape. A dropped column is also strictly more severe than a dropped export format. The nearer-neighbour dev-gated channel, core's warnOnConflictingIdentity, is in a different package and behind a warn-once memo. Revert to dev-only if the maintainer prefers the card's literal wording.\",\n      \"options\": [\n        \"A — keep unconditional, matching the sibling channel inside ObjectGrid (shipped)\",\n        \"B — gate on NODE_ENV !== 'production', matching core's column-identity warning, at the cost of two shapes inside one component\"\n      ],\n      \"recommendation\": \"A, because channel consistency within the component is what the card asked for, the emission is bounded by the useEffect's schema-slice deps, and a metadata defect that reaches production is worth a line there too.\"\n    },\n    {\n      \"question\": \"NON-BLOCKING sequencing note the card raised: #5344 (ViewColumnInspector preserves whichever spelling a stored document already used) is the one path that can still hand ObjectGrid the retired spelling. Nothing here decides it, and this PR does not touch that surface.\",\n      \"options\": [\n        \"A — resolve #5344 by normalizing on write, which shrinks this diagnostic's population to hand-authored/off-platform metadata\",\n        \"B — leave #5344 as is; this diagnostic then covers the designer-produced case too\"\n      ],\n      \"recommendation\": \"A, and this card's diagnostic is complementary either way — it reports the disagreement wherever it originates, which is the half a producer-side fix cannot cover for metadata written outside the designer.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
