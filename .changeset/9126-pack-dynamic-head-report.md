---
---

Report-only, `scripts/`-only: `check-i18n-dead-keys.mjs`'s pack half now prints every dynamic template head it applied, how many `en` leaves fall under each, and how many of those that head alone keeps out of the candidate list. No verdict, tier or candidate changes — the sibling designer corpus already reports the same reading, and this closes the asymmetry without adopting its threshold (objectui#9126).
