{"id":5263787488,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-12T07:40:30Z","updated_at":"2026-08-12T07:40:30Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4433#issuecomment-5263787488","body":"**CLAIM + RULING** — PM seat `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4433-calendar-events-crash`, one dev agent, one PR, `Fixes #4433`. `finding` → `pm:queue`, dispatched at priority: a spec-legal node losing its component to an error card is the severest thing today's sweeps measured.\n\nRuling (delegated, veto window open): **the authored `events` action-metadata object must never reach `CalendarView`'s `events` prop** — strip the collision out of the spread per the #4428 deny-list precedent (the phase-2 whitelist question stays open; this crash does not wait for it). Conditions:\n\n1. **Measure the semantics first**: (a) does the authored `events` action metadata on a calendar-view node DO anything today through the renderer layer's own event wiring (did actions ever work on this node type, or was the key always a landmine)? State it — the fix must not silently disable a working action path, and if actions never worked here, that's recorded as the pre-existing state, not regressed. (b) Sweep the component's other computed props (`className`, `onEventClick`) for the same overwrite exposure and grade each — fix the crash-capable ones, record the benign ones.\n2. The gate's two halves come out together per the card's own design: the `CALENDAR_VIEW_OMITS` withheld-canary omission is removed (the `events` canary re-enters the sweep for this target) and the dedicated crash pin flips to asserting the calendar RENDERS with authored `events` present. **Gate on PR #4441 landing first** (the gate file is its surface) — poll.\n3. Red-first: the card's own minimal repro (the error boundary + `events is not iterable`) captured pre-fix; post-fix the same node renders `role=\"region\" aria-label=\"Calendar\"`.\n4. Changeset patch (@object-ui/plugin-calendar — crash fix, no API movement; grade up per precedent only if an exported type moves), never major.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
