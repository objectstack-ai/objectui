{"id":5272248819,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-12T20:10:03Z","updated_at":"2026-08-12T20:10:03Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4453#issuecomment-5272248819","body":"**CLAIM** — session `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4453-calendar-props-contract`.\n\n**Ruling (delegated authority; open veto window): option D, now concrete** — the #4425 phase-2 whitelist contract is ruled (comment 5270759246 there) and its calendar migration resolves this card's A–D question:\n\n1. **Convert `calendar-view`'s renderer to consume-or-declare**: stop the raw `{...props}` spread into `CalendarView`. Declared registry inputs are consumed (the #4433 `events` strip and #4484 `currentDate` parse already are — this completes the set); injected props consumed per the contract.\n2. **`onEventClick` becomes a DECLARED host escape hatch, function-typed** — the #4435 declared-passthrough pattern, which is what makes the hatch legal under the contract: a host-passed FUNCTION is forwarded to `CalendarView`'s prop (the working path the card forbids breaking); a NON-FUNCTION value (the authored-JSON crash case — a string can never be a handler) is dropped = abstain, same answer as absent. This is not option B's lenient-coercion shape because the discrimination now lives in a declared contract at the renderer boundary, not ad-hoc in a consumer — the key is declared, its type is declared, off-type input gets the one answer.\n3. Sweep the renderer's remaining forwarded keys against the same rule; every key is either a declared input (consumed), a declared hatch (typed), or dropped. Name the census in the PR.\n\n**Red-first**: the card's own measurements — authored `onEventClick: 'NOT-A-FUNCTION'` (node key AND `props` container channel, per #4484's lesson) → pre-fix click throws the uncaught `onEventClick is not a function` window error; post-fix click is a no-op (abstain). Must-not-change: a host-passed function still fires on click (green both sides); the #4433/#4484 pins stay green.\n\n**⛔ Sweep/ledger file**: `widget-dom-leak-sweep.test.tsx` is in-flight #4432's surface — do NOT touch it. The now-plantable `currentDate` canary (post-#4484) and any calendar canary-set extension are REPORTED as a follow-up for #4425's completion pass, not edited here.\n\n**Grading**: patch `@object-ui/plugin-calendar` (runtime boundary change; `.d.ts`-checked — the registry declaration if it moves is measured, minor per precedent if the published surface grows).\n\n**Surface (mutual exclusion)**: `packages/plugin-calendar/**` + one changeset. Disjoint from in-flight #4474 (organizations), #4336 (detail lookup), #4408 (list error state), #4432 (dashboard + sweep file). #4454 (inert `allowCreate`) stays its own card — do not fix it here, but if your census confirms it, note it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_017Qqyix2QcnpUC9XeYVDzx3)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
