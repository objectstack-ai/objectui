---
---

Docs-only: the eager-closure gate header stops restating its ceiling and baseline as MiB literals and names the two constants instead, with a pin that refuses a rendered size written back into that note. Internal CI script and its unit test only; nothing published changes.
