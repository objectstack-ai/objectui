{
  "number": 5930,
  "kind": "issue",
  "title": "`ui:dropdown-menu` renders an item's authored `icon` as raw TEXT — the catalog fixture named with-icons.json draws the words \"edit\", \"copy\", \"trash\" beside its labels",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "claude[bot]",
    "id": 209825114,
    "type": "Bot"
  },
  "labels": [
    "domain:ui",
    "pm:dispatched"
  ],
  "assignees": [
    "yinlianghui"
  ],
  "milestone": null,
  "type": "Bug",
  "created_at": "2026-08-24T02:05:47Z",
  "updated_at": "2026-08-24T11:43:47Z",
  "closed_at": "2026-08-24T11:43:47Z",
  "closed_by": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "merged_at": null,
  "comments": 5,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/issues/5930",
  "body": "Found while measuring the icon-name population for #5633, which needed to know which resolver each authored `icon` string reaches. Filed unassigned; not repaired there, because it is a different defect class (no resolution at all, rather than a resolution that misses).\n\n## What happens\n\n`packages/components/src/renderers/overlay/dropdown-menu.tsx` renders a menu item's `icon` as **raw text**, in both the item and the submenu-trigger arm:\n\n```tsx\n// line 40 — DropdownMenuSubTrigger\n{item.icon && <span className=\"mr-2\">{item.icon}</span>}\n// line 52 — DropdownMenuItem\n{item.icon && <span className=\"mr-2\">{item.icon}</span>}\n```\n\n`item.icon` is an authored **string**. Nothing resolves it. So the schema's own documented shape — the registration at line 99 describes items as `{ type?: \"separator\"|\"label\", label, icon, shortcut, disabled, children: [] }` — produces the literal word beside the label.\n\nLive specimen in the catalog: `examples/schema-catalog/src/schemas/components-overlay-dropdown-menu/with-icons.json` declares `\"icon\": \"edit\"`, `\"copy\"` and `\"trash\"` on its items. The file is literally named `with-icons.json`, and it renders `edit Edit`, `copy Copy`, `trash Delete`.\n\n## Why nothing catches it\n\n- The key parses and publishes: `icon` is part of the declared item shape.\n- No renderer test asserts a glyph here.\n- It is invisible to the #5633 gate **by design**: that gate judges membership of lucide's runtime `icons` record for names that reach a resolver reading it, and this site reaches **no** resolver. Its verdict on these three names is \"declined\", which is the right answer to the question it asks and the wrong shape of silence for this defect.\n\n## The decision this needs\n\nWhich surface the item icon should resolve through, if any:\n\n- The **record** surface (`resolveIcon` from `renderers/action/resolve-icon.ts`) — consistent with `action:*` and `ui:button` next to it; a retired spelling renders nothing.\n- The **dynamic** surface (`LazyIcon` / `getLazyIcon`) — more forgiving, but it degrades an unknown name to the `Database` glyph, which trades a no-icon failure for a wrong-icon one. #5622 and #5633 both recorded that as ruled out for authored icon fields.\n- Or: `icon` is not a real authored key on this component and should be retired from the declared item shape and the fixture instead.\n\nThat is a contract question, not an implementation detail, which is why this is filed rather than patched.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
