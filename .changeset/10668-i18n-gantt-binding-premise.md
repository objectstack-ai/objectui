---
---

Test-only change in `@object-ui/i18n`: the residue-namespaces suite now checks that `GanttView` takes `t` from `useGanttTranslation()` by pattern, since `GanttView` no longer takes `language` (objectui#10668). No published behaviour changes.
