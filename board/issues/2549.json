{
  "number": 2549,
  "kind": "pull_request",
  "title": "feat(detail): editable record highlights on the shared inline-edit draft (#2407 P2)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-16T01:26:16Z",
  "updated_at": "2026-07-16T02:47:00Z",
  "closed_at": "2026-07-16T02:46:58Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-16T02:46:58Z",
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2549",
  "body": "## What & why\n\n**P2 (final phase) of #2407** — builds on step 0 (`<InlineFieldInput>`, #2529) + P1 (`InlineEditContext` + atomic save, #2542). Makes the **highlights strip editable in place**, sharing **ONE draft + ONE atomic Save** with the details body. This completes the issue's goal: the most edit-worthy fields (Status / Budget / End Date / …) are now inline-editable, and a highlight edit + a body edit commit together.\n\n## Changes\n\n| Area | Change |\n|---|---|\n| **`HeaderHighlight`** | Consumes `useInlineEdit()`. Hover-pencil + double-click enter the shared record edit session; each editable highlight renders the same `<InlineFieldInput>` the body uses (`value = draft[name] ?? data[name]`, write via `setField`). Computed (`formula`/`summary`/`rollup`/`auto_number`), `readonly`, and system fields expose no editor. Empty highlights are kept while editing so they can be filled. **Compact-layout UX:** an actively-edited column widens to the \"wide\" basis and renders the editor full-width (Salesforce-style expand-on-edit). |\n| **`RecordDetailView`** (app-shell) | Hosts ONE `<InlineEditProvider canEdit={affordances.edit}>` spanning **both** `record:highlights` and `record:details`, plus the single record-level `<InlineEditSaveBar>` (sticky) — so highlight + body edits commit together in ONE `update(obj, id, draft, { ifMatch })`. This is the provider lift P1 deferred. |\n| **`record:details`** | Drops its P1-local `InlineEditProvider`/`InlineEditSaveBar` (a nested provider would split the draft from the highlights) and just consumes the shared context. |\n| **`record:highlights`** | Threads the bound `DataSource` through to `HeaderHighlight` for lookup/user editors. |\n\n## Guardrails preserved\n\nComputed / `readonly` / system highlights are non-editable; `canEdit` object-lifecycle gate; OCC (`ifMatch` + `ConcurrentUpdateDialog`); the atomic update carries only user-edited keys. The legacy (non-schema) `DetailView` path is untouched (it never had inline edit).\n\n## Verification\n\n- `plugin-detail` **type-check + vite build clean**; `app-shell` type-check clean **w.r.t. this change** (only the pre-existing, unrelated `@objectstack/formula` sandbox module error remains — not touched here).\n- **205 tests pass (22 files)** incl. a new `HeaderHighlight` editable suite: read-only without a provider; double-click enters the shared session; computed highlight exposes no editor; a highlight edit lands in the **shared** draft; `canEdit=false` offers no editor.\n- ✅ **Browser-verified** (real Chromium, harness replicating the app-shell provider structure): read-only by default → double-click a **body** field → the **highlights strip becomes editable too** → edit a highlight (budget 50000→88000) **and** a body field (owner Alice→Bob) → click Save → **exactly ONE** `update('project','p1', {budget:'88000', owner:'Bob'}, {ifMatch:'v1'})` carrying **both** keys → edit exits; no JS errors. (Screenshot shared with the maintainer.)\n\n## Acceptance criteria (P2)\n\n- [x] Hover / double-click a highlight (Status / Budget / …) enters the same record edit session, editable in place.\n- [x] Highlight + body edits commit together in ONE Save.\n- [x] Computed / readonly highlight fields expose no editor.\n- [x] Compact-layout editor is legible (expand-on-edit widening).\n\n## Notes for review\n\n- Left as **draft** for your eyes on the compact-editor UX (the expand-on-edit widening for a select/date/lookup in a narrow highlight column is the subjective part). Happy to iterate on the interaction (e.g. a popover instead of in-column expand) before merge.\n- With this, #2407 is complete across step 0 (#2529) + P1 (#2542) + P2 (this).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Y1f7TvqYMo41g9RbQ6H2od)_"
}
