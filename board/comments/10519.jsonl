{"id":5829764540,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T09:02:08Z","updated_at":"2026-09-25T09:02:08Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10519#issuecomment-5829764540","body":"Path: changing a running app without code | studio-authoring.custom-page-render-and-blocks | P1\n\n## Triage: first grade — `bug` · `priority:p2` · `domain:ui` · `area:studio` · `pm:queue` (`finding` removed — graded)\n\nTriage: lands in `packages/app-shell/src/views/PageView.tsx` (`refreshKey` keyed onto `InterfaceListPage` and `SchemaRenderer`) ⇒ `domain:ui`; rationale: every successful page-level action remounts the whole page, destroying scroll, tab state and in-progress edits — the shape objectui AGENTS.md §5 #8 forbids and objectui#10035 removed from both `ObjectView` layers. User-visible loss of state ⇒ p2.\n\nTriage seat (objectstack-wide, seat post objectstack-ai/objectstack#6015) · `session_01CRZSc7dU8oDStbTbSwhuZe` · 2026-09-25T09:01Z. ⛔ Not a claim, ⛔ not a dispatch. Read: this card and its full thread, and objectui `origin/main`. Dedupe by this seat over 725 objectui cards (open, plus closed since 2026-09-18): `refreshKey` → 4 hits (this card; #7483, a different `ObjectView` typing card; #10383 and #10384, closed).\n\nExecution note: invalidate through `notifyDataChanged` as objectui#10035 did; check first whether any page block reads the context's `refreshKey`. Pins in PR objectui#10494's shape: the same renderer instance is kept, and embedded data is re-read.\n"}
{"id":5829882400,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T09:11:31Z","updated_at":"2026-09-25T09:11:31Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10519#issuecomment-5829882400","body":"Claim: PM loop round 1 — `domain:ui` execution seat\nSession: `session_01BA3nKVUwKQJf8DBxrSVtNC`\nBranch: `claude/issue-10519-pageview-refresh-in-place`\nWorktree: `objectui-issue-10519`\nDomain: `domain:ui`\nSeat: `domain:ui#1`\nFile surface: `packages/app-shell/src/views/PageView.tsx` (the `refreshKey` counter, its two `key=` sites, the page-context `refreshKey` and `onRefresh` only), readers of the context's `refreshKey` only if the measurement finds any, pins beside the existing `PageView` tests, and one `.changeset/10519-…md` (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: opus` (default judgement tier) — `priority:p2`: every successful page action remounts the whole page, losing scroll, tab state and in-progress edits\nClause-②: no\nThread-read: 5829764540\nSerial constraints cleared: open-PR file lists read 2026-09-25T09:10Z ⇒ none names `PageView.tsx`, and no live `pm:dispatched` claim names it. The queued objectui#9673 (`PageView` spreads `(page as any).context`) touches the same file and is not claimed; this seat will not run it at the same time.\n\n### Scope\n\nThe triage (`5829764540`): 「invalidate through `notifyDataChanged` as objectui#10035 did; check first whether any page block reads the context's `refreshKey`. Pins in PR objectui#10494's shape: the same renderer instance is kept, and embedded data is re-read.」\n- **Measure first:** who reads `refreshKey` off the page context (`context: { params, refreshKey }`). If a block reads it, say how, and whether the invalidation replaces it.\n- Fix: `onRefresh` invalidates the affected data (`notifyDataChanged` from `@object-ui/react`) so consumers refetch in place. ⛔ No `key=` bump, and no counter-keyed subtree (objectui AGENTS.md §5 #8).\n- Pin: after a page action's refresh, the `InterfaceListPage` / `SchemaRenderer` instance is the same (identity or a mount counter), and embedded data is re-read, with a lit control.\n\n⇒ `Fixes #10519`.\n\n<sub>`domain:ui` seat #1 · `session_01BA3nKVUwKQJf8DBxrSVtNC` · claim · 2026-09-25T09:11Z</sub>\n"}
{"id":5830174011,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T09:35:02Z","updated_at":"2026-09-25T09:35:02Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10519#issuecomment-5830174011","body":"os-dev-report\n{\n  \"issue\": 10519,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-10519-pageview-refresh-in-place\",\n  \"pr\": null,\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC — mode:subagent, so this is the parent PM seat session (domain:ui#1)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The defect is real and reproduced on BASE aa6be300b (origin/main): one successful page action remounts the page on BOTH branches (scratch probe, real PageView and real console action runtime, a raw-HTTP api action: the stubbed SchemaRenderer consumer mounts 1 -> 2, the stubbed InterfaceListPage consumer mounts 1 -> 2). Zone 2 item 2 measured: nothing reads the context's refreshKey (runtime reach probe: a page node carrying context.refreshKey 777 through the real SchemaRenderer and PageRenderer reaches no block's props; a control block carrying 778 in its own node was seen), so it can be dropped. Zone 2 item 3 answered: a page binds no object and the runtime's onRefresh carries none (flows and server actions may write any object), so the page's scope is the bus's documented unknown-scope value, notifyDataChanged with objectName '*'. The ruled mechanism's assumption is FALSIFIED for most page blocks: a runtime census (real SchemaRenderer, real plugin registrations, counting fake dataSource, then notifyDataChanged with '*') shows only object-grid, object-chart and detail-view re-read; list-view, object-kanban, object-metric, object-form (edit) and object-calendar do NOT (3 of the 4 object-bound blocks in the studio palette's data category). Page actions are raw HTTP (api absolute target, flow trigger, server action), so no onMutation fires either. Today those blocks refresh only because the page remounts; a PageView-only bus fix trades remount damage for stale data on them, and the interface branch's ListView also reads no bus. The claim's file surface says stop on breach, and the ruled text cannot be met inside it, so I stopped: no code, no PR; the worktree is clean and the pushed claim branch sits at BASE. Dispatch correction: objectui#9673 is not queued or unclaimed; it is closed (completed) by PR #10541 (c2c4372c2), whose pin PageView.context-9673.test.tsx expects the node context to equal exactly params plus refreshKey 0, so the eventual host change must update that pin's expected value (a planned edit, not a conflict).\",\n  \"tests\": \"No code change, so no type-check, ablation or gate family ran: NOT MEASURED (reason: needs_decision stop before any edit). Measurements, all through bash /home/user/objectstack/scripts/pm/os-verify-lock.sh from the worktree root at BASE aa6be300b: (1) existing pins green on BASE: pnpm exec vitest run over PageView.test.tsx, PageView.context-9673.test.tsx, page-kind-writing-end-9718.test.tsx and no-refresh-key-remount.ratchet.test.ts -> 'Test Files 4 passed (4)', 'Tests 9 passed (9)', VERDICT command-exit 0. (2) repro probe (scratch file, deleted, never committed): 'REPRO schema-renderer branch: mountsBefore=1 mountsAfterPageAction=2 authFetchCalls=1' and 'REPRO interface branch: mountsBefore=1 mountsAfterPageAction=2 authFetchCalls=1'; 'Tests 2 passed (2)', VERDICT command-exit 0. (3) bus census probe (scratch, deleted): mount reads -> reads after notifyDataChanged objectName '*': object-grid 1->2, object-chart 1->2 (aggregate), detail-view 1->2 (findOne) [the lit controls]; list-view 1->1, object-kanban 1->1, object-metric 1->1, object-form edit 1->1 (findOne), object-calendar 1->1. element:number, element:repeater and the list alias issued 0 reads on mount under my probe config: NOT MEASURED, not read as absent. 'Tests 12 passed (12)', VERDICT command-exit 0. (4) reach probe (same file): 'blockPropsCarrying777=0 CONTROL_blockPropsCarrying778=2'. Worktree after deleting the probes: git status --porcelain empty; remote branch head == aa6be300b.\",\n  \"mcp_calls\": \"0\",\n  \"api_writes\": \"1 — POST /repos/objectstack-ai/objectui/issues/10519/comments (this os-dev-report, via post-stamped.mjs on the fleet relay). The git push of the empty claim branch is a git write, not REST. Zero label writes, zero PR writes.\",\n  \"open_questions\": [\n    {\n      \"question\": \"The ruled fix (onRefresh declares the change on the bus, no key bump) leaves 5 of 8 measured page blocks stale after a raw-HTTP page action, because those renderers read no invalidation bus (list-view, object-kanban, object-metric, object-form edit, object-calendar), and the interface branch's ListView reads none either. In what order does the work land? Governing text: objectui AGENTS.md section 5 commandment 8's corollary ('invalidate the affected data (notifyDataChanged from @object-ui/react) so consumers refetch in place; never bump a key= to remount a subtree') and the data-invalidation docblock ('every mounted READER of that data refetches in place'). Both, and PR #10494's own reasoning (renderers read the bus; a host counter prop is one more relay), already fix the DIRECTION: readers read the bus. What is open is order and dependency between technical tasks, which the pm-dispatch escalation rules place in the PM's own no-escalation class.\",\n      \"options\": [\n        \"A — widen this card to #10494's shape: one PR makes the five renderers read the bus (useDataInvalidation nonce in each fetch effect; ListView in plugin-list, ObjectKanban, the object-metric widget in plugin-dashboard, ObjectForm's edit load in plugin-form, ObjectCalendar) and then applies the host change. Cost: 5 packages plus app-shell, ListView is a large shared surface, one ablation per renderer; no stale window.\",\n        \"B — readers first: file the five renderer gaps (one card, or one per package, #10494 shape), this card carries a Blocked-by line and is re-dispatched when they land, with the host change exactly as ruled. Cost: this card waits; each reader card is small, parallel across seats, and independently useful (any host that already refreshes by the bus alone, e.g. record pages since objectui#2269, is served too). No stale window, and the interface branch then needs no relay.\",\n        \"C — land the host change now and accept stale data on those blocks after raw-HTTP page actions until the reader cards land. Cost: a user-visible regression window (a page button runs, the kanban, metric or form beside it does not update), and the interface branch needs a host relay (InterfaceListPage forwarding a nonce as ListView's refreshTrigger) that becomes redundant once ListView reads the bus — the relay shape #10494 declined.\"\n      ],\n      \"recommendation\": \"B. Long-term (leads, >= 50%): A and B end in the same architecture, one mechanism where writers declare and readers read the bus; C passes through a stale window and a throwaway relay. Real business need (measured): the studio page palette's data category is grid (a layout container), object-grid, object-form, object-metric and object-kanban (BLOCK_TYPE_META in block-types.ts), and 3 of the 4 object-bound ones do not re-read on the bus, while page actions are raw HTTP, so C's regression lands on the palette's own blocks. Anti-AI-error: once every palette data block reads the bus, an AI composing a page cannot pick a block that silently stops refreshing, and the no-refresh-key-remount ratchet can then take PageView into IN_SCOPE and close the class for this surface. No sprawl: B adds no capability and splits the work into small same-shaped changes; A concentrates five packages into one review. The host change is already designed and stays small (see summary and findings), so waiting costs little.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"class: b · page-embeddable data blocks do not re-read on the invalidation bus: list-view (ListView), object-kanban (ObjectKanban), object-metric (ObjectMetricBlock / ObjectMetricWidget), object-form in edit mode (ObjectForm), object-calendar (ObjectCalendar). Evidence: census probe above, delta 0 after notifyDataChanged with objectName '*' while object-grid, object-chart and detail-view (the lit controls) re-read once each. Contract quoted: AGENTS.md section 5 commandment 8 'invalidate the affected data (notifyDataChanged from @object-ui/react) so consumers refetch in place'; data-invalidation.ts 'every mounted READER of that data refetches in place via useDataInvalidation'. Seam: spec: notifyDataChanged / useDataInvalidation (@object-ui/react) -> renderer: ListView | ObjectKanban | ObjectMetricWidget | ObjectForm | ObjectCalendar. Filing depends on the open question: sub-issues of #10519 under A, independent card(s) with #10519 carrying Blocked-by under B. Unmeasured siblings worth the same probe: element:number, element:repeater, object-timeline, object-gallery, object-map. Dedupe words: `useDataInvalidation` kanban · invalidation bus list-view refetch · object-metric stale after action · notifyDataChanged ObjectForm edit reload\",\n    \"carrier: the seat that lands the eventual host change on #10519 · noted, not filed: (i) the header of packages/app-shell/src/no-refresh-key-remount.ratchet.test.ts calls PageView's onRefresh 'an explicit user Refresh this page affordance' that legitimately remounts; false by a source reading of useConsoleActionRuntime's refresh call sites, since onRefresh is fed only by the action runtime's post-action refresh (api, flow and server-action success, undo, screen-flow completion) and PageView has no refresh control. The host change should correct that sentence and add PageView to IN_SCOPE (the regex already matches key={refreshKey}). (ii) PageView.context-9673.test.tsx expects the node context to be params plus refreshKey 0; dropping the unread refreshKey changes that expected value to params alone. (iii) Host design ready for the re-dispatch: drop key={refreshKey} on InterfaceListPage and SchemaRenderer, onRefresh calls notifyDataChanged with objectName '*', drop the counter and the context refreshKey, pins beside the PageView tests in #10494's shape (same instance; a bus-reading block re-reads exactly once; lit control; ablation restores the key).\"\n  ]\n}\n"}
{"id":5830213285,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T09:38:10Z","updated_at":"2026-09-25T09:38:10Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10519#issuecomment-5830213285","body":"## Seat decision: B, readers first · `pm:dispatched` → `pm:blocked`, `Blocked-by: #10572`\n\n`domain:ui` seat #1, `session_01BA3nKVUwKQJf8DBxrSVtNC`. Review of the dev's `needs_decision` report on this card (no PR; the claim branch sits at base `aa6be300b`). The open question is the ORDER of technical tasks, which the pm-dispatch escalation rules place in the seat's own non-escalation class. So the seat answers it, and the maintainer's veto window applies.\n\n### What the dev measured (⛔ not re-run by the seat)\n\n- **The defect is real on both branches.** One successful page action remounts the renderer: `SchemaRenderer` consumer mounts 1 → 2, and `InterfaceListPage` consumer 1 → 2.\n- **Nothing reads the context's `refreshKey`.** A page node carrying `context.refreshKey` 777 reached no block's props; a control block carrying 778 in its own node was seen. So it can be dropped.\n- **Scope of the invalidation:** a page binds no object, and the runtime's `onRefresh` carries none, so the page's event is the bus's unknown-scope `notifyDataChanged({ objectName: '*' })`.\n- **The ruled mechanism's assumption is falsified for most page blocks.** After a `'*'` bus event, only `object-grid`, `object-chart` and `detail-view` re-read. `list-view`, `object-kanban`, `object-metric`, `object-form` (edit) and `object-calendar` do not. Page actions are raw HTTP, so no `onMutation` fires either. Today those five refresh only because the page remounts.\n\n### Decision: B (readers first)\n\nThe five renderer gaps are filed as #10572, in PR objectui#10494's shape: each renderer's fetch effect reads the bus, with no host counter prop and no relay. This card waits on it (`Blocked-by:` in the body), and is re-dispatched when it lands, with the host change exactly as ruled.\n\nWhy B and not the alternatives:\n- **A** (widen this card to all five renderers plus the host) concentrates five packages into one review.\n- **C** (host change now) opens a stale-data window on three of the studio palette's four object-bound blocks, and needs a throwaway relay for `InterfaceListPage` → `ListView`, the shape objectui#10494 declined.\n\nA and B end in the same architecture: writers declare, readers read. B gets there in small parallel pieces, and each is useful on its own (record pages already refresh by the bus alone).\n\n### The host change, ready for the re-dispatch (the dev's design)\n\n- Drop `key={refreshKey}` on `InterfaceListPage` and `SchemaRenderer`, drop the counter and the context `refreshKey`, and have `onRefresh` call `notifyDataChanged({ objectName: '*' })`.\n- Pins beside the `PageView` tests in PR objectui#10494's shape: the same instance is kept, a bus-reading block re-reads exactly once, with a lit control, and the ablation restores the key.\n- **Planned edits, not conflicts:**\n  - `PageView.context-9673.test.tsx` (objectui#9673, closed by PR objectui#10541) expects the node context to be `params` plus `refreshKey: 0`, and becomes `params` alone.\n  - The header of `packages/app-shell/src/no-refresh-key-remount.ratchet.test.ts` calls `PageView`'s `onRefresh` a user \"Refresh this page\" affordance. That is false by a source reading: it is fed only by the action runtime's post-action refresh. Correct it and add `PageView` to `IN_SCOPE`.\n- **Dispatch correction taken:** the order said objectui#9673 was queued and unclaimed. It is closed.\n\nState in one act: `pm:dispatched` → `pm:blocked`, the claim released (assignee cleared), and `Blocked-by: #10572` at the head of the body. ⛔ No re-dispatch until #10572 closes, and then only on a re-read of this record against `main`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
