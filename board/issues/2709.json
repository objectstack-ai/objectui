{
  "number": 2709,
  "kind": "pull_request",
  "title": "feat(fields): select+multiple → multi-value chip picker; restore fields/core lint gates",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "package: core",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-20T01:46:27Z",
  "updated_at": "2026-07-20T02:15:28Z",
  "closed_at": "2026-07-20T02:15:27Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-20T02:15:27Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2709",
  "body": "Two follow-ups from the ADR-0059 next-steps list, as two clearly-separated commits.\n\n## 1. `select` + `multiple` → multi-value chip picker (commit 1)\n\nA `select` field/param declared `multiple: true` is a multi-value selector (spec allows `multiple` on `select`), but it rendered a **single-value** dropdown that could hold only one value. `SelectField` now delegates to the multi-value chip picker (the `multiselect` widget) when `multiple` is set, storing a `string[]`.\n\n**Why delegate inside `SelectField`** (not at a type-resolution layer): every surface that renders the `select` widget inherits multi-select **identically** — the object form, the inline grid editor, and the app-shell `ActionParamDialog` — with no per-surface drift. (Special-casing it only in the param adapter would have diverged params from the form, which is exactly the trap called out when this was first scoped.) Single selects keep the cascading dropdown; multi + per-option `visibleWhen` cascading is not a combination in use today (noted in code + README).\n\n- Tests: chip toggles render (not a combobox) when `multiple`, toggle as an array, badges in readonly.\n- **Browser-verified** (real Chromium): single select renders a combobox; the multiple param renders three chip toggles, Red/Green go `aria-pressed` false→true on click. Screenshot confirms filled Red+Green chips inside the dialog. Throwaway harness removed after.\n\n## 2. Restore `@object-ui/fields` + `@object-ui/core` lint gates (commit 2)\n\nBoth package lints were **red at baseline on `main`**, so the gate could not catch new violations. Cleared every error (no behavior change):\n\n- **fields** `react-hooks/rules-of-hooks`: `ImageField` / `TextAreaField` hoist their hooks above the `readonly` early return; `index.tsx` `useFieldTranslate` no longer wraps the provider-safe `useObjectTranslation` in try/catch (wrapping a hook is the violation).\n- **core**: `no-useless-assignment` / `no-useless-escape` (×2) / `prefer-const` fixed directly; `no-control-regex` (intentional filename control-char range) and `preserve-caught-error` (package targets ES2020 whose lib types only the 1-arg `Error`, so `{ cause }` won't compile — message is inlined) carry justified scoped disables.\n\n## Checks\n\n- `@object-ui/fields` + `@object-ui/core`: **lint clean (0 errors) and type-check green** — the gates now pass, having been red before.\n- Tests: `fields` + `core` (76 files / 1436) and the select-consuming suites `app-shell` + `components` + `plugin-form` + `plugin-grid` (297 files / 2435) all green.\n\nChangeset: `@object-ui/fields` minor, `@object-ui/core` patch.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01Rg7oavKSUdb2KzYEEYrCHX\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Rg7oavKSUdb2KzYEEYrCHX)_"
}
