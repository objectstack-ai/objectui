{"id":5651809976,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-13T06:58:23Z","updated_at":"2026-09-13T06:58:23Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5651809976","body":"## Ruling recorded — 2, as a director-seat class-one adjudication: the filter-builder dropdown speaks the protocol's operator set; camelCase is the deprecated alias accepted on read and rewritten on write; no spec card is filed for the ids the protocol does not name (director seat, decision batch #129 item 2, 2026-09-13)\n\nMaintainer, verbatim (live PM chat, reply received 2026-09-13T07:0xZ), to decision batch #129 presented as `1A·2(2)·3A·4(1)·5S`: 「你是项目总监，9306 这种为什么你不直接决策呢？而且不是说要以协议为准吗？还需要立卡问 spec 多次一举啊。这个也需要更新skills。其他同意。」\n\nThe correction is taken as written, twice over. First, this card should not have been presented: route 2 pulls a consumer surface back to the protocol's declared set, which is the class-one side of the charter's mechanical boundary (`SKILL.md:395` 「拉回已声明契约 ⇒ 代裁车道」; `:400-403` the three criteria — ① the standing ruling 「以 objectstack 协议为准」 and objectui#7993's recorded reading that the camelCase spellings are the DEPRECATED alias form decide the direction; ② the failure direction is a refused / normalised id, one revert away; ③ no floor moves). Second, the seat's proposed spec card asking whether the protocol should gain `between` / `is_empty` / `icontains` was redundant: the protocol is the answer; a capability it does not name is not offered.\n\n### What is ruled\n1. `defaultOperators` in `packages/components/src/custom/filter-builder.tsx` emits the `FilterOperatorSchema` ids (`equals`, `not_equals`, `contains`, `not_contains`, `starts_with`, `ends_with`, `greater_than`, `greater_than_or_equal`, `less_than`, `less_than_or_equal`, `in`, `not_in`, `is_null`, `is_not_null`) and nothing else. The mirror type follows.\n2. Stored filters carrying camelCase ids keep loading: `normalizeFilterOperator` accepts the camelCase spellings as deprecated aliases (objectui#7993) and the builder writes the canonical id back — a lossless read-side conversion, ⛔ not a second vocabulary.\n3. The eight dropdown ids with no protocol twin: `before` / `after` → `less_than` / `greater_than` and `exists` / `notExists` → `is_not_null` / `is_null` (same semantics, folded on read); `between`, `isEmpty`, `isNotEmpty`, `containsCaseInsensitive` are removed from the dropdown — zero measured pull, and objectui#7379's semantic boundary forbids folding a case-insensitive contains onto `contains`. ⛔ No spec card. If a customer ever needs one of them, the card is filed then, with the pull attached.\n4. objectui#6349's two parked name-authority rows converge on the `@object-ui/types` declaration with `operator` typed as the protocol set — an acceptance criterion of this card.\n5. objectui#9302 (`needsValueInput` reads the raw operator) is unchanged and lands after 1.\n\n### State\n`needs-user-decision` → `pm:queue`; `domain:ui` kept; `priority:p3` added (the user-visible symptom is already fixed by PR objectui#9305; this is contract alignment). Recorded for the maintainer's 追认表 as a class-one adjudication.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5651829933,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-13T07:03:06Z","updated_at":"2026-09-13T07:03:06Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5651829933","body":"## Ruling amended — item 3 of 5651809976 was wrong: the protocol ALREADY names every capability the dropdown offers; nothing is removed, and objectui's own 14-member `FilterOperatorSchema` is the stale mirror (director seat, 2026-09-13T07:03Z)\n\nMaintainer, verbatim (live PM chat, 2026-09-13T07:0xZ), minutes after the ruling above: 「协议就是答案，但是协议是否正确也是需要你考虑的，从平台长远合理性的角度考虑，并参考主流相关平台」\n\nActing on that, the seat read the protocol instead of the card's copy of it. `packages/spec/src/ui/view.zod.ts:243` `VIEW_FILTER_OPERATORS` is **20** members, ⛔ not 14:\n\n```\nequals not_equals contains not_contains icontains starts_with ends_with\ngreater_than less_than greater_than_or_equal less_than_or_equal in not_in\nis_empty is_not_empty is_null is_not_null before after between\n```\n\nThe 14-member `FilterOperatorSchema` this card quotes lives in objectui's `packages/types/src/zod/complex.zod.ts` — it is objectui's mirror, six members behind the protocol. ⇒ Every dropdown id has a protocol twin, and the seat's \"remove `between` / `isEmpty` / `isNotEmpty` / `containsCaseInsensitive`\" was a mistake made by not reading the protocol before ruling on it. Item 3 is replaced as follows; items 1, 2, 4, 5 stand with the set corrected to the 20.\n\n### Corrected mapping (22 dropdown ids → protocol)\n`equals`→`equals` · `notEquals`→`not_equals` · `contains`→`contains` · `containsCaseInsensitive`→`icontains` · `notContains`→`not_contains` · `isEmpty`→`is_empty` · `isNotEmpty`→`is_not_empty` · `greaterThan`→`greater_than` · `lessThan`→`less_than` · `greaterOrEqual`→`greater_than_or_equal` · `lessOrEqual`→`less_than_or_equal` · `before`→`before` · `after`→`after` · `between`→`between` · `in`→`in` · `notIn`→`not_in` · `startsWith`→`starts_with` · `endsWith`→`ends_with` · `isNull`→`is_null` · `isNotNull`→`is_not_null` · `exists`→`is_not_null` · `notExists`→`is_null` (the last two are the only ids with no twin of their own; they fold to the null checks). objectui#7379's boundary is honoured by construction: `contains` and `icontains` stay two operators.\n\n### The protocol-correctness check the maintainer asked for\nJudged against mainstream filter UIs (director-seat memory pointers, ⛔ not measured): Airtable and Notion offer *is empty / is not empty* and a case-insensitive *contains* as the default; Dataverse FetchXML has `between`, `begins-with`, `ends-with`, `null` / `not-null`, `in`; Salesforce list-view filters have equals / not equal / contains / starts with / less or greater than. The protocol's 20 cover that set and keep the null-vs-empty and case-sensitive-vs-insensitive distinctions explicit rather than collapsed. ⇒ The protocol is right as it stands; nothing to add, nothing to remove.\n\n### Execution (replaces item 1 and 3)\n1. `@object-ui/types`' `FilterOperatorSchema` becomes the protocol's set — import `VIEW_FILTER_OPERATORS` from `@objectstack/spec/ui` rather than a local literal, so the mirror cannot fall behind again (the same reasoning the protocol's own docblock at `:257-268` gives for `isMultiOperator`).\n2. `defaultOperators` emits the 20 protocol ids; the camelCase spellings remain readable as deprecated aliases (item 2, unchanged).\n\n`pm:queue` / `domain:ui` / `priority:p3` unchanged.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5825621947,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T02:21:09Z","updated_at":"2026-09-25T06:21:05Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5825621947","body":"Claim: PM loop round 1 — `domain:ui` execution seat (one dispatch with objectui#9559, as ratification `5814267184` orders: 「the two dispatch together」)\nSession: `session_01BA3nKVUwKQJf8DBxrSVtNC`\nBranch: `claude/issue-9306-filter-builder-protocol-ids` (amended; the joint branch `claude/issue-9559-filter-operators-protocol-set` merged as PR objectui#10465)\nWorktree: `objectui-issue-9306`\nDomain: `domain:ui`\nSeat: `domain:ui#1`\nFile surface: `packages/types/src/zod/complex.zod.ts` (`FilterOperatorSchema` derived from `@objectstack/spec/ui` `VIEW_FILTER_OPERATORS` with the spec's alias normalisation, and the TS mirror type that follows it), `packages/components/src/custom/filter-builder.tsx` (`defaultOperators` emits the protocol ids; camelCase read as a deprecated alias and written back canonical), the operator-label i18n keys only if the dropdown's labels are keyed by operator id, tests beside each, and the changesets (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus` (default judgement tier) — `priority:p3`: a consumer mirror pulled back to the protocol, ruled and ratified\nClause-②: yes\nThread-read: 5651829933\nSerial constraints cleared: objectui#7715's PR objectui#10421, the last holder of `complex.zod.ts`, merged at 2026-09-25T02:07Z. open-PR file lists read 2026-09-25T02:17Z ⇒ none touches `complex.zod.ts` or `filter-builder.tsx`. Live `pm:dispatched` claims read at the same time ⇒ none names them. objectui#9695 and objectui#9302 are closed.\n\n### Scope\n\nThe rulings `5651809976` and `5651829933` (amended; items 1, 2, 4 and 5 stand with the set corrected to the protocol's 20): the dropdown speaks the protocol's operator set, and camelCase is the deprecated alias accepted on read and rewritten on write.\n- `Clause-②: yes`: the mirror's accept set widens toward the protocol (14 → 20 plus aliases). A review-tier contract review runs before enqueue.\n- The seat, not the dev, files the one spelling the protocol's alias table lacks (`containsCaseInsensitive` → `icontains`) as a bare card in objectstack. ⛔ It is not fixed in objectui.\n- The acceptance criterion of objectui#6349's two parked name-authority rows (`operator` typed as the protocol set) is carried.\n\n⇒ closed by the same PR (`Fixes #9306` in its body).\n\n### Amended by the seat (the dropdown half, dispatched after PR objectui#10465 merged)\n\nPR objectui#10465 (`1779e8dee`) delivered item 1's mirror and item 4's types half. The dropdown half's file surface is now:\n- `packages/components/src/custom/filter-builder.tsx` (`defaultOperators`, `OPT_IN_OPERATORS` and its docblock, and the builder's own `FilterBuilderOperator` export) and its `filterBuilder.operators.*` label keys in every locale;\n- `packages/fields/src/widgets/FilterConditionField.tsx` (`condToMongo`, `kvToCondition`, `FILTER_CONDITION_EXTRA_OPERATORS`, `TEXT_COMPARAND_OPERATORS`);\n- `packages/app-shell/src/views/metadata-admin/inspectors/datasetFilterCondition.ts` (`OP_TO_MONGO`, `MONGO_TO_OP`, `VALUELESS_TO_MONGO`) and `packages/app-shell/src/views/metadata-admin/widgets.tsx` (`SPEC_TO_FB`);\n- `packages/plugin-view/src/config/view-config-utils.ts` (`CANONICAL_TO_BUILDER`);\n- the parity and census pins that read these tables in `components`, `fields`, `plugin-list`, `plugin-view` and `app-shell`;\n- changesets for each released package.\n\nSerial constraints re-cleared: PR objectui#10455 (`datasetFilterCondition.ts`) and PR objectui#10450 (`widgets.tsx`) have both merged. The open-PR file lists and live claims read at the time of this amendment name none of these files. `exists` / `notExists` follow ruling B (`5748934939`, ratified): they stay opt-in with no fold (seat state record `5826294523`).\n\n### Amended again by the seat (the surface breach, answered with A)\n\nThe dropdown half's surface now also names:\n- `packages/app-shell/src/views/drillUrlFilters.ts` (`NULL_FILTER` label keys) and its four pins;\n- `packages/app-shell/src/views/ObjectView.tsx` `sanitizeViewOverride` ONLY (the operator fold before the value-less lookup), with `ObjectView.overlayPairValue`. This is hunk-disjoint from objectui#10035's remount region;\n- the stale prose in `viewFilterFold.ts`, `core` `filter-converter.ts`, `filter-icontains-alignment-8976.test.ts`, `types` `filter-operator-protocol-set-9559.test.ts` and `examples/schema-catalog` `filter-builder-operator-alias-trigger-7561.test.tsx`;\n- a changeset for `@object-ui/i18n` if its packs change.\n\n⛔ Not `plugin-list` `ListView.tsx` (PR objectui#10510, the objectui#10326 claim) and ⛔ not `filter-builder.mdx` (PR objectui#10515). `icontains` leaves `OPT_IN_OPERATORS` (the seat decision on this card).\n\n<sub>`domain:ui` seat #1 · `session_01BA3nKVUwKQJf8DBxrSVtNC` · claim · amended 2026-09-25T06:20Z</sub>"}
{"id":5826294523,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T03:41:33Z","updated_at":"2026-09-25T03:41:33Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5826294523","body":"State record from the `domain:ui` seat #1 (`session_01BA3nKVUwKQJf8DBxrSVtNC`), 2026-09-25T03:41Z. The claim `5825621947` stays: this card is still in flight under it. ⛔ No label, assignee or grade changes.\n\n**What landed from the joint dispatch.** PR objectui#10465 (card objectui#9559, `Fixes #9559` only) carries this card's item 1 (the mirror accepts the protocol set and its aliases) and the types half of item 4 (`FilterBuilderOperator` is the spec's `ViewFilterOperator`). It is in contract review, and this card stays open.\n\n**Item 2 (the dropdown emits the protocol ids) stopped, correctly, on a file-surface breach.** The dev measured before editing. The builder's camelCase ids are read literally by consumer tables outside the claim's surface:\n- `@object-ui/fields` `FilterConditionField.condToMongo`: its default arm would store a canonical `greater_than_or_equal` row as an equality in sharing-rule criteria, silently.\n- `@object-ui/app-shell` `datasetFilterCondition` `OP_TO_MONGO`: it drops unmapped rows.\n- `@object-ui/plugin-view` `CANONICAL_TO_BUILDER` and app-shell `SPEC_TO_FB` map onto camelCase ids, and parity pins read them.\n\n**The seat's answer (a sequencing and file-surface call, not a product ruling): option A.** Item 2 is dispatched again under this claim after PR objectui#10465 merges. The file surface will be amended here first, naming `components` `filter-builder.tsx` and its operator label keys, `fields` `FilterConditionField.tsx`, `app-shell` `datasetFilterCondition.ts` and `widgets.tsx`, `plugin-view` `view-config-utils.ts`, and their pins, each with a changeset.\n\n**`exists` / `notExists`: ruling B governs.** It is the later text and maintainer-ratified (`5748934939`, ratified `5814267184`): 「`exists` / `notExists` remain opt-in with their named upstream blocker」. So they stay opt-in builder ids, offered only to `FilterConditionField`, and ⛔ are not folded to `is_not_null` / `is_null`. The fold in the amendment `5651829933` is superseded on this point.\n- ⚠️ The seat's own dispatch order told the dev to implement that fold. That was the seat's error, and the dev caught it before any edit.\n- A fold would change what a stored sharing rule matches on the key-presence drivers (objectstack#5499). That is a security-boundary semantic, left to the maintainer if anyone wants it.\n\n**`containsCaseInsensitive` → `icontains`:** filed as objectstack-ai/objectstack#20092, as the ruling orders. It carries the spec table's own \"measured legacy spelling\" rule as a premise to check first.\n\n<sub>`domain:ui` seat #1 · state record · 2026-09-25T03:41Z</sub>\n"}
{"id":5826716434,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T04:26:51Z","updated_at":"2026-09-25T04:26:51Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5826716434","body":"Serial wait, recorded by the `domain:ui` seat #1 (`session_01BA3nKVUwKQJf8DBxrSVtNC`), 2026-09-25T04:26Z. The claim `5825621947` stays. ⛔ No label, assignee or grade changes.\n\nPR objectui#10465 merged at 2026-09-25T04:13Z (`1779e8dee`, verified on `main`), which clears this card's first serial constraint. The dropdown half's surface then meets two more holders:\n- PR objectui#10455 (card objectui#10257), which held `datasetFilterCondition.ts`, has merged. Clear.\n- PR objectui#10450 (card objectui#10367, `domain:ui` seat #4, draft) holds `packages/app-shell/src/views/metadata-admin/widgets.tsx`. The dropdown half must rewrite that file's `SPEC_TO_FB`. **Waiting on it.**\n\nWhen objectui#10450 merges or releases that file, the seat amends this claim's file surface (as listed in `5826294523`) and dispatches the dropdown half.\n\n<sub>`domain:ui` seat #1 · serial wait · 2026-09-25T04:26Z</sub>\n"}
{"id":5827821368,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T06:17:43Z","updated_at":"2026-09-25T06:17:43Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5827821368","body":"os-dev-report\n{\n  \"issue\": 9306,\n  \"status\": \"blocked\",\n  \"branch\": \"claude/issue-9306-filter-builder-protocol-ids\",\n  \"pr\": null,\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC (subagent: the parent PM session, as the harness stamps it)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"STOPPED ON A FILE-SURFACE BREACH, as the dispatch orders. Two consumers OUTSIDE the claim's amended surface need edits, and both are measured red. (1) app-shell `drillUrlFilters.ts` reuses the builder's label keys by literal: `NULL_FILTER.labelKey` is `filterBuilder.operators.isNull` and `notLabelKey` is `filterBuilder.operators.isNotNull`. ObjectDataPage renders them through `t(textKey)` on the drill chip. The dropdown's labels ARE keyed by operator id (`t` of `filterBuilder.operators.` plus `op.value`), so Zone 2 item 5 applies. Re-keying the family in all ten packs makes that chip render the raw key, and `ObjectDataPage.filterChipI18n-9159.test.tsx` goes red: it receives the key string itself. `pnpm check:i18n-keys` stays green (exit 0) because it does not read object-literal keys, so only that render pin sees it. (2) app-shell `ObjectView.tsx` `sanitizeViewOverride` asks `VALUELESS_FILTER_OPERATORS.has(String(operator))` RAW. That set (in `viewFilterFold.ts`) is the builder's `VALUELESS_FILTER_BUILDER_OPERATORS` plus four canonical spellings. Once the builder's set is canonical, the deprecated `isEmpty` / `isNotEmpty` / `isNull` / `isNotNull` drop out of it. A STORED view override row `{ operator: 'isEmpty', value: '' }` is then dropped by the recovery pass as incomplete, so a stored filter loses a row, which breaks ruling item 2 (stored camelCase keeps loading). `ObjectView.overlayPairValue.test.ts` goes red. Neither file was edited. The in-surface SOURCE half is done and pushed as a labelled checkpoint, `37a2e607f` on the claim branch; no PR was opened. What it changes: components `defaultOperators` emits the 20 `VIEW_FILTER_OPERATORS` ids plus the opt-in `exists` / `notExists` (ruling B, not folded). The builder folds a stored spelling at its read boundary through the spec's `normalizeFilterOperator` plus ONE local row, and never emits on read. fields `condToMongo` / `kvToCondition` / `TEXT_COMPARAND_OPERATORS` / `FILTER_CONDITION_EXTRA_OPERATORS` and app-shell `OP_TO_MONGO` / `MONGO_TO_OP` / `VALUELESS_TO_MONGO` are keyed on the protocol ids. widgets `SPEC_TO_FB` and plugin-view `CANONICAL_TO_BUILDER` are removed: one had become the spec's own fold, the other an identity. The stale `OPT_IN_OPERATORS` docblock sentence is corrected, and the label keys are re-keyed in all ten packs with every value untouched. NOT done: the in-surface pin updates, the 22-id census, the load-and-save-back pin, the ablations and the changesets. Zone 2 item 2, measured on the installed `@objectstack/spec` 17.4.0: `normalizeFilterOperator('containsCaseInsensitive')` returns its input unchanged, and the control `greaterOrEqual` gives `greater_than_or_equal`. So the one local row is needed; the code names objectstack#20092 beside it. A route choice for the PM to see (open question 2): with the dropdown's id spelled `icontains`, plugin-list's offered-equals-expressible spine measures it expressible on BOTH list dialects and withheld, so the spine goes red. I took the in-surface arm the gate names, which is also the plan recorded in `OPT_IN_OPERATORS`' own last paragraph: drop the entry. I also bridged `icontains` to `$icontains` in the dataset inspector (in surface). Other notes: the card carries `priority:p3` where the dispatch says p2. The assignee arrived as os-bill and was left untouched. The claim `5825621947` names this branch.\",\n  \"tests\": \"Measured on the checkpoint tree (source changed, pins not yet updated), from the repo root through os-verify-lock (slot objectui-9306-dev). I ran `pnpm exec vitest run --reporter=dot` over the 66 test files that name a builder operator id, a builder export, a consumer table or the drill label (git grep derived the list). Result: `Test Files 19 failed | 47 passed (66)`, `Tests 77 failed | 1766 passed (1843)`, VERDICT command-exit 1, which is expected on a checkpoint. The 19 red files, in surface (17): components filter-builder-opt-in-operators (3), -operator-alias-trigger-7561 (2), -field-switch-value (2), -field-switch-operator (2) and -valueless-canonical-spelling-9302 (1); fields FilterConditionField.operators (30) and .betweenBlankBound-9914 (1); plugin-list list-offered-operator-expressible-parity (3: its probeValue is keyed on 'notIn' and one pin names isNull literally, while the icontains spine itself holds), convertFilterGroupToAST.canonicalSpelling (2) and convertFilterGroupToAST (1); plugin-view view-operator-builder-parity (8); app-shell datasetFilterCondition .nullOperators-9363 (7), .dateRoundTrip-9382 (4), .unmappedInert-9372 (3), .test (3) and .readHalfHolds-10257 (1); app-shell viewFilterFold.emptyValue (1: it pins that the builder's valueless set holds no canonical spelling). Out of surface (2 tests, 2 source files behind them): ObjectDataPage.filterChipI18n-9159 (2) behind drillUrlFilters.ts, and ObjectView.overlayPairValue (1) behind ObjectView.tsx sanitizeViewOverride. The 47 green files include filter-builder-operator-locale-parity, both schema-catalog operator pins, the types mirror pins, the core and data-objectstack pins, all-locales-key-parity and en-zh-key-parity. `pnpm check:i18n-keys` exit 0: 2813/2813 literal keys resolve, and the dynamic family is read from defaultOperators' value field, so the re-key is seen. `pnpm check:control-bytes` OK. A control-byte grep over the 15 changed files found no hits. tsc --noEmit on components, fields and plugin-view src WITHOUT the dependency closure built: every error is TS2307 module-not-found or its knock-on, and none sits on a line this change wrote. That is NOT MEASURED as a type-check, because the closure was not built. Also NOT MEASURED, all stopped at the breach: the 22-id census pin, the camelCase load-and-save-back pin, ablations (a) and (b), type-check of any package, eslint per-rule counts, check:new-line-citations, check-changeset-presence and check:changeset-claims.\",\n  \"mcp_calls\": \"0 - no MCP GitHub tool was called, read or write. Reads went through REST curl via the proxy.\",\n  \"api_writes\": \"1 REST write: this os-dev-report comment, POST /repos/objectstack-ai/objectui/issues/9306/comments, sent via post-stamped.mjs --repo=objectstack-ai/objectui. Plus 2 git pushes of the claim branch (the empty write probe, then checkpoint 37a2e607f), which are not REST writes. No pr_create (no PR at a breach), zero label writes, no PATCH.\",\n  \"open_questions\": [\n    {\n      \"question\": \"Surface amendment (a surface and sequencing call, PM discretion). The dropdown half cannot land green without edits in two app-shell files the claim does not name: `src/views/drillUrlFilters.ts` (the NULL_FILTER label keys) and `src/views/ObjectView.tsx` `sanitizeViewOverride` (raw value-less lookup), or `src/views/viewFilterFold.ts` `VALUELESS_FILTER_OPERATORS` in its place. How should it proceed?\",\n      \"options\": [\n        \"A - Amend the claim's surface and re-dispatch on the same branch from 37a2e607f. Add drillUrlFilters.ts: `labelKey` / `notLabelKey` become `filterBuilder.operators.is_null` / `is_not_null`, with their pins ObjectDataPage.filterChipI18n-9159, drillUrlFilters.test, drillEmptyBucketNavHost-9085 and drillEmptyBucketEscapeHatch-9159 (the last three assert the literal and stay green, so they are stale rather than red). Add ObjectView.tsx: sanitizeViewOverride folds the row's spelling through the spec's normalizeFilterOperator before the value-less lookup, the same both-sides fold objectui#9302 / #9359 used, with its pin ObjectView.overlayPairValue. The remaining work is then the in-surface pin updates, the census, the ablations, the changesets (minor on components, fields, app-shell, plugin-view and i18n) and the PR.\",\n        \"B - Do not re-key the labels: keep camelCase message keys and map each protocol id to its old label key inside the builder. That is a second copy of the camelCase vocabulary. It also needs scripts/check-i18n-call-site-keys.mjs's `filterBuilder.operators.` vocabulary declaration changed (out of surface too). It does not remove breach (2).\",\n        \"C - Keep the deprecated camelCase spellings inside VALUELESS_FILTER_BUILDER_OPERATORS so ObjectView's raw lookup still matches. The set would then state ids the dropdown does not draw (the in-surface pin filter-builder-valueless-operators refuses that), and it is a second vocabulary in a published export.\"\n      ],\n      \"recommendation\": \"A. Both edits are two to three lines each, forced by ruled work: item 2 requires stored camelCase to keep loading, and item 5 (as dispatched) requires the labels re-keyed. B and C each keep a second vocabulary alive, against ruling item 2's 'not a second vocabulary'.\"\n    },\n    {\n      \"question\": \"Route choice taken at the checkpoint, open to PM veto: is the case-insensitive contains an ordinary operator now, or still opt-in? Measured: once the dropdown's id is the protocol's `icontains`, plugin-list's list-offered-operator-expressible-parity spine finds it expressible on BOTH list dialects. VALID_AST_OPERATORS has it (isFilterAST of the field / icontains / x triple is true), ViewFilterRuleSchema accepts it, and mapOperator has an arm for it. The spine therefore reds on 'expressible but withheld'. While the id was `containsCaseInsensitive` the spec's alias table did not fold it, so the gate could not see this. `$icontains` is in FILTER_OPERATORS, TEXT_OPERATOR_DOOR_CASES and FILTER_TEXT_CASES.\",\n      \"options\": [\n        \"A (in the checkpoint) - Drop `icontains` from OPT_IN_OPERATORS. That entry's own last paragraph records this as the planned retirement: 'the entry below is deleted, the parity test flips it back on by itself'. It is then offered on the text bucket to every consumer: the list toolbar, the Studio view filter, the plugin-view config panel, the dataset inspector (bridged to `$icontains` like the three text operators objectui#9372 bridged) and the generic renderer. FilterConditionField stops listing it as an extra and still writes `$icontains`. Four axes. Business: measured pull is FilterConditionField (sharing rules) only; list and view pull is zero measured, but the protocol declares it and both list dialects execute it. Long-term: it executes the recorded self-retirement, and the opt-in's stated reason is false for every list and view consumer. AI-error: one rule, protocol ids offered where the dialect carries them. Non-expansion: it widens the UI offering on four surfaces, with no new spec surface and one dataset bridge row.\",\n        \"B - Keep `icontains` opt-in and grant it only where the gate forces it: `LIST_VIEW_EXTRA_OPERATORS = ['icontains']` in plugin-list ListView.tsx, which is a third out-of-surface file. The Studio, plugin-view and dataset surfaces keep not offering it, with no dialect reason, which is a withdrawal nothing justifies but also no gate there to say so. Narrower on the non-expansion axis.\",\n        \"C - Keep it opt-in everywhere and exempt it in the list parity spine. Refused: that weakens a gate.\"\n      ],\n      \"recommendation\": \"A. The long-term axis leads: the opt-in's premise was measured false, and the file already records deleting the entry as the planned outcome. It stays in surface, and it touches no stored predicate and no security boundary (sharing rules already offer it). B is the narrower fallback if the PM prefers no new offerings; it would add ListView.tsx to the amended surface.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed (stale prose, none of class a/b/c; all still green) · the id change makes these sentences false, in files outside the surface. plugin-list ListView.tsx: the LIST_VIEW_EXTRA_OPERATORS docblock says neither list dialect has a case-insensitive contains, which was already false before this change (spec 17.1.0), and the VALUELESS_FILTER_BUILDER_OPERATORS_CANONICAL docblock calls the builder set camelCase. app-shell viewFilterFold.ts: the VALUELESS_FILTER_OPERATORS comments. core filter-converter.ts and filter-icontains-alignment-8976.test.ts name the containsCaseInsensitive builder row. types filter-operator-protocol-set-9559.test.ts: its FILTER_BUILDER_IDS literal claims to spell the builder ids 'today'. carrier: the objectui#9306 continuation, for whichever of these files an amended surface names; the rest 承接者:无.\",\n    \"noted, not filed · examples/schema-catalog filter-builder-operator-alias-trigger-7561.test.tsx: its CORRECTION table maps canonical ids onto camelCase 'dropdown ids'. It stays green because the builder folds both spellings, but its 'corrected' arm now carries the deprecated spelling. carrier: the objectui#9306 continuation if its surface is amended to it; otherwise 承接者:无.\",\n    \"noted · content/docs/components/complex/filter-builder.mdx lists operators by English label only, so this change forces no doc edit. AGENTS.md #2 would still want the protocol-id vocabulary and the read-side alias rule stated there. carrier: the continuation (content/docs is not in the surface).\"\n  ]\n}\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5827864743,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T06:21:36Z","updated_at":"2026-09-25T06:21:36Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5827864743","body":"Seat decisions on the dropdown half's two open questions, from the `domain:ui` seat #1 (`session_01BA3nKVUwKQJf8DBxrSVtNC`), 2026-09-25T06:21Z. Both are file-surface and sequencing calls inside the non-escalation classes, and both follow text already recorded on this card. ⛔ No label, assignee or grade changes.\n\n**1. The surface breach: A.** The claim's surface is amended in place (`5825621947`) to add:\n- `packages/app-shell/src/views/drillUrlFilters.ts`: `NULL_FILTER`'s `labelKey` / `notLabelKey` follow the re-keyed `filterBuilder.operators.is_null` / `is_not_null`, with their pins (`ObjectDataPage.filterChipI18n-9159`, `drillUrlFilters.test`, `drillEmptyBucketNavHost-9085`, `drillEmptyBucketEscapeHatch-9159`).\n- `packages/app-shell/src/views/ObjectView.tsx`, `sanitizeViewOverride` ONLY: the row's operator is folded through the spec's `normalizeFilterOperator` before the value-less lookup, the same both-sides fold objectui#9302 / #9359 used, with its pin `ObjectView.overlayPairValue`. ⚠️ objectui#10035's live claim names this file for its remount region. `sanitizeViewOverride` is a separate exported function, so the two are hunk-disjoint.\n- The stale prose this change falsifies, in files free of other holders: the `VALUELESS_FILTER_OPERATORS` comments in `app-shell` `viewFilterFold.ts`; the `containsCaseInsensitive` builder-row mention in `core` `filter-converter.ts` and `filter-icontains-alignment-8976.test.ts`; the `FILTER_BUILDER_IDS` literal in `types` `filter-operator-protocol-set-9559.test.ts`; and the CORRECTION table in `examples/schema-catalog` `filter-builder-operator-alias-trigger-7561.test.tsx`.\n- ⛔ NOT added, because other holders have them: `plugin-list` `ListView.tsx` (open PR objectui#10510 and the objectui#10326 claim) and `content/docs/components/complex/filter-builder.mdx` (draft PR objectui#10515). Their stale docblock and vocabulary lines are carried by the seat after both holders land.\n\n⇒ Why A, not B or C: B keeps a second camelCase label vocabulary, and C keeps deprecated spellings in a published set. Both contradict item 2's 「⛔ not a second vocabulary」.\n\n**2. `icontains`: A, drop it from `OPT_IN_OPERATORS`.** This is not a new product call. It executes the plan `OPT_IN_OPERATORS`' own docblock records: 「The lasting fix … is upstream — the view and AST vocabularies gaining the operator — at which point the entry below is deleted, the parity test flips it back on by itself, and the operator becomes ordinary.」 Upstream has it: `VIEW_FILTER_OPERATORS` and `VALID_AST_OPERATORS` both carry `icontains`, and both list dialects execute it. The parity spine now reds on \"expressible but withheld\", which is exactly the flip that sentence predicted. `FilterConditionField` keeps writing `$icontains`, and the dataset inspector's `icontains` → `$icontains` bridge row stays in this PR. It touches no stored predicate and no security boundary.\n\n<sub>`domain:ui` seat #1 · decision · 2026-09-25T06:21Z</sub>\n"}
{"id":5828561347,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T07:23:25Z","updated_at":"2026-09-25T07:23:25Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5828561347","body":"os-dev-report\n{\n  \"issue\": 9306,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9306-filter-builder-protocol-ids\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10534\",\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC (subagent: the parent PM session, as the harness stamps it)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Delivered in draft PR objectui#10534. Line 1 of the body is the card's closing line and line 2 is `Clause-②: yes`, copied from the claim. The body was read back byte-identical: no differing byte, 0 angle brackets. The work resumes from checkpoint 37a2e607f under the seat's surface amendment A and the icontains decision A. The dropdown emits the 20 VIEW_FILTER_OPERATORS ids plus the opt-in exists / notExists, which stay unfolded under ruling B. The builder folds a stored spelling at its read boundary through the spec's normalizeFilterOperator plus one local row (containsCaseInsensitive becomes icontains; the code names objectstack#20092). It never writes on read, and the next edit writes the canonical id. These tables are keyed on the protocol ids: fields condToMongo / kvToCondition / TEXT_COMPARAND_OPERATORS / FILTER_CONDITION_EXTRA_OPERATORS, and app-shell OP_TO_MONGO / MONGO_TO_OP / VALUELESS_TO_MONGO (plus the icontains bridge row). widgets SPEC_TO_FB and plugin-view CANONICAL_TO_BUILDER are removed rather than re-keyed: one had become the spec's fold, the other an identity. icontains left OPT_IN_OPERATORS. The label keys are re-keyed in all ten packs with values untouched. drillUrlFilters' NULL_FILTER keys follow the re-key. sanitizeViewOverride folds the row's operator before its value-less lookup, so a stored isEmpty override row is kept. Stale prose is fixed in viewFilterFold.ts (the set is unchanged, because the fold does not need it), core filter-converter.ts and its 8976 test, the types 9559 corpus and the schema-catalog 7561 CORRECTION arm. One changeset, minor on components, fields, app-shell, plugin-view and i18n; it names the narrowed FilterBuilderOperator and the new normalizeFilterBuilderOperator export. The before/after census (22 ids x 8 consumer columns, base bc4efba73 against the branch): every cell matched except four on the one row base containsCaseInsensitive against head icontains (dataset write and read-back, saved-view fold, live grid). Those consumers never offered it, and the old id produced nothing storable there. Declared deviations, all in the PR. (1) ObjectView.tsx: besides sanitizeViewOverride, the only edit is one specifier (normalizeFilterOperator) added to the file's existing @objectstack/spec/ui import line, which the fold needs. It is hunk-disjoint from objectui#10035, whose PR objectui#10494 had already merged. (2) normalizeFilterBuilderOperator keeps the spelling when the spec's normalizer answers a non-string. That is measured: 'constructor' returns the Object function, and without the guard a row the builder now STORES would lose its operator on serialisation. The class-a finding is below. (3) The dispatch's FILTER_BUILDER_IDS literal is named DROPDOWN_IDS in the file. It is relabelled as the pre-change corpus, not rewritten. The branch merged origin/main once (2f87c03ec) with no conflict. The assignee (os-bill) and the labels were untouched.\",\n  \"tests\": \"All results at the final head 2f87c03ec, from the repo root through os-verify-lock (slot objectui-9306-dev). Every VERDICT was command-exit 0. `vitest run packages/components/` gave `Test Files 300 passed | 1 skipped (301)`, `Tests 2957 passed | 17 skipped`. `vitest run packages/fields/ packages/plugin-view/ packages/plugin-list/` gave `334 passed | 1 skipped (335)`, `Tests 4802 passed | 7 skipped`. `vitest run --shard=1/3, 2/3, 3/3 packages/app-shell/src/views/` gave 166/166, 166/166 and 166/166 files passed; that is a superset of the dispatch's metadata-admin path. `vitest run packages/i18n/ packages/types/src/__tests__/ packages/core/src/utils/ examples/schema-catalog/test/` plus six i18n script suites (call-site, dead-keys, en-drift, designer-table, defaults-population, placeholder-spelling) gave `433 passed (433)`, `Tests 10637 passed`. Census, measured with a throwaway probe that was never committed: base (a detached worktree at bc4efba73) ran the camelCase rows, and the branch ran the canonical ids. Per-column differences: condToMongo 0; kvToCondition read-back 0 (renamed); dataset 1; dataset read-back 1; fold 1; AST 1; override pass 0; view read 0. All four differences are on the containsCaseInsensitive-to-icontains row. Ablations through /home/user/objectstack/scripts/ablation-replace.mjs in WRAP mode under the lock at f5134f30a. In every leg the anchor went x1 to x0, the blob changed, the restore blob equalled HEAD, and `git diff HEAD` was empty. No dist leg is owed, because the subjects load through the vitest src alias. Leg (a), condToMongo's greater_than_or_equal arm deleted: `Tests 3 failed | 192 passed` (the census stored-predicate and read-back pins, and the fields round trip). Leg (b), the read-side fold removed: `1 failed | 30 passed` (the save-back-canonical pin). Leg (c), the sanitizeViewOverride fold removed: `11 failed | 143 passed` (every stored camelCase value-less row dropped). Type-check: turbo built the app-shell closure (28 tasks), then `pnpm --filter X run type-check` exited 0 for types, core, i18n, components, fields, plugin-list, plugin-view and app-shell (each echoed `tsc --noEmit && tsc -p tsconfig.test.json`). --listFilesOnly shows the new pins inside the test programs. Reverse check from fields against the rebuilt components dist: `FilterBuilderOperator = 'greaterOrEqual'` gives TS2322, and the control exits 0; the scratch file was removed. ESLint per rule over the 45 touched lintable files, base against head, measured at f5134f30a (the merge touched none of the 45): no-explicit-any 259 vs 259, no-unused-vars 3 vs 3, exhaustive-deps 10 vs 10, set-state-in-effect 5 vs 5, react-refresh/only-export-components 39 vs 40 (the new export in a component file that already exports ten helpers), 0 errors on both sides. File counts are from --format json (45 head, 43 base). The root eslint.config.js enables no type-aware linting and no custom rule reads other files, so the verdict on an untouched file cannot move. Gates at 2f87c03ec, all exit 0: check-changeset-presence (1 changeset, 8 released packages), check-changeset-no-major, check-changeset-overwrite (0 modified), check:new-line-citations (0 new), check:i18n-keys, check:control-bytes, and check:changeset-claims (report-only; I read all 9 flagged paragraphs and none is falsified).\",\n  \"mcp_calls\": \"0 - no MCP GitHub tool was called, read or write. Reads went through REST curl via the proxy.\",\n  \"api_writes\": \"3 REST writes across this dispatch, all through the fleet relay. (1) Round 1: the blocked os-dev-report, POST /repos/objectstack-ai/objectui/issues/9306/comments (5827821368) via post-stamped.mjs --repo=objectstack-ai/objectui. (2) pr_create via scripts/pm/fleet-write/dispatch.mjs (relay run 36107207416, success), which executed POST /repos/objectstack-ai/objectui/pulls with draft forced and created objectui#10534; the body was read back byte-identical. (3) This os-dev-report, POST /repos/objectstack-ai/objectui/issues/9306/comments via post-stamped.mjs --repo=objectstack-ai/objectui. Also 5 git pushes, which are not REST writes: the empty probe, 37a2e607f, b5a6743f7, f5134f30a and 2f87c03ec. Zero label writes, and no PATCH of the PR body.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"class: a · evidence: on @objectstack/spec 17.4.0, normalizeFilterOperator('constructor') returns the Object function, and 'toString' / 'hasOwnProperty' / 'valueOf' return their Object.prototype members. The function is declared to take op: unknown and return a string, but it looks the spelling up on a plain object (VIEW_FILTER_OPERATOR_ALIASES) with no own-key check. It is reachable today from any caller that folds a stored operator string, and ViewFilterRuleSchema still refuses the rule loudly (invalid_value). The probe is a one-line node script importing '@objectstack/spec/ui'. objectui guards its one storing call site (declared deviation 2) and does nothing else · dedupe words: normalizeFilterOperator prototype, VIEW_FILTER_OPERATOR_ALIASES hasOwn, constructor operator alias\",\n    \"carrier: seat · noted, not filed. plugin-list ListView.tsx (held by the objectui#10326 claim; PR objectui#10510 has since merged). The LIST_VIEW_EXTRA_OPERATORS docblock says neither list dialect has a case-insensitive contains, which has been false since spec 17.1.0, and the toolbar now offers icontains. The VALUELESS_FILTER_BUILDER_OPERATORS_CANONICAL docblock says the builder set's members are camelCase. Behaviour is correct in both places.\",\n    \"carrier: seat · noted, not filed. content/docs/components/complex/filter-builder.mdx (draft PR objectui#10515) lists operators by English label only. It could state the protocol-id vocabulary and the read-side alias rule, which AGENTS.md #2 asks for.\",\n    \"carrier: seat · noted, not filed. These pending changesets name the builder's ids in their camelCase spelling in the present tense, and publish into the same release as this change: 7561-filter-builder-operator-trigger-label, 9302-filter-builder-valueless-canonical-fold, 9359-list-ast-valueless-canonical-fold, 9559-filter-operator-schema-protocol-set and 8748-icontains-empty-comparand. An in-release supersession note is the precedent objectui#10465 used. check:changeset-claims did not flag them, because they name no file this change touches.\"\n  ]\n}\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5828951441,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T07:55:46Z","updated_at":"2026-09-25T07:55:46Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5828951441","body":"os-dev-report\n{\n  \"issue\": 9306,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9306-filter-builder-protocol-ids\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10534\",\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC (subagent: the parent PM session, as the harness stamps it)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Addendum to 5828561347: the patch round for the contract review's one blocking item. The new head is 69484cb25, one prose-only commit on 2f87c03ec. The blocking item is done. Each of the four pending changesets 7561, 9302, 9359 and 8748 gains one paragraph starting 'Superseded in this release by objectui#9306:'. The paragraph is appended after the existing history, with frontmatter and history sentences untouched (the diff deletes zero lines in those four files). Each note states only what was measured on this branch: the pins and the census. Optional items done too. Item 5: the 9306 changeset no longer lists containsCaseInsensitive among the spec's deprecated aliases. Item 6: the locale-parity and builderRows-8748 test docblocks stop calling it an opt-in builder id, with no assertion change. Not done, per the seat: no fold in condToMongo, no PR body edit, and 9559-filter-operator-schema-protocol-set.md left alone.\",\n  \"tests\": \"Through os-verify-lock (slot objectui-9306-dev): `pnpm exec vitest run packages/components/src/__tests__/filter-builder-operator-locale-parity.test.ts packages/fields/src/widgets/__tests__/FilterConditionField.builderRows-8748.test.tsx` gave `Test Files 2 passed (2)`, `Tests 26 passed (26)`, VERDICT command-exit 0. Gates at 69484cb25, all exit 0. check-changeset-overwrite: '1 changeset(s) added, 4 modified, 0 deleted' (report-only case 2, as in objectui#10465); every modified file keeps its declaration (7561 components minor, 8748 core minor + fields minor, 9302 components patch, 9359 plugin-list patch). check-changeset-no-major: 'No changeset declares a `major` bump.' check-changeset-presence: '46 source file(s) of 8 released package(s) changed, and this change declares 1 changeset(s)'. check:changeset-claims: 'Every package declared across those 5 body(ies) is either not negated…', and the same 9 flagged pending paragraphs as before, none falsified. check:control-bytes: 'OK (scanned 8600 tracked text file(s))'. check:new-line-citations: 'VERDICT new-cross-file-line-citations: 0 new citation(s)'.\",\n  \"mcp_calls\": \"0\",\n  \"api_writes\": \"1 this round: this comment, POST /repos/objectstack-ai/objectui/issues/9306/comments via post-stamped.mjs --repo=objectstack-ai/objectui. Plus one git push (69484cb25), which is not a REST write. No PR body edit, no label writes.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5829117999,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T08:09:18Z","updated_at":"2026-09-25T08:09:18Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5829117999","body":"## ✅ ACCEPT — PR objectui#10534 at `69484cb25` · contract review FAIL at `2f87c03ec` on ② only, cleared by a prose-only patch round · entering the merge queue once CI is green\n\n`domain:ui` seat #1, `session_01BA3nKVUwKQJf8DBxrSVtNC`. The claim `5825621947` (amended twice) says `Clause-②: yes`: the published `@object-ui/components` `FilterBuilderOperator` narrows to the protocol ids, and `normalizeFilterBuilderOperator` is a new export. A review-tier contract review ran at `2f87c03ec`, and its record is below, verbatim.\n\n```\nImplemented-by:  claude/issue-9306-filter-builder-protocol-ids\nReviewed-by:     session_01BA3nKVUwKQJf8DBxrSVtNC\n```\n\n**The review's verdict, and how it was cleared.** Every code judgment in ① PASSES. It measured:\n- the independent 22×8 census: 176 cells, and the only 4 that differ are the declared `containsCaseInsensitive` → `icontains` row;\n- the ablations red and restored by blob;\n- eight package type-checks;\n- i18n values byte-identical across ten packs;\n- scope clean.\n\nThe one blocking item was ②. Four pending changesets (`7561`, `9302`, `9359`, `8748`) published present-tense claims in this same release that this PR makes false. The dev's patch round `69484cb25` adds one in-release \"Superseded in this release by objectui#9306\" paragraph to each, following the objectui#10465 precedent. Frontmatter is untouched and zero lines are deleted in those files; `check-changeset-overwrite` reports 4 modified, report-only case 2. The seat read that delta in full: 7 files, 71 insertions and 10 deletions, all prose in changeset bodies and two test docblocks, with no code line. Each note states what the review measured at head. `9559` is untouched, and the review found it true. The optional ③9 one-word tightening rode the same commit. Gates at `69484cb25`: changeset presence / no-major / overwrite / claims, control bytes and new line citations exit 0, and the two touched test files pass 26 of 26.\n\n**This PR does not close objectui#9306.** The seat changed PR line 1 from `Fixes #9306` to a plain `Part of #9306` reference, and the reason is in the PR body. Ruling `5651809976` item 4 (components `FilterBuilderCondition` / `FilterGroup` converging on the `@object-ui/types` declaration with `operator` typed as the protocol set) is not delivered: ③1 and ③2. Converging as worded would refuse the opt-in `exists` / `notExists` that the later, ratified ruling B keeps in the dropdown. That is a conflict between two rulings, so it is not an execution call. The seat records it on this card after the merge, and the card stays open.\n\n**Seat decisions on the review's flags, one line each:**\n- ③3, the spec's `normalizeFilterOperator` answering `Object.prototype` keys (the dev's class-a finding): already fixed on objectstack `origin/main`, where `packages/spec/src/ui/view.zod.ts` has an own-property guard on both alias lookups. It is not yet in a published `@objectstack/spec` (main still reads 17.4.0). ⇒ dropped as a new filing. objectui picks it up at the next spec pin, and this PR's guard in `normalizeFilterBuilderOperator` stays harmless.\n- ③4, the `ListView.tsx` stale docblocks (`LIST_VIEW_EXTRA_OPERATORS` / the value-less sets' \"camelCase\" prose) ⇒ carrier: this seat, after this merges. `content/docs/components/complex/filter-builder.mdx` names no operator id (0 hits), so nothing there is false ⇒ dropped.\n- ③5, two test docblocks ⇒ fixed in `69484cb25`.\n- ③6, `condToMongo` / `groupToCondition` fed a raw pre-change row directly ⇒ Acceptance notes, not folded. `condToMongo` is marked `@internal`, every in-repo feed is canonical (measured), and stored sharing criteria read back through `kvToCondition`, which emits canonical ids. 承接者:无.\n- ③7 (`foldFilterGroupToSpecRules` passes `containsCaseInsensitive` verbatim, unreachable, and the census pin records it) and ③8 (unrelated operator mini-vocabularies) ⇒ Acceptance notes. 承接者:无.\n\n**Maintainer veto window** (named in the round report; landing is not held):\n1. The dropdown emits `greater_than`, `not_in`, `is_null`, … instead of `greaterThan`, `notIn`, `isNull`. Stored camelCase filters still open, and are rewritten canonical on the next edit.\n2. `icontains` is offered everywhere and is no longer opt-in.\n3. `@object-ui/components` `FilterBuilderOperator` narrows (`'greaterOrEqual'` no longer compiles). Released as `minor` under the repository rule.\n\n## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `2f87c03ecd12148754f4102bbc84b0b113f479ae`\n\nReviewed from 2026-09-25T07:27Z to 2026-09-25T07:48Z against `origin/main` `6881e9e47768c91cc7d7575777d987984aac20a4` (merge-base `09ab32bffada08f006a20844492e042cc66f5b3c`; main is 4 commits ahead of the merge-base, touching 14 files, none of them a file of this PR; the PR is a draft, `mergeable: true`, state `behind`, 4 commits, the last a merge of `origin/main`; the REST file list and `git diff --name-only <merge-base> <head>` are the same 46 files). Head at dispatch `2f87c03ec` is still the head. Inputs read in full: the PR body; the card objectui#9306 body and all 8 comments, including the ruling `5651809976`, its amendment `5651829933`, the claim `5825621947` (as amended twice), the state record `5826294523`, the serial wait `5826716434`, the blocked report `5827821368`, the seat decisions `5827864743` and the final report `5828561347`; objectui#9559 ruling B `5748934939` and its ratification `5814267184`; the contract review record of objectui#10465 at `scratchpad/pr-10465/cr.md`; the complete diff of the 46 files between the merge-base and head; objectui `AGENTS.md` #11 (citations) and the changeset rules; objectstack `AGENTS.md` Post-Task Checklist step 3; the installed `@objectstack/spec` 17.4.0 dist (`VIEW_FILTER_OPERATORS`, `VIEW_FILTER_OPERATOR_ALIASES`, `normalizeFilterOperator`, `ViewFilterRuleSchema`); the new changeset and the five pending changesets the dev flags, at head; `.changeset/config.json`.\n\nMethod. Two detached scratch worktrees under `scratchpad/pr-10534/`: `wt` at head and `wt-base` at the merge-base (for the before half of the census), each installed with `pnpm install --offline --frozen-lockfile` (head `pnpm-lock.yaml` is identical to `origin/main`'s; `@objectstack/spec` resolves to 17.4.0 in the root and in every package's `node_modules`). Every vitest, build and `tsc` run went through `os-verify-lock.sh` in three lock turns (held 56 s, 22 s and 284 s; waited 0 s, 0 s and 47 s). Readings come from the PR's pins, 8 related untouched pins, three throwaway probe files (one per consumer package, deleted afterwards) run on both trees, the repository's own gates, and a node script over the spec dist. The `vitest.config.mts` alias map (identical at base and head) resolves `@object-ui/components`, `fields`, `plugin-list` and `plugin-view` to `src`, so the test legs measure the sources; the type legs measure the built dist. No GitHub write of any kind; the shared checkouts were only fetched and read; both worktrees were removed at the end with `git status --porcelain` empty.\n\n### ① Derived judgments\n\n1. **The emitted id set.** Measured at head by the new pin `filter-builder-protocol-ids-9306.test.tsx` (31 tests, green): `[...FILTER_BUILDER_OPERATORS].sort()` equals `[...VIEW_FILTER_OPERATORS, 'exists', 'notExists'].sort()`, and the spec set is 20 members (read from the dist: `equals not_equals contains not_contains icontains starts_with ends_with greater_than less_than greater_than_or_equal less_than_or_equal in not_in is_empty is_not_empty is_null is_not_null before after between`). `defaultOperators` in the diff is exactly those 20 plus `exists` / `notExists`; the six buckets are typed `readonly FilterBuilderOperator[]`, so a stale spelling is a compile error (type-check below). `OPT_IN_OPERATORS` is `[\"exists\", \"notExists\"]` in the diff; `filter-builder-opt-in-operators.test.ts` (green) pins `icontains` offered on the text bucket with no grant, at the index after `contains`, and absent from the six non-text buckets, and `list-offered-operator-expressible-parity.test.ts` (green) pins it offered by the list toolbar and expressible on both list dialects. `exists` / `notExists` fold to themselves (`normalizeFilterBuilderOperator('exists') === 'exists'`, pinned), and the spec rule refuses both (`ViewFilterRuleSchema.safeParse` → `invalid_value@operator`, measured), so ruling B's \"opt-in, not folded\" holds. The 22-row mapping in the pin is the amendment `5651829933`'s table with the last two rows changed to identities, as the state record `5826294523` orders. **PASS**\n\n2. **The read-side fold.** `normalizeFilterBuilderOperator` is `UNFOLDED_LEGACY_OPERATOR_IDS.get(op) ?? (typeof normalizeFilterOperator(op) === 'string' ? that : op)`, and the local map has exactly one row, `containsCaseInsensitive → icontains`, with objectstack-ai/objectstack#20092 named beside it. The row is needed, measured on the dist: `normalizeFilterOperator('containsCaseInsensitive')` returns its input, while the control `greaterOrEqual` returns `greater_than_or_equal`; the other 20 non-identity legacy ids are all rows of the spec's 36-row alias table (the pin walks them, and my node read of the table agrees). No second vocabulary: nothing in the diff maps onto a camelCase id any more (`SPEC_TO_FB` and `CANONICAL_TO_BUILDER` are gone, see 3), and the only tables keyed on the builder's ids are keyed on the protocol ids. The boundary is `normalizeGroupOperators`, applied to `useState`'s initialiser and to the `value` effect, returning the same object when nothing moved; no `onChange` is issued there. Pinned: rendering the 22-row camelCase group calls `onChange` zero times, every trigger names its operator, and the next edit (Add filter) writes all 22 rows with the canonical id and nothing else changed; a canonical group is written back byte-for-byte. `filter-builder-operator-alias-trigger-7561.test.tsx` (green) keeps the \"not migrated on render\" pin for `gt`, `greater_than` and now `greaterThan`. The non-string guard (declared deviation 2): measured on the dist, `normalizeFilterOperator('constructor')` returns the `Object` function, `toString` / `hasOwnProperty` / `valueOf` return their prototype members and `__proto__` returns an object, because the table is a plain object read without an own-key check; since the builder now stores the fold's answer, a row spelled that way would lose its operator on serialisation. The guard is one `typeof` check, keeps the spelling, changes nothing for any string answer, and is pinned for the four prototype names. Correct, minimal and pinned; the defect itself stays upstream (③ 3). **PASS**\n\n3. **Consumer tables and the 22×8 census.** Read in the diff: `condToMongo` has an arm for all 22 ids on the protocol spellings (`default` still the equality); `kvToCondition` emits the protocol ids from the unchanged `$`-tokens (both `$notContains` and legacy `$ncontains` still read); `TEXT_COMPARAND_OPERATORS` is the five text ids; `FILTER_CONDITION_EXTRA_OPERATORS` is `['exists', 'notExists']`; `OP_TO_MONGO` / `MONGO_TO_OP` re-keyed with the one new row `icontains ↔ $icontains`, `VALUELESS_TO_MONGO` re-keyed, `conditionToGroup`'s `$exists` / `$null` arms emit the canonical ids; `SPEC_TO_FB` removed and `FilterBuilderField` hands `r.operator` over as stored; `CANONICAL_TO_BUILDER` and `__CANONICAL_TO_BUILDER` removed and `specToBuilderOperator` returns the canonical member (`toFilterGroup` goes through `parseSpecFilter`, which calls it); `NULL_FILTER.labelKey` / `notLabelKey` re-keyed; `sanitizeViewOverride` asks `VALUELESS_FILTER_OPERATORS.has(normalizeFilterOperator(String(operator)))` in both the triple and the object arm. Census, reproduced independently: one probe per consumer package, run on the merge-base tree with the 22 camelCase rows and on head with the 22 ids they became, over eight columns (`condToMongo`, its `kvToCondition` read-back, `groupToCondition`, its `conditionToGroup` read-back, `foldFilterGroupToSpecRules`, `convertFilterGroupToAST`, `sanitizeViewOverride`, `specToBuilderOperator`), read-backs compared modulo the rename. 176 cells; 4 differ, all on the `containsCaseInsensitive → icontains` row: dataset write `undefined` → `{ f: { $icontains: 'x' } }`, dataset read-back `null` → `icontains` (representable), fold rule `containsCaseInsensitive` → `icontains`, AST node `['title','containsCaseInsensitive','x']` (`isFilterAST` false) → `['title','icontains','x']` (`isFilterAST` true). The sharing-rule column is identical for all 22 (`equals` → `{ f: 'x' }`, `not_equals` → `$ne`, `icontains` → `$icontains`, `is_empty` → `$in [null,'']`, `is_null` → `$null: true`, `exists` → `$exists: true`, `between` → `$gte/$lte`, …), and the override column is `kept` 22 of 22 on both trees. The base fold for `containsCaseInsensitive` wrote a rule the spec enum refuses (measured `invalid_value`) and the base AST node failed `isFilterAST`, so the four new cells replace nothing storable. No cell differs besides the declared row. The dev's `filter-builder-protocol-ids-census-9306.test.ts` (green, 22 rows × 6 columns plus the two `CONTROL`s) and the plugin-list / plugin-view legs hold the same table permanently; its control `condToMongo(row('no_such_operator', 5))` → `{ f: 5 }` proves the equality hazard is live. **PASS**\n\n4. **Stored data compatibility.** Sharing-rule criteria and dataset filters are stored as `$`-tokens, which this PR does not touch; measured through `kvToCondition` and `conditionToGroup` in the census read-back columns, every token written before the change reads back onto a protocol id the dropdown draws (22 of 22, and 20 of 20 for the dataset bridge, which never held the existence pair). Saved views: the fold stores the same rule for the camelCase and the canonical spelling of all 21 rows the spec folds (census `fold` leg and the `viewFilterFold.emptyValue` pin, a stored `isEmpty` row → `is_empty`, byte-equal to the canonical row's result). View overrides: the five deprecated value-less spellings plus the legacy triple `['closed_at','isEmpty','']` are kept by `sanitizeViewOverride` (pinned in `ObjectView.overlayPairValue.test.ts`, with the `notEquals ''` control still dropped), and on the merge-base tree the raw lookup also kept them, so this is parity, not a change. The builder itself: a 22-row camelCase group loads with every label, writes nothing, and re-saves canonical on the next edit (item 2). Per-browser list panel state written under the old ids reaches `convertFilterGroupToAST`, which emits one node for the legacy and the canonical spelling of all 21 (plugin-list census leg, green). Every in-repo feed of `condToMongo` is `filterGroupToMongo` over `localGroup`, which is `kvToCondition`'s rows or the builder's `onChange` (both canonical), and every feed of `groupToCondition` is the builder's `onChange`; a raw legacy row that skipped the builder would hit the equality / drop arms (measured, 14 rows each) — unreachable in-repo, noted in ③ 6. **PASS**\n\n5. **i18n.** A script over `git show` of the ten packs at the merge-base and at head: 22 rows each, keys re-mapped exactly per the 22-row map, translated values identical in order, for `ar de en es fr ja ko pt ru zh`. The components' inline `createSafeTranslation` defaults are re-keyed the same way in the diff. `pnpm check:i18n-keys` exit 0 (3032 en keys; the `filterBuilder.operators.` family is one of the 18 dynamic families with a static vocabulary, 114 member keys checked exactly). At runtime: `filter-builder-operator-locale-parity.test.ts` (dropdown ids against the packs), `ObjectDataPage.filterChipI18n-9159.test.tsx` (the drill chip rendered through the real packs, the pin that went red at the checkpoint), `all-locales-key-parity` and `en-zh-key-parity`, all green at head. No non-test source references a `filterBuilder.operators.<camelCase>` key (grep; the two hits are historical prose in `scripts/check-i18n-call-site-keys.mjs`). **PASS**\n\n6. **Public type surface.** `@object-ui/components` re-exports `./custom` → `./filter-builder`, so `FilterBuilderOperator` (now `(typeof defaultOperators)[number]['value']` over the 22) and the new `normalizeFilterBuilderOperator` are published. In-repo importers of the components `FilterBuilderOperator`: none outside the package (the new pin imports it by relative path); of `normalizeFilterBuilderOperator`: the app-shell census through `@object-ui/components`; of `SPEC_TO_FB` / `CANONICAL_TO_BUILDER`: none (prose mentions only). Type-check, measured in the scratch tree: `turbo run build --filter='@object-ui/app-shell^...' --concurrency=2` 28 tasks (2 from cache), exit 0; `pnpm --filter @object-ui/<p> run type-check` (`tsc --noEmit && tsc -p tsconfig.test.json`, types additionally its examples program) exit 0 with zero `error TS` lines for types, core, i18n, components, fields, plugin-list, plugin-view and app-shell; the type-level pin `Equal<FilterBuilderOperator, ViewFilterOperator | 'exists' | 'notExists'>` is inside the components test program. Reverse check from `packages/fields` against the built dist (scratch tsconfig, deleted afterwards): `const bad: FilterBuilderOperator = 'greaterOrEqual'` is TS2322 naming the 22-member union; the control without that line, importing the type, `normalizeFilterBuilderOperator`, `FILTER_BUILDER_OPERATORS` and `VALUELESS_FILTER_BUILDER_OPERATORS`, exits 0. The two same-named exports are still split: `@object-ui/types` `FilterBuilderOperator = ViewFilterOperator` (20) and `@object-ui/components` (22); they now differ only by the opt-in pair, and the components one is a strict superset, but they are two symbols, and the components `FilterBuilderCondition.operator` is still `string` (③ 1, 2). **PASS with note**\n\n7. **Scope.** All 46 files sit inside the claim `5825621947` as amended plus decision `5827864743`: `filter-builder.tsx` and its label keys in ten packs; `FilterConditionField.tsx`; `datasetFilterCondition.ts` and `widgets.tsx`; `view-config-utils.ts`; `drillUrlFilters.ts` and its four pins; `ObjectView.tsx` with `ObjectView.overlayPairValue.test.ts`; the stale-prose files (`viewFilterFold.ts` comments only, set literal unchanged; `filter-converter.ts` and `filter-icontains-alignment-8976.test.ts` comments only; `filter-operator-protocol-set-9559.test.ts`; the schema-catalog 7561 test); the parity and census pins in components, fields, plugin-list, plugin-view and app-shell; one changeset. `ObjectView.tsx` has two hunks: the added specifier `normalizeFilterOperator` on the existing `@objectstack/spec/ui` import, and `sanitizeViewOverride`'s docblock and body (declared deviation 1). `packages/plugin-list/src/ListView.tsx` and `content/docs/components/complex/filter-builder.mdx` are not in the diff (grep of the file list). **PASS**\n\n8. **Pins and ablation.** At head, one vitest run over the 26 touched test files, 8 related untouched pins and the 3 probes: 37 files, 976 tests, exit 0. Leg B, the read-side fold removed (`normalizeGroupOperators` returns its input; blob `a97200178` → `365b7f736`): `Test Files 1 failed | 2 passed (3)`, `Tests 1 failed | 83 passed (84)`, the red test \"saves back: the author's next edit writes every row's CANONICAL id, and nothing else moves\"; restored by blob, equal to `HEAD:` (`a97200178`). Leg A, one camelCase key put back in `condToMongo` (`greater_than_or_equal` → `greaterOrEqual`; blob `e93dd866a` → `27a16359a`): `Test Files 3 failed (3)`, `Tests 4 failed | 205 passed (209)`, red: the census \"stores the predicate\" and \"reads back\" rows for `greater_than_or_equal`, the fields round trip for it, and the `betweenBlankBound-9914` `$gte: ''` control; restored by blob, equal to `HEAD:` (`e93dd866a`); `git status --porcelain` empty apart from my probe files, which were then deleted. **PASS**\n\n9. **Merge with `origin/main`.** `git merge-tree --write-tree origin/main 2f87c03ec` exit 0, tree `ba71a787da267ad04e54c374aa2b4bcd1df6738e`, no conflict lines. The 14 files main changed since the merge-base (two changesets, three docs pages, `plugin-map` `ObjectMap.tsx` and its test, six `app-shell/src/environment/*` files) have an empty intersection with the PR's 46. The PR is `behind` only by those 4 commits. **PASS**\n\n### ② Semver\n\nOne changeset, `.changeset/9306-filter-builder-protocol-operator-ids.md`, `minor` on `@object-ui/components`, `fields`, `app-shell`, `plugin-view` and `i18n`, all members of the 40-package fixed group. `check-changeset-presence`: \"44 source file(s) of 8 released package(s) changed, 1 changeset\" — the three packages the changeset does not name changed only a comment (`core` `filter-converter.ts`) or test files (`plugin-list`, `types`), and the fixed group bumps them together; `check-changeset-no-major` exit 0; `check-changeset-overwrite`: 1 added, 0 modified, 0 deleted. objectstack `AGENTS.md` step 3: `Clause-②: yes` (PR body line 2, no arm) takes at least `minor` — met; the accept set widens on read (both spellings) and the offered set widens (`icontains` everywhere), the published type narrows and is declared as breaking in the body under the repository's `minor` rule. Every sentence of the new changeset against the diff and the readings: the 20 members plus the unfolded pair (item 1); camelCase as the deprecated alias form, with one imprecision noted in ③ 9; the fold plus one local row, writes nothing on open, canonical on the next edit (item 2); \"no row changes the predicate it stores … the only cells that differ are `icontains` on the three consumers that never offered the case-insensitive contains, where the old id produced no storable filter\" (item 3: 4 cells on three consumers counting the dataset bridge with its read-back, base fold rule refused by the enum, base AST node refused by `isFilterAST`); the type narrows, `'greaterOrEqual'` no longer compiles, the two exported sets hold the canonical ids, hosts receive them, the new export (item 6); `icontains` no longer opt-in, its old reason gone at spec 17.1.0 (`.changeset/spec-pin-17-1-0-5328.md` records `icontains` as a canonical member \"as of 17.1.0\"), the docblock's planned deletion (the removed docblock text in the diff), two operators (pins); the i18n re-key with values unchanged (item 5); `FILTER_CONDITION_EXTRA_OPERATORS = ['exists','notExists']`, `$icontains` still written, `kvToCondition` canonical (diff, census); `toFilterGroup` canonical (source reading, via `parseSpecFilter` → `specToBuilderOperator`); the three app-shell notes (diff, census). All true; level correct.\n\n**Pending changesets in the same release — blocking.** Read in full at head. Four of the five the dev flags state, in the present tense, facts about the very exports this PR changes, and each is false at release:\n\n- `.changeset/7561-filter-builder-operator-trigger-label.md` (`@object-ui/components`): \"the mounted `SelectItem`s, whose ids are this builder's own camelCase vocabulary (`greaterThan`, `notIn`)\"; \"the vocabulary the dropdown EMITS is byte-identical — no second spelling is mounted, so `onChange` still hands back `lessThan` and never `lt`\" (measured at head by the same file's updated pin: the dropdown hands back `less_than`); \"Which of the two vocabularies should win remains open\".\n- `.changeset/9302-filter-builder-valueless-canonical-fold.md` (`@object-ui/components`): \"`VALUELESS_FILTER_BUILDER_OPERATORS`, whose members are this builder's six camelCase dropdown ids\" (measured: the six are `is_empty is_not_empty is_null is_not_null exists notExists`); \"**The exported set is unchanged, deliberately.**\"; \"Which operator vocabulary should WIN is a separate, still-open question\".\n- `.changeset/9359-list-ast-valueless-canonical-fold.md` (`@object-ui/plugin-list`): \"`VALUELESS_FILTER_BUILDER_OPERATORS` — the FilterBuilder's six camelCase dropdown ids\"; \"Both carry the dropdown's camelCase ids, so the canonical spelling has no measured producer into this reader today\" (the toolbar's builder now produces canonical ids); \"**The exported set is unchanged, deliberately.** It states a fact about what the BUILDER'S DROPDOWN draws\"; \"still-open question\".\n- `.changeset/8748-icontains-empty-comparand.md` (`@object-ui/fields`): \"`condToMongo` now drops a text-operator condition whose comparand is empty (`contains`, `containsCaseInsensitive`, `notContains`, `startsWith`, `endsWith`; …)\" and \"The value-less operators (`isNull` / `exists` / `isEmpty` and their negations) … are untouched\" — at release `condToMongo` has no arm for four of those five ids and `TEXT_COMPARAND_OPERATORS` does not contain them, so a row carrying one of those literal ids is not dropped but stored as the `default` equality (source reading of the head file; the census's head run with raw legacy ids shows the equality arm for those spellings).\n- `.changeset/9559-filter-operator-schema-protocol-set.md` (`@object-ui/types`): every sentence is about the mirror and stays true at head (`containsCaseInsensitive`, `exists`, `notExists` still refused; aliases accepted and normalised; `FilterBuilderOperator` in types is `ViewFilterOperator`). Not falsified.\n\n`changeset version` concatenates every pending body into the named package's `CHANGELOG.md` for one version, so the `@object-ui/components` entry would say in one paragraph that `onChange` still hands back `lessThan` and in another that hosts receive the canonical ids; objectstack `AGENTS.md` makes a factual error in a released entry a dedicated docs-only PR with the false text left in the tarball, and objectui's `check:changeset-claims` text says \"If a claim did go false, CORRECT THE BODY\", addressed to the diff that falsified it. The precedent the dispatch names, objectui#10465, corrected `6939` and `7760` in place with an in-release supersession note and left `check-changeset-overwrite` to report it (report-only, its case 2). The remedy is prose-only in those four files (a one-paragraph \"superseded in this release by objectui#9306: the dropdown now emits the protocol ids, the exported set holds them, the text arms are `icontains` / `not_contains` / `starts_with` / `ends_with`\"), frontmatter untouched, no code. Until that is on the branch the release would publish four false claims about the exports this PR changes, so this is the one blocking item.\n\n### ③ Boundary flags\n\n1. Ruling item 4 (`5651809976`, standing under the amendment): \"objectui#6349's two parked name-authority rows converge on the `@object-ui/types` declaration with `operator` typed as the protocol set — an acceptance criterion of this card.\" Measured at head: `packages/components/src/custom/filter-builder.tsx` still declares its own `FilterBuilderCondition { operator: string }` and `FilterGroup`, and `packages/types/src/complex.ts` declares `FilterBuilderCondition { operator: FilterBuilderOperator }` and `FilterGroup` (conditions may nest); the PR touches neither declaration. The claim's amended surface does not name them and the state record credits objectui#10465 with \"the types half of item 4\" only, yet the PR closes the card (`Fixes #9306`). The seat should record where the components half of item 4 is carried (objectui#6349 or a follow-up) before the card closes on merge.\n2. Two published `FilterBuilderOperator` exports remain (`@object-ui/types`: 20; `@object-ui/components`: 22), now a superset relation rather than near-disjoint; each has a type-level pin against the spec. Pre-existing; reconciling them is the same work as flag 1.\n3. Spec (class a, the dev's finding, confirmed): on `@objectstack/spec` 17.4.0 `normalizeFilterOperator` answers `Object.prototype` keys with the prototype's members (`constructor` → the `Object` function; `__proto__` → an object) although declared to return a string. objectui's other callers of the spec fold (`viewFilterFold`, `sanitizeViewOverride`, plugin-list's value-less check) only compare the answer, so a non-string simply fails membership there; the builder is the one storing site and guards it. The prior record's case-fold asymmetry (`NotEquals` accepted, `Equals` refused) is unchanged and still a line for objectstack#20092.\n4. Stale prose outside the surface, carried by the seat (holders named in `5827864743`): `packages/plugin-list/src/ListView.tsx` \"Accepts both the FilterBuilder vocabulary (camelCase)…\" and \"that dropdown's own camelCase ids\" (the value-less docblocks) and \"Neither vocabulary has a case-insensitive contains or an existence operator, so this surface opts into nothing\" above `LIST_VIEW_EXTRA_OPERATORS = []` — the first half false since spec 17.1.0 and now the toolbar offers `icontains`; behaviour correct (the derived `VALUELESS_FILTER_BUILDER_OPERATORS_CANONICAL` is the identity now). `content/docs/components/complex/filter-builder.mdx` names no operator id (0 grep hits), so nothing there is false; the protocol-id vocabulary and the read-side alias rule could be stated.\n5. Two untouched test docblocks still describe `containsCaseInsensitive` as an opt-in / builder id: `packages/components/src/__tests__/filter-builder-operator-locale-parity.test.ts` (\"It includes the opt-in ids (`containsCaseInsensitive`, …\") and `packages/fields/src/widgets/__tests__/FilterConditionField.builderRows-8748.test.tsx`. Both green; prose only.\n6. `condToMongo` / `kvToCondition` are published from `@object-ui/fields` (`export * from './widgets/FilterConditionField.js'`, marked `@internal`), and `groupToCondition` from app-shell. Fed a raw pre-9306 row directly, bypassing the builder, `condToMongo` stores the `default` equality and the dataset bridge drops the row (measured: 14 rows each on head). Every in-repo feed is builder output or `kvToCondition` / `conditionToGroup` output, so it is unreachable in-repo; a host calling the `@internal` export with rows it stored under the old ids would hit it. A one-line fold inside `condToMongo` through `normalizeFilterBuilderOperator` would close it; the seat's call.\n7. `foldFilterGroupToSpecRules(group('containsCaseInsensitive'))` still passes the spelling verbatim into a rule the spec enum refuses (measured on both trees); the builder folds it first and no view surface ever offered it, and the census pin records the reading.\n8. Separate pre-existing operator mini-vocabularies, none a FilterBuilder consumer, untouched and out of scope: `ObjectFieldInspector.tsx` `LOOKUP_OPERATORS` / `SUMMARY_OP_TO_FC` (`eq ne gt lt gte lte contains in notIn`), `RecordPickerDialog.tsx`'s lookup-filter switch, `types/field-types.ts` (`'in' | 'notIn'`), `types/views.ts` and `views.zod.ts` (`'startsWith' | 'gt' | 'lt' | …`). Noted for the vocabulary ledger only.\n9. The new changeset's first paragraph lists `containsCaseInsensitive` among \"the spec's deprecated alias form (objectui#7993)\"; the spec's table has no row for it (measured), which the next paragraph states correctly. A one-word tightening (\"plus `containsCaseInsensitive`\"); body-only, optional.\n10. Not measured here: the dev's ESLint per-rule counts, and the full package suites beyond the 37 files run (the dev ran components 301 files, fields / plugin-view / plugin-list 335, app-shell views in three shards and the i18n / types / core / schema-catalog set at `2f87c03ec`). `check:changeset-claims` (report-only, exit 0) flags 9 paragraphs that name a touched file; the two read in full (`9159-drill-url-isnull-operator`, `formfieldspec-dependson-5040`) stay true, and the other seven (`5293`, `6661`, `6939`, `7070`, `7125`, `8218`, `8415`), read by every line naming a filter-builder symbol, an operator or a touched file, concern the group shape, the condition `id`, `toSortItems`, `widgets.tsx`'s form spec, `ObjectView.tsx`'s date fields or retired locale keys — none states the dropdown's operator ids, so none is falsified.\n11. The turbo build served 2 of 28 tasks from cache; the type legs consumed that dist.\n12. The PR is a draft with `mergeable_state: behind`; the four main commits since the merge-base do not overlap its files (item 9).\n\n### Verdict\n\n**FAIL.** Blocking: ② — the pending changesets `7561-filter-builder-operator-trigger-label`, `9302-filter-builder-valueless-canonical-fold`, `9359-list-ast-valueless-canonical-fold` and `8748-icontains-empty-comparand` publish, in this same release, present-tense claims about the dropdown's emitted ids, `VALUELESS_FILTER_BUILDER_OPERATORS`' members and `condToMongo`'s text arms that this PR makes false (readings above); an in-release supersession note in each, prose-only with frontmatter untouched, per the objectui#10465 precedent, clears it. Every code judgment in ① passes.\n\n<sub>`domain:ui` seat #1 · review · 2026-09-25T08:08Z</sub>\n"}
{"id":5829287670,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T08:23:00Z","updated_at":"2026-09-25T08:23:00Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9306#issuecomment-5829287670","body":"## Decision needed — ruling item 4's components half conflicts with ruling B · `pm:dispatched` → `needs-user-decision`\n\n`domain:ui` seat #1, `session_01BA3nKVUwKQJf8DBxrSVtNC`. PR objectui#10534 landed items 1–3 and 5 as `641fb55b0`, and objectui#10465 landed the types half of item 4 (`1779e8dee`). Its line 1 was changed to `Part of #9306`, so this card stayed open. What remains is ruling `5651809976` item 4's **components half**, and it cannot be executed as worded. The claim is released in this act (assignee cleared), because no dev is in flight.\n\nGoverning text: ruling `5651809976` item 4 (「objectui#6349's two parked name-authority rows converge on the `@object-ui/types` declaration with `operator` typed as the protocol set — an acceptance criterion of this card」); objectui#9559 ruling B `5748934939`, ratified `5814267184` (`exists` / `notExists` stay opt-in and unfolded); AGENTS.md 0.1 (protocol first; no second vocabulary).\n\n### The conflict, measured on `main` after the merge\n\n| premise | reading | re-check |\n|:--|:--|:--|\n| components still declares its own condition and group | `packages/components/src/custom/filter-builder.tsx`: `FilterBuilderCondition { id: string; field: string; operator: string; value }` and `FilterGroup { id: string; logic; conditions: FilterBuilderCondition[] }` | `git grep -n \"^export interface FilterBuilderCondition\\|^export interface FilterGroup\" origin/main -- packages/components/src/custom/filter-builder.tsx` |\n| the types declaration's operator is the protocol's 20 | `packages/types/src/complex.ts`: `FilterBuilderOperator = SpecViewFilterOperator`, and its `FilterGroup.id` is optional, with nestable conditions | `git grep -n \"export type FilterBuilderOperator\" origin/main -- packages/types/src/complex.ts` |\n| the dropdown's ids are 20 + 2 | components `FilterBuilderOperator` = the 20 plus the opt-in `exists` / `notExists` (ruling B) | `git grep -n \"export type FilterBuilderOperator\" origin/main -- packages/components/src/custom/filter-builder.tsx` |\n| the opt-in pair has a real consumer | `packages/fields/src/widgets/FilterConditionField.tsx` passes `extraOperators={FILTER_CONDITION_EXTRA_OPERATORS}` (`['exists','notExists']`) | `git grep -n \"FILTER_CONDITION_EXTRA_OPERATORS\" origin/main -- packages/fields/src` |\n\n⇒ Converging the components condition on the types declaration \"with `operator` typed as the protocol set\" types out two operators the dropdown still emits under ruling B. Item 4's premise predates ruling B. The two texts disagree, and an execution seat does not choose between rulings.\n\n### 四维分析（选项与推荐）\n\n**选项 A（推荐）：以 types 声明为唯一名字权威，components 显式派生并加上 opt-in 扩展。** components 删掉手写的 `FilterBuilderCondition` / `FilterGroup`，从 `@object-ui/types` 派生（例如 `Omit<…, 'operator'> & { operator: FilterBuilderOperator }`，其中 components 的 `FilterBuilderOperator` = 协议 20 个加 `exists` / `notExists`）。`FilterGroup.id` 的可选性、条件能否嵌套，也在这次收敛里一并对齐到 types 声明（需要 dev 先测：组件是否真能渲染嵌套条件）。\n**选项 B：严格照第 4 项原文，operator 只收协议 20 个。** 那就得把 `exists` / `notExists` 从下拉框拿掉，或者折叠到 `is_not_null` / `is_null`。这等于推翻裁决 B，或者复活已被 B 否掉的修订 `5651829933`。\n**选项 C：把 opt-in 这一对加进 types 的镜像。** types 的 `FilterBuilderOperator` 目前就是 spec 的 `ViewFilterOperator`，往里加非协议 id 就成了第二份词表。\n\n- **实际业务需求**：`exists` / `notExists` 有实测消费方，`FilterConditionField`（共享规则条件、数据集过滤）在用。B 会删掉一个有人拉动的能力。A 和 C 都保住它。\n- **项目长远合理性**（权重 ≥50%）：A 让 types 声明成为唯一名字权威，components 的差异写成一个具名扩展，第 4 项\"收敛到一个声明\"的意图完整兑现。C 污染了\"镜像即 spec\"的不变量。B 在长远上并不更干净，只是把问题转成了产品能力的删除。\n- **防 AI 写错**：今天 components 的 `operator: string` 什么都收，AI 写 `'greaterThan'` 或拼错都能静默编译通过。A 把它收窄到下拉框真能发出的 22 个 id，属于结构性收紧。B 同样收紧，但代价是删能力。C 让 types 镜像接受非协议 id，是放宽。\n- **创业阶段不扩散**：A 不新增能力，只是收窄加去重。B 是收缩，但收缩的是一个有实测拉动的能力，不符合\"零拉动才收\"。C 扩大了镜像面。\n\n**推荐 A。** 长远轴领起（一个名字权威，差异具名），防错轴同向（`string` → 22 个 id），业务轴保住有消费方的 opt-in 对。执行时 dev 要先测两个前提：嵌套条件在 components 里是否可达；两个 `FilterGroup.id` 的必填性差异对 `onChange` 回传有没有影响。任何一个前提被证伪，就报 fork 回箱，⛔ 不硬做。\n\n### State\n\n`pm:dispatched` → `needs-user-decision` (one replace), and the assignee is cleared. `domain:ui` and `priority:p3` stay. The card leaves the active queue until a ruling is recorded.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
