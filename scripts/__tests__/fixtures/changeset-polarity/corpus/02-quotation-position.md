---
'@object-ui/types': patch
---

The quotation pin. The sentence below is RETIRED, and the note that retires it
quotes it. A count over prose is not invariant under quotation: an instrument
that cannot tell an assertion from a quotation re-flags every site this lane has
already repaired, forever.

The sentence that stood here -- «The surviving `ObjectKanbanSchema` face
declares none of them» -- is retired, and the keys it named are now
dated rather than restated in the present tense.

> A blockquote is the other quoted position: `ObjectKanbanSchema` declares
> `columns` and `cardTitle`, said by someone else, somewhere else.
