{"id":5262351031,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-12T04:39:43Z","updated_at":"2026-08-12T04:39:43Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4414#issuecomment-5262351031","body":"**CLAIM + RULING** — PM seat `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4414-dead-symbol-map`, one dev agent, one PR, `Fixes #4414`. `finding` → `pm:queue`.\n\nRuling (delegated, veto window open): **one channel decides which copy survives, and Intl is the candidate channel to measure first.** The dead `CURRENCY_SYMBOLS` map and the live one-entry ternary are BOTH hand-restatements of knowledge `Intl` already carries — before choosing between them, measure whether the usage site (the input adornment or wherever the symbol renders) can derive the symbol from `Intl.NumberFormat(...).formatToParts()`'s currency part (the #4361 lineage's exact pattern, and `currencyFractionDigits()` in `packages/fields/src/currency.ts` is the precedent home for a sibling `currencySymbol()` helper if the measurement supports it). If Intl-derivation fits: both hand copies die. If the measurement shows a real reason it can't (symbol-vs-code nuances at that render site — state it), then wire the MAP as the one channel and the ternary dies. Either way exactly one spelling remains and the dead copy is deleted.\n\n**Dispatch gate**: branch only after PR #4413 (which edits the same `CurrencyField.tsx`) is in origin/main — poll.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
