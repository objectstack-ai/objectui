{
  "number": 2863,
  "kind": "pull_request",
  "title": "fix(action): honor the spec disabled predicate on every action-rendering surface (#1885 follow-through)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: components",
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-27T12:30:30Z",
  "updated_at": "2026-07-27T12:41:51Z",
  "closed_at": "2026-07-27T12:41:50Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-27T12:41:50Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2863",
  "body": "Surfaced by the framework #1878 security-props recheck (the disposition doc's last OBJECTUI item), then widened by browser dogfooding. Companion showcase specimen: objectstack-ai/objectstack#3643.\n\n## Problem\n\nThe spec Action field is `disabled` (boolean | CEL — disabled when TRUE); the schema has **no `enabled` key**. #1885 (PR #1743) wired it in `action:button` only. An AI authoring `disabled: 'record.status == \"closed\"'` per spec got a fully clickable button on **five** other surfaces:\n\n| Surface | Before |\n| --- | --- |\n| `action:group` inline leaf | read non-spec `enabled` only |\n| `action:group` dropdown item | read non-spec `enabled` only |\n| `action:icon` | read non-spec `enabled` only |\n| `action:menu` item | read non-spec `enabled` only |\n| app-shell `DeclaredActionsBar` | read **neither** (`disabled={loading}`) |\n| plugin-detail `record:quick_actions` | **had** a `disabled` impl, but its `typeof === 'string'` split dropped the `{dialect:'cel', source}` envelope the server compiles authored CEL into (#2661), so it never fired on real metadata — found live in the browser (`disabled: false` on a record that should grey) |\n\n## Fix\n\nAll surfaces now follow the #1885 idiom: feed `toPredicateInput(action.disabled)` to `useCondition` **whole** (envelopes route to the canonical formula engine), `disabled` primary, legacy `enabled` kept as a deprecated fallback where it previously worked (`DeclaredActionsBar` gains `disabled` only — declared actions are spec-shaped and never carried the non-spec key).\n\n## Verification\n\n- New `DropdownActionItem` tests: disabled-when-TRUE / false-stays-clickable / disabled-wins-over-enabled / boolean literal (8/8 in file).\n- plugin-detail renderers + components action suites: 25/25; app-shell views: 50/50 earlier run.\n- **Browser-verified** against the showcase `showcase_archive_task` specimen (`disabled: 'record.done != true'`, arriving as a CEL envelope): greyed on an in-progress task, clickable on a done one, with `visible` hiding Mark Done on the same screen (hide-vs-grey contrast).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
