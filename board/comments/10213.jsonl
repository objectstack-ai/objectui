{"id":5773742295,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-22T08:53:20Z","updated_at":"2026-09-22T08:53:20Z","html_url":"https://github.com/objectstack-ai/objectui/pull/10213#issuecomment-5773742295","body":"changeset-claim-re-read\n\n## ⚠️ 1 address(es) in this pull request's own prose name a tree it replaced\n\nEach was read from a tree **this change itself moves**, so a reader who follows it lands somewhere else. ⛔ Nothing here blocks and nothing here says the sentence is false — the question asked is arithmetic: does this diff move the line that number points at?\n\n- in this body, `AGENTS.md:326` — this change **rewrites** `AGENTS.md:326`\n\n  > 触到的行只有 **`AGENTS.md:326`** 这一行。\n\n⛔ **The repair is not to correct the number.** Changing `:246` to `:274` is true today and born false again on the next insertion — objectui#9509 states that before anything else. **Bind the number to the tree it was read from** (`` `:246` at `b8a006883d`, `:274` at this head ``), which cannot re-stale because each number names its own tree; or state a **rule** instead of a coordinate, the way objectui#9495 replaced a file count with \"every file in `git diff --name-only` against the merge base\".\n\n## ⚠️ 5 pending changeset(s) describe a file this change touches\n\nTheir bodies publish **verbatim** into the CHANGELOG at the next release, so this is a request to **re-read them against your diff** — addressed here because you are the one seat that can answer it without re-deriving anything.\n\n⛔ Nothing here blocks, and nothing here is a verdict on your change. This gate exits 0, is not a required context, and judges **name resolution, never meaning**: it asked whether a pending body names a file you touched. \"Is this sentence still true?\" is the one question it will not answer, and the one you are being asked to answer.\n\n### `.changeset/10106-settled-schema-equal-payload.md`\n\n- names `AGENTS.md` → `AGENTS.md` — edited by this change\n\n  > The hook refetches `getObjectSchema` whenever the adapter identity changes, and every consumer its doc comment instructs — `ObjectGantt`, `ObjectCalendar`, `ObjectView`, `ObjectTimeline`, `ObjectGallery` — keys its record fetch on the returned `def`, which is what `AGENTS.md` §5 commandment #10 tells a consumer to do (key on the payload, never on a memo identity). The producer half of that same sentence is what this fixes: a byte-identical answer arriving as a fresh object re-fired every one of those fetches, and the duplicate query carried the same `$expand` set as the one before it. Measured first-hand on `ObjectGantt` with an instrumented adapter, with a control swap in the same command; the counts, and the control that makes them a discrimination rather than a counting artefact, are asserted by `packages/react/src/hooks/__tests__/useSettledSchema.equalPayload-10106.test.tsx` (objectui#10106).\n\n### `.changeset/6357-basechema-bind-declaration.md`\n\n- names `AGENTS.md` → `AGENTS.md` — edited by this change\n\n  > `bind` was read by ten production sites and declared by no schema shape. It resolved as `any` through `BaseSchema`'s index signature and rode `.passthrough()` on the validator, while three separate documents taught it as an authorable key of *every* node: this repo's own `AGENTS.md` §4 (\"Every node in the UI tree follows this shape (`@object-ui/types`)\"), the published agent-facing `skills/objectui/rules/protocol.md` (\"Every UI component node MUST follow this shape\"), and `content/docs/fields/grid.mdx`. So the agent-facing protocol told authors to write a key the published types did not know existed.\n\n### `.changeset/6505-predicate-valued-gate-rules.md`\n\n- names `AGENTS.md` → `AGENTS.md` — edited by this change\n\n  > **The accept set widens to what the protocol already declares and the runtime already accepts, not beyond it.** `AGENTS.md` §4 declares both keys as expressions, `SchemaRenderer` evaluates them through `hasDeclaredPredicate` + `evaluateCondition`, `@objectstack/spec` normalizes every authored predicate into a `{ dialect, source }` envelope, and the objectui#3862 / objectui#3955 rulings are entirely about which expression spellings count as declared. This table was the one place in the repo that disagreed, so this restores declared = enforced rather than changing a contract.\n\n### `.changeset/7122-spec-17-3-0-parity-reconciliation.md`\n\n- names `AGENTS.md` → `AGENTS.md` — edited by this change\n\n  > Two breaking consequences for a consumer that names the type directly. **(1)** It no longer declares `editMode`; the key is now carried by the spec's `ServiceObject`, so `ObjectSchemaMetadata` still has it, but code written against the extension type ALONE loses it. **(2)** `interface` → type alias also ends **declaration merging**: a consumer that reopened `declare module '@object-ui/types' { interface ObjectSchemaClientExtensions { … } }` to add its own client-side member no longer compiles, because an alias cannot be reopened. `minor` rather than `major` per `AGENTS.md`'s version-alignment rule — objectui's own breaking changes are graded `minor` with the semantics stated in the body, since any `major` in the fixed group would push all 39 packages off `@objectstack`'s major.\n\n### `.changeset/7714-lookup-draft-stays-client-side.md`\n\n- names `AGENTS.md` → `AGENTS.md` — edited by this change\n\n  > `minor` rather than `major` per `AGENTS.md`: objectui's major tracks `@objectstack`'s, so objectui's own breaking changes ship as `minor` with the breaking semantics stated.\n\nRead the **paragraph**, not the line: both false halves of the objectui#8617 claim sat in one paragraph, and correcting either alone would have left it asserting the same wrong thing.\n\nIf a claim did go false, **correct the body**. That is precedented and prose-only, frontmatter untouched; `check-changeset-overwrite.mjs` will report the correction as its own case 2 (\"correcting a declaration on purpose … legitimate\"), which is the intended shape — one gate asks for the read, the other records the write.\n\nNot covered, stated so nobody reads this as more: a born-false claim that spells no line address at all (objectui#9495 coordinated one by ORDINAL — \"a grep finds that member first\" — and deciding that means reading what the sentence means), a claim spelled as a symbol or a package rather than a backticked file name, and a file named ambiguously.\n\nCompared the checked-out tree with `0cf2d6644` (merge-base with origin/main): 1 file(s) changed outside `.changeset/`, read against 1262 pending declaration(s) that publish a body (1829 pending in total). · [run](https://github.com/objectstack-ai/objectui/actions/runs/35707238149)\n"}
{"id":5773828944,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-22T09:00:46Z","updated_at":"2026-09-22T09:00:46Z","html_url":"https://github.com/objectstack-ai/objectui/pull/10213#issuecomment-5773828944","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `46680d1fb6b4db76b8a5edf747710662b516bb26`\n\n### ① Derived judgments\n\nRendered in-seat by the skills seat 1 session at the served tier the maintainer named on this date (the fable tier word; ruling record objectstack#7623 comment 5771798588). The contract is the pm-dispatch conflict rule the card cites — 「两条细则冲突 ⇒ 按更严的一条行动并立卡;⛔ 不当场改文本了结」 — with the os-dev dispatch contract as the stricter clause.\n\n- **(a) One line moves — right.** `git diff origin/main 46680d1 -- AGENTS.md` is +1 / −1 at :326, the landing-flow clause under 「多 agent 协作纪律」; 589 lines before and after. Nothing else in the file changes.\n- **(b) The direction is the stricter clause — right.** The old sentence carved out one class from self-landing (a diff hitting a governed surface), so a dispatched os-dev's draft over a NON-governed file set was told to flip ready and arm auto-merge, against objectstack `.claude/agents/os-dev.md` :16 「交付物两件:该 issue 实现完毕并推成 draft PR,加下方的 JSON 报告。」 and :311 「报告在本地验证走完时交付,CI 收敛等待归 PM 不归你」 (read by this seat at `372931e`). The rewrite adds exception ②, keyed on WHO acts rather than which files changed: a PR carrying a `Claim:` comment, implemented by a dispatched os-dev-class agent, stops at draft whatever the governed-surface verdict says; ready, enqueue and auto-merge are the dispatching seat's acts, and 「PM loop 的复核步骤要求落地的是第二个 actor」 states why. That is the os-dev contract, and `references/landing-operations.md`:64 「auto-merge 由 PM 挂,dev 永不碰」 agrees.\n- **(c) The residual case is explicit — right.** 「没有派发、卡上没有 `Claim:` 的自发改动不在 ② 内,仍按本条自行入队」 — the main sentence keeps its subject; ② is stated as not a corollary of ① and not lifted by a NOT GOVERNED verdict (「① 问的是改了哪些文件,② 问的是谁在动手」). No human-contributor case exists in the file (the dev grepped; the whole document addresses an agent), so the dispatch's conditional did not apply — correctly reported rather than invented.\n- **(d) The citation form — right.** The os-dev contract is quoted INSIDE `AGENTS.md` by content anchor with the sibling path, never by `path:line` (objectui's ban at `AGENTS.md:108`; `check-new-cross-file-line-citations.mjs` reads 0 new). Line numbers live in the PR body only. The card's own line addresses had drifted (:330 → :326; os-dev.md :310 → :311) and were re-measured, not trusted.\n- **(e) Gates — re-run by this seat on its own worktree at this head:** `check-control-bytes` OK, `check-changeset-presence` (no changeset owed), `check-new-cross-file-line-citations` 0, `check-doc-links` valid, `check-shell-escape-residue` OK; `check-governed-queue-guard.mjs --test AGENTS.md` exit 3 — GOVERNED, the Tier H basis. No objectstack edit was made (read-only quotes).\n\n### ② Semver level\n\nNone — a governed instruction file; no package source; no changeset owed; no label written (`documentation` is the labeler's).\n\n### ③ Boundary flags\n\n- **`pnpm install` not run** — declared narrowing; the applicable gates are dependency-free. Accepted.\n- **The 受管面 section (:494 onward) still describes only the governed-surface reason for stopping at draft** and does not point back at exception ② — noted by the dev as polish for the next PR on that section; ⛔ not widened here. Accepted.\n- **objectui carries no `os-dev.md` of its own**, so a reader of this clause needs the sibling checkout to open the contract it quotes — a stated content-anchor trade-off, and the reason the card was re-homed here. Accepted.\n- **CI at this head**: objectui's test shards were `in_progress` at the seat's read (markdown-only diff); Tier H — the maintainer merges; the seat reports the checks green before that hand-off. Self-referentially, this PR is itself a dispatched draft and stays one.\n- **Concurrency**: objectui#9563 (PR #10212) rewrites the body-mutation clauses at :470–:480 — a different region of the same file; the later lander merges once.\n\nImplemented-by: `claude/issue-9992-agents-non-governed-pr-lands-by-the-seat`\nReviewed-by: `session_01Wnstp2kTth7sGXfr8fXypc`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
