{
  "number": 2712,
  "kind": "pull_request",
  "title": "refactor(managedBy): consolidate lifecycle-bucket logic into one shared source of truth",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: core",
    "package: types",
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-20T02:08:59Z",
  "updated_at": "2026-07-20T02:32:47Z",
  "closed_at": "2026-07-20T02:32:45Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-20T02:32:45Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2712",
  "body": "Follows framework ADR-0103 (the `managedBy: 'system'` taxonomy split) and the Console copy work in #2705. Eliminates the hand-mirrored bucket logic that ADR-0103's whole design had to work around.\n\n## Problem\n\nThe object-lifecycle `managedBy` bucket taxonomy was **hand-mirrored** in several places, kept \"in lockstep\" by comments:\n\n- `app-shell/utils/crudAffordances.ts` — the canonical `ManagedByBucket` union + `DEFAULTS` matrix + `resolveCrudAffordances`.\n- `app-shell/components/ManagedByBadge.tsx` — its **own** `Bucket` union, `isWriteOptedIn`, and the ADR-0103 writable-system derivation.\n- `plugin-detail/renderers/record-details.tsx` — its **own** `NON_EDITABLE_BUCKETS` set (a comment explains it's duplicated because plugin-detail can't depend on app-shell).\n\nAnd the schema type was open-ended — `managedBy?: 'platform' | 'better-auth' | (string & {})` — so `config` / `system` / `append-only` slipped through the escape hatch and an unknown value silently defaulted to fully-editable `platform`.\n\n## Change (behavior-preserving)\n\nSingle source of truth, split by the dependency graph (plugin-detail can only reach `@object-ui/types` and `@object-ui/core`, both React-free):\n\n- **`@object-ui/types`** owns the closed `ManagedByBucket` union + `MANAGED_BY_BUCKETS`, and `ObjectSchema.managedBy` is tightened to it — **unknown buckets are now a compile error**, not a silent fallthrough.\n- **`@object-ui/core`** owns the runtime logic: `resolveCrudAffordances`, `isWriteOptedIn`, `isSystemWritable`, `isObjectInlineEditable`.\n- **`app-shell/utils/crudAffordances.ts`** becomes a thin re-export of `@object-ui/core` (existing `./crudAffordances` imports keep working); `ManagedByBadge` consumes the shared `isSystemWritable`; `record-details.tsx` replaces the hand-mirrored `NON_EDITABLE_BUCKETS` with `isObjectInlineEditable` (which is exactly the resolved `edit` affordance — verified equivalent bucket-by-bucket).\n\nTranslated copy (badge variants, empty-state messages) stays in app-shell — only the booleans and the union move down.\n\n## Verification\n\n- Full dependency-graph build typechecks (29/29 packages) — the tightened type and rewired imports compile everywhere.\n- `resolveCrudAffordances` / `rowCrudAffordances` / `ObjectForm.managedEdit` / `managedByEmptyState` existing suites stay green (37 tests through the re-export shim); `plugin-detail` suite 237 pass.\n- New `@object-ui/core` `managedBy.test.ts` adds direct coverage, including the previously-untested `isSystemWritable` derivation.\n\n## Scope notes\n\n- The tightening is compile-safe: the only non-bucket `managedBy` strings in the repo (`package`/`admin`/`user`) belong to the unrelated permission-set *provenance* vocabulary, which is not typed against `ObjectSchema.managedBy`.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Fp9yZxRQ3mb7p4vVwqFXKE)_"
}
