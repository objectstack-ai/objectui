{"id":5548720581,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-05T02:24:17Z","updated_at":"2026-09-05T02:24:17Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7483#issuecomment-5548720581","body":"## 分诊 — `domain:ui` / `priority:p3` / `pm:queue` / `finding`\n\n**锚定 (anchoring)**：`packages/app-shell/src/views/ObjectView.tsx` ⇒ `domain:ui`（已发布库）。\n\n### 在 `origin/main` `a472b07` 上复核 —— 成立，⚠️ 行号漂 2\n\n```\npackages/app-shell/src/views/ObjectView.tsx:865   export function ObjectView({ dataSource, objects, onEdit, externalRefreshKey }: any) {\npackages/app-shell/src/views/ObjectView.tsx:910   function ObjectViewInner({ dataSource, objects, onEdit, externalRefreshKey }: any) {\n```\n\n（卡片写 `:863` / `:908`。）\n\n### ⭐ 卡片那张对照表是它最有价值的部分 —— 我复核了两个关键行，它成立\n\n```\npackages/app-shell/src/views/DashboardView.tsx:38   export function DashboardView({ dataSource }: { dataSource?: any }) {\npackages/app-shell/src/views/PageView.tsx:26        export function PageView() {\n```\n\n⭐ **`DashboardView` 是那个锋利的对照**，卡片的推理完全正确：它的 props 值类型**也是 `any`**，但因为参数是**对象类型**而不是**裸 `any`**，JSX 的多余属性检查照样抓得住拼错的 prop 名。\n\n⇒ **本卡的修法因此非常便宜**：值类型可以全部保持 `any`，要改的只是**参数上那个裸 `any`**（它抹掉的是 prop **名字**，不是 prop 值的类型）。这不是\"给 ObjectView 补一套完整类型\"的工程，是一次签名形状的调整。\n\n### ⭐ 探针实验的设计也值得记\n\n卡片没有停在\"类型是 any 所以不检查\"的推断上，而是**种了一个缺陷**（`dataSource` → `dataSourceX`）跑门禁，得到 `Semantic phase: 443 of 443 block(s) judged, 0 failed`，**再把同一个变异施加到三个兄弟组件上确认它们都会红**。⭐ 这把结论从\"门禁看不见\"精确到\"**问题在这个签名，不在门禁**\"—— 正是本仓要求的那种带对照的测量。\n\n### 定级理由\n\n`priority:p3`：\n- **运行时无缺陷**：`ObjectView` 工作正常。\n- 代价是类型面，但落在一个**路由级公共组件**上 —— 卡片说得准：这正是宿主集成的边界。四个真实 prop（`dataSource` / `objects` / `onEdit` / `externalRefreshKey`）全部未声明，任一拼错**编译通过、运行时失败或静默无效**。\n- ⭐ 第二个代价更值得记：**文档覆盖成了纸面覆盖**。#5174 的全部意义是\"被覆盖的块应当能失败\"，而 `packages/app-shell/README.md` 的 `ObjectView` 块现在被 `check:doc-snippets` 覆盖了，探针却证明它只检查\"导出存在 + JSX 良构\"。⚠️ 这个限制**已在 batch-11 报告里公布**，不是未知数 —— 但它是这个签名的限制，不是门禁的。\n- 不上 p2：无用户可见故障，且修法便宜、无争议。\n\n### 定型：Bug/tidy，不踩 manual floor\n\n给参数一个对象类型 ⇒ **不改任何运行时行为**、不加宽授权面。卡片援引的先例也成立：`DashboardRendererProps.dataSource` 被刻意声明成 `dataSource?: any`，理由是\"declaring it **changes what is DECLARED without changing what any call site is held to**\"。同一招在这里可用，且单靠它就买到 prop 名检查。\n\n### ⛔ 给执行席的边界\n\n1. **两处都改**（`:865` 与 `:910`），⛔ 只改导出的那个会让内部组件继续无检查。\n2. **值类型保持 `any` 即可**（见上）。⛔ 不要顺手给四个 prop 补真类型 —— 那会把一次零风险改动变成一次可能弄红调用方的改动，而本卡的收益（prop 名检查）不需要它。\n3. ⚠️ **卡片提到的\"consumer sweep\"是必须的**：声明 prop 名之后，**任何今天传了一个从来不存在的 prop 的调用点会开始报错**。那是正确的红，但要在 PR 里列出来。⛔ 不做这次扫描就改，可能弄红别人的 PR。\n4. **收工判据**：把卡片那个种缺陷的探针加成一个钉子（`dataSourceX` 必须报 TS2322），⛔ 否则下一次有人把它改回 `any` 时没有东西会叫。\n5. 顺手在 batch-11 的报告/`UNGATED_DOCS` 记录里销掉\"这个块只检查导出存在\"的那条限制说明 —— 修好之后它不再成立。\n\n⛔ 分诊席不认领、不派单、不写码。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5647422745,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-12T17:12:50Z","updated_at":"2026-09-12T17:12:50Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7483#issuecomment-5647422745","body":"## Direction set by decision batch #68 — and a measurement that transfers to this card\n\nobjectui#7912 (the same defect class on `SchemaRendererProvider`'s `dataSource` prop and on\nthe context type the whole renderer tree reads back) has been **ruled**: both sites are the\npublished `DataSource` contract from `@object-ui/types`, not `any`. Maintainer verbatim\n「同意」, decision batch #68. \"Keep `any` and document that the host adapter's shape is not\nTypeScript's job\" was **refused** — it contradicts AGENTS.md #0.1 / #6.\n\n⛔ Nothing here is decided by that: `ObjectView`'s props are a different published surface and\nthis card keeps its own ruling. What transfers is the direction and, more usefully, the method\nand two numbers.\n\nImplementation state: **PR objectui#9310**, open as a draft (not landed). This note is posted\nat PR-open rather than after landing, so that whoever takes this card is not blocked on a\nmerge queue.\n\n### What transfers\n\n**Measure each `any` separately before writing code, and expect the prediction to be wrong.**\nOn objectui#7912 the two `any`s were narrowed one at a time, whole-repo, over the 33 packages\nthat depend on `@object-ui/react`, with only that package's `dist` re-emitted so no build\ncascade entered the reading (baseline: 0 diagnostics):\n\n- the **context type** — the one that reaches every reader — cost **7** diagnostics at 7 line\n  positions in 5 files across 4 packages.\n- the **props** — the injection points — cost **52** diagnostics at 52 line positions in 27\n  files across 11 packages.\n\nThe ruling predicted the opposite ordering, and it was wrong by 7.4x: readers already guarded\nand never reached past `find` / `getObjectSchema`, while the injection points were\noverwhelmingly test doubles that were never complete adapters. If this card's `any` is read\nback by consumers as well as written by hosts, price both halves rather than one.\n\n**Two traps worth carrying over:**\n\n1. A package's `type-check` is `tsc --noEmit && tsc -p tsconfig.test.json`. When the first leg\n   fails, the second never runs — so a variant that reds a package's `src` leaves that\n   package's test tier **NOT MEASURED**, and the count silently under-reports.\n2. Key any cast inventory on the **binding**, not on the identifier. A read site can be\n   cast-free and still be `any`, because something upstream widened the binding — on\n   objectui#7912 that was a local re-declaration of the imported context as a `Context` of\n   `any`, which made every read in that file look typed.\n\n**The declared type can admit absence without being half-typed.** `DataSource | null | undefined`\n(the spelling `useSettledSchema` already used one hop away) keeps the states a host really has\n— a preview surface, a page whose adapter has not connected — while refusing a string, an\nempty object, a data bag, and a partial adapter. That distinction is what let the narrowing\nland without a cast at the seam.\n\n**A type-level pin needs controls that can fire.** Its characteristic failure is the harness\ndegrading types to `any`, which inverts every row while looking perfect. objectui#9310 carries\na misspelled-key refusal, a missing-required-prop refusal, and a \"`DataSource` really declares\n`getObjectSchema` as REQUIRED\" row alongside the rows about `dataSource`; reverse verification\nflipped exactly the 5 intended rows and left all 3 controls refused.\n\n### One thing to read before starting here\n\nobjectui#9308, filed from that work: at the `SchemaRenderer` seam the same key carries a\nsecond, undeclared meaning (the data scope that `bind` paths and `data.*` predicates resolve\nagainst). Typing the adapter meaning prices the other one out for a conformant host. Check\nwhether `ObjectView`'s props surface has an analogous second meaning before narrowing it,\nbecause that is the cost item that does not show up as a diagnostic.\n\nPosted by an ObjectUI development agent seat (Claude Code) while implementing objectui#7912.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5809238375,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T06:50:25Z","updated_at":"2026-09-24T06:50:25Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7483#issuecomment-5809238375","body":"Ruling: UI closure batch 2 item 9 · letter declare (ObjectView's parameter gets a declared props type) · maintainer 「同意」 (chat, director seat summon #29, `session_01EcrTi7s5oDYPHS4Pi7h31d`) 2026-09-24T06:50Z\n\nThread re-read to its last comment (5647422745) in this act. Read on objectui `origin/main` `62597c5`: `packages/app-shell/src/views/ObjectView.tsx:1057` is still `export function ObjectView({ dataSource, objects, onEdit, externalRefreshKey }: any)`.\n\n**Ruled: declare it, following the objectui#7912 direction** (maintainer 「同意」, decision batch #68; landed as PR #9310). Export an `ObjectViewProps` interface naming the four props, with `dataSource` typed as the published `DataSource` contract, and use it for both `ObjectView` and `ObjectViewInner`. The parameter is never bare `any` again, so JSX excess-property checking catches a misspelled prop the way it already does for `DashboardView` and `AppShell`. As 5647422745 advises, measure the diagnostics each narrowing costs across dependants before writing code, and keep a value type loose only where that measurement shows a narrow type cannot land in this PR. ⛔ No other published surface changes.\n\nExecution: `pm:queue` · `priority:p3` · `domain:ui` stand; a published type narrows ⇒ `Clause-②: yes`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5828070713,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T06:39:41Z","updated_at":"2026-09-25T06:39:41Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7483#issuecomment-5828070713","body":"Claim: PM loop round 4 — `domain:ui` execution seat 2\nSession: `session_014mXUNuFomfj24w7s1pZzhN`\nBranch: `claude/issue-7483-objectview-props-type`\nWorktree: `objectui-issue-7483`\nDomain: `domain:ui`\nSeat: `domain:ui#2`\nFile surface: `packages/app-shell/src/views/ObjectView.tsx` (an exported `ObjectViewProps` interface for `ObjectView` and `ObjectViewInner`, replacing the bare `any`), the package entry's type re-export if `ObjectView` is exported there, any dependant call site the measured narrowing breaks, tests (a type pin) beside it, and one `.changeset/7483-…md` (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: opus` — `dispatch-gates --tier`: 「no path-derived mandate … floor sonnet · default opus · ceiling fable」; executes ruling `5809238375` (declare)\nClause-②: yes\nThread-read: 5809238375\nSerial constraints cleared: `app-shell/src/views/ObjectView.tsx` was last held by PR objectui#10494 (objectui#10035), merged `3759c568e`. Open-PR file lists read 2026-09-25T06:39Z: no open PR touches it. Live `pm:dispatched` claims of seats 1, 3 and 4 do not name it. `ObjectView` / `ObjectViewInner` are still `: any` on `origin/main`.\n\n`Clause-②: yes` — a published component's props type narrows from `any` to a declared interface.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5828686918,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T07:34:18Z","updated_at":"2026-09-25T07:34:18Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7483#issuecomment-5828686918","body":"os-dev-report\n\n```json\n{\n  \"issue\": 7483,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-7483-objectview-props-type\",\n  \"pr\": null,\n  \"session\": \"session_014mXUNuFomfj24w7s1pZzhN (mode:subagent: the dispatching seat's id)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Premise holds on objectui origin/main efbf18de5: `export function ObjectView({ dataSource, objects, onEdit, externalRefreshKey }: any)` and `function ObjectViewInner(... }: any)` are both still bare any. The ruling's measurement (H2) is complete: the values that can land are `dataSource: DataSource` (required, non-null), `objects: any[]` (kept loose because ObjectSchemaMetadata[] prices at 75 diagnostics), a required `onEdit: (record: Record of string to unknown) => void`, and `externalRefreshKey?: number`. That set costs 3 diagnostics, all inside ObjectView.tsx and repairable by typing. Stopped before writing code on one conflict the card and order do not settle: the ruled name `ObjectViewProps` is already declared and exported by @object-ui/plugin-view (`export interface ObjectViewProps` in packages/plugin-view/src/ObjectView.tsx). A second declaration in app-shell turns scripts/__tests__/one-authority-per-exported-name-6273.test.ts red ('ObjectViewProps — a NEW colliding name', measured; base green). That gate's ratchet says adding a baseline line is not a supported way to pass, and the ruling forbids changing plugin-view's surface.\",\n  \"tests\": \"BUILD closure: `pnpm exec turbo run build --filter='@object-ui/app-shell^...' --concurrency=2` under os-verify-lock, `VERDICT command-exit 0` (29 packages). MEASUREMENT: app-shell `tsc --noEmit -p tsconfig.json` and `tsc -p tsconfig.test.json` run INDEPENDENTLY per variant (so a red src leg cannot hide the test leg); unique diagnostics are the union of both legs. B0 bare any (base): 0 and 0. V0 names only (all values any; dataSource and objects required; onEdit and externalRefreshKey optional): 0. V1 `dataSource: DataSource` required: 3, all ObjectView.tsx: 2x TS2559 where dataSource is handed to the internal runtime-metadata seam (persistRuntimeMetadata / createRuntimeMetadata; its ctx member is `Partial` of ViewCacheInvalidator, a weak type the DataSource contract shares no member with; this is the adapter's invalidateViewKeys capability, i.e. the second meaning comment 5647422745 asked to look for), plus 1x TS2722 at the debounced `dataSource.updateViewConfig(` call inside setTimeout (property narrowing does not reach the closure). AppContent 0 (its LoadingScreen early return on a falsy adapter narrows the const); tests 0 (doubles are `as any`). V2 `dataSource?: DataSource or null`: 5 (V1's 3 plus 2x TS2322 where it is forwarded to plugin-view ObjectView, whose `dataSource: DataSource` is required). V3 `objects: ObjectSchemaMetadata[]`: 75 unique in 6 files. 70 are in ObjectView.tsx: 57 TS18048 (ObjectViewInner re-derives objectDef via objects.find), 8 TS2345, 3 TS2551 (undeclared `list_views`), 1 TS2339 (`navigation`), 1 TS2322. The other 5 are TS2322 on test fixtures. V4 optional `onEdit` typed (record: Record of string to unknown) returning void: 2 TS2322 (forwarded to RecordDetailView, whose onEdit is required); the REQUIRED spelling costs 0. V5 `externalRefreshKey?: number`: 0. V6 combined candidate: 3 (= V1's three). AUTHORITY GATE `pnpm exec vitest run scripts/__tests__/one-authority-per-exported-name-6273.test.ts`: base exit 0 'Tests 11 passed (11)'; ruled name exit 1 'Tests 1 failed | 10 passed (11)', row 'declares no name twice outside the named, shrinking baseline' reports 'ObjectViewProps — a NEW colliding name'; same V6 under the non-colliding name ConsoleObjectViewProps exit 0 'Tests 11 passed (11)'. PIN PREVIEW, a scratch probe beside ObjectView.tsx, never committed: on base (P1) the misspelled prop `dataSourceX`, the README block that omits objects, and a correct four-prop control all compile with 0 diagnostics (silent). Under V6 (V8, non-colliding name) the misspelling is TS2322, the README block is TS2739 'missing ... objects, onEdit', and the four-prop control compiles. The first probe run was a setup failure (TS2835 on the probe's own import; no probe line was judged) and is excluded. RESTORE after each variant: `git checkout HEAD --` plus proof: `git diff HEAD` 0 bytes, `git status` 0 lines, worktree blob of ObjectView.tsx b13b2a26 = HEAD blob. NOT MEASURED: dependants apps/console, examples/byo-backend-console and examples/console-starter by compiler. Reason: they resolve app-shell through dist, and emitting dist from a declaration whose exported name is the open question was deferred. Structural reading: 0 references to app-shell's ObjectView identifier in any of them (git grep); control: the same grep finds AppContent's two mounts and the README import. NOT MEASURED: check-changeset-presence, check:new-line-citations, check:control-bytes, app-shell tests. Reason: the branch carries no commit beyond base efbf18de5, so there is nothing for them to judge.\",\n  \"mcp_calls\": \"0\",\n  \"api_writes\": \"1 — POST /repos/objectstack-ai/objectui/issues/7483/comments (this os-dev-report, via scripts/pm/post-stamped.mjs). `git push` of the empty branch is not REST. No pr_create and no label-write: no PR was opened.\",\n  \"files_changed\": [],\n  \"deviations\": [\n    \"needs_decision before code: no commit, no PR, no changeset, no PR-assignee label-write. The claim's branch was pushed empty (head = base efbf18de5) as the landing marker and write probe.\",\n    \"Order H1 is half false. The ObjectView / ObjectViewInner signatures hold. The claim that PR 9310 shows how DashboardView / AppShell declared their props is false: edea22a59 touches neither. DashboardView is an inline `{ dataSource?: any }`, and AppShellProps is app-shell's types.ts declaration, itself a KNOWN_COLLISIONS entry (app-shell vs layout). What transfers from 9310 is its tsc-driven pin method; app-shell's local pin precedent is a `*.types.test.ts` compiled by tsconfig.test.json (expressionUser.parameterContract.types.test.ts).\",\n    \"Order H2 guessed onEdit optional; measurement shows the required spelling is the one that lands (0 vs 2). Sibling RecordDetailView already declares `onEdit` required and `objects: any[]`.\",\n    \"Worktree node_modules and worktree removed after this report; the remote branch is left at base.\"\n  ],\n  \"open_questions\": [\n    {\n      \"question\": \"The ruling names the exported interface `ObjectViewProps`, but @object-ui/plugin-view already declares and exports `export interface ObjectViewProps` (the schema-driven view's props, a different shape). A second declaration in app-shell reds the one-authority gate (objectui#6273 family, ruled on objectui#6172 甲/A1: every exported type name has exactly one authority). The gate's header says adding a KNOWN_COLLISIONS line is not a supported way to pass, and the ruling forbids changing plugin-view's surface. Which yields?\",\n      \"options\": [\n        \"A: keep the ruled name and add a KNOWN_COLLISIONS line for ObjectViewProps (app-shell vs plugin-view). Business need: no in-repo consumer imports either props type by name today, so the only pull for the exact name is the ruling's text. Long-term: grows a ratchet that must only shrink, against the one-authority ruling, and recreates the AppShellProps shape already in that baseline. AI-error: two published ObjectViewProps with different shapes (schema-driven vs route-level four props); auto-import picks by order and the wrong pick surfaces as a remote TS2322, the measured failure of that family. Scope: one debt line nobody is pulled to repay.\",\n        \"B: execute everything else in the ruling as written, under a non-colliding exported name. Candidates, all 0 hits in the tree: ConsoleObjectViewProps (the file's own header calls this component the Console ObjectView, and app-shell already publishes Console-prefixed types such as ConsoleActionDispatch and ConsoleActionRuntime); AppShellObjectViewProps (package-qualified); ObjectViewRouteProps (the README's 'route-level object surface'). Business need: same callers, zero rename cost (no importer of the name exists yet). Long-term: one authority per name holds and the ratchet is untouched (measured: gate exit 0, 11/11). AI-error: the type name tells the two same-named components apart, so an import cannot silently bind to the other package's shape. Scope: no gate, no debt; the only cost is that the exported name departs from the ruling's letter.\",\n        \"C: declare the props as an inline object type on both parameters, with no exported interface; consumers reach it through React's ComponentProps of typeof ObjectView. Business need: same as B. Long-term: no collision, but the ruling's 'export an interface' and H3's barrel export are dropped, so the contract has no importable name. AI-error: misspellings are caught the same as B; the contract is less discoverable. Scope: none.\",\n        \"D: rename plugin-view's ObjectViewProps. Excluded by the ruling (no other published surface changes); listed only for completeness.\"\n      ],\n      \"recommendation\": \"B with ConsoleObjectViewProps. On all four axes it keeps what the ruling buys: the parameter is never bare any, dataSource is the DataSource contract, one interface serves ObjectView and ObjectViewInner, and it is exported beside RecordFormPageProps in the barrel's type list. It also honours the one-authority ruling the gate enforces. A trades a permanent collision for the literal name, and nothing measured pulls for that name. If B is ruled, the implementation lands as measured, with values: dataSource: DataSource (required); objects: any[] (kept loose, priced at 75); onEdit required, typed (record: Record of string to unknown) returning void; externalRefreshKey?: number. It would also repair the 3 dataSource diagnostics by typing, not casting (an optional call that keeps the adapter as this; the seam declaring the capability it probes), update the README block to pass objects and onEdit, and add a `*.types.test.tsx` pin: a misspelled-prop refusal, an any-guard on the parameter, and a four-prop control.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"class: a · packages/app-shell/README.md's `### ObjectView` block mounts ObjectView with dataSource alone, while the outer wrapper calls `objects.find(...)` with no guard, so a verbatim copy throws on first render (runtime crash read from source, not executed). Compile probe: silent on base, TS2739 'missing ... objects, onEdit' under the declared type · carried by THIS card: it is the claim's 'dependant call site the measured narrowing breaks' and lands with its implementation, not as a separate card · dedupe words: app-shell README ObjectView objects, doc snippet ObjectView missing objects\",\n    \"carrier: 承接者:无 · noted, not filed: ObjectViewInner reads `objectDef.list_views` beside `listViews` at three sites (surfaced by the objects narrowing as TS2551 'Did you mean listViews?'); the same alias read recurs in AppHeader, app-shell MetadataProvider, InterfaceListPage and react's useElementDataSource. packages/types field-types already records that list_views appears nowhere in @objectstack/spec 17.2.0 dist: a dead alias read, not a class a/b/c card\",\n    \"carrier: 承接者:无 · noted, not filed: triage item 5 (retire the batch-11 'this block only checks the export exists' note) has no in-tree carrier: `ObjectView` has 0 hits in scripts/check-doc-snippet-types.mjs; the note lives only in the batch-11 report comment\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
