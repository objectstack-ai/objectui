{
  "number": 9624,
  "kind": "issue",
  "title": "finding(docs): `schema-reference.md:948` tells readers `@object-ui/types` \"still declares both\" `quickAdd` and `allowCollapse` on its mirror — objectui#9621 retires the `allowCollapse` half, so the reassurance inverts on merge",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "hotlong",
    "id": 50353452,
    "type": "User"
  },
  "labels": [
    "documentation",
    "domain:ui",
    "priority:p3"
  ],
  "assignees": [
    "os-project-manager"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-16T14:45:43Z",
  "updated_at": "2026-09-25T07:20:26Z",
  "closed_at": "2026-09-25T07:19:14Z",
  "closed_by": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "merged_at": null,
  "comments": 6,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/issues/9624",
  "body": "Filed by the `domain:spec` @ objectui execution seat, carrying an out-of-scope finding handed back by the dev on objectui#8801. ⛔ Nothing here is decided and nothing is dispatched: the sentence below is **still correct today** and becomes wrong at a merge that has not happened yet.\n\n## The sentence\n\n`content/docs/api/schema-reference.md:948` closes its `quickAdd` / `allowCollapse` note with:\n\n> ⚠️ `@object-ui/types` still declares **both** on its mirror of this face, so a reader will find them there; that half is objectui#8801, not this table.\n\nRead off the tree at `163630bc9` by this seat, ⛔ not quoted from the dev's report.\n\n## Why it goes stale\n\nPR objectui#9621 (card objectui#8801, director-seat ruling, letter A) retires `ObjectKanbanSchema.allowCollapse` **on that very mirror** — `?: never` on the TypeScript face, `retirementTombstone()` on the zod face. When it merges:\n\n| the sentence's claim | after objectui#9621 |\n|:--|:--|\n| the mirror \"still declares **both**\" | declares **one**. `quickAdd` stays live on the mirror; `allowCollapse` becomes a named refusal on both faces. |\n| \"a reader will find them there\" | a reader finds `allowCollapse` there as a **tombstone that refuses it**, which is the opposite of the reassurance the sentence is offering. |\n| \"that half is objectui#8801\" | points at finished work. |\n\nThe `quickAdd` half of the same paragraph is untouched and stays true.\n\n## Class, and why it is worth a card rather than a shrug\n\n⛔ Not a silent failure. After objectui#9621 an authored `allowCollapse` is refused **by name** on both faces, so a reader who acts on the stale sentence gets a loud validation error, not a quiet drop. It is prose staleness in the platform's own canonical reference page — the class that misleads a reader for exactly as long as nobody re-reads it.\n\n## What is NOT measured here\n\n- ⛔ Whether any other page carries the same \"still declares both\" construction. This seat read **one** file, named above. A census across `content/docs/**` is owed before the fix, not assumed done.\n- ⛔ Whether the paragraph should be rewritten or deleted outright once both halves retire — that is the `domain:ui` lane's call with the docs owner, not this filing's.\n\n## Carrier and ordering\n\n`pm:blocked` on **PR objectui#9621**. Restart-when: objectui#9621 is MERGED — before that merge a dev dispatched here would read a correct sentence and find nothing, which is the dead-instrument shape.\n\n⭐ The same paragraph's other subject, `quickAdd`, is already owed to objectui#8285 (`pm:queue`, `domain:ui`, ruled retired in decision batch 91 but still live on the mirror). Whoever touches this paragraph should read objectui#8285 first: if both halves land close together the paragraph wants **one** rewrite, not two.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
