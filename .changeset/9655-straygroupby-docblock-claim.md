---
---

Test-only: repair a false claim in `ObjectView.strayGroupByLane-9242.test.tsx`'s docblock (objectui#9655). The note said the objectui#8365 `kanban.groupBy` refusal "already covers BOTH routes" and that an object-view document "never went through a validator"; both are false. The docblock now points at the instrument that enumerates the arm's install sites instead of restating a count, and says ACCEPTED where it said "not validated". No published behaviour changes — the only edit is a comment.
