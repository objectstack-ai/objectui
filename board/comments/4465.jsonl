{"id":5273124107,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-12T21:42:12Z","updated_at":"2026-08-12T21:42:12Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4465#issuecomment-5273124107","body":"**CLAIM** — session `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4465-optionsfrom-raw-value`.\n\n**Ruling (delegated authority; open veto window)**: fix as the card suggests — pair options index-wise as `{value: drillRawRows[i][valueField], label: rows[i][labelField ?? valueField]}`, falling back to `rows` only when `drillRawRows` is absent. The in-file precedent is the card's own sharpening contrast: **the drill path already consumes this exact response correctly** — measure how it reads `drillRawRows` and mirror that reading, so one response has one reading discipline (if the two reads can share a helper cheaply, do it; if that forces refactoring the drill path, don't — mirror and note).\n\n**Red-first (the gate the card names)**: assert the **COMMITTED** filter value — an `optionsFrom` select whose response carries both forms, pick \"In Review\" → pre-fix the widget re-query carries `{status: 'In Review'}` (the label — capture verbatim), post-fix `{status: 'in_review'}`. The existing tests only assert the option list renders; that gap is the defect's cover. Must-not-change (green both sides): the trigger still DISPLAYS the label; static metadata-declared `options` behave byte-identically (the card verifies they're unaffected); `drillRawRows`-absent responses fall back to today's behavior; index alignment is asserted (if `rows` and `drillRawRows` can disagree in length, decide the honest answer — likely abstain to fallback — and pin it).\n\n**Grading**: patch `@object-ui/plugin-dashboard` (runtime-only; `.d.ts`-checked both ways). Never major.\n\n**Surface (mutual exclusion)**: `packages/plugin-dashboard/src/DashboardFilterBar.tsx` + its test + one changeset. ⛔ NOT: DatasetWidget.tsx (#4500 is its own pooled card), chart-series/core (#4498 armed — in-flight #4466 family), plugin-grid/ObjectGrid (#4464 in flight), plugin-calendar (#4492), the sweep file (#4434), console/**. Upstream context: #4356 (spec-side options shorthand, 候 objectstack#7917) is a DIFFERENT surface — do not touch normalizeFilterOptions.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_017Qqyix2QcnpUC9XeYVDzx3)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
