{"id":5328732361,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T13:18:42Z","updated_at":"2026-08-18T13:18:42Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4574#issuecomment-5328732361","body":"First-touch grading (triage seat): **promoted `finding` → `pm:queue`**.\n\nThe fix is already designed to the ruled #4567 pattern (producer-side `React.useMemo` keyed on `objDraft.fields`, second memo — the filter set differs from the grid's). Promoting rather than shelving because the surface re-renders at keystroke rate and the card names a real latent hazard: any future data call in that effect's dependency chain turns this into a #4567 with no producer change. S mechanical; the card's \"why this is NOT a #4567\" section is the claimant's test brief — assert no new fetch appears, only identity stability.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335854916,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-19T00:13:20Z","updated_at":"2026-08-19T00:13:20Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4574#issuecomment-5335854916","body":"Claim: PM loop round 12 (refill)\nSession: `session_01RV6yuVCxymHYE16PL9vQkE`\nBranch: `claude/issue-4574-form-designer-fields-memo`\nWorktree: `objectui-issue-4574`\nDomain: `repo:objectui` (app-shell / studio-design)\nFile surface: the Studio Data pillar form designer under `packages/app-shell/src/views/studio-design/` + its tests (stop on breach; explain in the report)\nContainer & model: S, `mode:subagent`, `model: sonnet` (triage: \"S mechanical\")\nClause-②: no — identity stability only; no behaviour, no contract, no public surface.\nSerial constraints cleared: `packages/app-shell` is free again — **#5243's PR #5278 merged**, releasing `src/providers/`, and #5233's #5272 merged before it, releasing `src/views/`. No open PR or in-flight claim touches app-shell. The three other in-flight cards are in `packages/plugin-report` (#5225), `apps/console` (#4303), and `packages/react` + `packages/components` (#5123); PR #5281 holds `packages/core`. All disjoint.\n\n⚠️ **Card age**: filed 2026-08-13, six days ago, and app-shell has taken several merges today (#5272, #5278 among them). The dispatch order requires re-deriving the render path on current `origin/main` before editing — ⛔ no line numbers carried from the card.\n\nTriage's brief is carried through: fix to the **ruled #4567 pattern** (producer-side `React.useMemo` keyed on `objDraft.fields`, a **second** memo because the filter set differs from the grid's), and the card's own *\"why this is NOT a #4567\"* section is the test brief — **assert no new fetch appears, only identity stability**. A test that proves a fetch was removed would be proving the wrong thing, since there was never a duplicate query here.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5336071598,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-19T00:43:00Z","updated_at":"2026-08-19T00:43:00Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4574#issuecomment-5336071598","body":"os-dev-report\n\n```json\n{\n  \"issue\": 4574,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-4574-form-designer-fields-memo\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5285\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Re-derived on current main (after #5272/#5278): DataPillar's form-preview still built ObjectForm's `fields` array inline on every render; confirmed ObjectForm still lists schema.fields by identity in its field-generation effect, and confirmed that effect is a pure derivation (no dataSource call) with the schema fetch and record load as separate, unrelated effects. Added a second React.useMemo (`formFields`), keyed on objDraft.fields, alongside the existing #4567 `gridColumns` memo -- not shared, since the form's filter omits the grid's `actions` exclusion. Replaced the inline array at the ObjectForm call site with the memo.\",\n  \"tests\": \"New file packages/app-shell/src/views/studio-design/StudioDesignSurface.formFields.test.tsx: (1) three no-op re-renders leave every recorded schema.fields reference `toBe` the same object (Object.is identity, not toStrictEqual) since the settled mount, and dataSource find/findOne/getObjectSchema call counts stay flat -- asserts identity stability + no new fetch, per the card's brief, not fetch elimination (there was never a duplicate query on this path); (2) must-not-regress pin: a real '+ Add field' still produces a NEW fields identity (green in both pre-fix and post-fix states, mirroring gridColumns.test.tsx's own live-dependency pin). Reverse verification: committed fix+test (de68a2209), then `git checkout origin/main -- StudioDesignSurface.tsx` reverted only the source (test kept). Predicted before running: the identity-stability loop goes red (fresh inline array every render), fetch-count assertions stay green (never depended on schema.fields), test 2 stays green (invariant). Observed exactly that -- 1 failed/1 passed, failure at `expect(snap).toBe(stable)`: 'expected [ name, status ] to be [ name, status ] // Object.is equality' (content-equal, reference-different, confirming the assertion targets identity). Restored fix via `git checkout claude/issue-4574-form-designer-fields-memo -- <path>`; working tree clean against fix commit. No rebuild in this ablation's resolution path -- vitest imports the .tsx source directly for app-shell's own tests, no dist step to hide staleness. Final HEAD a4ba2ff85: `pnpm --filter '@object-ui/app-shell^...' build` clean; `pnpm --filter '@object-ui/app-shell' type-check` clean; `pnpm --filter '@object-ui/app-shell' build` clean; `pnpm --filter '@object-ui/app-shell' lint` 0 errors (2475 pre-existing warnings, none touching this change); `node scripts/check-control-bytes.mjs` OK; `node scripts/check-changeset-presence.mjs` OK (patch changeset added); `node scripts/check-changeset-no-major.mjs` OK; `pnpm exec vitest run packages/app-shell/src/views/studio-design/ --maxWorkers=2` -- 29 test files, 157 tests, all pass (run twice, before and after the changeset commit, both green). Narrowing declared: attempted the full `packages/app-shell/` suite (281 files) first but the shared /tmp/os-heavy-verify.lock was contended by other concurrent dev agents (confirmed via `fuser -v` -- held by another agent's flock/node process) long enough that it could not be observed to completion; per PM direction, narrowed to the studio-design/ directory (the pillar this PR touches) plus package-level build/type-check/lint, all completed in the foreground. packages/plugin-form/ (unmodified consumer) was not run locally -- out of narrowed scope. CI runs the full farm on the PR and is authoritative for the rest.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
