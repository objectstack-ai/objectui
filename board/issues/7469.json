{
  "number": 7469,
  "kind": "issue",
  "title": "`AppComponentSchema.actions[]` (`AppAction`) is rendered by the standalone runner only — app-shell / console do not render it, while `core/app-schema.mdx` \"Global Actions\" teaches it as the app's global toolbar buttons (ruled B: app-shell renders it too)",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "labels": [
    "domain:spec",
    "enhancement",
    "pm:queue",
    "priority:p3"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-03T07:30:59Z",
  "updated_at": "2026-09-25T07:52:43Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 11,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/issues/7469",
  "body": "> ## ⚠️ CORRECTED by the director seat, 2026-09-07 (decision batch #71)\n>\n> The original central claim below — \"has no reader\" — is **false**. `packages/runner/src/LayoutRenderer.tsx:240` renders the `'button'` arm as toolbar buttons and `:274` renders the `'user'` arm as an avatar dropdown (measured by two seats on `origin/main`; comments 5551453088, 5556489689, 5560256200). Both censuses missed it because they searched for the type name `AppAction`, which the runner never spells (it reads `app.actions` structurally), and the 12-file consumer list omitted `packages/runner`.\n>\n> **Ruling (maintainer, verbatim 「这个结合我们的平台，业务上应该有需求吧」): option B** — app-shell / console render `actions[]` too, sharing the runner's implementation; the docs section stays and names the hosts. See comment 5565742433. The text below is kept as the record of the original measurement and is superseded where it says \"no reader\" / \"rendered by nothing\".\n\n---\n\nSurfaced while executing #7344 (the per-key consumer measurement for `AppActionSchema.onClick`), measured on `origin/main` `d88e20f55`. Filed unassigned; observation class (dead declared surface), not a defect card — triage decides.\n\n## What was measured\n\n`packages/types/src/app.ts` declares `AppComponentSchema.actions?: AppAction[]` (\"Global Actions (User Profile, Settings, etc)\"), and `packages/types/src/zod/app.zod.ts` mirrors it as `AppActionSchema` (`type: 'button' | 'dropdown' | 'user'`, `label`, `icon`, `avatar`, `description`, `items`, `shortcut`, `variant`, `size`).\n\n- Zero references to `AppAction` outside `packages/types` (`git grep -nE \"\\bAppAction\\b\" -- packages apps examples ':!packages/types'`, docs and changelogs excluded).\n- Every consumer of `AppComponentSchema` / `AppSchema` (12 files: `layout/src/AppShell.tsx`, `AppSchemaRenderer.tsx`, `NavigationRenderer.tsx`, app-shell `UnifiedSidebar.tsx`, `useNavActionDispatch.ts`, the metadata-admin `AppPreview.tsx` / `AppNavCanvas.tsx` / `clientValidation.ts`, `studio-design/*`, console `AppManagementPage.tsx`) reads `navigation`, `areas`, `layout`, `theme` — none reads `.actions`. Control on the same files: `AppSchemaRenderer.tsx` reads `navigation` 25 times. **(Superseded: `packages/runner` was not in this list and does read it.)**\n- The only `.actions` readers in app-shell are `objectDef.actions` (server-declared OBJECT actions — `DeclaredActionsBar`, `ObjectView`, `InterfaceListPage`) and `notification.actions`; `header-bar.tsx`'s `schema.actions` is `HeaderBarSchema`, a different declaration.\n\nSo a value authored under `actions[]` on an app node is parsed, kept, and rendered by app-shell / console — not by the runner, which renders it. **(Superseded wording; see banner.)**\n\n## Why it matters\n\n`content/docs/core/app-schema.mdx` (\"Global Actions\") documents the array as \"global toolbar buttons\" with a typed `const crm: AppComponentSchema` example that carries one — an author copying the page and running it in the console gets a green parse and no button (the runner does render it). PR #7468 (#7344) retired the array's `onClick` member (the handler-expression string dialect, #6182 ruling) and edited that page's `AppAction` row and example line only as far as two gates forced; the rest of the section still teaches the array.\n\n## Options for triage (original; ruled B on 2026-09-07)\n\n- A. Retire `AppComponentSchema.actions` / `AppAction` / `AppActionSchema` on both faces (tombstone + docs section removed), the #4895 route. **Refused — it would remove the runner's live toolbar and user menu.**\n- B. Wire a reader: render `actions[]` in the app-shell chrome header (the documented intent). **Ruled.**\n\nDedupe: MCP `search_issues` (REST search is 403 on this session) for \"AppComponentSchema actions AppAction global toolbar actions no reader app-shell layout dead declared key\" → 28 results, none about this container (nearest: #4841 app-shell schema-path slots, #7437 inert `action:button` bag, #4418 action renderers vs `ActionSchema`); control query on the #6182 title wording hit #6182 / #7344 / #5927.\n\nRefs: #7344 · PR #7468 · #6182 · #4895 (the retirement route precedent) · ADR-0049 · #6854 · #7719 · PR #7720."
}
