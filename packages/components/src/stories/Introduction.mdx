import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Introduction" />

# ObjectUI — Server-Driven UI Engine

**ObjectUI** is a universal, server-driven UI (SDUI) engine built on **React + Tailwind + Shadcn**.
It interprets JSON metadata into pixel-perfect, accessible, and interactive enterprise interfaces.

## 🏗 Architecture

| Package | Role |
|---|---|
| `@object-ui/types` | JSON Protocol — Pure TypeScript interfaces |
| `@object-ui/core` | Engine — Schema registry, validation, expressions |
| `@object-ui/components` | Primitives — Shadcn UI wrappers (Button, Card, Badge…) |
| `@object-ui/fields` | Field Widgets — 47+ input field renderers |
| `@object-ui/layout` | Shell — Header, Sidebar, AppShell |
| `@object-ui/react` | Runtime — `SchemaRenderer`, `useRenderer` |
| `@object-ui/plugin-*` | Plugins — Grid, Kanban, Charts, Gantt, Map… |
| `@object-ui/data-*` | Data Adapters — REST, ObjectQL, ObjectStack |

## 🎯 How It Works

Everything is driven by **JSON schemas**. A UI tree is a recursive structure of `UIComponent` nodes:

```json
{
  "type": "page",
  "children": [
    {
      "type": "grid",
      "props": { "cols": 3, "gap": 4 },
      "children": [
        { "type": "metric", "label": "Revenue", "value": "$45,231" },
        { "type": "metric", "label": "Users", "value": "2,350" },
        { "type": "metric", "label": "Growth", "value": "12.5%" }
      ]
    }
  ]
}
```

The `SchemaRenderer` recursively walks this tree and resolves each `type` to a registered React component.

## 📖 Storybook Guide

This Storybook is organized by capability:

- **Getting Started** — Introduction and data binding guides.
- **Primitives** — Atomic Shadcn UI wrappers driven by JSON (Button, Card, Input, Dialog…).
- **Fields** — All 47+ field widget renderers (text, number, select, date, lookup…).
- **Plugins** — Complex views powered by specialized libraries (Grid, Kanban, Gantt, Charts, Map…).
- **Templates** — Composed page-level layouts (Dashboard, Page, Reports, Sidebar).
- **Apps** — Full application demos (CRM) using all of the above.

## 🔗 Links

- [GitHub Repository](https://github.com/objectstack-ai/objectui)
- [ObjectStack Spec](https://github.com/objectstack-ai/spec)
- [Shadcn UI](https://ui.shadcn.com/)
