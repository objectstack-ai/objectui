{
  "number": 3414,
  "kind": "pull_request",
  "title": "fix(plugin-dashboard): 透视表 cell key 改用 JSON 编码,维度值含空格不再串格 (objectstack#5473)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "labels": [
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-05T23:30:52Z",
  "updated_at": "2026-08-05T23:37:06Z",
  "closed_at": "2026-08-05T23:37:05Z",
  "closed_by": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "merged_at": "2026-08-05T23:37:05Z",
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/3414",
  "body": "Fixes objectstack-ai/objectstack#5473\n\n## 前提复核(对 origin/main 58a00f0b4)\n\n空格拼接仍在,并已用探针复现串格。行维度 `region`、列维度 `quarter`,两行:\n\n| 行 | region | quarter | amount | 旧 cell key |\n| --- | --- | --- | --- | --- |\n| 0 | `New` | `York Q1` | 111 | `New York Q1` |\n| 1 | `New York` | `Q1` | 222 | `New York Q1` |\n\n探针输出:`cellIndex` 只剩一个条目 `[ 'New York Q1', 1 ]`。渲染出来是「`New` 那一格显示 222」(另一行的度量值),111 在整张表里无处可达,drill-through 按同一个 index 钻到第 1 行的记录。全程无报错。\n\n## 改动\n\n1. **cell key** 改 `pivotCellKey(rowId, colId) = JSON.stringify([rowId, colId])`,边界由 JSON 自己的引号/转义承担,不再依赖「数据不会包含的字符」。\n2. **row id 一并换成 JSON**(`pivotRowId = JSON.stringify(values)`)。同函数里保留两种编码(cell key 用 JSON、row id 用 U+0001)是下一个读者的坑,而且 U+0001 那条仍然是「假设值里不会有这个字符」;换掉之后全函数只有一种编码策略。\n3. **行小计的 key 收敛到同一个 encoder**。这一处不是顺手改,是本次改动的必经半径:`rowTotalById` 查的就是 `rowHeaders[].id`,row id 编码一变,它必须跟着变,否则查不中。\n\n顺带说明第 3 点暴露的既有缺陷(改动前就存在,不是本次引入):它原先手写 `join('')`,而表头用的是 `join(U+0001)` —— 只有「恰好一个行维度」时两者才碰巧相等,而仓库里每一条 pivot 用例都正好是这个形状。于是 **3 个及以上维度的透视表,Total 列一直是空的**。统一 encoder 后自然修复,并补了用例钉住。\n\n## 测试:钉行为,不钉编码\n\n既有用例直接钉 key 字面形态(`cellIndex.get('Open High')`)—— 这正是「一个把两行合并的 key」能长期读起来像对的原因。整条重写为 `cellAt(p, 行标签, 列标签)`:用模块自己的 `pivotCellKey` 按渲染器的路径解析,断言的是「哪个(行,列)组合落到哪个扁平行下标」,编码从此可以自由演进,保证不变。\n\n新增/重写:碰撞对不再串格(issue 的 New / York Q1 例)、多行维度且值含空格的同类碰撞、渲染 + drill-through 命中正确记录、多行维度的行小计。#3389 那条(objectstack#5450 补的多行维度用例)保留 fixture、去掉编码断言 —— 它钉的东西改为「两个行维度不会合并」本身。该文件不再以任何形式(字面量/转义/fromCharCode)引用控制字符。\n\n## 反向验证(方向先声明,再运行)\n\n把三处 encoder 还原成改前形态后,预测 4 红 2 绿,实际完全一致:\n\n- 红 `expected 1 to be 2` —— 碰撞对合并成一个 cell(两条)\n- 红 `expected [ 'New', '222', '—' ] to deeply equal [ 'New', '111', '—' ]` —— 正是 issue 描述的症状\n- 红 `expected [ '—', '—' ] to deeply equal [ '10', '20' ]` —— 多行维度行小计\n- **绿(关键)**「cross-tab 基础用例」与「两个行维度不会合并」:旧的控制字符 join 确实提供了行维度之间的边界,这两条若变红就说明我的重写把 #3389 的保证删掉了而非保留。\n\n## 验证\n\n- `npx vitest run --maxWorkers=2 packages/plugin-dashboard/src/__tests__/DatasetWidget.test.tsx`(仓根跑,按文件名确认):47 passed\n- plugin-dashboard 全量:22 files / 208 tests passed\n- `tsc --noEmit`:干净;`lint`:0 errors\n- `pnpm check:control-bytes`:OK,另按文件自查 `[\\x00-\\x08\\x0b\\x0c\\x0e-\\x1f]` 无命中\n\n## 越界未改(已另开单)\n\n`packages/plugin-report/src/DatasetReportRenderer.tsx` 是同一家族的另一处:`bucketId` 用**空串** join 多个维度值(连边界都没有),cell key 同样是空格拼接。属另一个 widget,本 PR 不碰,单独开单。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01GTRjn8xBqp75dk7kFupVRt)_"
}
