{
  "number": 3803,
  "kind": "issue",
  "title": "finding(app-shell): useAutoRunCreate 在 render 阶段读 ref,`?runAction=create_environment` 的 autoTrigger 可能被无关 re-render 抹掉",
  "state": "closed",
  "state_reason": "not_planned",
  "draft": null,
  "author": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "labels": [
    "pm:dispatched",
    "pm:queue"
  ],
  "assignees": [
    "yinlianghui"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-08T16:59:09Z",
  "updated_at": "2026-08-10T11:18:01Z",
  "closed_at": "2026-08-10T11:18:01Z",
  "closed_by": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "merged_at": null,
  "comments": 4,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/issues/3803",
  "body": "观察件,顺手记录 —— 在 #3482 / PR #3801 改 `EnvironmentListToolbar` 加载态时路过,**与该 PR 的改动无关**,`origin/main` 上早已存在,故未在那个 PR 里动它。\n\n## 事实\n\n`packages/app-shell/src/environment/EnvironmentListToolbar.tsx` 的 `useAutoRunCreate()` 在 **render 阶段**读 ref:\n\n```\nconst consumed = useRef(false);\nconst shouldRun = requested && !consumed.current && ctaKind != null;\nuseEffect(() => {\n  if (!shouldRun) return;\n  consumed.current = true;\n  // …strip runAction from the URL\n}, [shouldRun]);\nreturn shouldRun;\n```\n\neslint 已经在标它(`react-hooks/refs`,warning):`Cannot access ref value during render`。在 `origin/main`(核验 sha `7b3e04820`)上跑 eslint 同样出这条,不是新引入的:\n\n```\n> 107 |   return shouldRun;\n      |          ^^^^^^^^^ Cannot access ref value during render\n```\n\n## 为什么可能是真缺陷(未实测,故按 observation-class 提)\n\n`shouldRun` 决定创建动作是否带 `autoTrigger: true`(欢迎页 CTA 的 `?runAction=create_environment` 深链靠它自动打开创建对话框,#844)。effect 里把 `consumed.current` 置 true 后,**任何后续 re-render** 都会把 `shouldRun` 算成 `false`,`autoTrigger` 随之从 action prop 上消失。\n\n于是取决于 `action:bar` runner 消费 `autoTrigger` 的时机:如果在 effect 落地后、runner 真正消费前发生一次无关 re-render(父级 `onUpgrade` 改 state、列表数据刷新、entitlements 因 `refreshKey` 重解析等),深链就静默失效 —— 用户从欢迎页点「创建你的环境」落到列表页,却没有对话框弹出,只能自己再找一次创建按钮,正是 #844 要消除的那个动线。\n\nrender 纯度被破坏本身也让 StrictMode 双调用 / 并发渲染中被丢弃的那次 render 与被保留的那次读到不同的 `consumed.current`。\n\n**我没有复现这个失效**:现有两条深链测试是绿的,它们不制造中间的无关 re-render。所以严重度请 PM 的 triage 轮定(#4949 的经验:提交时自评的严重度两个方向都不可靠)。\n\n## 修的方向(供参考,未实施)\n\n「一次性信号」应该是 state 而不是 render 阶段读的 ref —— 例如在 effect 里 `setConsumed(true)`,或把「已消费」直接建模成 URL 参数被 strip 后的自然结果(`requested` 从 `useState` 初始值改为每次渲染读 URL,strip 之后自然为 false),这样 render 不再读可变 ref。哪种更对取决于 runner 消费 `autoTrigger` 的确切时机,需要先把上面那个中间 re-render 的场景写成测试钉住。\n\n## 相关\n\n- 落点文件:`packages/app-shell/src/environment/EnvironmentListToolbar.tsx`(`useAutoRunCreate`)\n- 深链动线来源:#844;顺带经过的单:#3482 / PR #3801\n- 已按 #4949 的口径先搜过开放 issue(`EnvironmentListToolbar` / `useAutoRunCreate` / `ref during render` / `runAction`),无重复。未打 `pm:queue`,未指派。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01GTRjn8xBqp75dk7kFupVRt)_\n"
}
