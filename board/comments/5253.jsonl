{"id":5334150569,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T21:14:50Z","updated_at":"2026-08-18T21:14:50Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5253#issuecomment-5334150569","body":"Triage: queued as Bug by inheritance of #5201's ruling together with its reasons — the filer re-verified each reason on the `default` fallback branch (live `max_length` authoring spelling; hand-written FormSchema path with no normalizing producer; measured `maxlength=null` plus a stray invalid attribute), so the mother ruling transfers. Reference fix shape carries over unchanged: local destructure as in the landed `input` branch (#5201) and `textarea` (#3439), not a widening of the shared strip table that feeds four branches.\n\nFreshness: #5201's PR merged today — implement and re-measure against current `main`. Test criteria: copy #5201's (`getAttributeNames()` + `getAttribute('maxlength')`, assert both halves).\n\nSame file as any in-flight form.tsx work — executing seat clears serial constraints at claim time as usual. Size/model suggestion: S, sonnet/opus.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335419626,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:20:36Z","updated_at":"2026-08-18T23:20:36Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5253#issuecomment-5335419626","body":"Claim: PM loop round 12 (refill)\nSession: `session_01RV6yuVCxymHYE16PL9vQkE`\nBranch: `claude/issue-5253-form-default-branch-ceiling`\nWorktree: `objectui-issue-5253`\nDomain: `repo:objectui` (components)\nFile surface: `packages/components/src/renderers/form/form.tsx` — the `default` fallback branch only — plus its tests (stop on breach; explain in the report)\nContainer & model: S, `mode:subagent`, `model: opus`\nClause-②: no — restores a declared ceiling to enforcement; accept set unchanged, no public surface widened.\nSerial constraints cleared: **`form.tsx` is free.** Triage flagged \"same file as any in-flight form.tsx work — executing seat clears at claim time\", so I cleared it explicitly rather than by assumption: the two recent cards on this file, **#4259** (PR5238, `FormSchema.onChange`) and **#5201** (PR5255, the `input` branch ceiling — this card's mother ruling), are **both merged**. No open PR and no in-flight claim touches `packages/components`. The four other in-flight cards are in `app-shell/src/views`, `app-shell/src/providers`, `plugin-view`, and `.github/workflows` — all disjoint.\n\nFreshness, per triage: #5201's PR merged **today**, so the reference implementation is in the tree and the branch you are fixing sits next to it. Re-measure against current `origin/main` before editing — do not port line numbers from the card.\n\n⛔ The fix shape is inherited, not open: **local destructure in the `default` branch**, mirroring the landed `input` branch (#5201) and `textarea` (#3439). ⛔ **Not** a widening of the shared strip table — that table feeds four branches and widening it changes three you were not asked to touch.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335665492,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:45:53Z","updated_at":"2026-08-18T23:45:53Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5253#issuecomment-5335665492","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 5253,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-5253-form-default-branch-ceiling\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5276\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Re-measured the default fallback branch of packages/components/src/renderers/form/form.tsx against current main (87d9202b1, i.e. AFTER #5201's PR merged) and reproduced the filer's measurement exactly: max_length:50 gave attrs=[\\\"class\\\",\\\"max_length\\\",...] with maxlength=null plus a stray inert max_length=\\\"50\\\", while maxLength:50 gave maxlength=\\\"50\\\" by the coincidence of naming a real DOM attribute. #5201's merge did NOT cover this branch. Fixed with the inherited shape — a local destructure of the legacy key off domFieldProps plus a locally resolved maxLength ?? max_length applied after the spread — exactly mirroring the landed input (#5201) and textarea (#3439) arms. The shared stripRendererOnlyProps table is untouched: its only appearance in the diff is inside an explanatory comment, so the checkbox/switch/select arms it also feeds are unchanged. Three files changed, all inside the sanctioned surface.\",\n  \"tests\": \"All gates run from the repo root at final commit 6ce9edfcd with a clean working tree. New test packages/components/src/renderers/form/__tests__/form-builtin-default-branch-max-length.test.tsx — 6/6 pass; asserts both halves per the inherited criteria (getAttributeNames() AND getAttribute('maxlength')), and pins the routing with a counter-probe (ComponentRegistry.get('form') truthy while get('zzunknown') and get('field:zzunknown') are undefined, so the two undefineds are a reading, not an empty registry). REVERSE VERIFICATION, predicted before running: routing guard green, camelCase green, legacy max_length RED, stray attribute RED, canonical-wins RED on its 2nd assertion only, uncapped green => 3 failed / 3 passed. OBSERVED: identical — 'Tests 3 failed | 3 passed (6)', with 'expected null to be 50', 'expected [class, max_length, ...] to not include max_length', and canonical-wins failing only on the stray half (its cap assertion was already green pre-fix — the concrete proof a cap-only test would have let the invalid attribute survive). No rebuild gates this ablation: the subject is reached via a relative SOURCE import (../../../renderers -> ./form), not across a package exports->dist boundary, so the mutation reaches the tested code directly; restoring left the tree byte-identical to the commit (git status clean) and the suite back at 6/6. Full gate run: pnpm exec vitest run packages/components/ => 'Test Files 163 passed (163) / Tests 1479 passed (1479)' (covers the sibling input/textarea ceiling suites and the checkbox/switch/select arms sharing the untouched strip table); pnpm --filter @object-ui/components type-check => exit 0, script name echoed so it was not a zero-match no-op, and it runs 'tsc --noEmit && tsc -p tsconfig.test.json' so the new test file is covered; pnpm --filter @object-ui/components lint => exit 0, '891 problems (0 errors, 891 warnings)' (pre-existing package baseline); pnpm --workspace-concurrency=2 --filter '@object-ui/components^...' build => exit 0; node scripts/check-control-bytes.mjs => OK (4664 tracked text files) plus a direct grep -naP over the three changed files, clean; node scripts/check-changeset-presence.mjs => OK, 1 changeset declared. Additionally derived and ran the gates my actual diff touches (objectui has no scripts/pm/dispatch-gates.mjs — that mechanism is objectstack-only): check:self-import, check:esm-specifiers, check:phantom-deps — all green. Heavy build/test steps were serialized under flock -E 99 -w 540 /tmp/os-heavy-verify.lock; one lint attempt returned 99 (queued behind another agent) and was retried after spending the interval on the lock-free gates.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5335750236,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T23:58:37Z","updated_at":"2026-08-18T23:58:37Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5253#issuecomment-5335750236","body":"**ACCEPT** — PR #5276 flipped ready and enqueued. All 21 checks resolved: 19 success, 2 skipped by design.\n\nPath surface: the changeset, `packages/components/src/renderers/form/form.tsx`, and the new test. No governed surface.\n\n**I verified the constraint mechanically rather than taking the PR body's word**: the ruling said ⛔ do not widen the shared `stripRendererOnlyProps` table, and grepping the diff for that identifier returns exactly one line — inside an explanatory comment. No code change reaches the table, so the `checkbox` / `switch` / `select` arms it also feeds are genuinely untouched.\n\nTwo things this card measured that its own body did not say:\n\n1. **There are two independent defects here, not one.** Legacy `max_length` capped *nothing* and left a stray inert `max_length=\"50\"` on the DOM — invalid HTML that reads like a working cap to whoever greps the file next. Meanwhile camelCase `maxLength` capped correctly **by coincidence**: it happens to name a real DOM attribute, so it survived the raw spread. One declaration, two outcomes, split by spelling.\n\n2. **The reverse verification demonstrated why \"assert both halves\" is not boilerplate.** The \"canonical wins\" case failed on its stray-attribute assertion **only** — its cap assertion was already green before the fix. So a cap-only test would have passed against the bug and let the invalid attribute ship. That is the concrete justification for the inherited test criteria, produced by running them rather than by arguing for them.\n\nPremise re-measured on `main` **after** #5201's merge, reproducing the filer's numbers exactly — so #5201's landing genuinely did not cover this branch, which is the thing most likely to have gone stale between filing and dispatch.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
