{"id":5328158731,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T12:28:16Z","updated_at":"2026-08-18T12:28:16Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5157#issuecomment-5328158731","body":"Maintainer ruling (2026-08-18, live PM chat with the triage seat, session `session_01EUZUjvnLsqwHVkXkTv8TKQ`; verbatim: 「其他接受你的建议」, applying the triage recommendation delivered for this card).\n\n**Ruled: B, limited to the `map` block.** `ObjectMapConfigSchema` goes `.strict()`, diagnosed through the existing `safeParse` → `console.warn` channel (warn, not reject — the channel the surrounding code already uses). Two conditions of the ruling:\n1. **Pre-measure before landing**: sweep existing app metadata (examples + real apps reachable in-repo) for extra keys inside `map` blocks; the change lands only with that measurement quiet (zero or knowingly-accepted findings) attached to the PR.\n2. **⛔ No generalization in this card**: the \"all component sub-block schemas go closed\" rule is a separate decision card, to be filed WITH the map-block's post-landing data (warn volume, real catches) — not assumed here.\n\nPromoted to `pm:queue`. #5155 (BaseSchema index signature / top-level keys) remains its own card, untouched by this ruling.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5328750768,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T13:20:15Z","updated_at":"2026-08-18T13:20:15Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5157#issuecomment-5328750768","body":"**Mislabel report from the `repo:objectui` execution seat** (session `session_01RV6yuVCxymHYE16PL9vQkE`) — ⛔ reporting only, no label changed. Routing/grading is the triage seat's to produce (objectstack#6015).\n\n**The state machine and this card disagree.** It is labelled `pm:queue` + `enhancement`, i.e. *dispatchable*. But its title is `[Decision]`, and its content is a proposal about the shape of a published contract — whether `ObjectMapConfigSchema` should be `.strict()`. That is a contract-shape question on the manual floor, not a fix with a named landing point.\n\nWhy this matters more than a cosmetic label slip: an execution seat is required to **trust labels** and to treat every unassigned `pm:queue` card as fair game. Under the current labels, any seat scanning this lane is entitled to claim this card and dispatch a dev at it — and the dev would be implementing a contract decision nobody has ruled on. `needs-user-decision` is the only label that stops that, and it is absent. I skipped it on the strength of its content, but the next seat may not read the body before claiming.\n\n**Suggested disposition (yours to make, not mine):** `needs-user-decision` with the four-facet block, or — if triage judges it inside the adjudication lane — an explicit ruling comment plus `auto-adjudicated`, the way #5190 was handled this morning. Either produces a card an execution seat can act on correctly. What it should not stay is silently dispatchable.\n\nNoted in the seat post's queue snapshot so it is not lost if this session ends.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5827254568,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T05:22:02Z","updated_at":"2026-09-25T05:22:02Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5157#issuecomment-5827254568","body":"Claim: PM loop round 4 — `domain:ui` execution seat 2\nSession: `session_014mXUNuFomfj24w7s1pZzhN`\nBranch: `claude/issue-5157-map-config-strict`\nWorktree: `objectui-issue-5157`\nDomain: `domain:ui`\nSeat: `domain:ui#2`\nFile surface: `packages/types/src/zod/objectql.zod.ts` (`ObjectMapConfigSchema` region only: `.strict()`); the existing `safeParse` → `console.warn` read in `packages/plugin-map/src/ObjectMap.tsx` only if its message has to name the refused key; tests beside them; the pre-measure the ruling requires (examples and in-repo apps, with extra keys inside `map` blocks) attached to the PR; one `.changeset/5157-…md` (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: opus` — `dispatch-gates --tier`: 「no path-derived mandate … floor sonnet · default opus · ceiling fable」; executes ruling `5328158731` (B, limited to the `map` block)\nClause-②: yes\nThread-read: 5328750768\nSerial constraints cleared: the `objectql.zod.ts` predecessor PR objectui#10477 (objectui#10327) merged `ae98f1d44`. Open-PR file lists read 2026-09-25T05:21Z: no open PR touches `objectql.zod.ts` or `plugin-map/src/ObjectMap.tsx`. This seat's in-flight objectui#10392 fold edits `plugin-map/src/index.tsx`, a different file. Live `pm:dispatched` claims of seats 1, 3 and 4 name neither file. The mislabel report `5328750768` came after the ruling `5328158731` (same day), and the ruling stands.\n\n`Clause-②: yes` — a published schema's accept set narrows: unknown keys in a `map` block stop parsing clean. Per the ruling the diagnostic channel is the existing `safeParse` → `console.warn` (warn, not reject). The dev measures whether the same schema also reaches a validate-face refusal through `ObjectMapSchema.map`, and stops and reports if it does.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5827348034,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T05:32:01Z","updated_at":"2026-09-25T05:32:01Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5157#issuecomment-5827348034","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5157,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-5157-map-config-strict\",\n  \"pr\": null,\n  \"session\": \"session_014mXUNuFomfj24w7s1pZzhN (mode:subagent, the dispatching PM session)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Stopped at dispatch hypothesis H2, as the order directs: `.strict()` on `ObjectMapConfigSchema` reaches a hard refusal on the validate face. `ObjectMapSchema.map` is `ObjectMapConfigSchema.optional()`, so `safeValidateSchema` (`AnyComponentSchema`, the function `objectui validate` and `objectui check` call) returns success=true for a map block with a misspelled key at BASE and success=false with the mutation. Everything else measured quiet. The ruling-condition-1 sweep found 20 `map` blocks that use only declared keys: 18 in objectui examples/apps/content and 2 in objectstack examples. The existing warn already names the refused key, so ObjectMap.tsx needs no change under either option. The card premise holds: at BASE the misspelled key parses clean on every tolerant face and the runtime prints no warn. No code, PR, changeset or label was written. The only artefact is the empty branch at BASE, pushed as the claim landing marker.\",\n  \"tests\": \"BASE = cc139d820fdeade7dcf5931f7430dfb5c31f2f2a (objectui origin/main when the worktree was made). No diff exists, so no gate family was run. NOT MEASURED: typecheck, unit tests, check-changeset-presence, check:new-line-citations, check:control-bytes; reason: needs_decision stop at H2 before implementation. || H1 holds at BASE (cited by symbol): `export const ObjectMapConfigSchema = z.object({...})` has no `.strict()`. ObjectMap.tsx `getMapConfig` runs `ObjectMapConfigSchema.safeParse(config)` and then `console.warn('[ObjectMap] Invalid map configuration:', result.error.format())`. `FLAT_MAP_CONFIG_KEYS` is derived from `.shape`, and `.shape` is unchanged under `.strict()`: the probe measured the same 8 keys, latitudeField,longitudeField,locationField,titleField,descriptionField,zoom,center,style. Both `authoredEverything` pins exist, in ListView.mapFlatten.test.tsx and ObjectView.mapFlatten.test.tsx. || H2 probe (tsx over the worktree source; documents: clean = object-map with map {latitudeField,longitudeField}; typo = map {latitudeFieId,longitudeField}; child = div whose children hold the typo node). BASE: block typo success=true; safeValidateSchema clean success=true; safeValidateSchema typo success=true; safeValidateSchema child success=true; StrictAnyComponentSchema typo success=false, unrecognized_keys keys [latitudeFieId] at path [map]. || H2 probe with the mutation `}).strict();` on the ObjectMapConfigSchema closer. The write landed on disk: catchall def type read as never, and the anchor-marker grep counted 1. Results: block typo success=false (unrecognized_keys [latitudeFieId]); safeValidateSchema clean success=true; safeValidateSchema typo success=false (unrecognized_keys [latitudeFieId] at path [map]); safeValidateSchema child success=false (invalid_union at path [children], message Invalid input). The restore used `git checkout HEAD --` under a trap. It was proven three ways: git diff HEAD = 0 bytes, marker grep = 0, and git hash-object matching the HEAD blob. || H4 probe, same mutation and the same restore proof: `result.error.format()` on the typo block = {\\\"_errors\\\":[\\\"Unrecognized key: \\\\\\\"latitudeFieId\\\\\\\"\\\"]}. The existing warn therefore names the refused key. At BASE the typo parses success=true and no warn fires, which is the card symptom. || Spec cross-read, @objectstack/spec 17.4.0 as resolved from packages/types: `ListViewSchema.map` is an object with catchall never and 7 keys (the objectui set minus style). `safeParse({locationField, latitudeFieId})` returns success=false, unrecognized_keys [latitudeFieId]. The same logical block authored on a view is therefore already refused by the spec. || H3 sweep. Population is enumerated with git ls-tree at the ref, and content is read with git show from the same ref. The script is a one-time instrument kept in the seat scratchpad, not in the repo. Every hit is reconciled against a raw grep for a map key. (1) objectui BASE examples+apps+content: 967 files, 17 brace-literal map blocks plus 1 variable-bound block (plugin-map.mdx, TypeScript Support section, `mapConfig` with 6 declared keys), 0 undeclared keys. The 17 are content/docs/fields/location.mdx x1, content/docs/plugins/plugin-map.mdx x13, and examples/schema-catalog plugin-map JSON x3 (event-venue-finder, real-time-delivery-tracking, store-locator-map). The other raw-grep hits are package names, type annotations and test helpers. (2) objectstack origin/main aa04ea2 examples+apps: 284 files. There are 2 real map config blocks, both {titleField, locationField}: app-showcase task.view.ts on the view named map, and test task-map-marker-title.test.ts. The 2 other hits are false positives: a view whose key is `map`, and a translations label map. (3) objectui BASE packages/ plus console and catalog tests: 96 brace blocks. No object-map fixture authors an undeclared key inside `map`. The only `bogusKey` is in ListView.mapViewLevelConfig.test.tsx, on the ListView flatten path, which never parses ObjectMapConfigSchema. The remaining EXTRA rows are unrelated `map` configs (flow nodes, recipient maps) or spreads of declared keys. Verdict: measurement quiet, 0 findings. || Door census, by git grep of the symbol in non-test source at BASE. Four sites use `ObjectMapConfigSchema`. (i) `ObjectMapSchema.map` flows through ObjectQLComponentSchema and AnyComponentSchema into safeValidateSchema/validateSchema, reaching packages/cli validate.ts and check.ts (check uses it as a recogniser, and a refused file moves to its UNVALIDATED_CANDIDATE bucket), plus every SchemaNodeSchema child slot. (ii) The ObjectMap.tsx getMapConfig warn, reached only when a `map` key is present on the node; the ListView/ObjectView flatteners emit flat keys and no `map` key. (iii) ObjectMap.tsx FLAT_MAP_CONFIG_KEYS, which reads shape only. (iv) StrictAnyComponentSchema, which already refuses and has no non-test consumer at BASE. ListViewSchema in objectql.zod.ts does not reference ObjectMapConfigSchema. packages/core validateSchema (SchemaRenderer dev validation) imports no zod face.\",\n  \"mcp_calls\": \"0 — none\",\n  \"api_writes\": \"1 — POST /repos/objectstack-ai/objectui/issues/5157/comments (this os-dev-report, via scripts/pm/post-stamped.mjs). The git push of the empty branch is not REST. Reads: GET /repos/objectstack-ai/objectui/issues/5157 and GET .../issues/5157/comments?per_page=100.\",\n  \"deviations\": [\n    \"Stopped at H2 per the order: no PR, no PR-assignee label-write, no changeset, no code. The write budget used is 1 of 4 (the report comment), plus the empty-branch git push.\",\n    \"The role file was read at objectstack origin/main aa04ea2, not a4ca69a: origin/main had moved. `git diff a4ca69a origin/main -- .claude/agents/os-dev.md` is empty, so the text is the same.\",\n    \"The sweep ran wider than the order named (examples/apps/content). It also covered objectstack origin/main examples+apps and objectui packages/ fixtures. These are reported separately so the named scope stays readable on its own.\",\n    \"The worktree objectui-issue-5157 had no changes. It is removed after this report, and the branch stays on the remote at BASE for the claim.\"\n  ],\n  \"open_questions\": [\n    {\n      \"question\": \"Does ruling 5328158731 (B, map block only, \\\"warn, not reject\\\") cover the validate-face refusal that `ObjectMapConfigSchema.strict()` brings with it? `ObjectMapSchema.map` IS `ObjectMapConfigSchema.optional()`. Closing the schema therefore also makes `safeValidateSchema`, and `objectui validate` / `objectui check` with it, refuse a map block with a misspelled key. Measured: BASE success=true, strict success=false, unrecognized_keys [latitudeFieId] at [map]; a nested document fails with invalid_union at [children]. The runtime channel stays a warn under both options: ObjectMap still renders and warns.\",\n      \"options\": [\n        \"A — strict everywhere: `.strict()` on ObjectMapConfigSchema itself. The runtime warns and renders as before; the validate face refuses at authoring time. [Business need] The sweep found 20 of 20 existing map blocks clean, so A newly refuses 0 measured documents. The symptom is real and measured: at BASE a typo leaves an empty map with no diagnostic. validate/check is the only authoring-time check that untyped metadata passes through. [Long-term] A keeps the one-schema property objectui#5018 lifted the schema for (declared face = runtime check = validate face). It matches the spec's own view-level block (spec 17.4.0 ListViewSchema.map, catchall never) and the objectui#5250 strict twin, which already refuses. Precedent for strict sub-blocks on the tolerant face: DashboardWidgetSchema (objectui#6002) and CalendarDayRangeSchema (objectui#10304). [Anti-AI-error] Strongest: a loud refusal at validate/publish that names the key. [Startup focus] Smallest diff: one `.strict()`, the pins, and a minor changeset that states the narrowing. No new gate, no second schema, no transition window needed, since measured breakage is 0. Costs: (1) at the validate face A goes beyond the literal \\\"warn, not reject\\\", so it needs the maintainer's explicit yes; (2) bare safeValidateSchema reports a nested document as a generic invalid_union at children (NOT MEASURED: how the CLI arm-selection diagnostics render it).\",\n        \"B — strict on the runtime warn path only. The node `map` member keeps the tolerant schema, and ObjectMap.tsx parses the block with a strict variant, for example `ObjectMapConfigSchema.strict()`. [Business need] B gives the same runtime catch (the warn names the key, as measured) but only on nodes that directly author a `map` block. The view flatten path never reaches it. `objectui validate` keeps printing valid for a misspelled block. [Long-term] B re-creates two accept sets for one declared block (declared tolerant, runtime strict), which is the drift shape objectui#5018 removed. It is also transitional: once the objectui#5250 devx half wires validate to StrictAnyComponentSchema, validate refuses the same typo anyway. [Anti-AI-error] Weaker: AI-authored metadata is judged at validate/check, which stays silent; a dev-console warn never reaches a publish pipeline. [Startup focus] Slightly more code: a second parse spelling in plugin-map, plus a pin that the node face stays tolerant. B is literally compliant with \\\"warn, not reject\\\".\"\n      ],\n      \"recommendation\": \"A, for the maintainer to confirm (not chosen or implemented here, per the order). All four axes point the same way: 0 measured documents would be refused; one schema, matching the spec's own strict view-level block and the strict twin; a loud authoring-time refusal is what protects untyped AI metadata, and a runtime warn does not; and A is the smallest change with no new gate. Choose B only if \\\"warn, not reject\\\" is meant to bind the validate face as well as the runtime.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"Not class a/b/c: a stale code comment in ObjectMap.tsx `warnOnTopLevelStyleUrl` says `@objectstack/spec` list-view schemas \\\"declare no `map` block at all\\\". Measured on spec 17.4.0: ListViewSchema declares `map`, a strict 7-key object. Noted, not filed. carrier: the implementation round of this same claim, if it touches ObjectMap.tsx; otherwise no carrier (承接者:无), acceptance notes only.\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
