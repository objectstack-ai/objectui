{
  "number": 2706,
  "kind": "pull_request",
  "title": "fix(list): route remaining system-field groupings through the shared classifier",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-19T18:33:47Z",
  "updated_at": "2026-07-20T01:18:36Z",
  "closed_at": "2026-07-20T01:18:34Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-20T01:18:34Z",
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2706",
  "body": "## Summary\n\nFollow-up cleanup to #2702 (the `owner_id` default-column fix). Routes the remaining **display-oriented** system-field groupings through the shared `isSystemManagedField` / `SYSTEM_MANAGED_FIELD_NAMES` helper so the framework-injected `owner_id` is handled consistently everywhere, and removes the duplicated per-file name lists.\n\n## Changes\n\n- **`ObjectGrid` record-detail drawer** (`renderRecordDetail`) — the grid's record-detail overlay split fields into a business body vs. a muted \"meta\" section using a hardcoded list that omitted `owner_id`. Now uses the shared classifier, so `owner_id` (and other injected system fields) sit in the meta section, consistent with the grid's default columns. Hidden system fields are also kept out of the meta section.\n- **`deriveLookupColumns`** (record picker) — drops its local `SYSTEM_FIELDS` name set for the shared classifier; now also **flag-aware** (`field.system`), not just name-based.\n- **`RecordDetailDrawer`** — its default `systemFields` set is derived from the shared `SYSTEM_MANAGED_FIELD_NAMES` (the `systemFields` prop override is preserved).\n\n## Deliberately left distinct\n\n- `deriveRelatedLists`' `AUDIT_FK_FIELDS` — a narrow \"FK that exists on every object\" set (already correct on `owner_id`); broadening it would change related-list generation with no `owner_id` benefit.\n- plugin-detail's `NON_EDITABLE_SYSTEM_FIELDS` (inline-edit gate) — intentionally **excludes** `owner_id` because ownership is reassignable/editable.\n\n## Verification\n\n- Unit tests pass: `deriveLookupColumns`, both `RecordDetailDrawer` suites, `deriveRelatedLists`, plus the grid suites (`column-features`, `mobileCardLookupDisplay`). New test locks in `deriveLookupColumns` flag-awareness (a `system: true` field with a non-canonical name is skipped).\n- Typecheck of the touched files is clean — no new errors in `deriveLookupColumns.ts` / `RecordDetailDrawer.tsx` (remaining output is pre-existing repo baseline noise).\n\n## Affected packages\n\n`@object-ui/plugin-grid`, `@object-ui/fields`, `@object-ui/plugin-detail` (patch; changeset included).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01NMcdh2cNs165TSF4ub61oy\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01NMcdh2cNs165TSF4ub61oy)_"
}
