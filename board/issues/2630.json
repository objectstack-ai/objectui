{
  "number": 2630,
  "kind": "pull_request",
  "title": "feat(app-shell/react): adapt to framework 15.1 — atomic publish rendering + honest discovery",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: react",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-17T01:06:24Z",
  "updated_at": "2026-07-17T08:17:53Z",
  "closed_at": "2026-07-17T08:17:51Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-17T08:17:51Z",
  "comments": 5,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2630",
  "body": "## Summary\n\nConsole-side adaptation to two framework 15.1.0 changes, both shipped in `@objectstack/*@15.1.0`:\n\n### 1. ADR-0067 D2 — publishes are now all-or-nothing (framework#3074)\n\nA failed batch publish now returns `publishedCount: 0` with every draft in `failed[]` — the causal item with its real error, the rest marked `code: 'batch_aborted'`. Three consumers rendered this as N parallel errors (or worse):\n\n- **`formatPublishFailures`** (`metadataError.ts`, used by StudioDesignSurface's publish toast): a rolled-back batch now renders as **one banner** — \"Nothing was published — the batch rolled back\", the causal item with its field-anchored issues, and a \"{n} other drafts aborted with it\" summary. Pre-15.1 partial responses keep the existing per-item rendering (both shapes handled).\n- **`PackagesPage.publishDrafts`**: detects `batch_aborted` and throws the new `publishDraftsRolledBack` message (\"发布未生效 — 批次已整体回滚：{cause}\", en+zh added) instead of the misleading \"{published} published; {failed} failed\".\n- **`AiChatPage.onPublishDrafts`**: previously `throw new Error(String(failedCount))` — the failure toast literally read \"3\". Now formats `failed[]` through the shared helper.\n\n### 2. ADR-0076 D12 — honest discovery (framework 15.1, #2462/#3037)\n\nDiscovery no longer hardcodes every service as `available`; stubs report `stub`, serving fallbacks report `degraded`, and `handlerReady` says whether a real handler exists. Console slice (the \"trust only handlerReady\" remainder named in ADR-0076's status):\n\n- `DiscoveryServiceStatus` type: + `handlerReady?`, status union widened with `degraded`/`stub`.\n- New **`isServiceUsable()`** helper — deliberately **backward-tolerant**: absent fields keep the pre-15.1 default (usable); `enabled:false` / `handlerReady:false` / `stub` / `unavailable` gate off; **`degraded` stays usable** (it serves — callers may warn, never disable).\n- Consumed by `isAuthEnabled` / `isAiEnabled` (useDiscovery) and **`ConditionalAuthWrapper`** (the load-bearing auth gate — a stub auth service no longer wraps the app in a real AuthProvider).\n- Drive-by: the wrapper's timeout throw now carries `{ cause }` (repo lint rule).\n\n## Test plan\n\n- `metadataError` suite +2 cases (rolled-back banner; all-aborted defensive), new `useDiscovery.usable` suite (8 cases pinning the backward-tolerance contract).\n- Adjacent suites green: ConditionalAuthWrapper, PackagesPage, StudioDesignSurface, useChatConversation, data-objectstack connection — 106 tests.\n- ESLint clean on all changed files (0 errors).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Jie55be2rPShGvJnotyftE)_"
}
