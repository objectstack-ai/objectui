{"id":5389690168,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-24T01:30:16Z","updated_at":"2026-08-24T01:30:16Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5389690168","body":"Triage (daily round, session `session_01Kktexqp6uVuFMztvvTMf3V`, 2026-08-24): maintainer-directed follow-up (2026-08-22 ruling on #4869, C half quoted in body) → `domain:ui` + `pm:queue`, Task: ADR-0049 enforce-or-remove retirement of `table.defaultSort`. Follow the retirement playbook (deprecation state, tombstone, pins); any `docs/adr/**` half stays draft for human merge. The direction is ruled — do not re-open it; only the mechanics are the dispatch.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5398412525,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-24T16:46:32Z","updated_at":"2026-08-24T16:46:32Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5398412525","body":"Claim: PM loop round 38 (refill slot)\nSession: `session_01CSoz9uGhaaSgiq3hshtN7L`\nBranch: `claude/issue-5861-retire-table-defaultsort`\nWorktree: `objectui-issue-5861`\nDomain: `domain:ui`\nContainer & model: `L`, `mode:subagent`, `model: opus`\nClause-②: **yes** — a published key is retired and, per item 1 below, a reader is expected to start **refusing** it. Declared; Opus under this seat's recorded Fable-quota exemption.\n\n**File surface:** `packages/types/src/objectql.ts` (the `ObjectGridSchema.defaultSort` declaration) and its zod mirror; `packages/plugin-view/src/**` including its pins; the ADR-0087 registries and generated baselines the retirement playbook names; the two documentation pages that describe the key; a changeset.\n\n⛔ **Two hard fences on that surface:**\n- ⛔ **`packages/types` is shared with #5852, in flight right now** (retiring `DashboardConfig.aria` + its zod mirror). Stay in `objectql.ts` and its mirror; ⛔ if your edit needs the dashboard-config declarations or the same barrel lines, **stop and report**. `packages/types` also had #6053, #6032, #6068 land today and **#6085 is enqueued** (`field-types.ts` + `index.ts`) — branch from a base containing what has landed.\n- ⛔ **Anything under `docs/adr/**` is OUT of this card.** That is a governed surface: draft PR only, human merge is the review record, ⛔ never enqueued — so mixing it into a code PR makes the whole PR unlandable by the PM. If the retirement genuinely requires an ADR edit, ✅ **say so in the report and leave it undone**; it becomes its own draft-only PR.\n\n⚠️ **Serial, adjacent semantics:** **#6088 (#5795) is enqueued** and normalises related-list sort through `@object-ui/core`'s `convertSortToQueryParams` — the same sink this card's key feeds. **#5729 is in flight in `packages/plugin-grid`** on the per-column sortability signal. ⛔ Do not touch `plugin-grid` or `plugin-detail`; ✅ if you need sort-string handling, reach `convertSortToQueryParams` rather than adding a parallel reader.\n\n## 裁决 — ruled, and ⛔ not re-openable\n\nMaintainer ruling 2026-08-22 on #4869 (decision-inbox digest, verbatim 「接受所有」): Option A now, **plus** *\"the follow-up card for the C half — an ADR-0049 enforce-or-remove retirement of `table.defaultSort` as its own card, not folded into this one.\"* Option A has landed.\n\nTriage (2026-08-24 01:30Z), verbatim:\n\n> maintainer-directed follow-up … → `domain:ui` + `pm:queue`, Task: ADR-0049 enforce-or-remove retirement of `table.defaultSort`. Follow the retirement playbook (deprecation state, tombstone, pins); any `docs/adr/**` half stays draft for human merge. **The direction is ruled — do not re-open it; only the mechanics are the dispatch.**\n\n⛔ So: do not re-argue whether the key should go. ✅ Decide and execute *how*, and report any mechanic that cannot be executed as specified.\n\n## The three things the card says a removal must settle\n\n1. ⛔ **Refuse loudly — do not merely stop reading.** The key is currently honoured in **three places at two layers**: `ObjectGrid`'s own resolution, `ObjectView`'s grid forwarding, and `ObjectView`'s non-grid fetch. Dropping the read on one path while another still honours it re-opens the divergence **in mirror image** — the grid would sort and the calendar would not, **with no error anywhere**. ⚠️ Whichever route you take has to move **every reader at once**, and the end state must make an authored `defaultSort` produce a *visible* refusal, ⛔ not silence.\n2. **The registries.** ADR-0087 conversion plus the generated baselines, forms, docs and pin tests a removal needs — follow the `spec-property-retirement` playbook rather than improvising. ✅ Name each registry you touched and each you deliberately did not.\n3. ⚠️ **The existing pins are the ledger of what must be retired, and they currently assert the key WORKS.** `packages/plugin-view/src/__tests__/ObjectView.canonicalTableKeys.test.tsx` pins the legacy spelling as a working alias on **three** paths (grid forwarding, non-grid fetch, delegated `renderListView`), and `ObjectView.sortSink.test.tsx` pins the lowered value. ⛔ Do not delete them to make a retirement green — **flip** each to assert the new refusal, and say in the report which you flipped and which were genuinely dead. A pin written against a key you are retiring is the previous card handing you its receipt. (This exact move was executed correctly on #5725 today, on a pin that was even documented to flip.)\n\n## PM mechanism assumptions — measure, do not inherit\n\n1. ⛔ **Re-measure \"zero authored occurrences\" on YOUR merge-base, with a counter-probe.** The card measured it on `origin/main` @ `1d9100eeb`; ~30 PRs have landed since. Search `examples/**`, `apps/**`, `e2e/**`, `content/**`, and show a key you know **is** authored found by the identical method — a zero-hit grep without a known-present control is an untested instrument. ⚠️ Also probe the sibling **`objectstack`** checkout; that probe paid off twice today.\n2. ⚠️ **Expect two classes of surviving hit that are NOT authoring, and do not confuse them:** the registry/spec-parity ledger in `apps/console/src/__tests__/registry-inputs-spec-parity.test.ts`, and prose in `content/docs/plugins/plugin-view.mdx` + `content/docs/api/schema-reference.md` describing it as the deprecated half of a pair. ✅ Each needs a decision stated separately — the ledger entry probably moves to a retirement state, the prose probably gains a tombstone note. ⛔ Neither is \"an authored occurrence\".\n3. ⚠️ **The key is already live-for-reading but not-for-writing** — #4648 (maintainer, 2026-08-16) carved `object-grid.defaultSort` out of the **published** authoring inputs: *\"read as back-compat, deliberately not published — the canonical `sort` IS declared.\"* ✅ That shortens the blast radius and should be stated in the changeset: you are retiring a read, not withdrawing an advertised authoring key.\n4. ⚠️ **A zod mirror that merely stops declaring a key may buy no refusal at all.** Measured on #6068 today: a plain `z.object` with no `.strict()` **strips** unknown keys rather than rejecting them. ⛔ So \"the mirror no longer declares `defaultSort`\" is **not** the same as \"an authored `defaultSort` is refused\" — item 1 above demands the latter. Measure which your change actually produces and say so.\n5. **Out of scope, explicitly:** ⛔ this card is **not** about `ObjectViewProps.views[].sort` and its `direction`/`order` spelling mismatch — that is #5293, which stays open and untouched. (Its residue is filed as #6011.)\n\n## Verification\n\nPredict the direction **before** running. ⚠️ **Commit your own edit before you ablate** — measured on #5859 today: a `trap` restore using `git checkout --` reverted the dev's still-uncommitted edit along with the probe's mutation, and `git checkout --` **reported success both times**; the half-change was caught only by an independent `git diff --stat` re-read.\n\n⚠️ **Do any downstream counter-probe at the right layer**: downstream resolves `@object-ui/types` through `exports` to **`dist/`**, ⛔ not `src/` — a source-only mutation leaves the sweep green and reads as \"nothing consumes it\". Rebuild before reading and re-grep `dist/`.\n\nThe retirement's positive evidence is a **refusal that fires**, on **every** one of the three reader paths, each with its own leg. Every negative assertion needs a green counter-probe: a canonical `sort` must still work on all three paths in the same run, ⛔ otherwise \"defaultSort no longer sorts\" is also satisfiable by breaking sorting outright.\n\nProve every mutation on disk by grepping injected **and separately** removed text; ⚠️ print the landing site; ⚠️ assert anchor uniqueness before writing; ⚠️ restore under `trap … EXIT INT TERM` with a **cwd-independent** command; confirm `git diff HEAD --stat` is empty afterwards.\n\n**Gates by name, with exit codes:** `pnpm --filter @object-ui/types type-check` and `pnpm --filter @object-ui/plugin-view type-check` (⚠️ hyphenated; echo the script name; distinguish the **unbuilt-closure** red — `Cannot find module @object-ui/...` — from the **missing-`node_modules`** red), root-form vitest over each package touched quoting per-test names, the downstream **prefix-form** sweep with the contract direction **demonstrated** rather than asserted (⚠️ known gotcha, not a finding: `apps/site` and `packages/components` fail until `@object-ui/react-runtime` is built separately), eslint with merge-base delta (⚠️ plain `eslint .`; ⛔ no `--no-inline-config`). Run `node scripts/check-changeset-presence.mjs` rather than guessing — ⚠️ **#6073 landed today and widened that gate's notion of a package's source** beyond `/src/`. Changeset **`minor`** for a public-type break; ⛔ never `major`.\n\n⚠️ **Watch for the zero-match pnpm trap** (measured today): a filter that matches nothing prints *\"No projects matched the filters\"* and exits **0**, reading exactly like a pass. ⚠️ **Standing rule from #5874:** derive your gate set by enumerating **each CI job's own step list**, ⛔ not from top-level script names.\n\n## Deliverable\n\nDraft PR, body with `Fixes #5861`. ⛔ Do not mark ready, ⛔ do not enqueue, ⛔ no auto-merge — the PM lands it. Post the terminal report here (plain-text first-line marker `os-dev-report`; ⛔ avoid short angle-bracket placeholders in prose — this repo's sanitizer eats them, confirmed twice today) and return it. State, per gate: command, exit code, what a red would have meant; the re-measured authored-occurrence count with its control; which pins you flipped versus retired; which registries you touched and which you did not; whether an ADR edit is required and therefore left undone; and any deviation with its reason.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5398671726,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-24T17:09:48Z","updated_at":"2026-08-24T17:09:48Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5398671726","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5861,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-5861-retire-table-defaultsort\",\n  \"pr\": null,\n  \"premise_still_valid\": true,\n  \"summary\": \"The card's premises hold — re-measured zero authored occurrences, and the ruled direction (the key should go) is untouched and not re-argued. What does not hold is the dispatch's LOCATION assumption: the key whose retirement is ruled is declared and enforced in @objectstack/spec, not in objectui, so the retirement cannot be executed on this repo's file surface. ObjectGridPropsSchema (objectstack packages/spec/src/ui/component.zod.ts:2164) is a strictObject that still declares defaultSort as a LIVE authorable key, publishes it un-retired as ui/ObjectGridProps:defaultSort in authorable-surface/ui.json:734, and ACCEPTS an authored value today (measured). objectui's own zod mirror of ObjectGridSchema is a non-strict BaseSchema.extend consumed ONLY by parity tests — nothing parses metadata through it at runtime — so tombstoning it buys exactly zero refusal, which is PM assumption 4 confirmed in its strongest form. Item 1 demands a visible refusal rather than silence; the only place that refusal can exist is the upstream strictObject. Removing objectui's reads unilaterally would turn declared-but-unenforced into declared-and-silently-dropped (the ADR-0049 failure mode moved, not fixed) and, with plugin-grid fenced by #5729, would leave ObjectGrid still honouring the key — the mirror-image divergence item 1 explicitly forbids. objectui already states this ordering itself: the comment governing the ten ruled object-grid carve-outs in apps/console/src/__tests__/registry-inputs-spec-parity.test.ts says they 'retire only if @objectstack/spec retires the keys upstream'. I therefore shipped NO code: the producer half is filed as objectstack#11805, and the tripwire that makes the precondition visible ALREADY EXISTS and is measured live (see tests). Building another would be duplicate machinery — exactly the drift packages/test-support/src/spec-tombstones.ts was written to end.\",\n  \"tests\": \"Baseline measured on merge-base 0c282d979 (worktree clean throughout; git diff HEAD --stat empty, git status --short empty, verified after every probe). All heavy commands ran through scripts/pm/os-verify-lock.sh. NOTE: no code changed, so these gates measure the baseline and the load-bearing claims, not a diff.\\n\\n(1) SPEC-AUTHORABILITY PROBE — temporary vitest file under trap, structured output to disk. Result: object-grid listed=37, authorable=37, tombstoned=[]; defaultSort LISTED=true AUTHORABLE=true TOMBSTONED=false; tombstoneEvidence={listed:true, typedNever:false, describedRemoved:false} (BOTH recognition channels say live). Authored {defaultSort:{field,order}} ACCEPTED by safeParse = true. CONTROL bogus key accepted = false, so the schema really is strict and the refusal machinery works there. POSITIVE CONTROL: 7 blocks in ComponentPropsMap DO carry retiredKey tombstones (16 keys), so the zero for object-grid is a real zero, not a broken probe. Lock VERDICT command-exit 0.\\n\\n(2) ABLATION — direction predicted BEFORE running: the carve-out tripwire assertion shape goes RED on a tombstone and stays GREEN on a live key. Ran both legs in one run against the EXACT assertion registry-inputs-spec-parity.test.ts uses (authorableShapeKeys + toContain). LEG A (live key) GREEN: object-grid.defaultSort still authorable. LEG B (real tombstone, page:header.icon — a key retired by objectstack#6946) RED: 'AssertionError: expected [ title, subtitle, ...(8) ] to include icon'. Observed direction matched the prediction. This proves objectui's EXISTING gate fires the day upstream retires the key, with the message 'spec no longer declares object-grid.defaultSort — re-check the carve-out'. I deliberately used a real tombstone as the stand-in rather than mutating node_modules: pnpm hardlinks the content-addressable store, so editing installed dist would have reached every other agent's worktree. Mutation proved on disk by grepping injected text (grep -c on both leg markers, 1 each) with the landing site printed; restored under trap ... EXIT INT TERM with a cwd-independent rm, and absence re-confirmed after the trap fired.\\n\\n(3) REAL SUITES, same run — vitest exit 1 attributable solely to ablation LEG B; 115 passed, 1 failed. Green: apps/console/src/__tests__/registry-inputs-spec-parity.test.ts (the live tripwire), packages/plugin-view/src/__tests__/ObjectView.canonicalTableKeys.test.tsx, packages/plugin-view/src/__tests__/ObjectView.sortSink.test.tsx. A red here would have meant the baseline was already broken and my no-change conclusion was hiding it.\\n\\n(4) TYPE-CHECK GATES (hyphenated, script name echoed to prove they ran rather than zero-matched): 'pnpm --filter @object-ui/types type-check' TYPECHECK_EXIT=0, echoed '> @object-ui/types@17.6.0 type-check'; 'pnpm --filter @object-ui/plugin-view type-check' TYPECHECK_EXIT=0, echoed '> @object-ui/plugin-view@17.6.0 type-check'. Dependency closure built first: 'pnpm --workspace-concurrency=2 --filter @object-ui/plugin-view^... build' BUILD_EXIT=0. A red would have distinguished as instructed — 'Cannot find module @object-ui/...' is the unbuilt-closure red, a missing node_modules red looks different; neither occurred.\\n\\n(5) ZERO-MATCH PNPM TRAP CAUGHT LIVE, reporting as instructed: 'pnpm --filter @object-ui/types^... build' printed 'No projects matched the filters' and exited 0. Not a finding — @object-ui/types is the zero-dependency protocol package (AGENTS.md section 3), so it has no closure to build. Had I read that exit 0 as a pass I would have reported building a closure that does not exist.\\n\\n(6) AUTHORED-OCCURRENCE RE-MEASUREMENT on my merge-base 0c282d979 (card measured at 1d9100eeb). objectui: examples/ 0, e2e/ 0, apps/ 4, content/ 8. CONTROL, identical method, a key I know IS authored (searchableFields): examples/ 4 including real authored JSON metadata at examples/schema-catalog/src/schemas/plugin-view/object-view-list.json:11 and .../plugin-grid/object-grid-columns.json:13 — the instrument works, so the zeros are real. SIBLING objectstack PROBE (it paid off decisively here): examples/ 0, apps/ 0, packages/qa/ 0 authored; the only live hits are the SPEC DECLARATION itself, which is the whole finding. Authored occurrences: ZERO, confirmed.\\n\\nNOT RUN, with reason: the downstream prefix-form sweep, eslint merge-base delta, and node scripts/check-changeset-presence.mjs. All three judge a diff, and the diff is empty — there is no changed file to sweep, lint, or owe a changeset for. Declared, not skipped silently.\",\n  \"open_questions\": [\n    {\n      \"question\": \"The direction is ruled and I am not re-opening it. What needs a decision is WHERE and WHEN the retirement lands, because the executable route crosses a repo boundary and costs an @objectstack/spec MAJOR (pin is 17.2.0, so 18.0.0) — a release-planning call the card did not anticipate.\",\n      \"options\": [\n        \"A — Producer first: land objectstack#11805 (strict route per the spec-property-retirement playbook: delete the key from the strictObject shape, add the guidance-map entry, ADR-0087 conversion, exact-key RETIRED_KEYS_BY_MAJOR entry, D3 chain step, liveness ledger, generated baselines, pin tests, major changeset). objectui#5861 stays open as blocked-by and becomes mechanical on the pin bump: remove the three plugin-view reads plus ObjectGrid's two, flip the pins, drop the object-grid.defaultSort carve-out entry, tombstone the two doc pages. This is the shape objectstack#6946 plus objectui#3974 already executed end-to-end for the sibling key form.defaultSort.\",\n        \"B — Consumer only, now: remove objectui's reads while the spec keeps declaring the key. Rejected on the merits: the contract still ACCEPTS an authored defaultSort (measured), so the key becomes silently dropped rather than refused. That is item 1's forbidden silence, and it is the ADR-0049 failure mode relocated rather than closed.\",\n        \"C — Consumer only with a loud runtime refusal in objectui (throw or console.error on an authored table.defaultSort). Rejected: the renderer would refuse a key the pinned contract advertises as legal in its own describe text, which is declared-not-equal-enforced inverted — the exact class zod-mirror-parity.test.ts exists to catch. It also cannot move every reader, since plugin-grid is fenced by in-flight #5729, so ObjectGrid would keep honouring the key: the mirror-image divergence in the same key family at two layers.\",\n        \"D — Defer: leave the key, close #5861 as not-now, revisit at the next spec major. Honest but loses the ruling's intent, and the ten-key carve-out in registry-inputs-spec-parity.test.ts keeps carrying an entry nobody owns.\"\n      ],\n      \"recommendation\": \"A. Real business need: zero authored occurrences in BOTH repos re-measured today with a working control, so retiring costs no real user and the only thing standing is the contract advertising a key nobody writes. Long-term soundness for THIS project: contract-first (AGENTS.md #0.1) puts the fix at the producer; A is the only option that makes declared and enforced agree at the same layer, and it reuses the exact machinery objectstack#6946 and objectui#3974 already proved on the sibling key rather than inventing a renderer-side dialect. Making AI-written metadata hard to get wrong: A is the only option that yields a refusal at authoring time — the strictObject rejects the key by name with a migration prescription, which is precisely the loud failure an AI-authored metadata app needs, whereas B leaves a silently-ignored key (the worst case for a model that will read the key back and believe it took effect) and C produces a refusal that contradicts the published contract, teaching the next model that the contract cannot be trusted. Startup scope discipline: A adds no surface — it removes one, and the spec major is the honest price of an enforce-or-remove ruling rather than scope growth; B and C are cheaper today and both create a second de-facto contract, which is the expensive kind of cheap. The four axes agree here, so I am not presenting a trade-off I am hiding: the only genuine cost is the 18.0.0 bump, and that is the decision I am escalating rather than taking.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"filed as objectstack#11805: [spec] ADR-0049 enforce-or-remove retirement of ObjectGridProps.defaultSort — the producer half #5861 is blocked on. Unassigned, no labels, searched for duplicates first (none; objectstack#6946 is the closed three-key precedent, not a duplicate).\",\n    \"NOT filed, reported instead — PM assumption 3 needs a correction. #4648 carved object-grid.defaultSort out of the published CONSOLE REGISTRY inputs, which is true, but the key is still published in the spec's AUTHORABLE SURFACE (authorable-surface/ui.json:734, no [RETIRED] tag) and still accepted by the strictObject. So 'live-for-reading but not-for-writing' does NOT shorten the blast radius the way the dispatch expected: it is not offered as a console input, but it remains a live authorable contract key. Correcting the assumption rather than filing, since it is this card's own framing.\"\n  ]\n}\n```\n\n## Answers to the report questions the dispatch asked by name\n\n**Which pins I flipped versus retired: none, and neither is a gap.** `ObjectView.canonicalTableKeys.test.tsx` and `ObjectView.sortSink.test.tsx` pin the key as a working alias, and they are correct as written — the key still works, and must keep working while the contract still declares it. Flipping them to assert a refusal now would pin a refusal the contract contradicts. They stay live and become the flip-list for the follow-on once objectstack#11805 lands. I verified both are green today rather than assuming it.\n\n**Which registries I touched and which I did not.** Touched: **none.** Deliberately not touched, each with its reason: the ADR-0087 conversion registry and `RETIRED_KEYS_BY_MAJOR` (both live in objectstack `packages/spec`, and both are premature until the key actually leaves the shape); `packages/spec/liveness/` (same repo, and the strict route deletes the ledger row rather than marking it, so it must move with the deletion); the `object-grid.defaultSort` carve-out entry in `apps/console/src/__tests__/registry-inputs-spec-parity.test.ts` (its own comment forbids removing it before upstream retires — deleting it to make a retirement green is the move both that file and the playbook call out by name); `content/docs/plugins/plugin-view.mdx` and `content/docs/api/schema-reference.md` (prose describing a key that still works; a tombstone note there today would be false). `content/docs/releases/` untouched, per the standing prohibition.\n\nSeparately, the two `content/docs/api/schema-reference.md` hits at lines 537-538 and 603 are **not this key** — they are `CRUDSchema.defaultSort` / `defaultSortOrder`, a `string` field on a different carrier. Anyone sweeping this key by grep should not \"fix\" them.\n\n**Whether an ADR edit is required, and therefore left undone: no.** ADR-0049 and ADR-0087 already govern this retirement and no ADR enumerates the key (grepped `docs/adr/` in objectstack — zero hits). So nothing under `docs/adr/**` is owed, and the governed-surface fence never came into play.\n\n**Deviations, with reasons.** (1) **No PR, against the dispatch's `Fixes #5861` deliverable.** There is no correct change to make in this repo; a PR would either fake the retirement or duplicate an existing tripwire. Had I shipped one it would have needed `Part of`, not `Fixes`, since merging it must not close this card — the retirement remains owed. (2) **I filed an issue in a sibling repo.** The finding is this card's own blocker rather than an unrelated defect, and the durable artifact outlives this report. (3) The branch `claude/issue-5861-retire-table-defaultsort` was pushed empty as the write-route probe and carries **zero commits**; it is left in place as the claim landing marker. The worktree was removed cleanly without `--force`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5398700176,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-24T17:11:58Z","updated_at":"2026-08-24T17:11:58Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5398700176","body":"## PM ruling — **ESCALATE accepted.** No code was the right answer. → `pm:blocked` on objectstack#11805\n\n⛔ This is **not** a REWORK and ⛔ not a re-opening of the ruling. The direction — the key should go — is untouched. What the dev found is that **the dispatch's location assumption was wrong**, and it was wrong in a way that makes the card unexecutable on this repo's file surface.\n\n### The measurement that settles it\n\n`ObjectGridPropsSchema` (`objectstack/packages/spec/src/ui/component.zod.ts:2164`) is a **`strictObject`** that still declares `defaultSort` as a **live authorable key**, publishes it un-retired at `packages/spec/authorable-surface/ui.json:734`, and **accepts an authored value today** — measured, with the control that a bogus key is refused, so the strictness machinery there demonstrably works.\n\nMeanwhile objectui's own mirror (`packages/types/src/zod/objectql.zod.ts:130`) is a **non-strict `BaseSchema.extend`** consumed **only by parity tests** — nothing parses metadata through it at runtime. ⚠️ **So tombstoning it buys exactly zero refusal.** That is **PM assumption 4 confirmed in its strongest form**: I warned that a mirror which merely stops declaring a key may buy no rejection, and here it buys *none at all*, because the mirror is not on any runtime path.\n\nDispatch item 1 demanded a **visible refusal, not silence**. The only place that refusal can exist is the upstream `strictObject`. ⇒ The card cannot be executed here.\n\n### Why the two consumer-only routes are correctly rejected\n\n- **B (remove objectui's reads now):** the contract still **accepts** an authored `defaultSort`, so the key becomes **silently dropped** rather than refused. That is item 1's forbidden silence — the ADR-0049 failure mode **relocated, not closed**.\n- **C (loud runtime refusal in objectui):** the renderer would refuse a key the pinned contract advertises as legal **in its own describe text** — declared≠enforced *inverted*, which is the class `zod-mirror-parity.test.ts` exists to catch. And it cannot move every reader anyway: `plugin-grid` is fenced by in-flight #5729, so `ObjectGrid` would keep honouring the key — **the mirror-image divergence item 1 explicitly forbids.**\n\n✅ And objectui already states this ordering itself: the comment governing the ten ruled object-grid carve-outs in `apps/console/src/__tests__/registry-inputs-spec-parity.test.ts` says they *\"retire only if `@objectstack/spec` retires the keys upstream.\"* The dispatch and I both missed that; the dev read it.\n\n### The tripwire already exists, and was measured firing\n\n⚠️ Rather than build new machinery, the dev **proved the existing gate already fires** the day upstream retires the key. Ablation with a **real tombstone** — `page:header.icon`, retired by objectstack#6946 — against the exact assertion `registry-inputs-spec-parity.test.ts` uses: leg A (live key) **green**, leg B (real tombstone) **red** with *\"expected [ title, subtitle, …(8) ] to include icon\"*. Direction predicted before running, observed as predicted.\n\n✅ And the reason a real tombstone was used instead of mutating `node_modules` is exactly right and worth repeating: **pnpm hardlinks the content-addressable store, so editing installed `dist` would have reached every other agent's worktree.** That is a cross-agent hazard in the same family as the shared stash stack.\n\n⇒ Building a second tripwire would be duplicate machinery — the drift `packages/test-support/src/spec-tombstones.ts` exists to end.\n\n### Two corrections to my dispatch, both mine\n\n1. ⚠️ **Assumption 3 was wrong.** I said #4648 made the key \"live-for-reading but not-for-writing\" and that this **shortens the blast radius**. It does not: #4648 carved it out of the **console registry** inputs, but the key remains published in the spec's **authorable surface** (`ui.json:734`, no `[RETIRED]` tag) and **still accepted by the strictObject**. Not offered as a console input ≠ not a live authorable contract key.\n2. ⚠️ **A grep trap I did not warn about:** `content/docs/api/schema-reference.md:537-538` and `:603` document **`CRUDSchema.defaultSort` / `defaultSortOrder`** — a **string** field on a **different carrier**. ⛔ Anyone sweeping `ObjectGridSchema.defaultSort` by grep must not \"fix\" them. Now on the record for the follow-on.\n\nAlso caught **live**, exactly as the dispatch warned: `pnpm --filter @object-ui/types^... build` printed *\"No projects matched the filters\"* and exited **0**. ✅ Read as the zero-match trap and explained (types is the zero-dependency protocol package, AGENTS.md §3), ⛔ not counted as a passing gate.\n\n### Re-measurement, with a working instrument\n\nAuthored occurrences on merge-base `0c282d979`: **zero** in both repos. objectui — `examples/` 0, `e2e/` 0; the 4 `apps/` hits are the carve-out ledger and 3 of the 8 `content/` hits are the different-carrier `CRUDSchema` key. objectstack — `examples/` 0, `apps/` 0, `packages/qa/` 0 authored; **the only live hits are the spec declaration itself, which is the whole finding.** Control by the identical method (`searchableFields`) found real authored JSON metadata, so the zeros are measurements.\n\n### State\n\n- **objectstack#11805** filed for the producer half (duplicate-checked; objectstack#6946 is the closed three-key precedent, not a duplicate).\n- **This card → `pm:blocked` on objectstack#11805.** It becomes mechanical on the spec pin bump: remove the three `plugin-view` reads plus `ObjectGrid`'s two, flip the two pins, drop the carve-out entry, tombstone the two doc pages. ⛔ Pins stay **as they are** meanwhile — they correctly assert the key works **while the contract declares it**, and they are the flip-list for the follow-on.\n- **No ADR edit is required** — ADR-0049 and ADR-0087 already govern, and no ADR enumerates the key (`docs/adr/` grepped, zero hits). So nothing is left undone on the governed surface I fenced out.\n\n### ⛔ The one thing I will not decide\n\nRetiring the key costs an **`@objectstack/spec` MAJOR** — the pin is 17.2.0, so **18.0.0**. That is **release planning**, and ⛔ it is not the PM seat's call. It goes to the maintainer list as item ⑪, with the dev's recommendation attached: **route A, producer first**, on the grounds that zero authored occurrences in both repos means retiring costs no real user; contract-first (AGENTS.md #0.1) puts the fix at the producer; it reuses the machinery objectstack#6946 + objectui#3974 already proved on the sibling key `form.defaultSort`; and it is the only route that yields a **refusal at authoring time** — which for an AI-authored metadata platform is the whole point, since a silently-ignored key is the worst case for a model that reads it back and believes it took effect.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5450800597,"author":{"login":"os-sales","id":319429713,"type":"User"},"created_at":"2026-08-28T09:23:47Z","updated_at":"2026-08-28T09:23:47Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5450800597","body":"## Unlock scan: producer half has LANDED — and the escalated cost question is already answered\n\nTwo updates on this card, both measured.\n\n### 1. The blocker is resolved upstream\n\nobjectstack#11805 is **closed / completed**, landed by merged PR objectstack#12256 (`feat(spec): retire object-grid's legacy defaultSort fallback`, merged 2026-08-25). Measured on objectstack `origin/main`:\n\n- `packages/spec/src/ui/component.zod.ts:2299` now declares `defaultSort: retiredKey(...)` with migration guidance naming `sort` and the array wrapping.\n- `packages/spec/authorable-surface/ui.json:723` publishes `ui/ObjectGridProps:defaultSort [RETIRED]`. **Positive control in the same file**: the sibling `ui/FormView:defaultSort [RETIRED]` sits at line 455, so the `[RETIRED]` tag is a spelling this surface really uses and the reading is not a pattern artefact.\n\n### 2. ⭐ The `major` question this card escalated has been ANSWERED — it can come off the maintainer list\n\nThe prior PM ruling here escalated one thing and one thing only: that retiring the key looked like it cost an `@objectstack/spec` **MAJOR** (17.2.0 → 18.0.0), which is release planning and not a seat's call. It went to the maintainer list as item ⑪.\n\nThat is settled. `component.zod.ts:2279` records the maintainer's ruling verbatim:\n\n> 「#11805 退役 defaultSort,不需要major」\n\nThe retirement took the **`retiredKey()` tombstone route** rather than the strict-delete route, which is why no major was needed. So the escalation is closed by the ruling, and **item ⑪ should be struck from the decision box** — no maintainer input is outstanding on this card.\n\nWorth recording why the earlier reasoning went wide: the dispatch and the dev both reasoned from the `spec-property-retirement` playbook's *strict* route, which does cost a major. The playbook has a second route, and the maintainer picked it. A cost derived from one route of a playbook is not the cost of the outcome.\n\n### 3. But this card still does NOT return to the queue\n\nThe retirement is **merged and unpublished**, so the same discipline that parked #5987 and #6246 applies:\n\n- Newest published `@objectstack/spec` is **17.2.0, published 2026-08-23T07:01:46Z**; PR objectstack#12256 merged 2026-08-25. Maximum `published_at` across the 100 most recent objectstack releases is 2026-08-23T07:02:23Z, with **no** release on or after 2026-08-24.\n- This repo resolves `@objectstack/spec@17.2.0` in `pnpm-lock.yaml`.\n\n⚠️ And this card hits the **version-collision hazard** in its sharpest form: objectstack `main`'s `packages/spec/package.json` still reads **`17.2.0`** — the identical string to the published release that does **not** carry the tombstone. A restart criterion phrased as a version comparison would fire a false unlock and send a dev to delete reads while the installed contract still accepts the key. That is precisely the \"silently dropped rather than refused\" outcome this card's own item 1 forbids.\n\n`pm:blocked` is therefore replaced by `pm:on-hold`.\n\nRestart-when: `grep -c \"ObjectGridProps:defaultSort \\[RETIRED\\]\" node_modules/@objectstack/spec/authorable-surface/ui.json` returns 1 in this repo after a spec pin bump — i.e. the **installed** spec carries the tombstone. ⛔ Do not substitute a version comparison.\n\nRestart-touch: `pnpm-lock.yaml`.\n\n### On restart, the work is mechanical and the shape is already written\n\nPer the prior ruling, unchanged: remove the three `plugin-view` reads plus `ObjectGrid`'s two, flip the two pins (`ObjectView.canonicalTableKeys.test.tsx`, `ObjectView.sortSink.test.tsx`) from \"works as an alias\" to \"refused\", drop the `object-grid.defaultSort` carve-out entry in `apps/console/src/__tests__/registry-inputs-spec-parity.test.ts`, and tombstone the two doc pages. Pins stay **as they are** until then — they correctly assert the key works while the installed contract still declares it.\n\nTwo traps already paid for, carried forward so they are not re-hit:\n\n- `content/docs/api/schema-reference.md:537-538` and `:603` document `CRUDSchema.defaultSort` / `defaultSortOrder` — a **string field on a different carrier**. ⛔ A grep sweep for this key must not \"fix\" them.\n- The existing tripwire in `registry-inputs-spec-parity.test.ts` was **measured firing** against a real tombstone, so no new gate is owed; it will go red on the pin bump with \"spec no longer declares `object-grid.defaultSort` — re-check the carve-out\".\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5597322900,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-09T06:44:09Z","updated_at":"2026-09-09T06:44:09Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5597322900","body":"**Released from `pm:on-hold` → `pm:queue`** by the #8773 hold audit (PM session `session_019wtfW1ZxGnP1XKGc9uZVms`, 2026-09-09T06:5xZ).\n\n**Why the hold was never right:** this card is not waiting on an event — it is **the work a maintainer ruling explicitly ordered opened**. From #4869's decision digest (maintainer 2026-08-22, verbatim 「接受所有」):\n\n> Option A now, plus **\"the follow-up card for the C half — an ADR-0049 enforce-or-remove retirement of `table.defaultSort` as its own card, not folded into this one.\"**\n\nOption A has **landed** (`claude/issue-4869-objectview-sort-sink`). Its predecessor is done and the ruling that directed this half is on the record. A card carrying a live ruling with its prerequisite already merged belongs in the queue, not in a hold with no wake condition.\n\n⛔ It carried no `Restart-when:` and no `Blocked-by:`, so nothing was ever going to return it — it has sat since 2026-08-23.\n\nScope on pickup: the ADR-0049 enforce-or-remove retirement of `ObjectGridSchema.defaultSort` (declared `{ field, order }`, already marked `@deprecated Use sort instead` in `packages/types/src/objectql.ts`) — as its own change, per the ruling's explicit \"not folded into this one\".\n\n⚠️ **Two things to establish before writing code**, neither of which this seat has verified:\n1. Re-read #4869's landed Option A on current `main` — what it did and deliberately did not do bounds this half.\n2. `defaultSort` is a **published** key. Under 「创业阶段不渐进」 a retirement ships immediately without a staged window, but if removal turns out to touch an accept set beyond the deprecation already declared, stop and report rather than widening scope.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5808485875,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T05:51:07Z","updated_at":"2026-09-24T05:51:07Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5861#issuecomment-5808485875","body":"## 解锁:重启条件已经满足,剩下的是机械工作 —— 撤销过期的认领,定级 `priority:p3`,留在队列等派发\n\n分诊席(`session_01Tw7jnJinGHvoGSi8aFkhPJ`,objectstack 座位贴 #6015),2026-09-24T05:50Z。objectui 清理第六批(#8587 的挂起卡一次性清点):本席读完了卡面和评论(6 条,全部读到),并在本地 main 上重新测量。本卡有指派人,本席先报给维护者。**维护者答:「同意」。**\n\n### 方向早已裁定,不再讨论\n\n维护者 08-22 在 #4869 的裁定(「接受所有」)要求 `table.defaultSort` 按 ADR-0049\"要么执行,要么删除\"的原则退役。\n\n### 重启条件已经满足\n\n`5450800597` 写的重启条件是:**已安装的** spec 带有 `ObjectGridProps:defaultSort [RETIRED]` 墓碑,不能用版本号比较来代替。本地 main(09-21)实测:\n\n- `pnpm-lock.yaml` 解析到的 `@objectstack/spec` 是 **17.4.0**。\n- `apps/console/src/__tests__/registry-inputs-spec-parity.test.ts:971` 已经记录:`object-grid.defaultSort` 是在 **17.3.0** 被收走的第九个墓碑,spec 在 `safeParse` 时按名拒绝它。那条豁免项也已经删掉了。\n\n⇒ 契约层面已经按名拒绝这个字段,objectui 这边剩下的读取都是死代码,读的是一个契约不接受的字段。\n\n### 剩下的工作(`5398700176` 早已写好,本席按现状核对过)\n\n- 删除仍在读取这个字段的代码:`packages/plugin-view/src/ObjectView.tsx`(如 `:1038` 的 `schema.table?.defaultSort`)、`packages/plugin-grid/src/ObjectGrid.tsx`、`packages/plugin-grid/src/index.tsx`。⚠️ 所有读取点要一次移完,不能只改一处。\n- 把 `ObjectView.canonicalTableKeys.test.tsx` 和 `ObjectView.sortSink.test.tsx` 里断言\"这个字段能用\"的测试,**改写**为断言\"这个字段不再生效\"(契约层已经按名拒绝它)。⛔ 不删。同一轮里断言规范写法 `sort` 在三条路径上都照常生效。\n- 给 `content/docs/plugins/plugin-view.mdx` 和 `content/docs/api/schema-reference.md` 里描述这个字段的段落加上墓碑说明。⚠️ `schema-reference.md:537-538` 和 `:603` 写的是 `CRUDSchema.defaultSort`,那是另一个载体上的字符串字段,⛔ 不要动。\n\n### 认领与定级\n\n- 08-24 的认领(`5398412525`)停下来是对的:当时 spec 那一半还没落地(`5398671726`),无从下手。之后一个月没有新动静,本席撤销这个认领,让卡可以重新派发。这不是否定当时的工作,那份报告正是今天能直接执行的原因。\n- `priority:p3`:收掉一个契约已经拒绝的旧写法,用户看不到任何变化。保留 `pm:queue`。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
