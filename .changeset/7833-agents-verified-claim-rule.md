---
---

Add commandment #9 to the root `AGENTS.md`: a "verified" claim is only as good
as the check that re-verifies it, so a declaration of verification points at the
instrument that re-derives it rather than copying the instrument's answer into
prose (objectui#7833). Instruction file only; no package source changes and
nothing published changes.
