{
  "number": 3122,
  "kind": "pull_request",
  "title": "fix(list,grid,detail,tree,core): 消费者收敛到单键 — 列身份家族归零 (#3104 PR2)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: core",
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-31T11:45:36Z",
  "updated_at": "2026-07-31T12:02:43Z",
  "closed_at": "2026-07-31T12:02:41Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-31T12:02:41Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/3122",
  "body": "#3104 **PR2**(消费者收敛)。接 #3119(PR1,已合)。台账:objectstack#4115。\n\n## PR1 的 chokepoint 盖不到的地方,才是这一件的用户价值\n\nPR1 把归一挂在 `normalizeListViewSchema` —— **ListView 的组件边界**。但**独立渲染的 `object-grid` 节点**(页面直接 author 一个 grid,上面没有 `list-view`)从不经过那道 fold。`ObjectTree`、`RelatedList`、`record:details` / `record:related_list` 同理。\n\nObjectGrid 的自相矛盾在这些表面上就是活的:\n\n```ts\nconst ensureId = (list) => {\n  const names = list.map(f => f?.name || f?.field);   // 探针:name 优先\n  …\n};\nconst fields = schemaColumns.map(c => c.field);        // 投影:只读 field\n```\n\n于是 `{ name: 'account' }` 这样一列进到 `$select` 里是一个**字面的 `undefined` 空洞** —— 服务端根本不返回该字段,那一列每个格子都是空的。测试里的失败信息把它照了出来:\n\n```\nAssertionError: expected [ 'id', undefined ] to include 'account'\n```\n\n## 收敛\n\n| 表面 | 原读法 | 现在 |\n|---|---|---|\n| `ListView` ×9 + 它的 2 个请求构造器 | `name \\|\\| fieldName \\|\\| field` vs `f?.field` | `columnIdentity()` |\n| `RelatedList` ×8 | `accessorKey \\|\\| field \\|\\| name` | `accessorKey \\|\\| columnIdentity()` |\n| `ObjectGrid`(探针 + 投影) | name 优先 vs 只读 field | `columnIdentity()` |\n| `ObjectTree` | `name \\|\\| fieldName \\|\\| field \\|\\| key` | `columnIdentity() \\|\\| key` |\n| `buildExpandFields` | `field ?? name ?? fieldName` | `columnIdentity()` |\n| `record-details` / `record-related-list` | `field \\|\\| name (\\|\\| key)` | `columnIdentity() (\\|\\| key)` |\n\n**`accessorKey` 保住优先级** —— 它是 TanStack Table 的列键,不是 ObjectStack 元数据身份,只有 `field || name` 那截被收敛(而且保留 `||` 而非 `??`,空串的 fall-through 语义原样保留)。`key` 在 `ObjectTree` / `record-related-list` 里同样留作尾部兜底:它是通用条目键。\n\n## 两个 TypeScript 顺手挖出来的既有缺陷\n\nresolver 不再返回 `any` 之后,`tsc` 立刻指出 ListView 有两处会构造出 `value: undefined` / `name: undefined` 的条目(筛选字段下拉 + hide-fields 弹层)—— 对「没有可解析身份」的列。这种条目永远匹配不上任何一列,现在直接丢弃。不是我引入的,是原来 `f.name || f.fieldName || f.field` 的 `any` 把它藏住了。\n\n## 盘点再分流:24 → 22 → 0\n\nPR1 记了 24 个家族成员。读清楚它们实际喂给谁之后,**有两处是我分流错了**,这里改判而不是收敛 —— 它们根本不是列读取:\n\n- **`ViewPreview.tsx`** —— `toFormFieldEntry` 把 ViewItem 的**表单** section 适配成 `object-form` 选字段用的形状(`field` → `name`)。这是 **#3090 的两层 join**,不是列。用 `columnIdentity` 收敛它是范畴错误。\n- **`SchemaForm.tsx`** —— `summariseComposite` 把**任意** composite/数组元数据值渲染成弹层摘要,猜一个展示键。数组条目是 validations / actions / JSON schema 声明的任何东西,没有列词汇表可收敛。\n\n所以家族实际是 **22**,现在是 **0**。ratchet 从 34 降到 **12**(全部非家族residue)。\n\n## ratchet 现在钉三件事\n\n1. 家族计数 = **0**,总数 = 12(新增双读、或已列文件里多一处 → 红;变少也红)。\n2. **每个收敛过的表面确实走了共享 reader** —— 如果哪个表面不是「收敛」而是「悄悄把身份解析删了」,这条会红。\n3. **`RelatedList` 里 `accessorKey` 仍在 `columnIdentity` 之前**(source-level pin,注释里明写它是 source-level 而不是行为断言)—— 这条一旦破,导入的 TanStack 列会全部解析不出来、整列变空。\n\n## mutation-test\n\n| 变异 | 闸门 | 结果 |\n|---|---|---|\n| M4 把 ObjectGrid 的投影改回 `c.field` | `columnIdentity.test.tsx` | 🔴 `expected [ 'id', undefined ] to include 'account'` |\n\n(PR1 的 M1/M2/M3 仍在,ratchet 与复现测试未变。)\n\n## 验证\n\n```\nvitest  core + plugin-list/grid/detail/tree/view  →  166 files / 2344 tests 全绿\nturbo   type-check ×5 包                          →  17/17 successful\neslint  改动文件 --quiet                           →  0 error\n```\n\nPR1 的复现测试(`ListView.columnIdentity.test.tsx`)继续绿 —— 现在它同时覆盖 fold 和收敛后的读法两条保障。\n\n## 破坏面\n\n全部 patch。仓内行为变化只发生在「同一列两个键不一致」和「非 ListView 下游表面的遗留单键列」上 —— 前者是缺陷,后者是修复(原来那列是空的)。\n\n## 后续(PR3)\n\n闸门 + 响亮化:`hasConflictingColumnIdentity` 已在 PR1 就位待用;fold 从镜像改为删除;台账条目处置,同步 objectstack#4115。\n\n关联:#3119(PR1)、objectstack#4115(台账)、#3090(判别法)、#2598(chokepoint 先例)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01C2pdPmf2yZSd4wFDs1NHY5)_"
}
