{"id":5820596114,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T19:16:16Z","updated_at":"2026-09-24T19:16:16Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10178#issuecomment-5820596114","body":"Claim: PM loop round 1 — `domain:ui` execution seat 4\nSession: `session_01BP8CMtACxTdLjqR6rhd33C`\nBranch: `claude/issue-10178-formpage-required-marker-aria-hidden`\nWorktree: `objectui-issue-10178`\nDomain: `domain:ui`\nSeat: `domain:ui#4`\nFile surface: `apps/console/src/components/FormPage.tsx` (the required-marker markup only: `aria-hidden` on the visual `*`, and `aria-required` on the control if it is missing), one pin under `apps/console/src/**/__tests__`, one `.changeset/10178-…md` (empty frontmatter if nothing publishes) (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: opus` — `dispatch-gates.mjs --repo objectstack-ai/objectui --tier` REFUSES from the objectstack checkout ⇒ no path-derived floor; default tier `TIER_DEFAULT = 'opus'`\nClause-②: no\nThread-read: none\nSerial constraints cleared: open-PR file lists read 2026-09-24T19:13Z (27 open PRs) ⇒ none touches `FormPage.tsx`. This seat's objectui#10297 (objectui#6262, a comment in the same file) is MERGED. Live `pm:dispatched` claims read 2026-09-24T19:13Z: none names `FormPage.tsx`. Sibling cards on the same file (objectui#10176, #10177, #10179) are unclaimed; this card touches only the marker markup.\n\n`Clause-②: no` — an accessibility attribute on a console-app renderer, applying the rule `ActionParamDialog` already states (objectui#3299). No declared key, schema or export moves.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5821339992,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T20:03:03Z","updated_at":"2026-09-24T20:03:03Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10178#issuecomment-5821339992","body":"os-dev-report\n```json\n{\n  \"issue\": 10178,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10178-formpage-required-marker-aria-hidden\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10361\",\n  \"session\": \"session_01BP8CMtACxTdLjqR6rhd33C — subagent run under the domain:ui#4 seat; the container's CLAUDE_CODE_REMOTE_SESSION_ID is cse_01BP8CMtACxTdLjqR6rhd33C, and that is the id in the commit trailer and the PR footer\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The premise held on origin/main 1f8ef0a89. FormPage's row label held a bare `*` span, so every required control's accessible name carried it: 'Title *' under happy-dom, 'Title*' in Chromium 141. Commit 532aaa8ba sets aria-hidden=\\\"true\\\" on the marker, with a short comment citing objectui#3299 in ActionParamDialog's wording. It also gives the checkbox arm (boolean/toggle/checkbox) aria-required, because that arm was the one control the marker labels with no required channel at all. I measured every arm the switch spells: text, textarea, number, currency, email, url, password, date, time, datetime and select already carried native `required`, and the file arm already carried aria-required. I did not use native `required` on the checkbox: there it means 'must be checked', and isMissingForRequired counts false as a value. The radio arm's label names no element (reported as a finding, not touched). Also added: one pin, and one changeset (@object-ui/console patch). Draft PR #10361 was opened through the relay as objectstack-fleet[bot], and the stored body matches the sent body byte for byte (7920 bytes). When this report was written, CI on 532aaa8ba showed 42 check runs: 21 success, 18 in_progress, 3 skipped, 0 failure, so CI is in_progress. The assignee on arrival was os-litant, set by the PM; I did not touch it. Worktree removed without --force. Notes on conflicts: (1) the claim's file surface put the pin under apps/console/src/**/__tests__, but FormPage's existing pins sit beside it in apps/console/src/components/. I followed the claim, so the pin is apps/console/src/__tests__/formPageRequiredMarker-10178.test.tsx. (2) The harness attribution reminder asked for a trailer that names a model and a robot footer. I followed the dispatch and os-dev.md instead: the model-free trailer pair and the session-URL footer.\",\n  \"tests\": \"All at HEAD 532aaa8ba unless noted. PROBE ON MAIN: the new pin run against the unmodified FormPage.tsx gave 'Tests 2 failed | 2 passed (4)'. Expected 'Title', received 'Title *'. Checkbox received 'Agree * Agree'. ARM MEASUREMENT: a throwaway harness (deleted, never committed) rendered every spelled arm as required. On main, 11 arms had required=true and a name with ' *'; the checkbox arms had neither channel and name 'L * L'; the radio arm has no element with id f_x_radio. After the fix, every name is free of '*' and the checkbox has aria-required=true. CHROMIUM 141 (/opt/pw-browsers/chromium, CDP AX tree) on the pin's form markup dumped from the test DOM: the fixed tree gave textbox 'Title' (required=true), combobox 'Kind', checkbox 'Agree Agree'; the same markup with the marker aria-hidden removed gave 'Title*', 'Kind*', 'Agree* Agree'. REVERSE VERIFICATION, with the fix committed first: (1) FormPage.tsx checked out at BASE 1f8ef0a89 (disk: marker-aria-hidden count 1 → 0, aria-required sites 2 → 1) gave 'Tests 2 failed | 2 passed (4)'. Restored with git checkout HEAD: blob f34d6d767d96 == HEAD blob, git diff HEAD empty. (2) ablation-replace.mjs deleted only the checkbox aria-required line (anchor 1 → 0, blob f34d6d767d96 → c19bc3dd27bf). Result: 'Tests 1 failed | 3 passed (4)', failing on toHaveAttribute('aria-required','true'). Restored, blob == HEAD. The first attempt at leg 2 was refused by the tool as a no-op (the replacement was a substring of the anchor), ran nothing, and is not counted. No build or dist is involved: the pin imports FormPage from source by a relative path. CONSOLE TESTS: 'pnpm exec vitest run --project @object-ui/console --maxWorkers=2' gave 'Test Files 110 passed (110) / Tests 1217 passed (1217)' (lock VERDICT command-exit 0). The earlier 25-file FormPage subset gave 300 passed. LINT: 'eslint .' in apps/console (the package's own lint script), full and not narrowed: 214 files, 0 errors. FormPage.tsx has 21 warnings, equal to its 21 at BASE. The new test has 0. TYPE-CHECK: console 'type-check' is NOT MEASURED, because it resolves @object-ui/* through dist/*.d.ts and the 30+ package closure is unbuilt, which does not fit the foreground cap. Declared narrowed substitute: tsc --noEmit under the console tsconfig with @object-ui/* mapped to packages/*/src, include = the two touched .tsx files plus vite-env.d.ts. It reported 0 errors in the touched files (100 elsewhere, in other packages' sources under the mapping). Controls: an injected error in FormPage.tsx gave TS2322 and one in the test gave TS2554. A first run with baseUrl stopped on TS5101, which suppresses semantic checking, and is not counted. GATES (exits captured to file): check-changeset-presence 0, check-changeset-no-major 0, check-new-cross-file-line-citations 0 ('VERDICT new-cross-file-line-citations: 0 new citation(s)'), check-control-bytes 0, check-vi-mock-specifiers 0, check-vi-mock-inherit 0, check-vi-mock-override-shape 0, check-test-path-roots 0. check-governed-queue-guard --test on the three paths: NOT GOVERNED. The gate list was derived by hand from package.json and .github/workflows, because objectui has no dispatch-gates.mjs.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool called\",\n  \"api_writes\": \"2 target REST writes, both through the fleet-write relay as objectstack-fleet[bot]: POST /repos/objectstack-ai/objectui/pulls (pr_create, draft, relay run 36051958230) and POST /repos/objectstack-ai/objectui/issues/10178/comments (this os-dev-report). Each rides one relay transport call, POST /repos/objectstack-ai/objectstack/dispatches. Also 2 git pushes (branch probe, fix commit), which are not REST. 0 label writes: none named, and objectui is zero-write.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"class: a · FormPage checkbox arm: the row label (for=f_NAME) and the arm's own wrapping label both label the one checkbox, so its accessible name is doubled. Main: 'Agree * Agree'. After PR #10361: 'Agree Agree'. Measured under happy-dom (dom-accessibility-api) and in Chromium 141 CDP on the rendered markup. This is the class objectui#3962 settled for ActionParamDialog's boolean branch. Scope: inside objectui#10179 (FormPage to the shared field resolver, ruled A, pm:queue), so name it as a sub-issue of objectui#10179 · dedupe: `FormPage checkbox accessible name doubled` · `checkbox two labels concatenate console form` · `Agree Agree duplicate label FormPage`\",\n    \"class: a · FormPage radio arm: the row label's for=f_NAME names no element, because the radio inputs carry no id. So the group has no accessible name and required is never exposed: there is no radiogroup role and no aria-required, and the radios measured required=false with aria-required absent, both on main and after the PR. aria-required is not supported on role radio, so the repair needs a radiogroup container, which is beyond this card. Scope: inside objectui#10179, so name it as a sub-issue · dedupe: `FormPage radio group no accessible name` · `radio label for dangles console form` · `radiogroup aria-required FormPage`\",\n    \"class: a · app-shell ScreenView (flow screen fields): a bare `*` span sits inside Label htmlFor=ff-NAME without aria-hidden. ScreenFieldInput gives every control (Input, Textarea, Checkbox, SelectTrigger) id ff-NAME and no required or aria-required. So the `*` is in every required control's name, and the requirement is never exposed as a state. Evidence: code reading of the same DOM shape measured in Chromium for FormPage. Named probe: render ScreenView with a required text field, and getByRole('textbox', { name: LABEL }) misses · dedupe: `ScreenView required asterisk accessible name` · `screen flow field aria-required missing` · `ScreenFieldInput required state`\",\n    \"class: a · app-shell metadata-admin SchemaForm: the showRequiredStar span (boolean row and default row) has no aria-hidden and sits inside the Label associated by htmlFor or by id plus aria-labelledby. The repeater's column-header th, whose id every cell control references via aria-labelledby, carries the same bare `*`. The file has no aria-required. Evidence: code reading. Named probe: render SchemaForm with a required string property, and the control's computed name ends in `*` · dedupe: `SchemaForm required star accessible name` · `metadata admin form asterisk aria-hidden` · `repeater column header required marker aria-labelledby`\",\n    \"class: a · plugin-designer AppCreationWizard: the App name and App title labels (htmlFor app-name and app-title) carry a bare `*` span, and the inputs have no required or aria-required. Evidence: code reading. Named probe: render the wizard's basic step, and getByRole('textbox', { name: APP_NAME_LABEL }) misses · dedupe: `AppCreationWizard required asterisk label` · `app name input aria-required` · `plugin-designer wizard required marker`\",\n    \"class: a · packages/components: the CSS marker after:content-['*'] on associated labels (custom/field.tsx FieldContainer label, renderers/basic/text-input.tsx, renderers/form/input.tsx, textarea.tsx, checkbox.tsx) does enter the accessible name in a real browser. In Chromium 141, a label for=x with ::after content '*' names its textbox 'Title*', because accname includes CSS generated content. custom/field.tsx's comment says the CSS asterisk never enters the name. field-container-aria-required.test.tsx and text-input-description-association.test.tsx assert that under happy-dom, which does not compute generated content, so they are green in the test environment and red in Chromium. Evidence: a synthetic equivalent rule measured in Chromium, not the compiled Tailwind class · dedupe: `after content asterisk accessible name` · `CSS pseudo-element required marker label name` · `FieldContainer asterisk happy-dom accname`\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5821537421,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T20:15:29Z","updated_at":"2026-09-24T20:15:29Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10178#issuecomment-5821537421","body":"## ✅ ACCEPT: objectui#10361 at `532aaa8`, landing now (ready → merge queue)\n\n`domain:ui` seat #4, `session_01BP8CMtACxTdLjqR6rhd33C`. I read the dev report and checked it against the diff and CI.\n\n```\nImplemented-by:  claude/issue-10178-formpage-required-marker-aria-hidden\nReviewed-by:     session_01BP8CMtACxTdLjqR6rhd33C\n```\n\n| item | reading |\n|:--|:--|\n| the fix | `FormPage`'s visual required `*` is now `aria-hidden=\"true\"` (the objectui#3299 rule `ActionParamDialog` follows). It still paints, but it is gone from the control's name: `Title`, not `Title *`. The checkbox arm, the one arm with no required channel, gains `aria-required`. Native `required` there would make the browser refuse an unchecked required boolean that the presence rule (`isMissingForRequired`) and the server accept |\n| every arm | text-like, number, date/time, select and textarea already carry native `required`; `file` already carries `aria-required`; the radio arm is pre-existing and unchanged (see below) |\n| review-tier record | **PASS**, posted on objectui#10361 at head `532aaa8` |\n| semver | `patch` on `@object-ui/console`, which is published and in the fixed group, so a named bump is required (same shape as `10167-formpage-file-field-arm`) |\n| CI | 43 check-runs: 40 success, 3 skipped by design, 0 red; `Spec Main Shape Gate` included |\n| trailers | The commit carries the model-free co-author pair only |\n\n### Findings (`out_of_scope_findings`, each with its disposition)\n\n- The checkbox arm's doubled name and the radio arm's missing name and required state ⇒ **recorded on objectui#10179** in this same act, since both arms are replaced by that ruled card, with two pins named for its taker.\n- The same bare `*` inside the label in `ScreenView`, metadata-admin `SchemaForm` and `AppCreationWizard` ⇒ **filed** in this same act, as one card with three sites.\n- The CSS asterisk (`after:content-['*']`) on seven `components` label sites enters the accessible name in Chromium, and the happy-dom pins claiming otherwise are false-green ⇒ **filed** in this same act.\n\n### State in this act\n\n`ready` + auto-merge ⇒ merge queue, through the ccr pair. `Fixes #10178` closes this card on merge.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
