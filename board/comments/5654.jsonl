{"id":5380593582,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-22T13:12:31Z","updated_at":"2026-08-22T13:12:31Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5654#issuecomment-5380593582","body":"Concentration-round first-touch (triage seat): promoted `finding` → `pm:queue`, Task S. The card's own repair direction is adopted as the dispatch shape: export the degradation predicate from `paramToField` (e.g. `paramDegradesWithoutTarget(param)`) and have the dialog's hint condition read it, so \"who degrades\" and \"who gets hints after degrading\" are one member set answered once. ⛔ Hand-adding `master_detail` to the dialog line stays forbidden (a third copy of the set is the disease, not a cure). The behaviour change is the point (a degraded `master_detail` gains the #3405 placeholder + help text) and takes its own test per spelling row of the card's table. Convergence lineage #4770/#4790/#4815 noted.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5387173486,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-23T16:52:52Z","updated_at":"2026-08-23T16:52:52Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5654#issuecomment-5387173486","body":"Claiming this card.\n\n- session id: `01EuPCi56cnGyykygi3z9w4m`\n- branch: `claude/issue-5654-param-degrade-predicate`\n- seat: `domain:ui` os-dev\n\nShape per the triage ruling: export one degradation predicate from `packages/app-shell/src/utils/paramToField.ts` and consume it at `ActionParamDialog.tsx:351`, leaving exactly one membership table in the repo.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5387302697,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-23T17:18:05Z","updated_at":"2026-08-23T17:18:05Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5654#issuecomment-5387302697","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5654,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-5654-param-degrade-predicate\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5882\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Verified the card's premise on origin/main @1939c9610 before writing code: ActionParamDialog.tsx:351 still held `param.type === 'lookup' || param.type === 'reference'` over RAW spellings, while paramToField.ts degraded over RESOLVED widget keys (LOOKUP_WIDGET_TYPES = {lookup, master_detail}) — the two sets still overlapping without either containing the other. Fixed in the ruled direction: paramToField now exports paramDegradesWithoutTarget(param), and both its own fallback branch and the dialog's two hint readers ask it, leaving exactly ONE membership table on this surface. `master_detail` was NOT hand-added to the dialog line. The dialog's `field.type === 'text'` conjunct was dropped as subsumed (it was a proxy for 'did this degrade?'), licensed by an equivalence pin. Deliberate behaviour change: a targetless master_detail param now gets actionDialog.lookupPlaceholder + actionDialog.lookupHelpText; declared in a patch changeset. No doc touch — paramToField is internal to @object-ui/app-shell (not in its index.ts), so the new export changes no documented surface.\",\n  \"tests\": \"All legs at final HEAD 67d6d8af1, from the repo root, path-filtered; heavy legs through scripts/pm/os-verify-lock.sh, verdict lines quoted rather than a bare $?.\\n\\nTESTS — pnpm exec vitest run packages/app-shell/src/utils/paramToField.test.ts packages/app-shell/src/views/ActionParamDialog.test.tsx --maxWorkers=2 → 'Test Files 2 passed (2) / Tests 72 passed (72)', 'os-verify-lock: VERDICT command-exit 0'. The verbose run names all four new dialog cases and all three new predicate cases as ✓, so none was silently skipped.\\n\\nREVERSE VERIFICATION (discriminating leg) — reverted ONLY the consumer hunk (git checkout HEAD~1 -- packages/app-shell/src/views/ActionParamDialog.tsx), keeping the exported predicate, so the failure is an assertion and not a missing-export module error. Revert confirmed on disk before measuring (grep -c degradedPicker → 0; retired literal back at line 351; git diff HEAD --stat showed the file). Result: '× a targetless master_detail param degrades to text AND now carries both #3405 hints (objectui#5654) — AssertionError: expected null to be actionDialog.lookupPlaceholder', 'Test Files 1 failed | 1 passed', 'Tests 1 failed | 71 passed (72)'. Exactly one test moved. Restored with git checkout HEAD -- <file>; git diff HEAD --stat EMPTY and git status --porcelain EMPTY; suite green again.\\n\\nLEGS THAT DO NOT DISCRIMINATE (stated, not staged): the three other new dialog cases stay GREEN under that revert — they are controls pinning behaviour that must not move (`reference` keeps its hints, master_detail WITH a target mounts the picker and gets none, an unknown type renders text and gets none). The paramToField predicate cases also stay green under it (producer untouched); reverting the PRODUCER instead removes the symbol the dialog imports, so that leg fails as a module error rather than a behaviour signal — it was not run as evidence.\\n\\nANTI-VACUITY CONTROL inside the equivalence sweep: the loop asserts predicate(param) ⇔ paramToField replaced the param's own widget with the text fallback over every FORM_FIELD_TYPES member plus every alias, and then asserts the degrading rows are exactly ['lookup','master_detail','reference'] — a sweep where nothing degraded would otherwise pass as false===false on every row.\\n\\nTYPE-CHECK — dependency closure built first: pnpm --workspace-concurrency=2 --filter '@object-ui/app-shell^...' build → 'VERDICT command-exit 0'. Then pnpm --filter @object-ui/app-shell type-check → script name echoed ('> @object-ui/app-shell@17.6.0 type-check' / '> tsc --noEmit && tsc -p tsconfig.test.json'), so not a zero-match silent pass; 'os-verify-lock: VERDICT command-exit 0 · held the lock 61s · waited 103s'.\\n\\nREPO GATES (inputs touched), each quoting its own verdict line, exit code captured before any pipe:\\n  check-changeset-presence.mjs EXIT=0 — '✅ 4 source file(s) of 1 released package(s) changed, and this change declares 1 changeset(s): .changeset/action-param-degrade-predicate-5654.md.'\\n  check-changeset-no-major.mjs EXIT=0 — '✅ No changeset declares a `major` bump.'\\n  check-control-bytes.mjs EXIT=0 — '✅ check-control-bytes: OK (scanned 4891 tracked text file(s); skipped 85 binary).'\\n  check-i18n-call-site-keys.mjs EXIT=0 — 'Every in-scope call-site key resolves against the en pack (2928 keys)…' (no new keys: both hint keys already ship in all ten packs).\\nDoc gates not run: no docs touched, and no version literal was written into prose anywhere.\\n\\nLINT — narrowing DECLARED. eslint was run over the four changed source files, not the repo. (i) Population came from eslint's own resolution of those paths, not from my guess about scope; (ii) the count is read from --format json: 4 files linted, 0 errors, 22 warnings; (iii) invariance: eslint.config.js declares no `project`/`projectService`, so no rule here is type-aware and this diff cannot move a verdict on an untouched file — the repo's `ratchet` rules are per-file AST rules, so a per-file run is a complete measurement of the diff. All 22 warnings are pre-existing (no-explicit-any / react-refresh / react-hooks); 0 land on a line this branch added, checked by intersecting the reporter's line numbers with the added-line set of git diff -U0 $(git merge-base HEAD origin/main) HEAD. The full farm runs in CI.\\n\\nCI: reported at draft-PR time per the dispatch contract; gate convergence is the PM's read, not awaited here.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #5880: the target-required degrade set {lookup, master_detail} is stated once per package — app-shell's paramToField.ts:54 and plugin-grid's bulkParamToField.ts:40 — with nothing mechanical tying them; members identical today (no live defect), and the two predicates key on different fields (referenceTo vs object), so merging them is a decision rather than a mechanical convergence. Labels finding + domain:ui, unassigned, no pm:queue.\",\n    \"not filed (already covered): app-shell's resolveActionParams.ts:489 restates the reference-bearing family — that is #5874's table, already open with finding + domain:ui, and #5875 covers the stale 'fourth and last private copy' prose in the two files this PR edits. This PR does not touch those sentences; expect a textual merge with #5875, not a contradiction.\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01EuPCi56cnGyykygi3z9w4m)_\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
