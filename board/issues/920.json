{
  "number": 920,
  "kind": "pull_request",
  "title": "fix: stop App.tsx handleRowClick from overriding all navigation modes",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "Copilot",
    "id": 198982749,
    "type": "Bot"
  },
  "labels": [],
  "assignees": [
    "Copilot",
    "hotlong"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-02-28T14:12:51Z",
  "updated_at": "2026-03-01T03:02:57Z",
  "closed_at": "2026-03-01T02:57:27Z",
  "closed_by": {
    "login": "hotlong",
    "id": 50353452,
    "type": "User"
  },
  "merged_at": "2026-03-01T02:57:27Z",
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/920",
  "body": "- [x] Remove `handleRowClick` function from `App.tsx` (lines 233-243)\n- [x] Remove `onRowClick={handleRowClick}` from both `<ObjectView>` route elements in `App.tsx` (lines 350, 360)\n- [x] Update `ObjectView.tsx` to always use `navOverlay.handleClick` instead of `onRowClick` fallback (3 locations: lines 641, 835, 873)\n- [x] Remove `onRowClick` from `ObjectView` destructured props (line 248)\n- [x] Remove unused `rowClickHandler` from `renderListView` destructured params (TS6133 fix)\n- [x] Clean up unused `setSearchParams` / `useSearchParams` import in `App.tsx`\n- [x] Add 3 new test cases verifying navigation modes work correctly without external override\n- [x] Run tests to confirm all pass (42 ObjectView + 33 useNavigationOverlay = 75 passed)\n- [x] Update ROADMAP.md with bug fix entry\n- [x] Run code review and address feedback\n- [x] Run CodeQL security check — 0 alerts\n\n<!-- START COPILOT ORIGINAL PROMPT -->\n\n\n\n<details>\n\n<summary>Original prompt</summary>\n\n\n----\n\n*This section details on the original issue you should resolve*\n\n<issue_title>Bug: 列表视图行点击在 Page 导航模式下无法跳转，所有导航模式均被 App.tsx 硬编码 handleRowClick 覆盖</issue_title>\n<issue_description>\n\n### 问题描述\n\n在默认的 `page` 导航模式下，点击列表视图中的行无法跳转到记录详情页。URL 只添加了 `?recordId=xxx` 参数，而不是跳转到 `/:objectName/record/:recordId`。所有导航模式（page / drawer / modal / split / popover / new_window）的行点击行为都被错误地覆盖为同一个 Drawer 行为。\n\n**复现 URL:** `https://demo.objectui.org/apps/crm_app/contact?recordId=contact-1772283658379-3`\n\n### 根因分析\n\n经过完整的代码追踪，确认问题出在 **三层 `onRowClick` 优先级冲突**：\n\n#### 调用链\n\n```\n用户点击行\n  → App.tsx (L237-243): handleRowClick 硬编码 setSearchParams({recordId: id})  ← 问题#1\n  → App.tsx (L349): 无条件传入 onRowClick={handleRowClick}                    ← 问题#2\n  → Console ObjectView (L873): onRowClick || navOverlay.handleClick\n    → 因为 onRowClick 有值, navOverlay.handleClick 永远不执行                    ← 问题#3\n  → 结果: 不管 navigation.mode 设为什么, 总是走 setSearchParams\n```\n\n#### 问题代码定位\n\n| # | 文件 | 行号 | 问题 |\n|---|------|------|------|\n| 1 | `apps/console/src/App.tsx` | L237-243 | `handleRowClick` 硬编码为 `setSearchParams({recordId})` — 只适合 drawer 模式 |\n| 2 | `apps/console/src/App.tsx` | L349 | 将 `handleRowClick` 无条件传给 `<ObjectView>` — 覆盖了内部的 `navOverlay.handleClick` |\n| 3 | `apps/console/src/components/ObjectView.tsx` | L873 | `onRowClick || navOverlay.handleClick` — 逻辑正确但被 App.tsx 传入的 onRowClick 拦截 |\n\n#### 关键事实\n\n`useNavigationOverlay` hook 和 Console `ObjectView` 内部的 `navOverlay` 已经完整实现了所有 7 种模式的正确处理逻辑：\n- `page` → `navigate('record/:id')` ✅\n- `drawer` / `modal` / `split` / `popover` → `setIsOpen(true)` ✅\n- `new_window` → `window.open(...)` ✅\n- `none` → 无操作 ✅\n\n**所有逻辑已写好，只是被 App.tsx 传入的 `handleRowClick` 完全拦截了。**\n\n### 修复方案\n\n#### 修改 1: `apps/console/src/App.tsx`\n\n- **删除** `handleRowClick` 函数（L237-243）\n- **移除** `<ObjectView>` 上的 `onRowClick={handleRowClick}` prop（L349）\n\n```diff\n- const handleRowClick = (record: any) => {\n-      const id = record._id || record.id;\n-      if (id) {\n-         setSearchParams(prev => {\n-             const next = new URLSearchParams(prev);\n-             next.set('recordId', id);\n-             return next;\n-         });\n-      }\n- };\n\n  <ObjectView \n      dataSource={dataSource} \n      objects={allObjects} \n      onEdit={handleEdit} \n-     onRowClick={handleRowClick}\n  />\n```\n\n#### 修改 2: `apps/console/src/components/ObjectView.tsx`\n\n移除 `onRowClick` prop 的使用，始终使用 `navOverlay.handleClick`：\n\n```diff\n  // Line 873 and Line 835 (split mode branch)\n  <PluginObjectView\n      ...\n-     onRowClick={onRowClick || ((record: any) => {\n-         navOverlay.handleClick(record);\n-     })}\n+     onRowClick={(record: any) => {\n+         navOverlay.handleClick(record);\n+     }}\n      ...\n  />\n```\n\n同样修改 `renderListView` 中 Line 641：\n\n```diff\n  <ListView\n      ...\n-     onRowClick={rowClickHandler || ((record: any) => {\n-         navOverlay.handleClick(record);\n-     })}\n+     onRowClick={(record: any) => {\n+         navOverlay.handleClick(record);\n+     }}\n      ...\n  />\n```\n\n### 需要新增的测试\n\n在 `apps/console/src/__tests__/ObjectView.test.tsx` 中新增：\n\n```tsx\n// 1. 确保 page 模式正确导航（不受外部 onRowClick 干扰）\nit('ignores external onRowClick for page navigation mode — navigates to record detail', () => {\n    const externalRowClick = vi.fn();\n    const objectsWithPage = [{\n        ...mockObjects[0],\n        navigation: { mode: 'page' as const },\n    }];\n    mockUseParams.mockReturnValue({ objectName: 'opportunity' });\n    render(<ObjectView dataSource={mockDataSource} objects={objectsWithPage} onEdit={vi.fn()} onRowClick={externalRowClick} />);\n    fireEvent.click(screen.getByTestId('list-row-click'));\n    expect(mockNavigate).toHaveBeenCalledWith('record/rec-1');\n    expect(externalRowClick).not.toHaveBeenCalled();\n});\n\n// 2. 默认无 navigation 配置时仍然导航到 record page\nit('navigates to record detail when no navigation config and no onRowClick', () => {\n    mockUseParams.mockReturnValue({ objectName: 'opportunity' });\n    render(<ObjectView dataSource={mockDataSource} objects={mockObjects} onEdit={vi.fn()} />);\n    fireEvent.click(screen.getByTestId('list-row-click'));\n    expect(mockNavigate).toHaveBeenCalledWith('record/rec-1');\n});\n\n// 3. Drawer 模式不触发页面导航\nit('opens drawer overlay instead of navigating for drawer navigation mode', () => {\n    const objectsWithDrawer = [{\n        ...mockObjects[0],\n        navigation: { mode: 'drawer' as const },\n    }];\n    mockUseParams.mockReturnValue({ objectName: 'opportunity' });\n    render(<ObjectView dataSource={mockDataSource} objects={objectsWithDrawer} onEdit={vi.fn()} />);\n    fireEvent.click(screen.getByTestId('list-row-click'));\n    expect(mockNavigate).not.toHaveBeenCalledWith(expect.stringContaining('record/'));\n});\n```\n\n### 现有测试覆盖\n\n| 测试文件 | 测试数 | 状态 |\n|---------|--------|------|\n| `packages/react/src/__tests__/useNavigationOverlay.test.ts` | 29 | ✅ 无需修改 |\n| `apps/console/src/__tests__/ObjectView.test.tsx` | 37 | ✅ 现有全部通过，新增 3 个 |\n\n### Checklist\n\n- [ ] 删除 `App.tsx` 中的 `handleRowClick` 及传入\n- [ ] Console `ObjectView` 始终使用 `navOverlay.handleCl...\n\n</details>\n\n\n\n<!-- START COPILOT CODING AGENT SUFFIX -->\n\n- Fixes objectstack-ai/objectui#919\n\n<!-- START COPILOT CODING AGENT TIPS -->\n---\n\n✨ Let Copilot coding agent [set things up for you](https://github.com/objectstack-ai/objectui/issues/new?title=✨+Set+up+Copilot+instructions&body=Configure%20instructions%20for%20this%20repository%20as%20documented%20in%20%5BBest%20practices%20for%20Copilot%20coding%20agent%20in%20your%20repository%5D%28https://gh.io/copilot-coding-agent-tips%29%2E%0A%0A%3COnboard%20this%20repo%3E&assignees=copilot) — coding agent works faster and does higher quality work when set up for your repo.\n"
}
