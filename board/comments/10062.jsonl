{"id":5814523150,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T12:55:02Z","updated_at":"2026-09-24T12:55:02Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10062#issuecomment-5814523150","body":"Claim: PM loop round 1 — `domain:ui` execution seat\nSession: `session_01BA3nKVUwKQJf8DBxrSVtNC`\nBranch: `claude/issue-10062-dataset-filter-between-arm`\nWorktree: `objectui-issue-10062`\nDomain: `domain:ui`\nSeat: `domain:ui#1`\nFile surface: `packages/app-shell/src/views/metadata-admin/inspectors/datasetFilterCondition.ts`, its tests (incl. the objectui#9381 pin), `.changeset/9372-unmapped-operator-inert.md` (prose amendment only, frontmatter untouched, by this card's own authority), one `.changeset/10062-…md` (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus` (default judgement tier) — `dispatch-gates.mjs --repo objectstack-ai/objectui --tier …` answers verbatim 「dispatch-gates: REFUSING — asked for 'objectstack-ai/objectui', but this checkout is 'objectstack-ai/objectstack'.」 ⇒ no path-derived mandate; tier is this seat's judgement: an operator the bridge refused starts being emitted into stored dataset metadata, behind a completeness rule that must hold at both bounds.\nClause-②: yes\nThread-read: none\nSerial constraints cleared: every open PR's file list and every in-flight claim read 2026-09-24T12:52Z. The eight queued PRs (objectui#10225, #10230, #10231, #10233, #10234, #10236, #10237, #10241), seat #2's objectui#10044 / #10046, the spec seat's objectui#10071 / #10083, and this seat's objectui#10184 / #7190 ⇒ none touches `datasetFilterCondition.ts` or the #9372 changeset. No sibling `between` card is in flight.\n\n### Scope, from the ruling the card carries\n\nRuling batch #146 item 5, letter A (maintainer 「146 同意」, objectstack#18012): the `between` arm maps once a both-bounds completeness rule exists, and it exists (`isFilterValueComplete`, objectui#5025). Deliverables are the card's items 1–4: the arm maps with both bounds required; the #9381 pin flips; the #9372 pending changeset sentence is made true; the `$between` conformance reading is produced.\n\n### Why `Clause-②: yes`\n\nThe card expects it and this seat declares it up front: the bridge starts writing an operator it refused, into stored metadata that a published renderer reads. A `yes` cannot be revised on delivery. ⇒ A review-tier contract review is owed before enqueue.\n\n<sub>`domain:ui` seat #1 · `session_01BA3nKVUwKQJf8DBxrSVtNC` · claim · 2026-09-24T12:54Z</sub>\n"}
{"id":5815323018,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T13:45:37Z","updated_at":"2026-09-24T13:45:37Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10062#issuecomment-5815323018","body":"os-dev-report\n{\n  \"issue\": 10062,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10062-dataset-filter-between-arm\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10253\",\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC — the harness-stamped Claude-Session id (subagent, so the parent's)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The dataset-filter bridge (packages/app-shell/src/views/metadata-admin/inspectors/datasetFilterCondition.ts) now maps `between` to the spec's `{ $between: [lo, hi] }` and reads `$between` back as `between`. The write half's inline, length-blind completeness predicate is replaced by the builder's own isFilterValueComplete (objectui#5025): no second rule, and the same answer for scalar and list operators. So a pair with a missing or blank bound is not emitted: as the only row it holds and the stored filter stays; beside other rows only those rows commit. A read-half guard, scoped to the pair arity and asking the same function, keeps a stored `$between` that the bridge would not have written non-representable (Source tab). Without it, the mapping would expose a stored half pair to silent removal on the next commit of any other row. Deliverables: (1) the objectui#9381 pin is flipped; (2) the #9372 pending changeset's `between stays unmapped` paragraph is amended in prose only, frontmatter byte-identical, and check-changeset-overwrite reports it as the deliberate-correction case; (3) the `$between` conformance reading is added: the covered leg now reads TEMPORAL_CASES + TEMPORAL_TIME_CASES and contains `$between`, alongside FILTER_OPERATORS membership, FieldOperatorsSchema accepting the emitted pair and refusing non-pairs, and the builder offering `between` only on date/datetime/time; (4) the PR carries `Clause-②: yes` on line 2. Emitted shape vs the consumer: objectstack service-analytics at c1641868 (preview evaluator, filter normalizer) reads `$between` only as a 2-element array. Premise checks: the claim's newest `Claim:` names this branch; origin/main had moved from a507334d to a78cd378 with all three premise sentences still present and printed; installed @objectstack/spec 17.4.0 accepts `$between` as a 2-tuple and still accepts `[1, '']`, because the objectstack#18012 spec half is not installed. Assignee os-bill (the PM's) was left untouched. A new changeset, .changeset/10062-dataset-filter-between-arm.md, declares '@object-ui/app-shell': patch.\",\n  \"tests\": \"All runs from the objectui repo root through os-verify-lock.sh, except the check:* gates. Final HEAD 83eb57f0. RED-FIRST, new pins against the base source a78cd378 (the source diff was saved as a patch, the base file checked out, then the patch reapplied): 11 failed / 56 passed. The failures are exactly the mapping pins: 9363 partition; both-bounds, zero-bound, parity, round-trip and door in the new block; the 9382 sweep, its three between column pins and the panel draw. The missing-bound, half-pair-instead and non-representable pins passed. FINAL GREEN at HEAD 83eb57f0: `pnpm exec vitest run packages/app-shell/src/views/metadata-admin/inspectors/` gives Test Files 88 passed (88), Tests 1070 passed | 1 skipped (1071), VERDICT command-exit 0. The skip is a skipIf already in flow-node-config.spec-reconciliation. TYPE-CHECK: the closure was built with `turbo run build --filter=@object-ui/app-shell^... --concurrency=2` (Tasks 28 successful, 7 cached, VERDICT 0). Then `pnpm --filter @object-ui/app-shell type-check`: the script echoed `tsc --noEmit && tsc -p tsconfig.test.json`, VERDICT command-exit 0. `tsc -p tsconfig.test.json --listFilesOnly` lists all four edited test files among 4602. ABLATION, after commit, three legs through objectstack scripts/ablation-replace.mjs in wrap mode (anchor x1 to x0, blob changed, restore proven: blob == HEAD blob 22ca8b6f and `git diff HEAD` empty), each predicted before running, all three matching the prediction. A1, the card's leg: delete `between: '$between',` from OP_TO_MONGO. Result: 10 failed / 57 passed. The mapping pins went red; the missing-lo-or-hi, half-pair-instead, non-representable, operator-route and partial-group pins stayed green, and so did the panel draw (read half only). A2: read guard disabled. Result: 1 failed (non-representable) / 66 passed. A3: old inline predicate restored. Result: 5 failed (operator-route gesture, partial group, missing lo or hi, half-pair-instead, parity) / 62 passed; the mapping pins stayed green. No dist leg: the subject is imported relatively from source, and @object-ui/components is aliased to src in vitest.config.mts. GATES, exit 0 unless noted: check:control-bytes OK; check:new-line-citations 0 new; check-changeset-presence 1 declared; check-changeset-overwrite reports only M 9372 with the declaration unchanged (report-only); check:changeset-claims clean; check-changeset-no-major, check-changeset-fixed, check:pending-changeset-literals, check:spec-symbols, check:test-path-roots and check:installed-pin-claims all 0; check-governed-queue-guard --test on the 7 paths: NOT GOVERNED. LINT, narrowed and declared, with three pieces of evidence. (1) Population: CI's `turbo run lint` runs each package's `eslint .` under the root eslint.config.js; this run covered the 5 touched .ts/.tsx files. (2) Count from --format json: 5 files, 0 errors, 3 warnings, all no-explicit-any on existing `as any` lines (2 at base, 2 at HEAD). (3) Invariance: no parserOptions.project or projectService in eslint.config.js, and no eslint-rules module reads the filesystem (0 hits; control: 11 modules matching `create(`). The same readings are in the PR body. NOT MEASURED: CI, including the Spec Main Shape Gate, because it had not run when this report was written; CI convergence belongs to the PM.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called, read or write. All reads used unauthenticated REST GETs through the proxy: issues 10062 and 9695 plus their comments, PR 9381 and its files, and the PR list by head.\",\n  \"api_writes\": \"2 REST writes, both through the fleet-write relay (with-fleet.sh --via auto resolved to dispatch; each is one POST /repos/objectstack-ai/objectstack/dispatches that the board workflow executes as objectstack-fleet[bot]). (1) pr_create: POST /repos/objectstack-ai/objectui/pulls, draft forced, run 36007583045 success, PR #10253. Its body was read back byte-identical, 11185 bytes. (2) This os-dev-report comment: POST /repos/objectstack-ai/objectui/issues/10062/comments. Git pushes, which are not REST writes: 2 to claude/issue-10062-dataset-filter-between-arm (the empty-branch probe, then 83eb57f0). No label writes, no body PATCH, no MCP.\",\n  \"deviations\": \"One unlocked, unintended run. A shell command's trailing `npx eslint --no-inline-config --format json` received no file arguments and started a repo-wide lint in the worktree. It ran unlocked for about 2 minutes, was stopped by its own PIDs (387, 399, 400, all with cwd /home/user/objectui-issue-10062, identified before the kill), and never touched the tree; git status was clean afterwards. None of its output is used anywhere.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"class: a · The bridge's READ half accepts a stored scalar or list condition that its WRITE half drops as incomplete, so an edit to ANOTHER row silently removes the untouched condition. Probe, a temporary vitest file run at HEAD 83eb57f0 and then deleted: conditionToGroup({ $and: [{ stage: { $eq: 'won' } }, { name: { $eq: '' } }] }) is representable: true. Editing the stage row to 'lost' and calling groupToCondition gives { stage: { $eq: 'lost' } }, and the name condition is gone. The same happens to { region: { $in: [] } }, where the loss widens the result set from nothing to every 'won' row. Reachable today: FieldOperatorsSchema accepts { $eq: '' } and { $in: [] } (safeParse true), and DatasetSchema.filter takes them, so a Source-tab or AI-authored dataset filter carries them. The inspector commits on every change (DatasetFilterField.commitFilterGroup). It predates objectui#10062; the new `$between` guard deliberately stops at the pair arity. The file is datasetFilterCondition.ts, which will be free once PR #10253 lands · dedupe words: `conditionToGroup representable incomplete stored condition` · `dataset filter edit removes untouched condition` · `datasetFilterCondition read half $in [] $eq empty` · `isFilterValueComplete read half scalar list`\",\n    \"carrier: none (承接者:无) · The JSDoc of DatasetFilterField.commitFilterGroup in packages/app-shell/src/views/metadata-admin/inspectors/DatasetDefaultInspector.tsx still names `between` as its example of an unmapped operator; since objectui#10062 it is the incomplete-row route instead. The file is outside this claim's declared surface, so it was left untouched · noted in PR #10253 Acceptance notes, not filed\",\n    \"carrier: none (承接者:无) · observation: @objectstack/spec 17.4.0 DatasetSchema accepts any operator object under `filter` at parse ({ $between: '2026-01-01' }, a one-member $between, an invented $betweenx all parse true). The comparand door is FieldOperatorsSchema, which the dataset schema does not apply. The runtime's answer to those shapes was NOT measured, so this is not classed as (c) · noted in PR #10253 Acceptance notes, not filed\"\n  ]\n}\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
