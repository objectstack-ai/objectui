{
  "number": 3082,
  "kind": "pull_request",
  "title": "fix(app-shell): a published configSchema can no longer delete a node's sibling-block editors (objectstack#4045)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-31T00:59:07Z",
  "updated_at": "2026-07-31T01:13:05Z",
  "closed_at": "2026-07-31T01:13:03Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-31T01:13:03Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/3082",
  "body": "objectstack#4045 的跨仓库收尾 —— 堵掉 connector_action 事故的**根因**，而不只是那一个实例。\n\n## 问题\n\n`FlowNodeInspector` 这样解析表单字段：\n\n```ts\nconst resolved = serverFields ?? fieldsForNodeType(node?.type);\n```\n\n发布了 `configSchema` 就**整表替换**手写字段组。但一份 `configSchema` 按契约只描述 `node.config`（ADR-0018），而 `jsonSchemaToFlowFields` 产出的每个字段都根在 `['config', key]` —— 所以这个替换会把**根在别处的编辑器全部静默删除**。\n\n清点下来共 **18 个字段**在爆炸半径内：\n\n| 根 | 字段数 | 节点 |\n|---|---:|---|\n| `connectorConfig.*` | 3 | connector_action |\n| `waitEventConfig.*` | 5 | wait |\n| `boundaryConfig.*` | 6 | boundary_event |\n| 顶层 `timeoutMs` | 4 | http_request / script / subflow / connector_action |\n\n对 `wait` 和 `boundary_event` 而言，那个兄弟块**就是它们的全部契约** —— 一旦有人给它们发布 schema，属性面板会整个空掉。\n\n## 这不是假设 —— connector_action 已经这样丢过一次\n\n它的描述符发布了一份把 `connectorId`/`actionId`/`input` 声明成 **config 键**的 schema。连着真实后端时，生成的表单替换掉 `connectorConfig` 那组（连 connector / action 选择器一起），于是在 Studio 里配置 connector 节点的作者把三元组写进了 `node.config` —— 执行器永远不读那里，节点必然以 `connectorConfig.connectorId and .actionId are required` 拒绝执行。\n\nobjectstack#4210 已在服务端撤掉那份 schema。本 PR 是防止**下一份错位的 schema** 对 `wait` 或 `boundary_event` 做同样的事。\n\n## 改法\n\n新增 `mergeServerFlowFields()`，按「根」拆分归属：\n\n- **服务端拥有 config 根的字段** —— 它才是「执行器读什么」的权威，所以它的集合**替换**手写的 config 字段而不是与之合并（陈旧的客户端键不该残留）；\n- **非 config 根的字段始终保留**自手写表，保持声明顺序；\n- 服务端若发来与被保留的兄弟键**重名**的字段，直接丢弃而不是渲染两遍 —— 「两个编辑器编辑同一个值、其中一个写到没人读的地方」是同一个 bug 换张皮。\n\n没有发布 schema 时，手写组照旧整组使用，行为不变。\n\n## 验证\n\n**做了变异验证**，因为「通过」本身不算证据：把实现退回旧的 `return serverFields` 整表替换后，3 条新断言**全部转红**（其中一条是 connector_action 事故的原样复现）；恢复实现后 71 通过。\n\n- 相关三个套件：71 通过\n- 全仓 `pnpm test`：**8677 通过 / 0 失败**（749 文件）\n- `pnpm type-check`：通过\n\n⚠️ 一个过程记录：第一次跑测试时我用了 `pnpm --filter @object-ui/app-shell test -- flow-node-config`，报 107 通过 —— 那是**假绿**。vitest 配置是仓库根作用域的（`include: packages/**`），从包目录跑包相对路径匹配不到任何文件。正确姿势是在仓库根用仓库相对路径。\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01UDhMtxPLLoFaGtdpNA7xTU\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01UDhMtxPLLoFaGtdpNA7xTU)_"
}
