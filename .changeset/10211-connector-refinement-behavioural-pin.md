---
---

Test-only change to the app-shell binding-parity pin for `connector` (objectui#10211): it now asserts the ADR-0097 refinement by behaviour instead of counting `_zod.def.checks`, plus one comment-only sentence in `clientValidation.ts`. No published behaviour changes.
