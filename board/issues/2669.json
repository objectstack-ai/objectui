{
  "number": 2669,
  "kind": "pull_request",
  "title": "feat(studio): filter editor for roll-up summary fields (framework#1868)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-18T16:47:28Z",
  "updated_at": "2026-07-19T06:07:30Z",
  "closed_at": "2026-07-19T06:07:28Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-19T06:07:28Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2669",
  "body": "Studio-side companion to the framework's filtered roll-up summaries (objectstack-ai/framework#1868, framework PR objectstack-ai/framework#3227).\n\n## Context\n\nThe framework adds `summaryOperations.filter` — a `summary` field can now aggregate only the child rows matching a `where` predicate, so one child object feeds several different parent totals (an approved-only sum vs the grand total). Until now the object-designer field inspector could edit a summary's object / function / field / relationship but **not** its filter, so that capability was un-authorable from Studio.\n\n## Change\n\n`SummaryConfigFields` (in `ObjectFieldInspector.tsx`) gains a filter editor under **Rollup Options**:\n\n- A structured **field / operator / value** row editor (mirroring the existing `lookupFilters` editor), reading and writing the spec's `FilterCondition` object via `summaryFilterToRows` / `summaryRowsToFilter`.\n- **Type-correct output**: values are coerced to the child field's stored type, so a `boolean` field emits `{ billable: true }` (not the string `\"true\"`) and a numeric operator emits `{ amount: { $gte: 500 } }` — the FilterCondition then matches the real column.\n- Rows map to/from the flat FilterCondition and a top-level `$and`; a filter using logic the row editor can't represent (`$or` / nested) is shown **read-only with a note** rather than being clobbered on the next edit.\n- New `designer.field.summary.filter*` i18n keys (en + zh-CN).\n\n## Verification\n\n- `type-check` clean (0 errors in the changed files).\n- `ObjectFieldInspector.test.tsx`: 43/43, including **6 new tests** — renders the filter section, reads an existing `{ status: 'approved' }` into a row, adds a defaulted condition, coerces a boolean value, coerces a numeric-operator value, and shows the read-only note for an advanced `$or` filter.\n- **Live browser**: ran this console (`apps/console` dev) against a running showcase backend carrying the framework change; opened the `showcase_expense_report` object → `approved_amount` field, and the editor parsed the seeded `{ status: 'approved' }` into a **Field = Line Status / Operator = \"= equals\" / Value = approved** row.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01HbK4FqcHwp9jSwdhTtxYuC)_"
}
