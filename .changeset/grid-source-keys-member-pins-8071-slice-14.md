---
---

Test-only change: pins the member read behaviour of `object-grid`'s `aggregations`, `dataSource` and `navigation` keys (objectui#8071 slice 14) and moves the three keys from the console parity gate's member-pin exemption list into its pin registry. No published behaviour changes.
