{
  "number": 2724,
  "kind": "pull_request",
  "title": "refactor(managedBy): one parser for the userActions override shape (completes #2712)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: core",
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-20T04:00:47Z",
  "updated_at": "2026-07-20T07:11:52Z",
  "closed_at": "2026-07-20T07:11:50Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-20T07:11:50Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2724",
  "body": "Completes the objectui#2712 de-dup and closes item **#1** of the framework tracking issue [objectstack-ai/framework#3343](https://github.com/objectstack-ai/framework/issues/3343). Follows framework ADR-0103.\n\n## Problem\n\n#2712 consolidated the `managedBy` bucket **union** + affordance **set** mirrors, but deliberately left the small inline `userActions` handlers that still parse the override shape (bare boolean vs. #2614 `{ enabled, visibleWhen, disabledWhen }` object form) **by hand**. There were actually **four** such surfaces — three named in the issue plus `RelatedList`, which had its own copy of `predicatesOf`.\n\n## Approach — one parser in `@object-ui/core`\n\n- **`@object-ui/core`** promotes the internal `normalizeOverride` to the exported **`normalizeUserAction(v, base)`** — THE single parser — and adds **`userActionPredicates(v)`** for per-record CEL predicate extraction. `resolveCrudAffordances` now calls it too.\n- **`app-shell/utils/managedByEmptyState.ts`** — the writable-`system` create check and its local `EmptyStateUserActions` interface → `resolveCrudAffordances({ managedBy, userActions }).create`.\n- **`plugin-grid/rowCrudAffordances.ts`** — local `isOptedOut` / `predicatesOf` (and duplicated `RowCrudUserAction` / `RowCrudPredicates` types) fold into `normalizeUserAction`; the historical type names stay re-exported for import compat.\n- **`plugin-detail/RelatedList.tsx`** — inline `predicatesOf` → `userActionPredicates`.\n- **`plugin-form/ObjectForm.tsx`** — the hand-rolled `managedBy !== 'platform'` blanket lock + `userActions` unlock → the resolved affordance for the current mode (`edit` / `create`), the **same** `resolveCrudAffordances` contract the detail (`isObjectInlineEditable`) and grid surfaces use.\n\nAcceptance (from #3343): *no package re-parses the `userActions` override shape locally.* ✔\n\n## Behavior\n\nBehavior-preserving for `platform` / `system` / `append-only` / `better-auth`, with **one deliberate alignment**: an admin-editable **`config`**-bucket object (e.g. `sys_webhook`, `sys_permission_set`) is now editable in `ObjectForm`. It was previously over-locked as \"non-`platform`\", while detail/grid already treated it as editable (`config` resolves `edit: true`, and the framework declares `config` permits direct mutation). This is exactly the \"match detail/grid exactly\" fix the issue calls for.\n\n## Tests\n\n- `@object-ui/core`: new unit coverage for `normalizeUserAction` + `userActionPredicates`.\n- `plugin-form`: new `ObjectForm.managedEdit` cases pinning the aligned `config` edit gate and the create-mode routing (engine-owned locked vs. `userActions.create` unlocked).\n- All existing affordance / edit-gate tests stay green (`core` + `plugin-grid` + `plugin-form` + `plugin-detail` + `app-shell/utils`: 1767 + 226 pass locally); `type-check` green for all five packages; no new lint errors introduced.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
