{"id":5277220634,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T07:12:56Z","updated_at":"2026-08-13T07:12:56Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4526#issuecomment-5277220634","body":"Triage: `finding` — accepted, documented residue of #4518's own ruling, not a live defect (the client fails conservatively toward an honest lock, nothing regresses). Real divergence exists only on single-kernel hosts, where the server accepts what the client now blocks. Held for a future findings-triage round to grade; closure requires a design call the filer already scoped (plumb `environmentId` through `MetadataClient`/discovery vs. accept the lock as permanent documented behavior for single-kernel hosts) — that call should go to `needs-user-decision` if and when this gets promoted, since #4518's ruling already forbade adding a new server probe for this once.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5292638343,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-14T11:11:38Z","updated_at":"2026-08-14T11:11:38Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4526#issuecomment-5292638343","body":"**Graded: `finding` → `pm:on-hold` (frozen, not a v17 bug)** — theme PM for objectstack#8668 (Seat B), session `session_018x7oGZF6qqdUD1Km4e9n5a`, 2026-08-14.\n\nGraded under the epic's pre-work item 2, on the single question the charter allows: is this a v17 bug? **No.**\n\nReason: this is the *deliberate, disclosed* residue of PR #4525, and it fails in the safe direction. On a single kernel the server disengages its artifact tier, so an env-scope edit of a code-declared permission set would be accepted (200) while the console renders it read-only. The user-visible result is an honest lock, not a Save button that 403s — which is the failure mode #4518 existed to remove. Nothing is silently broken and no data is at risk.\n\nClosing it is not free either: the correct fix needs the kernel's environment topology on the client, and the only surface exposing it is `GET /discovery` — the probe the #4518 ruling explicitly forbade. So this cannot be worked without first reopening that ruling.\n\n**Restart condition (named, per hold discipline):** when the client gains a legitimate read of kernel environment topology — i.e. a ruling that permits the discovery probe, or a `/meta` payload that carries the environment scope — re-grade this card. Triggering files: `packages/app-shell/src/views/metadata-admin/PermissionMatrixEditor.tsx`, and any change to `useMetadataClient` / `MetadataClient` that adds an environment-scope read. A PR touching those should check this card in.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5310338895,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-16T23:57:07Z","updated_at":"2026-08-16T23:57:07Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4526#issuecomment-5310338895","body":"**相邻发现（来自 #4308 / PR #4885 的实施）** —— 评注在此而非另开孪生单，因为落点与本卡是同一个表达式。\n\n本卡记录的是 `artifactTierApplies = !packageId && codeIsArtifact` 中**前半**（env 门条件）在单内核宿主上的过锁残留。实施 #4308 时发现**后半** `codeIsArtifact` 本身也带一处独立的陈旧读法：\n\n`PermissionMatrixEditor.tsx:133-137`\n\n```ts\nfunction isArtifactBackedLayer(layered: { code?: unknown } | null | undefined): boolean {\n  const code = layered?.code;\n  if (code == null) return false;\n  return (code as { _packageId?: string })._packageId !== 'sys_metadata';\n}\n```\n\n它的 doc 自陈与 `ResourceEditPage` 的内联判定「byte-for-byte 相同」—— 而那份判定正是 #4308 的病灶：`_packageId !== 'sys_metadata'` **单独回答不了**「是不是代码包 artifact」。哨兵只在保存路径上成立；`sys_metadata` 的启动期回灌会按**真实包 id** 重新注册每一行，于是租户自有条目读回来长得像代码 artifact。框架侧同一个坑已由 cloud#970 修过，办法是改问 ADR-0010 `provenance`（`isTenantAuthored`，`objectql/src/registry.ts:697-711`，注释明写「`_packageId !== 'sys_metadata'` alone cannot answer it … Provenance is the axis that actually distinguishes the two, so ask it」）。服务端已把 `provenance` 挂在 layered 信封上（`MetadataLayered.provenance`），无需新增探测 —— #4518 裁定禁止的那类新探测在这里不需要。\n\n**为什么不作为 #4308 的一部分修**：包门下（`packageId` 存在）`artifactTierApplies` 为 false，`codeIsArtifact` 根本不参与，所以权限矩阵**不带** #4308 的可写包缺陷（那一半已由 #4446 / PR #4519 解决）。只有 env 门（无 `packageId`）才读到它 —— 也就是本卡的作用域。\n\n**影响面（未实测，故按 observation 报）**：env 门下编辑一个**租户自有**权限集时，会被误判进 artifact 档；A2（objectstack#6483）收紧后 `permission` 不再 `allowOrgOverride`，于是 `canWriteByType` 为 false，矩阵整屏只读。可达性取决于 metadata-admin 列表链接是否总带 `?package=`（ADR-0048 注释说会带），因此我没有确认线上真有人踩到 —— 严重度请按本卡既有的 on-hold 判断一并复核，不另行加权。\n\n若本卡后续动这个表达式，建议把两半一起归一：env 门条件（本卡）+ `codeIsArtifact` 改问 provenance（本条）。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5384179677,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-23T04:20:05Z","updated_at":"2026-08-23T04:20:05Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4526#issuecomment-5384179677","body":"Triage: **`domain:ui`** (lane only — `pm:on-hold` untouched). Landing: the `PermissionMatrixEditor` artifact-tier gate — application surface.\n\nRouted via the maintainer direct-dispatch channel, this session, verbatim: 「然后 批 4–5」. PM session `session_0124Qg8rLvpXnQDwCmpKUmaJ`. objectui three-stream split (maintainer 2026-08-21); not a Routine triage fire — the triage seat may re-grade.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5814482093,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T12:52:14Z","updated_at":"2026-09-24T12:52:14Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4526#issuecomment-5814482093","body":"## 维护者裁定:单内核下的\"过度锁定\"作为已知行为接受;本卡回到队列,`priority:p3`,只做\"按来源判断\"那一半\n\n分诊席(`session_01Tw7jnJinGHvoGSi8aFkhPJ`,objectstack 座位贴 #6015),2026-09-24T12:51Z。objectui 清理第八批(#8587 的挂起卡一次性清点):本席读完了卡面和评论(4 条,全部读到),并在本地 main(`62597c5`)上重新测量,把两个选项报给维护者。**维护者答:「第⑧批 同意」**,即采纳本席的建议。\n\n### 本卡说的是什么(两件事)\n\n1. **单内核下的过度锁定**(#4518 / PR #4525 留下的尾巴):在只有一个内核的部署上,服务端不启用制品保护(`environmentId` 为空),允许在环境层修改代码里声明的权限集;而编辑器仍把它显示为只读。这是偏保守的锁定,不会放过任何修改。要精确消除它,客户端必须知道内核的环境拓扑,也就是 #4518 的裁定明确禁止的那种探测。\n2. **按来源判断的缺口**(yinlianghui `5310338895`):`isArtifactBackedLayer` 判断的是 `_packageId !== 'sys_metadata'`,而启动时的数据回填会让这个判断失效。于是环境层上一个**租户自己的**权限集可能被当成制品,整个权限矩阵变成只读。这一点还没有实测;能不能触发,取决于列表页的链接是否总带着 `?package=`。\n\n### 本席实测(本地 main `62597c5`)\n\n- `PermissionMatrixEditor.tsx:150-154` 仍然是 `_packageId !== 'sys_metadata'`。它的文档注释 `:135-148` 写着\"#4526 被接手时,这里改用 provenance\"。\n- 同类页面 `ResourceEditPage` 已经改用 ADR-0010 的来源字段判断(`:1299`)。\n\n### 裁定\n\n- **第 1 件:作为已知行为接受,不再跟进。** 精确修复需要 #4518 禁止的探测,而现状只是锁得偏严,不会放过修改。重启条件 `5292638343`(有裁定允许做环境探测,或 `/meta` 开始带环境范围信息)仍然有效;任何一条发生时,另开新卡处理。\n- **第 2 件:回到队列,`priority:p3`。** 修法不需要任何新探测:和 `ResourceEditPage` 对齐,改问 `provenance !== 'org'`。\n\n### 执行要点\n\n- 只改 `isArtifactBackedLayer` 的判断,改成按 ADR-0010 的 `provenance` 判断,和 `ResourceEditPage.tsx:1299` 用同一个来源;同时更新 `:135-148` 的文档注释,说明第 1 件是已接受的行为。\n- 加一组正反对照测试:租户自己的权限集(`provenance: 'org'`)在环境层可编辑;代码声明的权限集仍然只读。\n- 如果顺手能测出第 2 件在真实部署上能否触发(列表链接是否总带 `?package=`),写进 PR 描述;测不出也不影响这次修改。\n\n### 定级\n\n`priority:p3`:第 2 件还没有实测到用户受影响,但一旦触发,租户会没法编辑自己的权限集;修法小,而且方向已经由同类页面验证过。摘 `pm:on-hold`,加 `pm:queue`。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
