{"id":5337054263,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-19T03:14:25Z","updated_at":"2026-08-19T03:14:25Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5116#issuecomment-5337054263","body":"Triage (first-touch, triage seat, session `session_01HXzdkKx5WjwwCTT3c7U5kB`): promoted `finding` → `pm:queue`, type Task — consumer-side tolerance of an undeclared spelling (AGENTS.md #0.1), unreachable through the contract per the card's measurement, plus a user-facing warn message that legitimizes the non-contract key.\n\nScope: delete the two `?.resource` limbs in `ReportView.tsx` (:171, :273) and reword the `:275` warn to mention only `object`. Premise-first: re-verify on current main that no producer writes `dataSource.resource` (the card measured none; `resource` is a real key only on `CRUDSchema`, a different surface) — if one is found, stop and report instead of deleting, since that would upgrade this to a two-renderers-disagree defect needing re-triage.\n\nSize/model suggestion: S, `mode:subagent`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5364510309,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T02:37:15Z","updated_at":"2026-08-21T02:37:15Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5116#issuecomment-5364510309","body":"Claim: PM loop round 21\nSession: `session_01RV6yuVCxymHYE16PL9vQkE`\nBranch: `claude/issue-5116-reportview-resource-alias`\nWorktree: `objectui-issue-5116`\nDomain: `repo:objectui`\nFile surface: `packages/app-shell/src/views/ReportView.tsx` (:171, :273, :275) + its tests (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`\nClause-②: **yes** — deletes consumer-side tolerance of an undeclared key, narrowing what the renderer accepts. Same family and same direction as #5120/#5350/#5351, all landed this round.\nSerial constraints cleared: `packages/app-shell/src/views/**` is free — PR #5480 (#5418) is MERGED. ⚠️ #5203 is in flight in the SAME package but in `layout/` and `hooks/` only — ⛔ do not touch either.\n\n⛔ Governed-surface check run before dispatch: the declared surface hits none of `AGENTS.md` / `CLAUDE.md` / `.claude/**` / `docs/adr/**` / `skills/**`. If the work turns out to need one, **stop and report** — published `skills/**` ownership is an open question with the maintainer and no seat may land it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5364775149,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-21T03:18:11Z","updated_at":"2026-08-21T03:18:11Z","html_url":"https://github.com/objectstack-ai/objectui/issues/5116#issuecomment-5364775149","body":"os-dev-report\n\n```json\n{\n  \"issue\": 5116,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-5116-reportview-resource-alias\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/5488\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Deleted both consumer-side `?.resource` limbs in ReportView.tsx (:171 availableFields, :273 fetch) and reworded the :275 warning to name only `object`. Premise re-verified on current main before deleting, as triage required: the producer census found ZERO sites writing `resource` onto a report `dataSource` in objectui or in the objectstack framework repo, with a counter-probe (same multiline regex, `object` instead) returning 182 + 15 hits to prove the zero is a reading and not a broken search; authored corpora (docs, skills/**, examples, fixtures, all json/yaml/md/mdx) were swept too and every authored `resource:` key resolves to a different surface (CRUDSchema.resource, the DataSource adapter's first parameter, LiveExportOptions.resource, useViewData options, evaluatePermission) — each inspected individually. `git log -S` traced the limb to the commit that first added data loading to this view: it was speculative in that very commit, no producer preceded it. Also confirmed the spec twin ElementDataSourceSchema is a STRICT object, so `resource` is rejected there rather than ignored — the tolerance was unreachable through the contract, exactly as the card measured. One in-hunk rename: the local `resource` variable at the fetch site became `objectName` (last in-code echo of the non-contract spelling on the line being fixed); named in the PR body. Scope held to 3 files; layout/ and hooks/ (#5203) untouched; no governed surface touched.\",\n  \"tests\": \"Per-shape behaviour measured BY RENDERING, before and after, via a throwaway probe that reports rather than asserts (deleted after use). object-only: queries `acct`, 1 row, fields `industry` -> unchanged. resource-only: queries `acct`, 1 row, fields `industry`, no warning -> NOT queried, 0 rows, fallback fields, warns 'ReportView: dataSource missing object property' (this is the behaviour removed; it now fails loudly instead of silently rendering off-spec metadata). object+resource: queries `acct` -> unchanged (`resource` was already inert). neither: not queried + warns 'missing object/resource property' -> not queried + warns 'missing object property'. New pin packages/app-shell/src/views/ReportView.dataSourceObjectKey.test.tsx (5 tests) covers all four shapes plus a warn-wording assertion; the resource-only cases put a REAL object with real fields behind the spelling, so 'not queried' proves it was not read rather than that nothing was there. REVERSE-VERIFICATION, both legs run from the COMMITTED state with direction predicted first: leg 1 (restore origin/main ReportView.tsx) predicted RED 3 failed/2 passed on `resource only` + `neither` + `warning names object` — observed EXACTLY that (expected 'acct' to be null / missing object/resource property / expected '' to contain 'object'). Leg 2 (restore ONLY the two || limbs, keep the reworded warning) predicted RED 2 failed/3 passed with `neither` now PASSING — observed EXACTLY that, which separates the two halves of the fix and shows `neither` is the assertion pinning the :275 reword. ABLATION REBUILD: no build was needed or done, and that is a property of the resolution path, not an omission — the test imports the component through a RELATIVE SOURCE specifier (./ReportView) and the root vitest alias table maps every @object-ui/* specifier to src/, so no dist/ is in the path; demonstrated rather than assumed, since the source-only edit moved the suite red and back green with no rebuild between. Restored and proven clean by BLOB HASH (worktree 13466eff4fc346c70267b5c2f44a3a9c910062b1 == committed blob), git status empty. GATE UNION RUN AFTER THE FINAL COMMIT, AT d8a61a5ce: vitest packages/app-shell/src/views/ = 285 files / 2817 passed / 1 skipped (pre-existing spec-version skipIf in flow-node-config.spec-reconciliation.test.ts, untouched, not mine); vitest packages/app-shell/src/console/ + src/__tests__/ = 59 files / 500 passed (the route + barrel consuming ReportView); new pin 5/5; `pnpm --filter @object-ui/app-shell type-check` clean with the script name echoed in the output (3 real type errors in the new test were found and FIXED, not suppressed — an untyped vi.fn gave mock.calls an empty tuple type; the spy now declares the real DataSource.find signature); eslint on both changed files 0 errors, and ReportView.tsx warning count measured 24 before / 24 after against an origin/main copy, so zero introduced; check:control-bytes, check:esm-specifiers, check:self-import, check:phantom-deps, check:doc-types all pass; check:doc-snippets passes after building the packages it resolves against, with its own sentinel/positive controls green (a real reading, not a skipped gate); check:eager-closure RATCHET passes at 3792.5 KB gzipped vs 3867.2 KB budget, 74.7 KB headroom, which required building apps/console and its closure. check-changeset-presence and check-changeset-no-major pass. Changeset added as `minor` (never `major` per AGENTS.md — it would drag the 39-package fixed group off the @objectstack major). No test skipped, disabled, or quarantined. Remote CI was still converging at report time (mergeable_state unstable), which is PM's leg per the dispatch contract.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
