{
  "number": 2615,
  "kind": "pull_request",
  "title": "fix(detail): inline-edit enrichment passes min/max/step; live e2e for the #2572 polish set",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "plugin",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-16T14:35:12Z",
  "updated_at": "2026-07-16T14:53:51Z",
  "closed_at": "2026-07-16T14:53:49Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-16T14:53:48Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2615",
  "body": "Live-dogfood follow-up to #2572 / PR #2604, found by driving the merged polish on the real showcase stack (console dev `:5191` + `framework/examples/app-showcase`).\n\n## Fix: numeric constraints were dropped by the field enrichment\n\n`DetailSection` and `HeaderHighlight` enrich the field they hand `InlineFieldInput` by copying an explicit whitelist of objectSchema keys (type, options, currency, precision, scale, format, reference…). `min`/`max`/`step` were not in that list — so although #2604 taught `NumberField`/`CurrencyField` to surface them, the showcase Project.budget (`currency`, `min: 0`) still rendered its number input with no range constraints, because the metadata never reached the widget. Both enrichments now pass `min`/`max`/`step` through (unit tests added; the DetailSection one pins `min`/`max`/`step` on the rendered spinbutton).\n\n## New: live e2e spec for the whole #2572 polish set\n\n`e2e/live/inline-edit-polish-2572.spec.ts` drives all five items end-to-end against the real stack and is green:\n\n1. expanded-lookup passthrough — the Account chip shows \"Northwind\" with **zero** hydration `findOne` fetches;\n2. approval-lock preflight — saving budget 150k→160k fires `showcase_budget_approval` (verified `pending` via the approvals API), and on reload every pencil/double-click affordance is gone and double-click no longer opens a session (note: this backend does **not** materialize `approval_status` on the record, so the lock engages via the pending-request branch of `approvalLocked` — with the field-only signal it would never lock);\n3. currency edits in a real `input[type=number][min=0][step=0.01]`;\n4. header Edit CTA disabled while the session is live;\n5. Esc cancels / Ctrl+Enter commits ONE atomic PATCH carrying only the edited key.\n\nThe run also flushed out a framework bug: the default CORS allow-list omitted `If-Match`, so every split-origin inline-edit save failed preflight with \"Failed to fetch\" — fixed separately in framework PR objectstack-ai/framework#3067 (the spec was verified green against that fixed stack).\n\nKnown cosmetic gap (pre-existing, not addressed here): `DetailView`'s \"Locked for approval\" band keys off `record.approval_status` only, so on backends that track the lock purely via approval requests the band never shows even though editing is correctly locked.\n\n## Verification\n\n- `plugin-detail` suites green locally (DetailSection.inlineEdit + HeaderHighlight.editable: 10/10).\n- Live spec green end-to-end (18.6s) against showcase backend `:4022` (with the framework CORS fix) + console dev `:5191`.\n- Live specs don't run in CI (they need a live backend), so this adds no CI cost.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01JQjNZDdqmz2QHGQndpjZrR\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01JQjNZDdqmz2QHGQndpjZrR)_"
}
