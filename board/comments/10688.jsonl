{"id":5837158074,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-25T18:06:13Z","updated_at":"2026-09-25T18:06:13Z","html_url":"https://github.com/objectstack-ai/objectui/pull/10688#issuecomment-5837158074","body":"changeset-claim-re-read\n\n## ⚠️ 11 pending changeset(s) describe a file this change touches\n\nTheir bodies publish **verbatim** into the CHANGELOG at the next release, so this is a request to **re-read them against your diff** — addressed here because you are the one seat that can answer it without re-deriving anything.\n\n⛔ Nothing here blocks, and nothing here is a verdict on your change. This gate exits 0, is not a required context, and judges **name resolution, never meaning**: it asked whether a pending body names a file you touched. \"Is this sentence still true?\" is the one question it will not answer, and the one you are being asked to answer.\n\n### `.changeset/5876-one-lookup-predicate.md`\n\n- names `ObjectDataTable.tsx` → `packages/plugin-dashboard/src/ObjectDataTable.tsx` — edited by this change\n\n  > `computeLookupExpand` in `ObjectDataTable.tsx` carried its own `isLookup`, byte-identical to the exported `isLookupType` in `recordFields.tsx` after objectui#5692 pointed both at `@object-ui/core`'s `EXPANDABLE_FIELD_TYPES`. Nothing kept them aligned: a future edit to either — a member added, the retirement gate moved — would have re-forked the `$expand` decision from the predicate whose docblock claims to drive it. `computeLookupExpand` now calls `isLookupType`, which gains its first production consumer, and the module no longer imports the shared family or the retirement gate at all.\n\n### `.changeset/6726-find-envelope-records-arms.md`\n\n- names `components/src/renderers/basic/data-list.tsx` → `packages/components/src/renderers/basic/data-list.tsx` — edited by this change\n\n  > | module | what it does | | --- | --- | | `components/src/hooks/related-count-store.ts` | related-list tab badge count | | `components/src/renderers/basic/data-list.tsx` | `element:repeater` rows | | `components/src/renderers/basic/elements.tsx` | `element:number` client-side aggregate | | `components/src/renderers/basic/record-picker.tsx` | `element:record_picker` options | | `plugin-detail/src/renderers/record-activity.tsx` | `record:activity` self-fetch | | `plugin-detail/src/renderers/record-history.tsx` | `record:history` self-fetch | | `plugin-view/src/ObjectView.tsx` | non-grid (kanban / calendar / gallery / timeline) fetch |\n\n- names `components/src/renderers/basic/record-picker.tsx` → `packages/components/src/renderers/basic/record-picker.tsx` — edited by this change\n\n  > | module | what it does | | --- | --- | | `components/src/hooks/related-count-store.ts` | related-list tab badge count | | `components/src/renderers/basic/data-list.tsx` | `element:repeater` rows | | `components/src/renderers/basic/elements.tsx` | `element:number` client-side aggregate | | `components/src/renderers/basic/record-picker.tsx` | `element:record_picker` options | | `plugin-detail/src/renderers/record-activity.tsx` | `record:activity` self-fetch | | `plugin-detail/src/renderers/record-history.tsx` | `record:history` self-fetch | | `plugin-view/src/ObjectView.tsx` | non-grid (kanban / calendar / gallery / timeline) fetch |\n\n- names `plugin-view/src/ObjectView.tsx` → `packages/plugin-view/src/ObjectView.tsx` — edited by this change\n\n  > | module | what it does | | --- | --- | | `components/src/hooks/related-count-store.ts` | related-list tab badge count | | `components/src/renderers/basic/data-list.tsx` | `element:repeater` rows | | `components/src/renderers/basic/elements.tsx` | `element:number` client-side aggregate | | `components/src/renderers/basic/record-picker.tsx` | `element:record_picker` options | | `plugin-detail/src/renderers/record-activity.tsx` | `record:activity` self-fetch | | `plugin-detail/src/renderers/record-history.tsx` | `record:history` self-fetch | | `plugin-view/src/ObjectView.tsx` | non-grid (kanban / calendar / gallery / timeline) fetch |\n\n### `.changeset/6783-readprops-degenerate-config-bag.md`\n\n- names `data-list.tsx` → `packages/components/src/renderers/basic/data-list.tsx` — edited by this change\n\n  > Five modules under `packages/components/src/renderers/basic/` — `elements.tsx`, `data-list.tsx`, `text-input.tsx`, `record-picker.tsx`, `metadata-viewer.tsx` — each carried a copy of the same reader, `{ ...(schema?.props ?? {}), ...(schema?.properties ?? {}) }`. `??` only replaces `null`/`undefined`, so a non-object bag went into the object spread and came back out as indexed keys: for `properties: 'not-a-bag'`, the config bag a renderer received was `{ '0': 'n', '1': 'o', … '8': 'g' }` — nine keys nobody authored. The five copies are now one `readProps` (`renderers/basic/readProps.ts`) that asks `isConfigBag`, and a degenerate bag on either side contributes no keys.\n\n- names `record-picker.tsx` → `packages/components/src/renderers/basic/record-picker.tsx` — edited by this change\n\n  > Five modules under `packages/components/src/renderers/basic/` — `elements.tsx`, `data-list.tsx`, `text-input.tsx`, `record-picker.tsx`, `metadata-viewer.tsx` — each carried a copy of the same reader, `{ ...(schema?.props ?? {}), ...(schema?.properties ?? {}) }`. `??` only replaces `null`/`undefined`, so a non-object bag went into the object spread and came back out as indexed keys: for `properties: 'not-a-bag'`, the config bag a renderer received was `{ '0': 'n', '1': 'o', … '8': 'g' }` — nine keys nobody authored. The five copies are now one `readProps` (`renderers/basic/readProps.ts`) that asks `isConfigBag`, and a degenerate bag on either side contributes no keys.\n\n### `.changeset/6939-objectql-record-source-refinement.md`\n\n- names `plugin-map/src/ObjectMap.tsx` → `packages/plugin-map/src/ObjectMap.tsx` — edited by this change\n\n  > Both renderers resolve their records from one of three keys, in this order — `getDataConfig` in `plugin-map/src/ObjectMap.tsx` and `plugin-gantt/src/ObjectGantt.tsx`: `data`, then `staticData`, then `objectName`. Both mirrors required `objectName` alone, so a document authored on `staticData` drew correctly and was refused by `safeValidateSchema` — six catalog entries, three per component.\n\n### `.changeset/7070-no-invented-gantt-date-fields.md`\n\n- names `plugin-view/src/ObjectView.tsx` → `packages/plugin-view/src/ObjectView.tsx` — edited by this change\n\n  > - `app-shell/src/views/ObjectView.tsx` — the console object page. The inline branch becomes `ganttViewOptions`, the sibling of `calendarViewOptions` and `timelineViewOptions`: the declared block spread whole, title floored at `'name'`, no date field invented. - `plugin-list/src/ListView.tsx` — the render branch AND the capability gate. - `plugin-view/src/ObjectView.tsx` — `generateViewSchema`, the authored `object-view` element route, which bypasses `ListView` entirely.\n\n### `.changeset/7125-dashboard-empty-state-keys-retired.md`\n\n- names `ObjectDataTable.tsx` → `packages/plugin-dashboard/src/ObjectDataTable.tsx` — edited by this change\n\n  > Not touched: `table.noRows` (`'No rows to display'`) and `engine.form.noRows` (`packages/app-shell/src/views/metadata-admin/i18n.ts`, read at `widgets.tsx`) — two different, same-named keys in different namespaces. Nor the comments in `WidgetEmptyState.tsx`, `DatasetWidget.tsx`, `ObjectDataTable.tsx` and `PivotTable.tsx` that record WHY three widgets with three strings became one shared empty state; the packs' own comment keeps that rationale and now names the retirement instead of a row that is gone.\n\n### `.changeset/7499-gantt-non-axis-floors-omitted.md`\n\n- names `plugin-view/src/ObjectView.tsx` → `packages/plugin-view/src/ObjectView.tsx` — edited by this change\n\n  > - `plugin-list/src/ListView.tsx` — the `object-gantt` render branch. - `plugin-view/src/ObjectView.tsx` — `generateViewSchema`, the authored `object-view` element route, which bypasses `ListView` entirely.\n\n### `.changeset/8653-listview-title-retired-rowactiondefs-pinned.md`\n\n- names `packages/plugin-view/src/ObjectView.tsx` → `packages/plugin-view/src/ObjectView.tsx` — edited by this change\n\n  > `title` — **retired.** `ListView` resolved its export filename through `schema.label || (schema as any).title`. `@objectstack/spec/ui`'s `ListViewSchema` refuses `title` **by name** (`unrecognized_keys: ['title']`) while `ObjectGridPropsSchema` **accepts** it; `packages/types` mirrors the platform contract rather than ruling over it, so declaring `title` on `ListViewSchema` would have made this repo accept what the platform save gate rejects. That asymmetry is also why objectui#6639 could take the *declare* branch for `ObjectGridSchema.title` one package over and this site could not. A parse-based census of `apps/ examples/ content/` and `packages/` found **zero** `list-view` nodes authoring `title`, so the retirement costs no author a filename. Over that same corpus the instrument reports **three** `object-grid` nodes carrying the key: **two authored** ones, both in `content/docs/api/schema-reference.md`, plus one that is not authored at all — `packages/plugin-view/src/ObjectView.tsx` composes `title: schema.table?.title` onto a grid node it builds, so it is a producer writing the key rather than an author declaring it. `ObjectGrid`'s own `title` reads are untouched — they remain declared, ruled and read.\n\n### `.changeset/9618-record-source-ladder-attribution.md`\n\n- names `ObjectMap.tsx` → `packages/plugin-map/src/ObjectMap.tsx` — edited by this change\n\n  > Their zod `.describe` strings and TS docs said the `data` → `staticData` → `objectName` ladder is resolved by `getDataConfig`. Neither renderer has had a function by that name since the ladder moved into `@object-ui/core`'s shared `resolveRecordSourceConfig` (objectui#7632), so the text now names that function. The ladder order is unchanged, and so is every accepted document. The `ObjectMapSchema` text still says `getDataConfig`, which is true: `ObjectMap.tsx` keeps a local wrapper by that name that delegates to the shared ladder. The `resolveRecordSourceConfig` docblock in `@object-ui/core` now says that the text it quotes is the map faces' text.\n\n### `.changeset/dashboard-expandable-family-convergence-5692.md`\n\n- names `ObjectDataTable.tsx` → `packages/plugin-dashboard/src/ObjectDataTable.tsx` — edited by this change\n\n  > `plugin-dashboard`'s two private copies of the reference-bearing field family now read `@object-ui/core`'s published `EXPANDABLE_FIELD_TYPES` instead of restating it — `LOOKUP_TYPES` in `recordFields.tsx` and the inline disjunction inside `computeLookupExpand` in `ObjectDataTable.tsx`. Both carry the identity pin the already-converged consumers carry, so a member-identical private copy fails rather than quietly re-forking the table.\n\n### `.changeset/object-view-unmirrored-keys-7779.md`\n\n- names `packages/plugin-view/src/ObjectView.tsx` → `packages/plugin-view/src/ObjectView.tsx` — edited by this change\n\n  > **What was measured.** Every reading was taken on the `object-view` node renderer (`packages/plugin-view/src/ObjectView.tsx`, registered by `plugin-view/src/index.tsx`) with `schema.objectName` / `schema.layout` as the positive controls of the same `schema.KEY` query, so each zero is a reading; the repo-wide census of `viewTabBar` finds the key in no source file outside `@object-ui/types` (two doc tables listed it as authorable and are corrected here). The spec side was read through the installed pin (`@objectstack/spec@17.2.0`, `ui` entry, 117 exported object schemas walked; control keys `objectName` / `columns` / `navigation` / `listViews` hit): the three spec-modelled keys are optional slots on `ListViewSchema` and `ObjectListViewSchema`; the six local keys have no spec slot anywhere.\n\nRead the **paragraph**, not the line: both false halves of the objectui#8617 claim sat in one paragraph, and correcting either alone would have left it asserting the same wrong thing.\n\nIf a claim did go false, **correct the body**. That is precedented and prose-only, frontmatter untouched; `check-changeset-overwrite.mjs` will report the correction as its own case 2 (\"correcting a declaration on purpose … legitimate\"), which is the intended shape — one gate asks for the read, the other records the write.\n\nNot covered, stated so nobody reads this as more: a born-false claim that spells no line address at all (objectui#9495 coordinated one by ORDINAL — \"a grep finds that member first\" — and deciding that means reading what the sentence means), a claim spelled as a symbol or a package rather than a backticked file name, and a file named ambiguously.\n\nCompared the checked-out tree with `4df0f3d9d` (merge-base with origin/main): 14 file(s) changed outside `.changeset/`, read against 1525 pending declaration(s) that publish a body (2111 pending in total). · [run](https://github.com/objectstack-ai/objectui/actions/runs/36171256380)\n"}
{"id":5837205784,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-25T18:09:36Z","updated_at":"2026-09-25T18:09:36Z","html_url":"https://github.com/objectstack-ai/objectui/pull/10688#issuecomment-5837205784","body":"## ✅ Console Performance Budget\n\n| Metric | Value | Budget |\n|--------|-------|--------|\n| **Eager closure** (gzip, 329 chunks) | **3060.3 KB** | 3104.5 KB |\n| Main entry chunk (gzip) | 148.1 KB | 350 KB |\n| Entry file | `index-B9i6Z3Ix.js` | — |\n| Status | **PASS** | — |\n\nThe **eager closure** is every chunk the entry reaches through static imports — what the browser fetches and parses before the app renders. The entry chunk on its own is a small fraction of it.\n\n---\n\n## 📦 Bundle Size Report\n\n| Package | Size | Gzipped |\n|---------|------|---------|\n| app-shell (consoleActionDispatch.js) | 0.20KB | 0.19KB |\n| app-shell (index.js) | 16.57KB | 6.15KB |\n| app-shell (runtime-config.js) | 20.68KB | 7.36KB |\n| app-shell (types.js) | 0.01KB | 0.04KB |\n| app-shell (urlParams.js) | 10.06KB | 3.86KB |\n| auth (ActiveOrganizationStorage.js) | 27.95KB | 10.04KB |\n| auth (AuthContext.js) | 0.31KB | 0.24KB |\n| auth (AuthGuard.js) | 2.07KB | 1.00KB |\n| auth (AuthProvider.js) | 40.17KB | 10.58KB |\n| auth (AuthShell.js) | 3.49KB | 1.40KB |\n| auth (ForgotPasswordForm.js) | 12.21KB | 3.45KB |\n| auth (LoginForm.js) | 18.15KB | 5.39KB |\n| auth (PreviewBanner.js) | 0.90KB | 0.50KB |\n| auth (RegisterForm.js) | 6.65KB | 2.22KB |\n| auth (SocialSignInButtons.js) | 9.61KB | 3.89KB |\n| auth (UserMenu.js) | 3.39KB | 1.21KB |\n| auth (auth-gate-events.js) | 1.29KB | 0.66KB |\n| auth (authStyles.js) | 5.04KB | 1.72KB |\n| auth (createAuthClient.js) | 40.21KB | 10.80KB |\n| auth (createAuthenticatedFetch.js) | 8.52KB | 3.45KB |\n| auth (index.js) | 3.63KB | 1.64KB |\n| auth (invitation-status.js) | 1.22KB | 0.70KB |\n| auth (org-roles.js) | 6.66KB | 2.78KB |\n| auth (phone-identifier.js) | 1.11KB | 0.66KB |\n| auth (types.js) | 0.59KB | 0.35KB |\n| auth (useAuth.js) | 5.30KB | 1.02KB |\n| auth (useWorkspaceAdminStatus.js) | 11.08KB | 4.58KB |\n| collaboration (CommentThread.js) | 27.13KB | 7.95KB |\n| collaboration (LiveCursors.js) | 3.17KB | 1.27KB |\n| collaboration (PresenceAvatars.js) | 6.49KB | 2.64KB |\n| collaboration (PresenceProvider.js) | 2.79KB | 1.13KB |\n| collaboration (index.js) | 1.68KB | 0.73KB |\n| collaboration (useCollaborationTranslation.js) | 6.05KB | 2.52KB |\n| collaboration (useCommentSearch.js) | 1.98KB | 0.88KB |\n| collaboration (useConflictResolution.js) | 7.75KB | 1.86KB |\n| collaboration (useMentionNotifications.js) | 1.81KB | 0.68KB |\n| collaboration (usePresence.js) | 6.33KB | 1.84KB |\n| collaboration (useRealtimeSubscription.js) | 7.91KB | 2.01KB |\n| components (index.js) | 547.33KB | 130.98KB |\n| core (index.js) | 9.52KB | 3.79KB |\n| create-plugin (index.js) | 27.94KB | 9.51KB |\n| data-objectstack (index.js) | 223.91KB | 62.28KB |\n| fields (index.js) | 259.18KB | 65.61KB |\n| i18n (LocalizationContext.js) | 1.76KB | 0.96KB |\n| i18n (builtinAggregateLabels.js) | 0.86KB | 0.49KB |\n| i18n (currency.js) | 2.59KB | 1.22KB |\n| i18n (fallbackInterpolation.js) | 6.25KB | 2.77KB |\n| i18n (i18n.js) | 8.87KB | 3.64KB |\n| i18n (index.js) | 5.24KB | 2.27KB |\n| i18n (pickLocalized.js) | 9.86KB | 3.95KB |\n| i18n (provider.js) | 39.40KB | 12.91KB |\n| i18n (useDisplayLocale.js) | 3.52KB | 1.76KB |\n| i18n (useObjectLabel.js) | 34.34KB | 9.17KB |\n| i18n (useSafeTranslation.js) | 5.60KB | 2.33KB |\n| layout (index.js) | 39.28KB | 11.09KB |\n| mobile (MobileProvider.js) | 0.92KB | 0.49KB |\n| mobile (ResponsiveContainer.js) | 0.94KB | 0.38KB |\n| mobile (breakpoints.js) | 1.51KB | 0.70KB |\n| mobile (createOfflineDataSource.js) | 5.61KB | 1.75KB |\n| mobile (index.js) | 1.99KB | 0.87KB |\n| mobile (offlineQueue.js) | 3.91KB | 1.35KB |\n| mobile (pwa.js) | 0.97KB | 0.49KB |\n| mobile (serviceWorker.js) | 1.48KB | 0.62KB |\n| mobile (serviceWorkerSource.js) | 3.41KB | 1.48KB |\n| mobile (useBreakpoint.js) | 1.54KB | 0.65KB |\n| mobile (useGesture.js) | 6.96KB | 1.98KB |\n| mobile (useOfflineSync.js) | 1.99KB | 0.72KB |\n| mobile (usePullToRefresh.js) | 6.62KB | 2.45KB |\n| mobile (useResponsive.js) | 0.72KB | 0.42KB |\n| mobile (useSpecGesture.js) | 5.52KB | 2.10KB |\n| mobile (useTouchTarget.js) | 1.01KB | 0.54KB |\n| permissions (MePermissionsProvider.js) | 13.52KB | 4.88KB |\n| permissions (PermissionContext.js) | 0.31KB | 0.25KB |\n| permissions (PermissionGuard.js) | 0.89KB | 0.45KB |\n| permissions (PermissionProvider.js) | 6.24KB | 2.16KB |\n| permissions (discardProofCache.js) | 1.04KB | 0.55KB |\n| permissions (evaluator.js) | 8.33KB | 3.07KB |\n| permissions (index.js) | 0.93KB | 0.41KB |\n| permissions (store.js) | 0.91KB | 0.42KB |\n| permissions (useFieldPermissions.js) | 1.28KB | 0.53KB |\n| permissions (usePermissions.js) | 4.83KB | 2.27KB |\n| plugin-ai (index.js) | 16.01KB | 3.93KB |\n| plugin-calendar (index.js) | 51.52KB | 14.64KB |\n| plugin-charts (index.js) | 80.42KB | 22.22KB |\n| plugin-chatbot (index.js) | 198.36KB | 47.20KB |\n| plugin-dashboard (index.js) | 133.85KB | 35.54KB |\n| plugin-designer (index.js) | 216.25KB | 44.39KB |\n| plugin-detail (index.js) | 232.85KB | 61.58KB |\n| plugin-editor (index.js) | 2.23KB | 1.05KB |\n| plugin-form (index.js) | 152.55KB | 39.16KB |\n| plugin-gantt (index.js) | 169.71KB | 41.93KB |\n| plugin-grid (index.js) | 216.70KB | 59.29KB |\n| plugin-kanban (index.js) | 48.36KB | 15.09KB |\n| plugin-list (index.js) | 115.82KB | 28.76KB |\n| plugin-map (index.js) | 22.85KB | 7.58KB |\n| plugin-markdown (index.js) | 13.88KB | 4.80KB |\n| plugin-report (index.js) | 43.55KB | 11.99KB |\n| plugin-timeline (index.js) | 30.84KB | 9.01KB |\n| plugin-tree (index.js) | 10.52KB | 3.69KB |\n| plugin-view (index.js) | 87.83KB | 22.01KB |\n| providers (DataSourceProvider.js) | 0.75KB | 0.39KB |\n| providers (MetadataProvider.js) | 1.37KB | 0.59KB |\n| providers (ThemeProvider.js) | 1.90KB | 0.85KB |\n| providers (UploadProvider.js) | 11.66KB | 3.50KB |\n| providers (index.js) | 0.45KB | 0.23KB |\n| providers (types.js) | 0.01KB | 0.04KB |\n| react-runtime (index.js) | 5.62KB | 2.34KB |\n| react (LazyPluginLoader.js) | 4.47KB | 1.63KB |\n| react (SchemaRenderer.js) | 116.21KB | 38.14KB |\n| react (data-invalidation.js) | 5.05KB | 2.08KB |\n| react (index.js) | 4.03KB | 1.86KB |\n| react (schema-input.js) | 4.25KB | 2.04KB |\n| react (spec-input.js) | 0.20KB | 0.18KB |\n| sdui-parser (body-dialect.js) | 4.78KB | 2.09KB |\n| sdui-parser (codegen.js) | 6.58KB | 2.74KB |\n| sdui-parser (dashboard-widget-options.js) | 3.08KB | 1.30KB |\n| sdui-parser (index.js) | 5.78KB | 2.56KB |\n| sdui-parser (input-type.js) | 2.84KB | 1.40KB |\n| sdui-parser (kanban-quick-add.js) | 3.89KB | 1.87KB |\n| sdui-parser (parse.js) | 25.28KB | 7.80KB |\n| sdui-parser (provenance.js) | 3.66KB | 1.82KB |\n| sdui-parser (types.js) | 0.28KB | 0.23KB |\n| sdui-parser (validate.js) | 18.27KB | 6.20KB |\n| types (ai.js) | 4.11KB | 2.06KB |\n| types (api-types.js) | 0.20KB | 0.18KB |\n| types (app.js) | 2.87KB | 1.00KB |\n| types (base.js) | 0.20KB | 0.18KB |\n| types (blocks.js) | 0.20KB | 0.18KB |\n| types (complex.js) | 2.93KB | 1.49KB |\n| types (crud.js) | 0.20KB | 0.18KB |\n| types (dashboard-filter-alias.js) | 6.23KB | 2.74KB |\n| types (data-display.js) | 3.75KB | 1.85KB |\n| types (data-protocol.js) | 0.20KB | 0.19KB |\n| types (data.js) | 0.20KB | 0.18KB |\n| types (designer.js) | 1.85KB | 0.85KB |\n| types (disclosure.js) | 0.20KB | 0.18KB |\n| types (error-code.js) | 1.54KB | 0.88KB |\n| types (expression.js) | 0.20KB | 0.18KB |\n| types (feedback.js) | 0.20KB | 0.18KB |\n| types (field-types.js) | 0.20KB | 0.18KB |\n| types (form.js) | 0.20KB | 0.18KB |\n| types (http-inflight.js) | 8.87KB | 3.73KB |\n| types (http-retry.js) | 4.32KB | 2.02KB |\n| types (icon-key-migration.js) | 4.26KB | 1.63KB |\n| types (index.js) | 4.74KB | 2.25KB |\n| types (layout.js) | 0.20KB | 0.18KB |\n| types (managed-by.js) | 0.19KB | 0.18KB |\n| types (mobile.js) | 5.00KB | 2.39KB |\n| types (navigation.js) | 0.20KB | 0.18KB |\n| types (objectql.js) | 0.20KB | 0.18KB |\n| types (overlay.js) | 0.20KB | 0.18KB |\n| types (permissions.js) | 2.52KB | 1.31KB |\n| types (plugin-scope.js) | 0.20KB | 0.18KB |\n| types (record-components.js) | 0.20KB | 0.19KB |\n| types (record-semantics.js) | 1.28KB | 0.67KB |\n| types (registry.js) | 0.20KB | 0.18KB |\n| types (reports.js) | 0.20KB | 0.18KB |\n| types (select-option.js) | 0.20KB | 0.19KB |\n| types (spec-report.js) | 5.05KB | 1.93KB |\n| types (spec-ui-namespace.js) | 0.20KB | 0.19KB |\n| types (strict-authoring-face.js) | 17.15KB | 6.32KB |\n| types (system-fields.js) | 3.33KB | 1.54KB |\n| types (theme.js) | 6.28KB | 2.87KB |\n| types (ui-action.js) | 8.11KB | 3.32KB |\n| types (views.js) | 0.20KB | 0.18KB |\n| types (widget.js) | 0.20KB | 0.18KB |\n\n### Size Limits\n- ✅ Core packages should be < 50KB gzipped\n- ✅ Component packages should be < 100KB gzipped\n- ⚠️ Plugin packages should be < 150KB gzipped\n"}
{"id":5837384173,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T18:22:09Z","updated_at":"2026-09-25T18:22:09Z","html_url":"https://github.com/objectstack-ai/objectui/pull/10688#issuecomment-5837384173","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `64516007d752947539542b48b722c3818eb70ef4`\n\nRead: card objectui#10664 (body + 4 comments) and PR objectui#10688 (body, 18-file list, check-runs) via REST; the head fetched into `refs/review/pr-10688` and main into `refs/review/main`; diff taken against merge-base `4df0f3d9d1ca1d956dbe0c97739e991fb53a8583`; AGENTS.md (版本号策略 and commandment #10) and `.changeset/config.json`. The `os-dev-report` comment's claims were not used as evidence; every judgment below is against the diff and the files at the head. No GitHub write of any kind was made.\n\nBinding text. Triage `5835320838`: 「gate the map's fetch on a settled schema (the objectui#7895 / #7903 shape), and give the repeater a stable sort key the way `filterKey` stabilises the filter. Pin 1 `find` per mount with `$expand` on the first call, and a sort change re-reading with the new `$orderby` while an equal sort does not. Census the other data renderers for the same mismatch as part of the claim.」 Claim `5835714138`: the file surface is the map's and repeater's fetch effects plus 「the fetch-effect dependency lists of each other data renderer the card's census finds with the same mismatch (named in the PR); pins beside each; and changesets `patch` per touched package. ⛔ Not any view's `error` lifecycle」, and 「Clause-②: no」.\n\n### ① Derived judgments\n\n**A. `object-map` (the named site) — RIGHT, the card's shape.**\n- `packages/plugin-map/src/ObjectMap.tsx:794-797`: the local `useState` definition and its own metadata effect (base lines removed in the hunk at old 1014-1034) are replaced by `useSettledSchema(recordSourceObjectName ?? '', hasInlineData ? undefined : dataSource)`. The key is the same object the removed effect read (`resolveRecordSourceObjectName`, `packages/core/src/utils/record-source.ts:78-83`: on the `object` provider it returns `dataConfig.object`, which is `dataObjectName`, the object the query names at `ObjectMap.tsx:1016`). Same hook and same placement as `ObjectTimeline.tsx:332/468` and `ObjectGallery.tsx:316/535` (the objectui#7895 / #7903 shape the triage binds).\n- `ObjectMap.tsx:862-865`: the gate `if (fetchesForItself && !objectSchemaReady) { setLoading(true); return; }` sits ahead of the query on the branch that issues it; `fetchesForItself` (`:834`) is `!Array.isArray(dataProp) && dataProvider === 'object'`, so host rows and an inline `value` set are not held, as the PR body says.\n- `ObjectMap.tsx:1058`: the dependency list keeps `objectSchema` (the hook's payload, one state value, equal-payload guarded at `packages/react/src/hooks/useSettledSchema.ts:189-195`) and adds `objectSchemaReady` (boolean) and `fetchesForItself` (boolean, a function of two members already listed). No memo identity is keyed (AGENTS.md #10). The `query` tuple at `:870` is unchanged in membership, so the objectui#10623 silent-re-read logic still compares the same inputs.\n- The `error` lifecycle is untouched: the only hunks in the error region (`:1007-1011`) are comment text; `setError` sites are unchanged. The claim's ⛔ holds.\n\n**B. `element:repeater` (folded objectui#10665) — RIGHT.** `packages/components/src/renderers/basic/data-list.tsx:117` adds `sortKey`, a `useMemo` over `[props.sort]` returning `JSON.stringify(props.sort)` or the empty string, the same idiom as `filterKey` on `:113`; `:168` lists it. `props.sort` reaches `$orderby` at `:145`. A string key by content, as the triage asks (「the way `filterKey` stabilises the filter」).\n\n**C. Census row `element:record_picker` — same mismatch, RIGHT.** `record-picker.tsx:109` `sort = composed?.sort ?? props.sort` reaches `$orderby` at `:140`; the base list had no sort. `:126` adds `sortKey` by content; `:163` lists it. Under-key of the identical shape as B.\n\n**D. Census row `object-data-table` — same mismatch (both directions), RIGHT.** `packages/plugin-dashboard/src/ObjectDataTable.tsx:678-681`: the definition moves to `useSettledSchema(schema.objectName ?? '', dataSource)`, keyed on the object the query names (`:746`). `:775-779`: the gate is scoped inside the branch that queries (`schema.objectName && !boundData && no schema.data`), holding `loading` while it waits, else `fetchData()`. This is the over-key the card describes (the definition landed after the first query and re-ran the effect). `:688` `lookupExpandKey = computeLookupExpand(schema, objectSchema).join(',')`: `computeLookupExpand` (`:561-600`) reads `schema.columns` (`:572`) and returns `[]` on a null definition (`:565`), so the key is a string derived from the exact expansion the query sends at `:740`, not from the column objects; a relabel yields the same string. `:789` lists `objectSchemaReady`, `objectSchema`, `lookupExpandKey`. The removed schema effect (base, old 745-756) returned without settling when `dataSource`, `objectName` or `getObjectSchema` was absent; the hook settles on all of those. The added `eslint-disable-next-line react-hooks/exhaustive-deps` at `:788` documents that `schema.columns` is read through the key.\n\n**E. Census row: dashboard filter bar options — same mismatch, RIGHT.** `DashboardFilterBar.tsx:305` (`$filter`) and `:338` (`runtimeFilter`) both send `from.filter`; the base list (`from?.object, from?.valueField, from?.labelField, dataSource`) omitted it. `:280` `optionsFilterKey = JSON.stringify(from?.filter ?? null)` (string), `:357` lists it.\n\n**F. Census row: `object-view` non-grid read — same mismatch, RIGHT.** `packages/plugin-view/src/ObjectView.tsx:1178-1179` lowers `currentNamedViewConfig?.sort || activeViewQueryInputs?.sort || schema.table?.sort` to `$orderby` at `:1236`; the first two sources were listed (`:1287`), the third was not. `:1085` `tableSortKey = JSON.stringify(schema.table?.sort ?? null)`, `:1288` lists it. The other sort/filter sources are content-stable (`activeViewQueryInputs` through `useStableIdentity` `:1044`, `authoredFilters` through `useResolvedFilterSegments` `:1055` over `schema.table?.filter` / `defaultFilters`), so a `table.sort` change alone did not re-run the base effect; the key is load-bearing.\n\n**G. Stranding under the two new gates — covered.** `useSettledSchema.ts:198-211`: no source, no key, no `getObjectSchema` function, or a thrown read all call `settle(null)`, so `ready` flips true with `def` null and the gated branch proceeds unexpanded. Pinned on both renderers: `ObjectMap.fetchGate-10664.test.tsx:181-199` (SETTLES ON A THROWN READ, SETTLES WITH NO SOURCE, each asserts one `find` and markers painted) and `ObjectDataTable.fetchGate-10664.test.tsx:162-179` (same two, each asserting `[null]` expansion with rows on screen). The map passes `undefined` as the source for an inline `value` set (`:796`), which settles at once; `fetchesForItself` is false there anyway. The existing `ObjectDataTable.refreshIndicatorLocale-10580.test.tsx` (source with no `getObjectSchema`, expects 1+1 finds) still holds under the hook's immediate settle.\n\n**H. No identity key per render (AGENTS.md #10).** Every new key is a string (`sortKey` x2, `optionsFilterKey`, `lookupExpandKey`, `tableSortKey`) or a boolean (`objectSchemaReady`, `fetchesForItself`); `objectSchema` is hook state, republished only on a changed payload. None is a `useMemo`/`useCallback` identity.\n\n**I. Files that must not be touched — untouched.** The 18-file list contains no `ObjectGrid.tsx`, `ListView.tsx` or `RelatedList.tsx`; the PR body names those three rows as not edited, with seat 2's PRs objectui#10278 / objectui#10643 for the first two and the DECLARED COST pin for the third. That pin exists at main (`packages/plugin-detail/src/__tests__/RelatedList.multiValueParentScope-7299.test.tsx:268-277`) and its stated reason is the one the PR body reports: gating 「strands rows entirely when a `DataSource` has no `getObjectSchema` or its schema fetch rejects」. The PR body carries `Fixes #10664` on its first line while naming the open rows.\n\n**J. Existing pins edited.**\n- `ObjectDataTable.expandFls-7230.test.tsx:119-129` (`expandFor`): base waited for `find.mock.calls.length` greater than 1 and read the last call; head waits for one `find`, settles 50 ms inside `act`, asserts `toHaveBeenCalledTimes(1)` and reads `calls[0]`. LOAD-BEARING, not a weakening: under the gate a mount issues exactly one `find`, so the old wait would time out and the whole FLS file would fail for a reason unrelated to FLS; the old helper pinned the double read this card removes. The seven FLS assertions (PIN 1-7, `:155-228`) are byte-for-byte unchanged (the diff touches only the `act` import and the helper). Guard direction: an ungated regression now reddens on the count, and even if a slow second call slipped past the 50 ms window, PIN 3/4/6/7 would read the unexpanded first call and fail, so the file cannot go green for the wrong reason.\n- `ObjectMap.invalidationRefetch-10623.test.tsx:22-25`: header prose only; the delta assertion is unchanged and still holds (1 mount read + 1 bus read).\n\n**K. New pins can fail — lit control and a negative leg, per file.**\n- `ObjectMap.fetchGate-10664.test.tsx` (real `SchemaRenderer`, package registration): SUBJECT `:132-140` expects exactly one `find` carrying `['owner']`; on the base the map reads twice with the first unexpanded, so it is red there. SUBJECT `:152-164` holds `getObjectSchema` and asserts zero `find` before release, sequence `['schema:store','find:store']`. SUBJECT `:166-179` asserts the switched object's only read carries `['manager']`, never the previous object's expansion. CONTROL `:201-215`: a definition that changes after mount still re-reads with `['owner','region']`; green on base and head, red if `objectSchema` leaves the dependency list (a `dataSource` swap keeps `ready` true for the same key, so the effect would read once with the old definition and never again).\n- `ObjectDataTable.fetchGate-10664.test.tsx`: SUBJECT `:106-115` expects `[['account','owner_dept']]` (base gives `[null, [...]]`); SUBJECT `:117-128` waits; SUBJECT `:130-144` adds a lookup column on a mounted widget and expects a second read with the new `$expand` (base: `schema.columns` unkeyed, no re-read); CONTROL `:146-160` relabels and expects no read (red for a key on column identity).\n- `data-list.sortKey-10664.test.tsx` and `record-picker.sortKey-10664.test.tsx` (both `properties` and `dataSource` binding for the picker): SUBJECT expects `[ASC, DESC]` `$orderby` after a sort change (base: no re-read); CONTROL expects no read for an equal sort in a fresh array (red for an identity key).\n- `DashboardFilterBar.optionsFilterKey-10664.test.tsx`: SUBJECT for both the dataset and the fallback read; CONTROL for an equal filter in a fresh object.\n- `ObjectView.tableSortKey-10664.test.tsx`: SUBJECT `{name:'asc'}` then `{name:'desc'}`; CONTROL equal sort, no read.\n\n**L. Asked and missing: none.** Map gated; repeater keyed; both triage pins present (1 `find` per mount with `$expand` on the first call; sort change re-reads, equal sort does not); census done with rows named in the PR; a `patch` changeset per touched package.\n\n**M. Done and not asked: nothing beyond the surface.** The four extra census rows are what the triage's 「census the other data renderers for the same mismatch as part of the claim」 and the claim's file surface explicitly cover. The two test edits are consequences of the gate. No spec, types, or export moved.\n\n**N. Observations (not defects).** (1) `ObjectMap.expandFls-7429.test.tsx:150-151` still carries the sentence \"The object-schema effect re-fires the fetch effect once the schema lands\"; its wait tolerates a single expanded call, so it passes, but the prose is now stale (not in this diff). (2) `JSON.stringify` at render on `from?.filter` and `schema.table?.sort` would throw on a non-serialisable value; it mirrors the existing `filterKey` idiom in the same files and those inputs are authored JSON. (3) A `dataSource` swap with an unchanged key reads once with the previous definition and again with the new one (the CONTROL asserts more than zero reads and the last expansion); that is `useSettledSchema`'s same-key readiness, shared with timeline/gallery/view and identical on the base; not the mount count the card pins.\n\n### ② Semver level\n\n- Four changesets, all `patch`: `@object-ui/plugin-map`, `@object-ui/components`, `@object-ui/plugin-dashboard`, `@object-ui/plugin-view`; each is in the `fixed` group of `.changeset/config.json`. Correct level: behaviour fixes, no API or accept-set change, no `major` (AGENTS.md 版本号策略 forbids `major` in changesets; breaking changes would be `minor`, and nothing here is breaking). Not a docs-only change (`content/docs/**` untouched), so the empty-frontmatter rule does not apply.\n- `10664-map-reads-once.md`, sentence by sentence: \"reads its records once per mount, and that read already carries the lookup expansion\" TRUE (`ObjectMap.tsx:862-865`, pin). \"loaded the object definition in an effect of its own and listed it among the fetch effect's dependencies\" TRUE (base hunk). \"The definition lands after the first query, so every mount issued two `find` calls, the first without `$expand`\" TRUE (base structure; the card's measurement). \"Switching the bound object also sent the new object's query with the previous object's expansion\" TRUE (base: the fetch effect re-ran on `dataObjectName` with the old `objectSchema` state still in hand). \"now reads the definition through `useSettledSchema` ... the way `object-timeline` (objectui#7895) and `object-gallery` (objectui#7903) already do\" TRUE (both verified at the head). \"A definition read that fails, or an adapter with no `getObjectSchema`, settles with no definition, so the map still loads, unexpanded\" TRUE (hook `:198-211`, pins). \"Host rows (`data`) and an inline `value` set are not held\" TRUE (`:834`, `:862`).\n- `10664-data-table-filter-bar.md`: \"reads its rows once per mount, already expanded, and a dashboard filter re-reads its options when `optionsFrom.filter` changes\" TRUE. \"loaded the object definition in an effect of its own and listed it among the fetch effect's dependencies, so every object-bound mount issued two `find` calls, the first without `$expand`\" TRUE (base hunk; `computeLookupExpand` returns `[]` on null). \"now reads the definition through `useSettledSchema` ... its query waits until that read settles; a failed read, or an adapter with no `getObjectSchema`, still loads the rows, unexpanded\" TRUE. \"Its expansion also depends on `columns`, which did not re-run the read: adding a lookup column to a mounted table now re-reads with the new `$expand`, and relabelling a column does not\" TRUE (`:572`, `:688`, pins). \"The filter bar's option list sends `optionsFrom.filter` on both of its reads, the dataset query and the record fallback, but did not re-read when only that filter changed\" TRUE (`:305`, `:338`, base list). \"It now does, and an equal filter in a new object does not re-read\" TRUE.\n- `10664-object-view-table-sort.md`: \"re-reads when `table.sort` changes\" TRUE. \"falls back to `table.sort` for its `$orderby` when neither the named view nor the active view declares a sort, but a change to `table.sort` alone did not re-run it, so the view kept the old order\" TRUE (`:1178-1179`, base list). \"keys the read on that sort's content, so an equal sort in a new array does not re-read\" TRUE.\n- `10664-repeater-picker-sort-key.md`: \"re-read their rows when the sort they send changes\" TRUE. \"Both blocks put their sort on `$orderby` (the repeater's `properties.sort`; the picker's `properties.sort` or its `dataSource` binding's `sort`), but neither re-ran its read when only the sort changed\" TRUE (`data-list.tsx:145`, `record-picker.tsx:109/140`, base lists). \"Each now keys its read on the sort's content, the way it already keys on the filter's, so an equal sort in a new array does not re-read\" TRUE.\n- Docs/README prose added by the diff: none (no `content/docs/**`, no README touched).\n\n### ③ Boundary flags\n\n- PR body flags and open questions, answered: (a) \"Latent AGENTS.md #10 identity keys, recorded and not changed here\" (`ObjectChart`, `ObjectMetricWidget`, `LineItemsPanel`, `ObjectGrid`) — outside this card's mismatch (the query content is keyed; only a forced discard would show) and outside the claim's file surface for the grid; recording them is the right call. (b) `useViewData` length-only key — no in-tree renderer consumer; out of the census population; noted, not owed here. (c) \"The map and the data table now hold their loading placeholder while a changed object's definition settles\" — confirmed at `ObjectMap.tsx:862-865` and `ObjectDataTable.tsx:775-776`; the gate closes only when the hook key moves, which on those branches is the queried object, so the held placeholder never masks an unchanged query. (d) The RelatedList question is raised in the report and explicitly not decided in the PR; the PR leaves the DECLARED COST decision standing, which is the correct posture for a seat-level ruling.\n- CI at the head `64516007`: 42 check-runs. First read 18:12Z: 29 success, 3 skipped, 10 in progress, 0 failing. Re-poll 18:15Z: 31 success, 3 skipped, 8 in progress (the 8 test shards and Spec Main Shape Gate; Type Check succeeded), 0 failing. Second re-poll: see the line below.\n- Second re-poll 18:20Z (final): 43 check-runs, 40 success, 3 skipped, 0 in progress, 0 failing. All eight test shards, Type Check and Spec Main Shape Gate concluded success, so the seven pin files and the edited `expandFls-7230` file are green at the head.\n- Draft: YES (`draft: true`).\n- `Clause-②: no` holds: no file under `packages/types`, `packages/spec` or any `index.ts` barrel is touched; `useSettledSchema` was already exported from `@object-ui/react` at main (`packages/react/src/hooks/index.ts:54`); no accept set or published export moves.\n- Main under the PR's files since the merge-base: NO. The merge-base is `4df0f3d9d1ca1d956dbe0c97739e991fb53a8583`, which is the current `origin/main`; the branch's last commit `6451600` is a merge of that main.\n\nImplemented-by: `claude/issue-10664-fetch-deps-match-query`\nReviewed-by: `session_01KUxVUa7e39aNjhkKi1gsoy`\n\n**VERDICT: PASS**\n"}
