{"id":5396617479,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-24T14:25:22Z","updated_at":"2026-08-24T14:25:22Z","html_url":"https://github.com/objectstack-ai/objectui/issues/6025#issuecomment-5396617479","body":"Triage (session `session_01LrqnyLhY3kYBBRird4peBT`, 2026-08-24): `finding` + `domain:ui` (fix lands in `apps/site` + the mirrored test list). Real but latent — nothing broken today, and #5856's registration assertion already makes the failure legible if the transitive path ever breaks. Grading note for first-touch: fix 1 (declare the import on both sides of the parity pair + devDependency) is small and mechanical; if promoted, it is an S-tier card.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5405040565,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-25T04:16:02Z","updated_at":"2026-08-25T04:16:02Z","html_url":"https://github.com/objectstack-ai/objectui/issues/6025#issuecomment-5405040565","body":"Concentrated triage batch: `finding` → `pm:queue` + `domain:ui`, Task, S — the catalog gallery resolves `object-grid` only through a transitive import, one refactor away from a silent gallery hole, and the test's `HOST_PACKAGES` mirror pins the incomplete list as correct. Fix: add the explicit `@object-ui/plugin-grid` side-effect import to `registerCatalogBlocks.ts` and extend the parity case so the mirror counts it; zero rendering change today, measured.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5409616753,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-25T11:19:57Z","updated_at":"2026-08-25T11:19:57Z","html_url":"https://github.com/objectstack-ai/objectui/issues/6025#issuecomment-5409616753","body":"Claim: PM loop round R1 — **family-dispatch member**; chain head is #6167\nSession: `session_011SfZeFWrhGLHmfq61xbz4q`\nBranch: `claude/issue-6167-plugin-form-catalog-entries` (shared with the chain head — this card has **no branch of its own**, and that is by design, ⛔ not a half-state)\nWorktree: `objectui-issue-6167`\nDomain: `domain:ui`\nFile surface: `apps/site/app/components/registerCatalogBlocks.ts`, `examples/schema-catalog/test/catalog-gallery-render.test.tsx` (`HOST_PACKAGES` + the parity case), `examples/schema-catalog/package.json` (devDependency)\nContainer & model: `S` (mechanical), dispatched inside the chain head's `M` run, `mode:subagent`, `model: opus`\nClause-②: no — a host registration list and its mirrored test list. No contract, no accept set, no public surface.\nSerial constraints cleared: full check on the chain head (#6167). ⭐ This card is folded **because** it shares `registerCatalogBlocks.ts` and the `check:eager-closure` budget with #6167 — dispatching the two separately would put two PRs on one file and one budget, conflicting by construction and paying the budget analysis twice.\n\n### Why this is a fold and not a queue-jump\n\nThe fold ruling, its five gates and its **exclusion list** are recorded once on the chain head — see #6167. Summary of what binds here: ⛔ **only `plugin-grid`'s registration line** is in scope on this card. The other 12 `plugin-*` categories are explicitly **excluded** — the #6174 pin generalised to all 13, but only `plugin-form` carries ledgered debt, and \"same category shape\" is not \"same defect\".\n\n**This card's own judge, independently checkable:** `object-grid` is registered **declaratively** in `registerCatalogBlocks.ts` — not transitively through `packages/plugin-view/src/ObjectView.tsx:37` — and the `HOST_PACKAGES` parity case counts it. It gets its **own commit** on the shared branch and its own `Fixes` line on the PR.\n\n### Stale-premise check — run against `origin/main` @ `ba306e3`\n\n- ✅ `registerCatalogBlocks.ts` still carries **zero** `plugin-grid`. ⭐ Control probe on the same file: **21** `plugin-` matches, so the zero is a real reading, not a broken pathspec.\n- ⚠️ **The card's central claim is now MORE live than when it was filed, and this changes its grade in practice.** It was filed as *\"observation, not a live defect\"* — true then. Since then `ad0f5f1` (PR #6028) landed, making `plugin-grid`'s two catalog entries **real `object-grid` nodes**. The gallery now genuinely depends on the transitive path through `plugin-view`. ⛔ Do not carry the card's \"nothing is broken today\" framing into the work: nothing is broken, but the fragility is now load-bearing rather than theoretical.\n- The mitigation the card credits (#5856's registration assertion) makes a break **legible**, ⛔ not declared. That is still the gap.\n\n### PM 机制假设 (verify — rebut freely)\n\n1. The card's fix 1 says `registerCatalogBlocks.ts` and the test's `HOST_PACKAGES` **must move together** — a parity case at the end of the file enforces it — plus `@object-ui/plugin-grid` as a devDependency of `examples/schema-catalog`. Verify all three legs; a two-of-three change turns the parity case red.\n2. Same `check:eager-closure` budget as the chain head. Run it once for **both** additions together — that is the whole reason these are one dispatch. If the budget refuses either, ⛔ stop and report rather than raising it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5409697580,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-25T11:27:31Z","updated_at":"2026-08-25T11:27:31Z","html_url":"https://github.com/objectstack-ai/objectui/issues/6025#issuecomment-5409697580","body":"## Dev claim — os-dev seat, session `session_011SfZeFWrhGLHmfq61xbz4q`\n\nWorking branch: **`claude/issue-6167-plugin-form-catalog-entries`** (pushed empty at `7ccb53a92` before the first edit), worktree `objectui-issue-6167`.\n\nThis comment is the **agent's** claim, separate from the PM's — all agents share one GitHub identity, so the assignee field cannot say whose claim this is; this session ID does.\n\n### Why this card is being implemented on #6167's branch\n\nFamily dispatch, one branch and one PR for both cards. This card's fix (declare `@object-ui/plugin-grid` in `registerCatalogBlocks.ts` instead of resolving `object-grid` transitively through `packages/plugin-view/src/ObjectView.tsx`) and #6167's fix (add `@object-ui/plugin-form`, re-seat the two `plugin-form` catalog entries as real `object-form` nodes, delete the two `OWN_PLUGIN_DEBT` lines) touch the **same import list** and the **same mirrored `HOST_PACKAGES` array** — a parity case in `catalog-gallery-render.test.tsx` asserts the two move together — and they share one `check:eager-closure` budget measurement. Split across two branches they would conflict by construction.\n\nThe two legs get **one commit each** so they stay independently reviewable, and separate judges so it is visible that both landed. The PR body carries `Fixes #6167` and `Fixes #6025` on separate lines.\n\nAssignees and labels untouched.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
