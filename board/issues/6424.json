{
  "number": 6424,
  "kind": "issue",
  "title": "finding(components): `data-table` reads two column keys `TableColumn` does not declare — `headerIcon` and `fitContent`",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "claude[bot]",
    "id": 209825114,
    "type": "Bot"
  },
  "labels": [
    "domain:ui",
    "pm:dispatched"
  ],
  "assignees": [],
  "milestone": null,
  "type": "Bug",
  "created_at": "2026-08-25T23:44:06Z",
  "updated_at": "2026-08-29T05:08:17Z",
  "closed_at": "2026-08-29T05:08:17Z",
  "closed_by": {
    "login": "os-sales",
    "id": 319429713,
    "type": "User"
  },
  "merged_at": null,
  "comments": 24,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/issues/6424",
  "body": "Found while implementing #6373 (typing `ObjectDataTable`'s column emit against the `TableColumn[]` slot it fills). Filed unassigned. Deliberately NOT absorbed into #6373, whose fence is what a PRODUCER **writes**; this is the same slot seen from the other end.\n\n## Measured (at `9602dc820`)\n\n`packages/components/src/renderers/complex/data-table.tsx`, comments stripped, every `col.<key>` read:\n\n| key | reads | declared by `TableColumn`? |\n|---|---|---|\n| `accessorKey` | 28 | yes |\n| `width` | 6 | yes |\n| `align` | 5 | yes |\n| `header` | 4 | yes |\n| `className` | 4 | yes |\n| `sortable` | 3 | yes |\n| `editable` | 3 | yes |\n| `cell` | 2 | yes |\n| `cellClassName` / `resizable` / `type` | 1 each | yes |\n| `name` | 2 | **no** — the alias #5120 holds, deliberately and documented |\n| **`headerIcon`** | **2** | **no** |\n| **`fitContent`** | **4** | **no** |\n\n`TableColumn` (`packages/types/src/data-display.ts:293`) declares exactly: `header`, `accessorKey`, `className`, `cellClassName`, `width`, `minWidth`, `align`, `fixed`, `type`, `sortable`, `filterable`, `resizable`, `editable`, `cell`.\n\nBoth undeclared reads are live, not vestigial:\n\n- `headerIcon` — rendered into the header cell (`data-table.tsx:1633-1634`).\n- `fitContent` — drives the auto-width pass (`:575`, `:583`) and the `width:1%` + nowrap cell at two render sites (`:1562`, `:1803`). Both of those two go through **`(col as any).fitContent`**, which is the shape \"declared != enforced\" takes in TypeScript and the same tell #5853 removed for `type`.\n\n## Why it matters\n\nAn author writing `{ accessorKey: 'x', headerIcon: <Icon/> }` on a `data-table` column gets a compile error for a key the renderer honours, and `TableColumnSchema.parse` silently strips it — while the same key placed there by an untyped producer works. So the runtime admits a vocabulary the declaration refuses: the second de-facto contract AGENTS.md #0.1 forbids, in the direction where the CONSUMER is the one out of step.\n\n## Not a duplicate\n\n- **#6004** names `headerIcon` among the keys `ObjectGrid.generateColumns()` attaches, and its remedy is producer-side typing. Its verdict on `headerIcon` decides one of these two keys and the two must agree — but `fitContent` is not in its list, and neither is the consumer-side read census this card is about.\n- **#5453** is the opposite case: a key (`wrap`) that is forwarded and read by **nobody**. Its evidence table cites `fitContent`'s read-count as the counter-probe that the grep works, without noting that `fitContent` is itself undeclared.\n- **#6373** typed the producer at `ObjectDataTable`'s emit seam; `table-declared-equals-enforced.test.tsx` is the consumer-side instrument, but it covers only the **static** `table` renderer's narrow surface (#5474, Option C), never `data-table`'s rich one.\n\n## Suggested disposition\n\nApply the rule #6373's PR states, in the declare branch this time: a key the consumer reads gets **declared** on `TableColumn` (interface + `TableColumnSchema` mirror + the `zod-mirror-parity` pairing), and the `as any` at the two `fitContent` sites goes with it. The alternative — retiring the reads — is a behaviour change and needs the usual measurement of who authors these keys today.\n\n## Related\n\n- #6373 — the producer half of the same slot, and the rule this would apply\n- #6004 — `headerIcon` also appears in its key triage (parent of #6373)\n- #5453 — the mirror case: a forwarded key nothing reads\n- #5474 — the static table's declared = enforced split, whose instrument this surface lacks\n- #5120 — the `name` alias, the one undeclared read that is held on purpose\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_011SfZeFWrhGLHmfq61xbz4q)_\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
