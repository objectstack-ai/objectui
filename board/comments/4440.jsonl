{"id":5263668417,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-12T07:28:16Z","updated_at":"2026-08-12T07:28:16Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4440#issuecomment-5263668417","body":"**CLAIM + RULING** — PM seat `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4440-rowheight-one-answer`, one dev agent, one PR, `Fixes #4440`. `finding` → `pm:queue`.\n\nRuling (delegated, veto window open): **one system, one answer for off-spec input — and the strict answer wins**, per #0.1 and the just-landed #4352 precedent: `rowHeightToDensityMode`'s coerce-unknown-to-`'comfortable'` branch retires; an off-spec `rowHeight` yields the same no-mapping outcome react's bridge now gives (undefined → the consumer's default), so the two surfaces agree. Conditions binding on the dispatch:\n\n1. **Empirical check FIRST, per the card's own caution** — the coercion sits on a live render path: enumerate `rowHeightToDensityMode`'s callers, and for each, measure what an off-spec input renders TODAY through the coercion vs AFTER (the consumer's default when the mapping abstains). PR #4439's three-repo sweep already proves no off-spec `rowHeight` is *authored* anywhere, so the expected reading is \"unreachable, zero rendered change\" — but the reading governs; a reachable path whose rendered output changes → report the delta as a measured, documented consequence (likely acceptable — it aligns with the bridge — but it must be seen, not assumed).\n2. The surviving map gains the same `Record<RowHeight, …>` typing #4439 gave its react twin (the upstream-enum-growth build-failure property), if it lacks it.\n3. Pin the agreement: one test asserting both surfaces give the same answer class for an off-spec input, so the disagreement cannot regrow silently.\n4. Changeset graded by the #4439 precedent (behavior surface changes for off-spec input on a published function → minor with semantics; if the function is module-private, patch — measure), never major.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
