---
'@object-ui/types': patch
---

The index-signature pin. `ObjectKanbanSchema` declares `swimlaneField`, which is
admitted by the inherited index signature and examined by nothing. An instrument
that reads the index signature as membership answers "declared" for every key
that was ever authored, and erases the finding this whole family is about.
