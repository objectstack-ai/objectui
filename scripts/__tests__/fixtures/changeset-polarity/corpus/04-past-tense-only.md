---
'@object-ui/types': patch
---

The past-tense pin. A historical sentence cannot rot, so it is outside the
population by construction: `SpinnerSchema` declared `color` when the node was
introduced, and the key was retired afterwards.
