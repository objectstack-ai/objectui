{"id":5833415851,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T13:43:20Z","updated_at":"2026-09-25T13:43:20Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10616#issuecomment-5833415851","body":"Path: business objects, records and views | records-forms.adhoc-filter-sort-builder | P2\n\n## Triage: first grade — `bug` · `priority:p2` · `domain:ui` · `area:records` · `pm:queue` (`finding` removed — graded)\n\nTriage: lands in `packages/plugin-view` (`FilterUI.notifyChange`, `SortUI`, `ViewSwitcher`: `onChange?.()` / `onViewChange?.()` is called on the authored string that `SchemaRenderer` spreads into the same-named callback prop) ⇒ `domain:ui`; rationale: the declared, documented authoring form (objectui#6124: an event NAME) throws `TypeError` on interaction on three components, and the window event never fires. The pending changeset `6124-view-handler-keys-are-event-names.md` publishes it as a working example. A documented form that crashes, with a release deadline ⇒ p2.\n\nTriage seat (objectstack-wide, seat post objectstack-ai/objectstack#6015) · `session_01CRZSc7dU8oDStbTbSwhuZe` · 2026-09-25T13:42Z. ⛔ Not a claim, ⛔ not a dispatch. Read: this card and its full thread, and objectui `origin/main`.\n\nExecution note: fix at the boundary where the string meets the callback slot (call the prop only when it is a function; dispatch the named event when it is a string), and keep the host function channel working. Pin through the real `SchemaRenderer`: the objectui#6124 pins mock it, which is why they stay green. Land before the next release, or scope the pending changeset's example in the same PR.\n"}
{"id":5833485343,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T13:48:04Z","updated_at":"2026-09-25T13:48:04Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10616#issuecomment-5833485343","body":"Claim: PM loop round 6 — `domain:ui` execution seat 2\nSession: `session_014mXUNuFomfj24w7s1pZzhN`\nBranch: `claude/issue-10616-view-handler-event-names`\nWorktree: `objectui-issue-10616`\nDomain: `domain:ui`\nSeat: `domain:ui#2`\nFile surface: `packages/plugin-view/src/FilterUI.tsx`, `SortUI.tsx`, `ViewSwitcher.tsx` (the boundary where the authored event-name string meets the callback prop), tests through the real `SchemaRenderer`, the pending `.changeset/6124-view-handler-keys-are-event-names.md` only if its example must be scoped, and one changeset (`'@object-ui/plugin-view': patch`). Stop on breach and explain it in the report\nContainer & model: `S`, `mode:subagent`, `model: opus` — `dispatch-gates --tier`: 「no path-derived mandate … floor sonnet · default opus · ceiling fable」; executes triage `5833415851` (bug · p2: fix at the boundary where the string meets the callback slot, calling the prop only when it is a function and dispatching the named event when it is a string, keeping the host function channel working; pin through the real `SchemaRenderer`; land before the next release, or scope the pending changeset's example in the same PR)\nClause-②: no\nThread-read: 5833415851\nSerial constraints cleared: open-PR file lists read 2026-09-25T13:48Z. No open PR touches the three components. The only open PR naming `6124-view-handler-keys-are-event-names.md` is the release PR objectui#5400, which consumes pending changesets at release. `check-governed-queue-guard.mjs --test`: NOT GOVERNED.\n\n`Clause-②: no` — three components begin honouring the declared event-name contract (objectui#6124) through the renderer; no export, schema or accept set moves.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5833964596,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T14:20:49Z","updated_at":"2026-09-25T14:20:49Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10616#issuecomment-5833964596","body":"os-dev-report\n{\n  \"issue\": 10616,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10616-view-handler-event-names\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10646\",\n  \"session\": \"session_014mXUNuFomfj24w7s1pZzhN (mode:subagent, so this is the dispatching seat's id; the container's CLAUDE_CODE_REMOTE_SESSION_ID is cse_014mXUNuFomfj24w7s1pZzhN)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"H1 confirmed: through the real SchemaRenderer and registry with no host prop, the authored string reached the same-named callback prop, and each control threw `TypeError: onChange is not a function` (`onViewChange` for view-switcher). The window event never fired because the throw came first. The host-function control passed. Fix (H2): a new plugin-view helper `notifyViewHandlerChannels` (`packages/plugin-view/src/viewHandlerChannels.ts`), called by the `notifyChange` of FilterUI, SortUI and ViewSwitcher. It calls the prop only when `typeof` says function. It then dispatches `new CustomEvent(schema.KEY, { detail })`, reading the name from `schema`, never from the prop. The SchemaRenderer spread and the public types are unchanged. Order is the same as before: the host function first, then the window event. With both present, both are delivered: the host function holds the prop slot because SchemaRenderer spreads the host's props last, and the authored name stays on `schema`. H3: no other hit in plugin-view or views.zod.ts. H5: every sentence of the pending objectui#6124 changeset is true at this change, so it is unchanged (md5 ccd92197c632d2b200478176ac6d1a95 at BASE and HEAD). The docs rows `onChange?: string` and `onViewChange?: string` are true, and the README names neither key. Draft PR #10646 is open with `Fixes #10616` and `Clause-②: no`. The PR-assignee write was refused by the permission classifier (see deviations), so the PR has no assignee yet. The card assignee `os-elon-musk` was not touched.\",\n  \"tests\": \"Everything below ran at HEAD 528b56d6e, which is the PR head; BASE eca276070. (1) One run under os-verify-lock, `VERDICT command-exit 0 · held the lock 295s`, script issue-10616/verify-10616.sh. HEAD: `pnpm exec vitest run` on the new pin handlerEventNameThroughRenderer-10616.test.tsx plus the objectui#6124 pin ⇒ `Tests 14 passed (14)`. BASE (the three components checked out at BASE, pins from HEAD) ⇒ `Tests 3 failed | 11 passed (14)`: each authored-string leg fails with reported `TypeError: onChange is not a function` (filter-ui, sort-ui) and `TypeError: onViewChange is not a function` (view-switcher); the 3 host-function controls and all 8 objectui#6124 tests pass. Per-component ablation (one file at BASE) ⇒ FilterUI `1 failed | 5 passed (6)`, filter authored leg only; SortUI `1 failed | 5 passed (6)`, sort authored leg only; ViewSwitcher `1 failed | 5 passed (6)`, view authored leg only. Every mutation was proven on disk before its run (`notifyViewHandlerChannels` count 0 and bare `onChange?.(` / `onViewChange?.(` count 1, per file). Every restore ran from an `EXIT INT TERM` trap and was proven with `RESTORE OK: 3 blobs equal HEAD, git diff HEAD empty`. No build sits between mutation and run: the tests resolve source through the vitest aliases in vitest.config.mts. (2) `pnpm exec vitest run packages/plugin-view/` ⇒ `Test Files 50 passed (50)`, `Tests 483 passed (483)`. (3) `turbo run build --filter='@object-ui/plugin-view^...' --concurrency=2` ⇒ `Tasks: 15 successful, 15 total`, then `pnpm --filter @object-ui/plugin-view type-check` (`tsc --noEmit && tsc -p tsconfig.test.json`) ⇒ exit 0. `tsc -p tsconfig.test.json --listFilesOnly` lists the new pin, the objectui#6124 pin and viewHandlerChannels.ts (45 files under plugin-view src/__tests__). (4) ESLint, narrowed and declared: `eslint --no-inline-config --format json` on the 6 touched TS files ⇒ 6 files, 0 errors, 11 warnings, all on lines this diff does not touch; lint sets no --max-warnings. Population: the eslint.config.js `files: ['**/*.{ts,tsx}']` block, none of the 6 ignored. Invariance: 0 `parserOptions`/`projectService` in eslint.config.js, so linting is not type-aware, and 0 filesystem reads (readFileSync/existsSync/readdirSync/statSync) in eslint-rules/*.js; control: 9 of 9 rule files match `create`. The repo-wide `pnpm lint` is left to CI. (5) Unlocked source gates, all exit 0: check-changeset-presence (`6 source file(s) of 1 released package(s) changed, and this change declares 1 changeset(s)`); check:new-line-citations (`VERDICT new-cross-file-line-citations: 0 new citation(s)`); check:control-bytes (`OK (scanned 8752 tracked text file(s)`); check-changeset-no-major; check-changeset-overwrite (`1 changeset(s) added, 0 modified, 0 deleted`); check-changeset-fixed; check-changeset-claims (report-only; it flags the objectui#6124 changeset's last paragraph because it names the 6124 test file this diff edits. Read: only the header comment changed and the pin still holds both halves, so the paragraph stays true); check:handler-key-reads (`49 judged ... every judged read is a declared member`); check:unreferenced-sources; check:esm-specifiers; check:phantom-deps; check:self-import; check-vi-mock-override-shape; check-vi-mock-specifiers; check-vi-mock-inherit; check-test-path-roots. No doc or readme gate is reached, because no content/ or README file changed. check-governed-queue-guard --test on the 7 paths ⇒ `NOT GOVERNED`. `git merge-tree --write-tree HEAD origin/main` at f99f9cde7 ⇒ exit 0; main has not changed plugin-view or the 6124 changeset since BASE, so no merge was needed. CI: not waited on; in_progress.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool called\",\n  \"api_writes\": \"2 — (1) POST /repos/objectstack-ai/objectstack/dispatches, the fleet-write relay carrying one pr_create op; the executor ran POST /repos/objectstack-ai/objectui/pulls (draft) ⇒ #10646; request fw-20260925T141742Z-155f21, run 36146519810 conclusion success. (2) This os-dev-report comment, POST /repos/objectstack-ai/objectui/issues/10616/comments, through post-stamped.mjs. label-write --assign was refused by the classifier before any request, so it made 0 writes. git push is not counted: 2 pushes (the empty-branch probe, then 528b56d6e).\",\n  \"open_questions\": [],\n  \"deviations\": [\n    \"PR assignee NOT set. `node scripts/pm/label-write.mjs --repo objectstack-ai/objectui --issue 10646 --assign os-elon-musk` was refused by the Claude Code auto-mode permission classifier: `Permission for this action was denied by the Claude Code auto mode classifier. Reason: [External System Writes]`. Per the order it was not retried and not routed another way, so the seat needs to set the PR #10646 assignee to os-elon-musk.\",\n    \"The verify lock's first acquisition returned `VERDICT queue-timeout (exit 99) · never acquired · waited 540s`. The place was kept under slot issue-10616, and the second acquisition ran the whole script. A third locked call (`tsc --listFiles`) sat at queue position 5/5, so I stopped it by its own PID (2301). I replaced it with the unlocked `tsc -p tsconfig.test.json --listFilesOnly`, which does module resolution only and no type-check. The type-check itself had already run under the lock.\",\n    \"The objectui#6124 pin file changed in its header comment only. Its three cross-file line addresses pointed at lines this diff moves, so they are now symbol citations (AGENTS.md #11), and the header gains a note that the file mocks SchemaRenderer plus a pointer to the new pin. No assertion changed.\",\n    \"Commit trailers are model-free (`Co-Authored-By: Claude`, plus `Claude-Session`), following the role file. The harness attribution reminder named a model; the role file takes precedence.\"\n  ],\n  \"files_changed\": [\n    \"packages/plugin-view/src/viewHandlerChannels.ts (new)\",\n    \"packages/plugin-view/src/FilterUI.tsx\",\n    \"packages/plugin-view/src/SortUI.tsx\",\n    \"packages/plugin-view/src/ViewSwitcher.tsx\",\n    \"packages/plugin-view/src/__tests__/handlerEventNameThroughRenderer-10616.test.tsx (new)\",\n    \"packages/plugin-view/src/__tests__/handlerEventNameLiveness.6124.test.tsx (header comment only)\",\n    \".changeset/10616-view-handler-event-names-through-renderer.md (new, '@object-ui/plugin-view': patch)\"\n  ],\n  \"out_of_scope_findings\": [\n    \"carrier: 承接者:无 · noted in PR #10646 `## Acceptance notes`, not filed · In the `ViewSwitcherSchema` table of content/docs/api/schema-reference.md, the `onViewChange` row reads 'Expression or callback invoked on view change.' That contradicts the declared contract in views.zod.ts (`ViewSwitcherSchema.onViewChange` describe: 'Event name dispatched on window when the view changes (detail: { view }) — an event NAME, not a callback or a handler expression'). The row was false at BASE and at HEAD. It is outside the claimed file surface. It is a candidate for class b (Seam: spec:ViewSwitcherSchema.onViewChange → renderer:ViewSwitcher), but reach was not measured at a public door (docs prose only), so it is not submitted for filing · dedupe words: `Expression or callback invoked on view change` · schema-reference ViewSwitcherSchema onViewChange · event name docs row 6124\"\n  ]\n}\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5834275313,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T14:42:03Z","updated_at":"2026-09-25T14:42:03Z","html_url":"https://github.com/objectstack-ai/objectui/issues/10616#issuecomment-5834275313","body":"## ✅ ACCEPT: objectui#10646 at `19efdc4`; ready and auto-merge follow this ACCEPT\n\n`domain:ui` seat #2, `session_014mXUNuFomfj24w7s1pZzhN`. I checked this against the diff and CI, not against the report's own account. Claim `5833485343`. It executes triage `5833415851`.\n\n```\nImplemented-by:  claude/issue-10616-view-handler-event-names\nReviewed-by:     session_014mXUNuFomfj24w7s1pZzhN\n```\n\n| item | reading |\n|:--|:--|\n| the change | `filter-ui`, `sort-ui` and `view-switcher` read their event-name key (`onChange`, `onViewChange`) through one plugin-view helper. The helper calls the prop only when it is a function, then dispatches `new CustomEvent(schema.KEY, { detail })` on `window`, with the name taken from `schema`. The authored string no longer reaches a call, so the first interaction no longer throws `TypeError: onChange is not a function` |\n| both channels | `SchemaRenderer` spreads the host's props last, so a host function keeps the prop slot while the authored name stays on `schema`. Host first, then event, as on base |\n| pins | `handlerEventNameThroughRenderer-10616.test.tsx` runs through the real `SchemaRenderer` and registry with no mock. The round-1 reviewer re-ran it: head 14/14; base 3 failed (the three `TypeError`s); a per-component ablation turns exactly its own authored leg red. The objectui#6124 pin changes in comments only |\n| `Clause-②: no` / semver | `patch` on `@object-ui/plugin-view`. The helper is not exported from the package entry; no export, schema or accept set moves |\n| published text | round 1 found one overstated changeset sentence (\"passes every node key\", while `SchemaRenderer` strips a metadata set). Round 2 changed that one sentence to \"passes these keys through\". The frontmatter md5 is `4d3ab4fd127d2f0feaabd7840d888656` at both heads. The pending objectui#6124 changeset (md5 `ccd92197c632d2b200478176ac6d1a95`, unchanged) is now true through the renderer |\n| reviews | round 1 **PASS** at `528b56d` and round 2 **PASS** at `19efdc4`, both at `CONTRACT_REVIEW_TIER`; the round-2 record is posted with this landing. The seat read both transcripts: every turn ran at tier |\n| serial | `git merge-tree` against main `f99f9cde7`: clean. No open PR touches these seven files |\n| governed | NOT GOVERNED |\n| CI | round-1 head: 40 success, 3 skipped, none red, `Test (shard 2/8)` green. Round-2 head: in progress with nothing red at the reviewer's last poll; auto-merge waits for it |\n| closing keywords | `Fixes #10616` only |\n\n### Findings, each with its disposition\n\n- Dev and round-1 reviewer: `content/docs/api/schema-reference.md`'s `ViewSwitcherSchema.onViewChange` row reads \"Expression or callback invoked on view change\", which contradicts the declared event-name contract. It predates this card (an objectui#6124 leftover) ⇒ **card owed**; the seat files it, folded with the next item.\n- Round-1 reviewer: the `packages/types/src/views.ts` JSDoc for all three keys cites \"`new CustomEvent(schema.KEY, …)` read at `plugin-view/src/ViewSwitcher.tsx` (FilterUI, SortUI)\". After this change that expression lives only in `viewHandlerChannels.ts` ⇒ **folded into the same docs card**.\n\n### Deviations recorded\n\n- **PR assignee not set.** The dev's `label-write --assign` was refused by the permission classifier. The seat does not re-issue a refused write; it is surfaced to the maintainer.\n\n### State\n\nIn one stroke: `ready` and auto-merge (squash), through the relay ops `pr_ready` + `automerge_enable`. The PR body stands as the dev wrote it: it says \"every non-metadata node key\", which is true.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
