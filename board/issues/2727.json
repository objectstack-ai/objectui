{
  "number": 2727,
  "kind": "pull_request",
  "title": "fix(plugin-charts): draw dashboard bars on first paint via one settle re-mount",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "plugin",
    "plugin: charts",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-20T04:57:59Z",
  "updated_at": "2026-07-20T05:01:45Z",
  "closed_at": "2026-07-20T04:58:23Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-20T04:58:23Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2727",
  "body": "## Problem\nDashboard bar/area/line widgets rendered **axes + labels but no bars** on first mount, only drawing after an unrelated re-render (theme toggle / resize / re-nav). Reproduced 2026-07-20 on the magic-flow rig (AI CRM `customer_status_dashboard`, dataset `ycde_customer_ds`). The analytics query returns 200 with correct rows — it's a **frontend mount-time race**, not a data bug.\n\n## Root cause (traced through recharts@3.9.2 source)\nNot a container-size bug — `ResponsiveContainer` already null-renders until its RO reports a positive box, which is why the axes/scale paint. The stuck bars are a **Recharts entrance-animation race**:\n- The bar entrance is a `requestAnimationFrame` tween that starts at height 0 (`JavascriptAnimate`: `useState(isActive ? 0 : 1)`).\n- Inside the react-grid-layout dashboard, the widget's box **settles over several frames right after mount**; the tween kicked off during that churn is interrupted before it advances past 0 → bars stuck at 0.\n- Any later re-render gives `Bar` a fresh `animationInput` ref → `useAnimationId` mints a new id → `<JavascriptAnimate key={animationId}>` remounts and replays the tween. That's why bars **appear on resize/theme-toggle**.\n\n## Fix\n`ChartContainer` (the single choke point every dashboard chart flows through) watches its own box with a `ResizeObserver`; once size changes **stop** at a positive box (80 ms debounce), it bumps a `settleNonce` used as `key` on `<ResponsiveContainer>` → **exactly one clean re-mount after the grid settles**, so the entrance animation runs uninterrupted and bars draw on first paint.\n- **One-shot** (disconnects after firing) → later window/grid resizes stay in-place, no re-animation.\n- Nonce only bumps under a real layout engine (positive stable box); headless (0×0) renders leave it at 0, so tests are unaffected.\n\n## Tests\n- New `ChartContainerImpl.settleRemount.test.tsx`: settle → exactly one re-mount; debounces mid-settle resizes into one; **never** re-mounts under 0×0.\n- `plugin-charts` **32/32** and `plugin-dashboard` **89/89** pass; package builds clean.\n\n## Verification note\nFull live magic-flow visual repro wasn't possible locally — the run-stack rig serves framework's *vendored* objectui and ignores worktree edits, so browser confirmation needs an objectui SHA-pin bump. Mechanism is confirmed from source + unit tests.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
