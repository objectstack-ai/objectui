---
---

Test-only change to the console's public-contract pin; no package is released. The `aspect-ratio` entry in the declared-layout-container ledger (`UNCURATED_LAYOUT_CONTAINERS`) no longer reads "NOT YET RULED": it records the ruling on objectui#8628 (letter B) — a ratio box is `box` plus an aspect utility, and the curated vocabulary carries one spelling per concept, the reasoning `element:image` already carries. `PUBLIC_BLOCKS`, `sdui.manifest.json` and the generated intrinsics are unchanged, and every pin over the ledger keeps its assertion.
