{"id":5748065370,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-20T06:14:37Z","updated_at":"2026-09-20T06:14:37Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9217#issuecomment-5748065370","body":"**`pm:queue` → `pm:on-hold`** by the `domain:ui#2` execution seat (PM session `session_018HrVaotisyhgmot9o2MLRq`), 2026-09-20T06:14Z. Reason: this card's acceptance ③ forbids inventing the reason-code shape, and **the shape is not installable here yet**. Source of the decision: this seat's own reading, taken while picking batch slot 3/3 — ⛔ no maintainer ruling is being interpreted, and ⛔ the priority and the lane are untouched.\n\nRestart-when: the `@objectstack/spec` version objectui's `pnpm-lock.yaml` resolves publishes a `FlowRuntimeState` carrying the policy reason — pull that version's tarball and read `dist/contracts/index.d.ts` (17.4.0 does not carry it)\n\n## The two readings that put it here\n\n**① The producer half EXISTS on objectstack `main`** (`cb005e06ae`, read in this act):\n\n- `packages/services/service-automation/src/engine.ts` keeps a `policyDisabledFlows` set and returns `SCHEDULED_WORK_DISABLED_REASON` for a name in it.\n- `packages/spec/src/contracts/automation-service.test.ts` pins the distinction this card needs: a `policyDisabled` `FlowRuntimeState` whose `reason` is defined and does **not** match `/binding failed/`.\n\n**② The consumer cannot install it.** objectui's `pnpm-lock.yaml` resolves `@objectstack/spec` to **17.4.0**, and `npm view @objectstack/spec version` answers **17.4.0** — the newest published version **is** the one already installed. That tarball's `dist/contracts/index.d.ts` declares:\n\n```ts\ninterface FlowRuntimeState {\n    name: string;\n    enabled: boolean;\n    bound: boolean;\n    status?: string;\n    triggerType?: string;\n    object?: string;\n}\n```\n\n⇒ **no `reason`**, and `SCHEDULED_WORK_DISABLED_REASON` appears nowhere in the published package. ⇒ 「上游已合并」 is true and 「消费方可安装」 is false, and the skill's cross-repo rule takes the second: hold, ⛔ not queue.\n\n⚠️ **Instrument note, because this seat's FIRST reading of it was wrong.** Grepping the tarball's `src/` alone returned **0** for `FlowRuntimeState` — the published package ships no `src/contracts/` at all, so that zero was a corpus artefact, ⛔ not an absence. The reading above is from the whole package, with a published symbol (`ShareRecipientType`, found in `src/` and in `dist/browser/`) lit as the control. ⭐ Anyone re-taking this must search the **whole package**, ⛔ not `src/`.\n\n## The dispatch shape, pre-written so the restart is a dispatch and not a re-investigation\n\n1. Re-take reading ② (whole tarball, control lit). If `reason` is there, note its exact spelling and the constant's value.\n2. Surface: `packages/app-shell/src/views/setup/packagedFlows.ts` and the Setup flows view that renders its rows, plus tests.\n3. ⚠️ That module's `FlowRuntimeStateRow` is deliberately `[key: string]: unknown` with every field optional (\"older backends answer with fewer of them\") — so the new key is read **defensively**, and the rendering must still degrade for a backend that sends nothing.\n4. Acceptance stays the card's own three clauses; ⛔ the policy sentence is rendered from the platform's string, never from a second copy of it in this repo.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
