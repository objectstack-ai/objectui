---
---

Internal tooling only (`scripts/**`): the cross-file line-citation census and its
differential gate gain a declared fixture-address class. No published package source,
no `files[]` entry and no publish-contract field moves, so nothing releases.
