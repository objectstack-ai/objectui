{
  "number": 2550,
  "kind": "pull_request",
  "title": "feat(kanban): accept CEL { condition, style } conditional-formatting rules (#1584 follow-up)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "package: types",
    "plugin",
    "plugin: kanban",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-16T01:30:03Z",
  "updated_at": "2026-07-16T02:48:25Z",
  "closed_at": "2026-07-16T02:48:23Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-16T02:48:23Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/2550",
  "body": "## Summary\n\nFollow-up to #1584 (merged in #2544). That PR moved kanban card styling onto the shared CEL evaluator (`resolveConditionalFormatting`), so the **runtime** already accepts the spec `{ condition, style }` rule shape on kanban cards — but the **type + zod schema** still only allowed the native `{ field, operator, value }` shape. So an author who wrote a CEL kanban rule got a validation error for something that works at runtime. This closes that type/runtime gap.\n\n## What changed\n\n- **`@object-ui/types`**\n  - `KanbanConditionalFormattingRule` is now a union: `KanbanNativeConditionalFormattingRule | SpecConditionalFormattingRule` (the native field/operator/value shape, or the spec `{ condition, style }` CEL shape) — matching list/grid `ConditionalFormattingRule`.\n  - `ObjectKanbanSchema`'s zod `conditionalFormatting` widened to a `z.union` of both shapes.\n  - Exports the new `KanbanNativeConditionalFormattingRule`.\n- **`@object-ui/plugin-kanban`** — the four narrow local/inline `conditionalFormatting` types (KanbanImpl, KanbanEnhanced, types.ts, index.tsx) now reference the shared `@object-ui/types` type instead of duplicating a narrow shape.\n\n## Back-compat\n\nThe native `{ field, operator, value }` shape keeps validating and type-checking unchanged; the change is purely additive (a union arm).\n\n## Tests\n\n`packages/types/.../kanban-conditional-formatting.test.ts` — zod accepts native, CEL `{ condition, style }`, and a mixed list; type-level assertion that `KanbanConditionalFormattingRule` admits both shapes. Full types + plugin-kanban suites pass (187 tests); `turbo type-check` green.\n\n## Context — other #1584 follow-ups considered\n\nWhile scoping this, I evaluated the follow-ups I'd suggested and made deliberate calls:\n- **Perf** (a program cache in `@objectstack/formula`): **measured, not warranted.** Benchmark showed CEL formatting at ~155µs/row (3 rules) vs ~26µs/row legacy — ~31ms for a 200-row × 3-rule full pass, i.e. single-digit ms at realistic page sizes / with virtualization. Not a hotspot; no framework change.\n- **Studio CEL editor for the list conditional tier** (advancing #1582): **deferred as a designed feature, not built here.** Investigation found the conditional-formatting *authoring UI does not exist* in Studio at all (it's not in the view schema the inspector renders), so it's a new feature needing design — while action `visible`/`disabled` and field `*When` already get Studio CEL authoring via `ConditionBuilder`. Better tracked under #1582 than shipped speculatively.\n- **Formatting live e2e**: needs framework showcase metadata (out of objectui scope per #1584).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_019LwrWThBJgvcfPtqNHpstB)_"
}
