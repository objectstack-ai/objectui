{"id":5730461162,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-18T13:10:04Z","updated_at":"2026-09-18T13:10:04Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9806#issuecomment-5730461162","body":"Claim: objectui#9806 — `domain:ui#2` execution seat\n\nSeat: `domain:ui#2`\nSession: `session_018HrVaotisyhgmot9o2MLRq`\nBranch: `claude/issue-9806-objectview-modifier-click-comment`\nClaimed at 2026-09-18T13:10Z.\n\n## Declared file surface\n\n`packages/plugin-view/src/ObjectView.tsx` and `packages/react/src/hooks/useNavigationOverlay.ts`, plus tests in those two packages and one `.changeset/` entry. ⛔ Nothing else without reporting first.\n\n## 在飞相交检查 — clean, and the two apparent hits were run down rather than waved off\n\nA probe of the in-flight `domain:ui` cards for `plugin-view` / `ObjectView` returned **2 comment hits**, on objectui#9533 and objectui#7206. Both were checked against the **PR file lists**, ⛔ not the prose (说明 ⑮):\n\n| | PR | subject hits | control |\n|:--|:--|--:|:--|\n| objectui#9533 (seat 1) | objectui#9803 | **0** of 15 files | 15 filenames listed, so the list is readable |\n| objectui#7206 (this seat) | objectui#9876 | **0** of 19 files | the 19-file list read in full at landing review |\n\n⇒ both hits are **prose**, ⛔ not declared surfaces. No serialization owed.\n\n## 前提过时检查, and ⚠️ the thing this card is really about\n\nThe defect is a **comment added by PR objectui#9797** whose closing sentence is false on the path it describes — 「what Cmd/Ctrl/middle-click should do when no host handler is present is the hook's own decision, taken before this callback runs」 — while `handleClick` returns EARLY on the handler `ObjectView` always passes.\n\n⭐ ⚠️ **This is the lane's most repeated defect class (说明 ⑳, six instances today) in its purest form: a sentence that publishes and is false.** ⇒ the deliverable is ⛔ not only 「fix the comment」. The dev settles, and says which:\n1. Is the **comment** wrong (⇒ rewrite it to what the code does), or is the **code** wrong (⇒ the early return is the defect and Cmd/Ctrl-click genuinely should reach the hook)? ⚠️ Those have opposite deliverables and the card leans one way; ⛔ the dev measures rather than inheriting the lean.\n2. ⭐ If it is the comment: **what makes the next one not happen?** A rewritten sentence with no instrument is the same defect deferred.\n\n⚠️ ⛔ This seat does not answer either. The card was filed by a reviewing seat that explicitly flagged it as ⛔ **not a re-spin cause** — PR objectui#9797 landed on its own merits, and that judgement is ⛔ not reopened here.\n\n<sub>`domain:ui#2` execution seat · `session_018HrVaotisyhgmot9o2MLRq` · claim · readings taken 2026-09-18T13:10Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5730876043,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-18T13:44:04Z","updated_at":"2026-09-18T13:44:04Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9806#issuecomment-5730876043","body":"os-dev-report\n\n```json\n{\n  \"issue\": 9806,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9806-objectview-modifier-click-comment\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/9893\",\n  \"session\": \"session_018HrVaotisyhgmot9o2MLRq\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Settled by reading, not inherited: the COMMENT is wrong, the code is not. At origin/main 6778809f5, useNavigationOverlay.handleClick returns early on `onRowClick` (calling it with the record and the event, then `return;`) BEFORE the `event.metaKey || event.ctrlKey || event.button === 1` branch, and plugin-view's ObjectView hands handleRowClick down unconditionally at both sites (the renderListView host slot and the default ObjectGrid), each of which forwards it straight into the hook as its onRowClick. So the hook's modifier branch is unreachable from this component and the sentence describes a delegation that never happens. Option (2) is REFUSED on the reading rather than deferred: the early return is exactly what objectui#9357 / #9360 / #9462 exist to provide, and inverting it would let a modifier branch outrank every host handler. What remains is a behaviour QUESTION on a published component, not a defect in this code. Delivered: the amended comment, a behavioural pin it cites by name, and one empty-frontmatter changeset.\",\n  \"comment_vs_code\": {\n    \"verdict\": \"the comment is wrong\",\n    \"hook_lines_origin_main_6778809f5\": [\n      \"299:      if (onRowClick) {\",\n      \"300:        onRowClick(record, event);\",\n      \"301:        return;\",\n      \"302:      }\",\n      \"308:      const isModifierClick = !!(\",\n      \"309:        event && (event.metaKey || event.ctrlKey || event.button === 1)\",\n      \"310:      );\"\n    ],\n    \"objectview_hand_down_lines\": [\n      \"2226:        onRowClick: handleRowClick,   (inside the renderListView host-composition call)\",\n      \"2264:        onRowClick={handleRowClick}   (the default ObjectGrid branch)\"\n    ],\n    \"receivers_forward_it_into_the_hook\": [\n      \"packages/plugin-grid/src/ObjectGrid.tsx:2253  onRowClick,   (inside useNavigationOverlay({ ... }))\",\n      \"packages/plugin-list/src/ListView.tsx:2484    onRowClick,   (inside useNavigationOverlay({ ... }))\"\n    ]\n  },\n  \"user_visible_consequence\": \"A Cmd/Ctrl/middle-click on an ObjectView row NAVIGATES IN PLACE and opens no browser tab of its own; more precisely it is INDISTINGUISHABLE from a plain click. Determined by DRIVING it, not by reasoning: the new pin renders the real ObjectView default branch with no host onRowClick, fires fireEvent.click with metaKey/ctrlKey/button:1 and with nothing, and reads the destination off an onNavigate spy — plain and modifier both reach onNavigate('r1','view'), never 'new_window'. A liveness control (bare ObjectGrid, no onRowClick) fires the same events and DOES diverge to 'new_window', so the equivalence is a reading and not a dead probe. NOT driven in a browser. SECOND FACT, not in the card: the gridSchema ObjectView builds carries neither `navigation` nor `onNavigate`, so even with the early return deleted the hook's modifier branch would find no destination here and fall through — a future behaviour card cannot be only 'delete the early return'.\",\n  \"instrument_left_behind\": \"packages/plugin-view/src/__tests__/ObjectView.modifierClickInPlace-9806.test.tsx, 11 cases, cited BY NAME in the amended comment, and the pin reads that citation back out of ObjectView.tsx with its own name derived from import.meta.url (so a rename reds too). SUBJECT: plain click and modifier click on a real ObjectView row asserted indistinguishable. LIVENESS CONTROL: bare ObjectGrid, no onRowClick — its two clicks diverge. ISOLATION CONTROL: the SAME ObjectGrid call with exactly one prop added (an unconditional onRowClick), which is what attributes the subject's equivalence to the early return rather than to the composition; the file states explicitly that the liveness control does NOT vary one thing, because claiming it did would be this card's own defect. SOURCE-TEXT: comments stripped (the subject file documents the construct asserted on), strip controlled in BOTH directions, and the hook presence leg keyed on the READ spelling `event.metaKey` with `metaKey?: boolean` pinned beside it so a member declaration can never pass for a read.\",\n  \"tests\": \"new pin alone: 'Test Files 1 passed (1)', 'Tests 11 passed (11)'. Cross-tree sweep through /home/user/objectstack/scripts/pm/os-verify-lock.sh: 'Test Files 81 passed (81)', 'Tests 1739 passed (1739)', 'os-verify-lock: VERDICT command-exit 0 · held the lock 93s · waited 17s'. plugin-view lint exit=0 (373 pre-existing warnings, 0 errors); plugin-view type-check exit=0 after 'pnpm --workspace-concurrency=2 --filter \\\"@object-ui/plugin-view^...\\\" build' (VERDICT command-exit 0) — before that build it exited 2 with 'Cannot find module @object-ui/components', a PREREQUISITE NOT MET and not a red gate. Gates exit 0: check-changeset-presence (empty frontmatter accepted), test-path-roots, new-line-citations (0 new), control-bytes, comment-mask-corpus, handler-key-reads, icon-record-names, doc-example-readers, changeset-claims, pending-changeset-literals, unreferenced-sources, vi-mock-specifiers. ABLATIONS (mutation landed proven by blob hash; restore proven by hash equality AND empty `git diff HEAD`; trap EXIT/INT/TERM, absolute paths; every direction predicted before the run and every prediction matched): A revert ObjectView.tsx to 6778809f5 (the false comment back) -> 2 failed / 9 passed, exactly the handshake and the raw-bytes stripper control; B gate handleClick's early return on non-modifier -> 4 failed / 7 passed, exactly the two subject modifier cases, the ordering case and the isolation control; C make the hand-down conditional -> 3 failed / 8 passed, the three subject cases; D make the comment stripper the identity function -> 1 failed / 10 passed, only the whole-file stripper case. GREEN-WHERE-RED-WAS-NOT-EXPECTED, reported: in ablation A every BEHAVIOURAL case stayed green — it must, this PR changes no behaviour; the legs that were red before this change are the handshake and the raw-bytes control. Also reported: ablation D proved the comment strip is load-bearing ONLY for the whole-file case — the handleRowClick body slice starts at the callback, so the comment above it is already outside the text read and that case is green either way; the pin's header now says so instead of the pleasant version.\",\n  \"cross_tree_sweep\": \"Derived, not guessed. The removed sentence was searched over EVERY tracked path at origin/main with comment markers stripped and newlines collapsed: exactly one hit, packages/plugin-view/src/ObjectView.tsx, with a same-run control (a different line-wrapped // comment in useNavigationOverlay.ts) that fired. That took three attempts — the first two published-shaped probes returned a CONFIDENT ZERO because the sentence is line-wrapped and every line carries a // prefix; the form reported here is the one that was re-run. Tests were then selected by whether they READ either changed file at runtime (readFileSync / sourceOf), with a control file that reads none: app-shell ObjectView.relayRungCensus-7559, the two react hooks arity pins and the rest of packages/react/src/hooks/__tests__/, six packages/types/src/__tests__/ readers, examples/schema-catalog/test/catalog-gallery-render, three scripts/__tests__/ files, the objectui#9462 behavioural pin in app-shell, and the whole of packages/plugin-view. All ran; all green.\",\n  \"pending_changeset_sweep\": \"The one pending entry on this subject is .changeset/9462-row-click-modifier-payload-forward.md, frontmatter declaring @object-ui/components + plugin-view + types as minor (a package-declaring one, so it DOES publish a CHANGELOG line). NOT falsified by this diff: every claim in it is scoped to a host that supplies a handler ('reaches a host handler', 'a host wiring a modifier-click handler ... always read undefined'), which is exactly the boundary this card sits outside of. No edit made to it. Three other entries mention Cmd/Ctrl/middle-click (9341-kanban-card-click-fires-once, and the two 9357 arity ones); all describe the payload's DELIVERY, which this diff does not touch.\",\n  \"closing_keyword\": \"Part of #9806, NOT a closing keyword. The behaviour half survives the merge — whether ObjectView should implement the modifier convention itself, and the gridSchema navigation gap that a repair would also have to answer. The PR body was scanned before and after publishing: 0 closing keywords anywhere, and the only other '#2'-shaped token sits inside a code span (`domain:ui#2`) so no stray cross-reference was created.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called, read or write\",\n  \"api_writes\": \"2 — POST /repos/objectstack-ai/objectui/pulls (the draft PR), POST /repos/objectstack-ai/objectui/issues/9806/comments (this report). No label endpoint was called: the dispatch named no label, and objectui's only same-named label is the one this seat is forbidden to apply here. 3 git pushes are not REST writes.\",\n  \"open_questions\": [\n    {\n      \"question\": \"Should plugin-view's ObjectView implement the Cmd/Ctrl/middle-click convention itself, given that the hook cannot do it for any component that supplies an unconditional handler?\",\n      \"options\": [\n        \"A — leave it. A modifier click stays indistinguishable from a plain click; the comment now says so and a pin holds it. Costs nothing, matches today's shipped behaviour, and leaves the convention unavailable on the most-used list surface.\",\n        \"B — implement it in handleRowClick. This is a behaviour change on a published component AND it needs more than a branch: the gridSchema ObjectView builds carries neither navigation nor onNavigate, so a modifier branch there has no destination to route to and one must be added.\",\n        \"C — invert the hook so its modifier branch runs before the early return. REFUSED here on the reading: it would take the decision away from every host that already supplies a handler, which is exactly what objectui#9357 / #9360 / #9462 built.\"\n      ],\n      \"recommendation\": \"A for this PR (it is what landed), with B as its own card if the convention is wanted on ObjectView. B's real cost is the gridSchema relay, not the branch — that is the part a one-line reading of this card would miss, and it is measured rather than guessed.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: useNavigationOverlay.ts's own comment above the early return says the payload is forwarded 'so parent handlers (e.g. ObjectView) can still implement Cmd/Ctrl/middle-click'. That is a CAPABILITY claim and it is true — ObjectView simply does not take it up — so it is adjacent to this card's class without being an instance of it, and it was left alone rather than edited on the way past. Successor: the behaviour card, if one is opened; otherwise nobody.\",\n    \"noted, not filed: the gridSchema ObjectView builds relays neither navigation nor onNavigate to the ObjectGrid it renders, so the hook's navigation modes are unreachable on that branch by a second, independent route. Not a defect on its own evidence (the view answers clicks through its own handleRowClick instead) and squarely inside the behaviour question above, so it is reported there rather than filed separately. Successor: whoever takes the behaviour half.\"\n  ],\n  \"refused\": \"Refused to grow the repair into the behaviour change, per the dispatch's stop-and-report instruction — but the reading did not reach option (2), so this is a refusal of the SHAPE, not a deferral of a defect. Refused to edit .changeset/9462-row-click-modifier-payload-forward.md. Refused to mark the PR ready, enable auto-merge or merge. Refused to apply any label.\",\n  \"dispatch_items_that_did_not_survive_reading\": [\n    \"The dispatch framed the choice as 'the comment is wrong' vs 'the code is wrong, the early return is the defect'. The second is not merely unsupported, it is CONTRARY to three landed cards: the early return is the mechanism objectui#9357 / #9360 / #9462 built so a host handler receives the modifier payload and decides for itself. What survives of that half is a behaviour QUESTION about whether ObjectView should implement the convention, not a defect in the early return.\",\n    \"The dispatch said the card 'leans toward (1)'. It does, and the reading agrees — but for a reason the card does not give: the card's remedy note records 'the fix would be a branch in handleRowClick', and that is incomplete, because the grid schema this component builds carries no navigation destination for such a branch to use.\",\n    \"The dispatch pointed at scripts/__tests__/body-dialect-census.test.ts's readCode helper as prior art for stripping comments. It is, and the helper is reused — but stripping turned out NOT to be load-bearing for the body-slice assertion (the comment sits above the callback), and that was only visible because the stripper itself was ablated. The pin says so rather than implying the strip is doing work it is not.\",\n    \"The dispatch's reading-discipline item 5 was earned live: two successive whole-tree probes for the removed sentence returned a confident zero, first because the sentence is line-wrapped and then because each line carries a // prefix. The probe was suspected first, corrected twice, and the published form is the one that was re-run with a control.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5730922613,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-18T13:47:48Z","updated_at":"2026-09-18T13:47:48Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9806#issuecomment-5730922613","body":"## ACCEPT — PR objectui#9893 is ready + auto-merge. ⭐ The card's OTHER half survives the merge, so this card goes to the decision box rather than closing.\n\nSeat `domain:ui#2`, `session_018HrVaotisyhgmot9o2MLRq`. `Part of #9806`, ⛔ **not** a closing keyword — verified: `Fixes` / `Closes` / `Resolves` all **absent** from the PR body, `Part of` **present**. `check-governed-merges.mjs --test` on the FINAL 3-path list: **0 of 3**.\n\n## 维护者速读\n\n这张卡原本说:`ObjectView` 上有一句注释是假的 —— 它写着「没有宿主处理器时,修饰键点击由 hook 自己决定」。**注释确实是假的,已修,并留下了会红的仪器。** 那一半做完了。\n\n⚠️ 但量出来的**行为**是:在 `ObjectView` 的行上 **Cmd/Ctrl/中键点击,和普通点击完全没有区别** —— 不会开新标签页。这是**驱动出来的**,不是推理:同一个测试里,一个裸 `ObjectGrid`(不带处理器)的同样两次点击**确实分叉**到 `new_window`。\n\n⇒ 剩下的问题是产品问题:**`ObjectView` 要不要自己实现「修饰键点击开新标签」这个浏览器惯例?**\n\n- **A 不做**(本次落地的状态):修饰键点击继续等同普通点击,注释现在如实这么说,并有 pin 钉住\n- **B 做**:⚠️ 这是对**已发布组件**的行为变更,而且**不止加一个分支** —— `ObjectView` 造的那张 gridSchema **既没有 `navigation` 也没有 `onNavigate`**,所以分支根本没有目的地可去,得先补上这条中继\n- **C 反转 hook** —— ⛔ dev 按读数**当场拒绝**了,我同意:那会把决定权从**每一个已经提供处理器的宿主**手里夺走,而那正是 objectui#9357 / #9360 / #9462 造出来的东西\n\n**问:A 还是 B?**\n\n## ⭐ The verdict was SETTLED by reading, ⛔ not inherited from the card's lean\n\nRe-read by this seat at source on `origin/main` `4b577229`, `packages/react/src/hooks/useNavigationOverlay.ts`:\n\n```\n:299    if (onRowClick) {\n:300      onRowClick(record, event);\n:301      return;\n:302    }\n…\n:308    const isModifierClick = !!(\n:309      event && (event.metaKey || event.ctrlKey || event.button === 1)\n:310    );\n```\n\n⇒ the early return sits **ahead** of the modifier branch, and `ObjectView` hands `handleRowClick` down **unconditionally** at both sites (`:2226` in the `renderListView` host-composition call, `:2264` in the default `ObjectGrid` branch), each of which forwards it into the hook as its `onRowClick`. ⇒ the hook's modifier branch is **unreachable from this component**, and the sentence describes a delegation that never happens.\n\n⭐ **The dev refused option (2) ON THE READING, ⛔ not by deferring it.** The early return is not a defect — it is the mechanism three landed cards built. What is left is a behaviour **question**, ⛔ not a bug in that code. ⇒ this seat's dispatch framed the choice as 「comment wrong vs code wrong」 and the second horn was **contrary to landed work**; the dev said so instead of picking it.\n\n## ⭐ The second fact, which is ⛔ NOT on the card and changes what option B costs\n\nThe `gridSchema` that `ObjectView` builds carries **neither `navigation` nor `onNavigate`**. ⇒ even with the early return deleted, the hook's modifier branch would find **no destination** here and fall through.\n\n⇒ ⭐ **a future behaviour card cannot be 「delete the early return」, and it cannot be 「add a branch to `handleRowClick`」 either.** The card's own remedy note says the fix would be a branch; that is **incomplete**, and the missing part is the gridSchema relay. Measured, ⛔ not guessed — and it is exactly the part a one-line reading of this card would have shipped wrong.\n\n## ⭐ The instrument, which was the half this seat asked for\n\n`packages/plugin-view/src/__tests__/ObjectView.modifierClickInPlace-9806.test.tsx`, 11 cases, **cited by name in the amended comment** — and the pin **reads that citation back out of `ObjectView.tsx`**, with its own name derived from `import.meta.url`, ⇒ **a rename reds it too**. That is a real answer to 「what makes the next false sentence redden」, ⛔ not a rewritten sentence with nothing behind it.\n\nThree legs, each doing a different job:\n- **subject** — plain click and modifier click on a real `ObjectView` row asserted **indistinguishable**;\n- **liveness control** — a bare `ObjectGrid` with no `onRowClick`, whose two clicks **do** diverge to `new_window` ⇒ the equivalence is a reading, ⛔ not a dead probe;\n- **isolation control** — the *same* `ObjectGrid` call with exactly **one** prop added, which is what attributes the equivalence to the early return rather than to the composition. ⭐ The file states outright that the liveness control does ⛔ **not** vary one thing, 「because claiming it did would be this card's own defect」.\n\n⭐ And the source-text leg is 说明 ㉑ done correctly: comments stripped (the subject file documents the construct it asserts on), the strip controlled in **both** directions, and the hook-presence leg keyed on the **read spelling** `event.metaKey` with `metaKey?: boolean` pinned beside it **so a member declaration can never pass for a read**.\n\n## Four ablations, every direction predicted BEFORE the run\n\nA (revert the file → the false comment back): **2 failed / 9 passed**, exactly the handshake and the raw-bytes control. B (gate the early return on non-modifier): **4 failed / 7 passed**. C (make the hand-down conditional): **3 failed / 8 passed**. D (make the comment stripper the identity): **1 failed / 10 passed**.\n\n⭐ **Two honesty items reported rather than smoothed:**\n- in ablation A every **behavioural** case stayed green — it must, this PR changes no behaviour — and the dev said so instead of implying the pin caught a behaviour regression;\n- ablation D proved the comment strip is load-bearing **only** for the whole-file case, because the `handleRowClick` body slice starts at the callback and the comment above it is already outside the text read. ⇒ **the pin's header now says that instead of the pleasant version.**\n\n## ⚠️ The cross-tree sweep took THREE attempts, and the first two were confident zeros\n\nSearching the removed sentence over every tracked path returned **0** twice: first because the sentence is **line-wrapped**, then because every line carries a `//` prefix. The published form is the re-run one, with a same-run control (a different line-wrapped `//` comment in `useNavigationOverlay.ts`) that fired, and it finds **exactly one** hit.\n\n⇒ ⭐ 说明 ⑯ again — **this lane's count is now ELEVEN**, and this one was earned against a dispatch that named the hazard in advance. Naming it did ⛔ not prevent it; the **control** caught it.\n\n## The pending-changeset sweep, and why nothing was edited\n\n`.changeset/9462-row-click-modifier-payload-forward.md` declares three packages **minor** ⇒ it **publishes**. ⛔ It is **not** falsified: every claim in it is scoped to 「a host that supplies a handler」, which is precisely the boundary this card sits **outside**. Three other entries mention modifier clicks and all describe the payload's **delivery**, which this diff does not touch. ⇒ ⛔ nothing edited, and the reasoning is on the record rather than the conclusion alone.\n\n## The four facets, for the A-vs-B decision\n\n**① 项目长远合理性** — the browser convention is worth having on the most-used list surface. ⚠️ But B is a behaviour change on a **published** component plus a new relay, and the hook already offers the capability to any host that wants it.\n\n**② 实际业务拉动** — real but small: a user cannot fan records into tabs from an `ObjectView`. ⛔ No incident, ⛔ no report; the defect that was **filed** was the sentence, and that is fixed.\n\n**③ 防 AI 犯错** — ⭐ points at **A having already delivered the valuable half**: the false sentence is gone and an instrument reddens if it returns. B adds no such protection; it adds behaviour.\n\n**④ 创业阶段不扩散** — A is landed and costs nothing further. B is a published-component behaviour change **plus** the gridSchema relay ⇒ its own card, its own fence, ⛔ never a rider on a comment repair.\n\n⇒ this seat's reading is **A now, B as its own card if the convention is wanted** — offered as a reading, ⛔ not as a recommendation to be adopted unread. ⛔ 功能新增 on a published component is 人工地板, so this seat does ⛔ not take it through the 代裁 channel.\n\n## Card state\n\n`pm:dispatched` off, `needs-user-decision` on, assignee cleared — the comment half is **delivered and landing**; what remains is a decision, ⛔ not work waiting for a dev. `priority:p2` and `domain:ui` untouched.\n\n<sub>`domain:ui#2` execution seat · `session_018HrVaotisyhgmot9o2MLRq` · review verdict · readings taken 2026-09-18T13:47Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5732403462,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-18T15:41:38Z","updated_at":"2026-09-18T15:41:38Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9806#issuecomment-5732403462","body":"## ⭐ LANDED — PR objectui#9893 merged 2026-09-18T15:35:16Z. The card stays in the decision box for the behaviour half.\n\nSeat `domain:ui#2`, `session_018HrVaotisyhgmot9o2MLRq`. The review verdict is `5730922613`.\n\n### Verified BY CONTENT\n\n`origin/main` `33e1697c`: the pin `packages/plugin-view/src/__tests__/ObjectView.modifierClickInPlace-9806.test.tsx` present (blob `a9d86772`); the pin's **name cited inside `ObjectView.tsx`** — **1** hit, which is what makes a rename redden; control `handleRowClick` in the same file **8**.\n\n### ⚠️ This PR took a detour worth recording\n\nIt was **ejected from the merge queue** at 14:21:57Z — its queue commit carried a **cancelled** `Test (shard 1/4)`, not a failed one. ⛔ Nothing on the PR said so: it read `clean`, 34/34 green, with `auto_merge: null` — which is **also** what an enrolled-and-queued PR reads.\n\n⇒ ⭐ the seat mis-diagnosed it as a silent stall, wrote the wrong discriminator into its own check-in, and made one redundant re-enrolment write before the **timeline** settled it: `added 14:00:56Z · removed 14:21:57Z · added 14:34:14Z`, then merged. The 14:34 re-enrolment had worked all along.\n\n⚠️ **The authority for merge-queue state is `GET /issues/{n}/timeline`**, ⛔ not the `auto_merge` field (cleared on enqueue) and ⛔ not the queue refs (which exist only for entries being actively built). And an **ejection** is a `removed_from_merge_queue` with **no `merged` event beside it** — ⛔ the actor is not the discriminator, since that same bot posts the removal on a successful merge too.\n\n### ⛔ The card stays open\n\n`Part of`, and the behaviour half survives: **should `ObjectView` implement the Cmd/Ctrl/middle-click convention itself?** ⚠️ And the cost is ⛔ not a branch — the `gridSchema` this component builds carries **neither `navigation` nor `onNavigate`**, so a modifier branch there has no destination. `needs-user-decision` stands.\n\n<sub>`domain:ui#2` execution seat · `session_018HrVaotisyhgmot9o2MLRq` · landing verification · readings taken 2026-09-18T15:41Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5749198566,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-20T10:21:10Z","updated_at":"2026-09-20T10:21:10Z","html_url":"https://github.com/objectstack-ai/objectui/issues/9806#issuecomment-5749198566","body":"Ruling: batch #196 item 4 · letter B · maintainer 「同意」 2026-09-20T10:21Z\n\nDirector seat, summon #25, `session_012GcsUbuqFGBibkEDMRC1eE`. Presented in detail with the recommendation **B**; the maintainer agreed 「同意」. Thread re-read to its last comment (5732403462) in the act that wrote this. Standing: the false comment is fixed and pinned (PR objectui#9893, `ObjectView.modifierClickInPlace-9806.test.tsx`, 11 cases); the hook's early return on a host `onRowClick` is the mechanism objectui#9357 / #9360 / #9462 built and stays; `plugin-view`'s `ObjectView` hands `handleRowClick` down unconditionally at both sites, its own branches read no event, and the `gridSchema` it builds carries neither `navigation` nor `onNavigate`, so a modifier branch there has no destination today.\n\n## Ruling — **B**: `ObjectView` implements the browser convention itself\n\n`plugin-view`'s `ObjectView` honours Cmd / Ctrl / middle-click on a row by opening the record as a full page in a new browser tab, the way a bare `ObjectGrid` already does: `handleRowClick` reads the event and takes the modifier branch; the `gridSchema` it builds gains the `navigation` / `onNavigate` relay so that branch has a destination (the `renderListView` host's `onNavigate`, or the component's own route builder where no host supplies one). ⛔ **C refused** (inverting the hook's early return takes the decision away from every host that supplies a handler — the dev's reading, endorsed); **A refused** (the most-used list surface without the convention every list has).\n\nFour-facet reading (this seat's own): ① the browser convention on the most-used list surface, consistent with `ObjectGrid`; the hook already offers it to any host, and `ObjectView` is our own host; ② real but small, no incident; ③ today a modifier click silently does nothing different — visible, no data risk; after B the pin flips from 「indistinguishable」 to 「diverges to `new_window`」; ④ one relay and one branch, no new concept.\n\n`Prior rulings read:` objectui#9357 / #9360 / #9462 (the payload channel and the early return — kept), thread: 5.\n\n## Execution, same stroke\n\n`needs-user-decision` → `pm:queue`; `priority:p2` · `domain:ui` stand. One PR: the relay, the branch, the pin's subject cases inverted (liveness and isolation controls kept); `Clause-②: yes` (published component behaviour); changeset `minor`.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
