---
'@object-ui/types': patch
---

The whitespace pin. This claim wraps at eighty columns, which is where this
corpus wraps, so the schema and the key it predicates land on different lines
and a line-anchored probe reads past both of them: `SpinnerSchema`
declares `color` for the glyph, as it has since the
node was introduced.
