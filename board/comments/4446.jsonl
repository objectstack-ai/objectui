{"id":5275194649,"author":{"login":"yinlianghui","id":6219465,"type":"User"},"created_at":"2026-08-13T02:23:37Z","updated_at":"2026-08-13T02:23:37Z","html_url":"https://github.com/objectstack-ai/objectui/issues/4446#issuecomment-5275194649","body":"**CLAIM** — session `session_017Qqyix2QcnpUC9XeYVDzx3`, branch `claude/issue-4446-permission-matrix-writable`.\n\n**Ruling (delegated authority; open veto window): provenance-gated.** The card's own trap is the structure:\n\n1. **Phase 1 — pin provenance (gates everything)**: establish whether the two pinning suites (`readonlyHeaderBadge.test.tsx:17-19`, `readonly.test.tsx:153`) encode a DECISION or merely DESCRIBE the code. Evidence: git blame → the PRs that introduced them → their issues/ruling comments. An explicit on-record ruling that `permission` must gate stricter than the server ⇒ **STOP and report** — the card becomes a decision-inbox item, not a patch.\n2. **Phase 2 — if descriptive (the expected outcome)**: align the outlier with the repo's own three-site convention (`allowOrgOverride || allowRuntimeCreate` — DirectoryPage ×2, EmbeddedItemEditor), resolving the badge/controls divergence DELIBERATELY: both read the same disjunction, one answer — no half-migration. The pins update alongside, citing this card. **Banner honesty is part of the fix**: when genuinely read-only, the text names the ACTUAL cause (per-type `allowOrgOverride:false` without `allowRuntimeCreate`), not `OS_METADATA_WRITABLE not enabled`; i18n per convention.\n3. **Verification against the server's truth**: the card measured `PUT /api/v1/meta/permission/<n>?package=<pkg>` → 200 via `allowRuntimeCreate` — mirror what the sibling sites trust (the directory entry's own flags), do NOT invent a new probe.\n\n**Red-first**: stock-boot shape (`allowOrgOverride:false`, `allowRuntimeCreate:true`) → pre-fix every control disabled + the wrong banner (capture); post-fix editable, Save present. Must-not-change (green both sides): a package with BOTH flags false stays read-only with the honest banner; `readOnly` prop still wins; org-override packages unchanged.\n\n**Grading**: patch `@object-ui/app-shell` (`.d.ts` measured). Never major.\n\n**Surface (mutual exclusion)**: `packages/app-shell/src/views/metadata-admin/PermissionMatrixEditor.tsx` + its two pin test files + one changeset. Disjoint from in-flight #4475v (organizations/manage), #4468 (date sites), #4419 (plugin-detail), #4497 (chart-series). ⛔ NOT: DirectoryPage/EmbeddedItemEditor (the correct siblings — read-only reference), `content/docs/releases/`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_017Qqyix2QcnpUC9XeYVDzx3)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
