{
  "number": 10310,
  "kind": "pull_request",
  "title": "fix(fields): report a failed ImageField upload instead of leaking the rejection",
  "state": "closed",
  "state_reason": null,
  "draft": true,
  "author": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "labels": [
    "package: fields",
    "tests"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-24T17:05:29Z",
  "updated_at": "2026-09-24T19:11:21Z",
  "closed_at": "2026-09-24T19:11:19Z",
  "closed_by": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "merged_at": "2026-09-24T19:11:18Z",
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectui/pull/10310",
  "body": "Fixes #10226\nClause-②: no\n\n## What changed\n\n`ImageField` (`@object-ui/fields`) has two upload paths: the native picker (`handleFileChange`) and the crop dialog's confirm (`handleCropConfirm`). On `main` at `8b1f066` both ran the upload inside `try … finally` with **no `catch`**. Both handlers are fire-and-forget: React ignores the promise an `onChange` handler returns, and `ImageCropperDialog` calls `onConfirm(blob, name)` without awaiting it. So a rejected upload (network error, 413, storage outage) showed nothing and escaped as an unhandled promise rejection.\n\nThis PR catches the failure on both paths and renders the same message `FileField` renders for the same failure through the same `useUpload()` transport: key `fields.file.uploadFailed`, same `name` / `error` arguments, same `defaultValue`. The message goes in the widget's existing error row (the one the oversize-pick rejections already use, objectui#4141).\n\n- **Picker path:** each pick is caught on its own, as in `FileField`'s `useFileUploads`. A failed pick is reported and not added. In a multi-select, the picks that did upload still land, and the oversize rejections are kept beside the upload failures.\n- **Crop path:** the original image stays (no `onChange`). The dialog still closes in `finally`, so the message shows in the field's error row instead of behind the dialog.\n- The message is a module-level helper, `uploadFailedMessage(t, name, err)`, in the same shape as `maxSizeError`. A component-scoped `useCallback` made the React Compiler skip one more memoization, so the helper lives outside the component.\n\nThe key exists in every locale pack `@object-ui/i18n` ships. `all-locales-key-parity.test.ts` re-derives that, and `pnpm check:i18n-keys` checks the call site's arguments and inline `defaultValue` against the `en` pack.\n\nFiles: `packages/fields/src/widgets/ImageField.tsx`, `packages/fields/src/widgets/ImageField.uploadFailure.test.tsx` (new), `.changeset/10226-image-upload-failure-reported.md` (patch, `@object-ui/fields`).\n\n## Evidence (head `737663e`)\n\n**Red first, on unmodified source.** I ran the new test against `8b1f066` before touching the widget. All three failure cases went red, and each saw exactly one `unhandledRejection` carrying `network down`, with no failure text in the DOM:\n\n```\n× picker: shows the translated failure, adds nothing, leaks no rejection\n× picker (multiple): adds the successful picks and reports only the failed one\n× crop: shows the translated failure, keeps the original image, leaks no rejection\nAssertionError: expected [ Error: network down ] to have a length of +0 but got 1   (x3)\nTestingLibraryElementError: Unable to find an element with the text: Failed to upload \"bad.png\": network down\nTests  3 failed | 2 passed (5)\n```\n\nThe two success controls (picker and crop, resolving transport) pass on both trees.\n\n**Ablation on the committed fix (head `737663e`).** A trap-guarded script replaced `ImageField.tsx` with its `8b1f066` blob, ran the test, and restored the file with `git checkout HEAD -- PATH`.\n- On-disk proof of the mutation: `uploadFailed` occurrences went 4 → 0 and `catch (err)` went to 0.\n- Result on the mutated tree: `Tests 3 failed | 2 passed (5)`, the same three failures as above.\n- Restore proof: the file's hash equals its HEAD blob, and `git diff HEAD` is empty.\n\n**Local gates, on head `737663e`, exit codes written to files before being read:**\n- `pnpm --filter @object-ui/fields type-check` (`tsc --noEmit && tsc -p tsconfig.test.json`) → exit 0. I built the dependency closure first with `pnpm --filter '@object-ui/fields^...' build`.\n- `pnpm exec vitest run packages/fields/` → exit 0, `Test Files 179 passed | 1 skipped (180)`, `Tests 3010 passed | 7 skipped (3017)`.\n- `eslint` on the two touched `.ts(x)` files → 0 errors. `ImageField.tsx` has 7 warnings against 6 on `8b1f066`; the one new warning is below. `lint.yml` sets no `--max-warnings`.\n- `check:i18n-keys`, `check:i18n-dead-keys`, `check:vi-mock-specifiers`, `check:vi-mock-inherit`, `check:vi-mock-override-shape`, `check:new-line-citations`, `check:control-bytes`, `check:test-path-roots`, `check:unreferenced-sources`, `scripts/check-changeset-presence.mjs`, `scripts/check-changeset-no-major.mjs` → all exit 0.\n\n**Scope of local runs:** repo-wide `pnpm lint`, the full `pnpm test`, and every other `check:*` family were left to CI. I ran lint only on the touched files. That narrowing is not a proof that no other file's verdict can move, and I did not check whether type-aware rules are on in `eslint.config.js`.\n\n## Acceptance notes\n\n- **One new lint warning, not fixed here.** `react-hooks/preserve-manual-memoization` now also fires on `handleCropConfirm`. Adding the `catch` makes the React Compiler skip memoizing that callback; it already skipped `openCropper` on `main`. This only affects optimization, and the file already carries the same warning. `lint.yml` sets no `--max-warnings`.\n- **Non-`Error` rejections read `undefined`.** For a rejection that is not an `Error`, `(err as Error).message` puts `undefined` in the message. `FileField` does exactly the same, and I copied its behaviour on purpose so the two widgets say the same thing. Carrier: none.\n- **Possible shared helper.** `FileField` and `ImageField` now each build the `fields.file.uploadFailed` call the same way. `file-size-guard.ts` explains why the size guard is shared rather than copied per widget, and the same argument could apply to this message. Moving it would touch `FileField.tsx`, which is outside this claim's file surface. Carrier: none.\n- **Title strings in the crop dialog are hard-coded English.** `ImageCropperDialog`'s title and description do not go through `t()`. I left them alone: they are not part of this defect and not in scope.\n- **Commit trailer.** The first commit (`2091538`) has a `Co-Authored-By` trailer that names a model version. The later commits use the plain `Claude` form. History was not rewritten.\n\nSession: `https://claude.ai/code/session_01D486C1axmXnrkJMNUfz2eb`\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01D486C1axmXnrkJMNUfz2eb)_\n"
}
