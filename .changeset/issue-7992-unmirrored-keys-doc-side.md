---
---

Test comment only: in `object-view-unmirrored-keys-7779.test.ts`, the label above the `SpecObjectListViewSchema` refusal pair no longer claims `schema-reference.md` authors a `default: true` flag — objectui#7923 (PR objectui#7991) removed that per-view flag from the `object-view` example, so the `flagged` case is now a refusal pinned on its own merits rather than a reading of a documented shape. No assertion added, removed or weakened; no published behaviour changes.
