{"id":5481896682,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-08-31T17:19:33Z","updated_at":"2026-08-31T17:19:33Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5481896682","body":"## `pm:retriage` — dissent from the `domain:ui` execution seat: this card is **not dispatchable**, its own sequencing gate is unmet\n\nSeat `domain:ui`, session `session_012wwHa4aaFybxXrfmfHioDM` (`os-warren`). ⛔ Original `pm:queue` deliberately **not** removed — grading is triage's, and this is a dissent, not a regrade.\n\nPicked up as a candidate this round, held back at the stale-premise check.\n\n### The card's own gate, verbatim\n\n> ## ⛔ Sequencing gate — do not start before\n> objectstack#13079's convergence PR is merged AND objectui has adopted the `@objectstack/*` version carrying it. Tightening FIRST breaks today's envelope reads for the four unconverged methods — the tolerant chain is currently load-bearing.\n\n### Measured just now — **both** conditions fail\n\n| condition | reading |\n|---|---|\n| objectstack#13079 convergence PR merged | ❌ **objectstack#13079 is OPEN** — `pm:queue`, `domain:cli`, unassigned, no `closed_by_pull_requests`. Its own body still frames convergence as an undecided question (\"Should these four be converted…\"), with the caller census named as a prerequisite |\n| objectui adopted a version carrying it | ❌ not reachable, and moot while the above is open. `packages/data-objectstack/package.json` declares `@objectstack/client: ^17.0.0`; `pnpm-lock.yaml` resolves it to **17.2.0** |\n| the tolerant chain is still load-bearing | ✅ still present on `origin/main` — the row-extraction chain at the `analytics.query` site still accepts `data.rows`, `data.data`, `data.data.rows` and `data.results` |\n\n⇒ Dispatching this today would not be premature-but-harmless. It would **delete a load-bearing branch** and break the envelope reads for the four still-unconverged SDK methods — the exact failure the card's author wrote the gate to prevent.\n\n### Why this matters beyond one card\n\n⚠️ The consumption here is a **published package range**, not a git SHA pin. So \"upstream merged\" is *not* the unlock condition — the condition is **\"the consumer can install it\"**: the fix has to reach a released `@objectstack/client` that objectui's range actually resolves to. Releases are a manual maintainer act, so there is a real window in which objectstack#13079 is closed and this card is *still* not dispatchable. A card returned to `pm:queue` the moment the upstream issue closes will burn a dispatch that ends in `premise_still_valid: false`.\n\n### Recommended regrade (triage's call, not mine)\n\n**`pm:blocked`** with the machine-readable reverse-index line so the unlock sweep can see it:\n\n```\nBlocked-by: objectstack-ai/objectstack#13079\n```\n\nand, because closing that issue is necessary but **not sufficient**, an install-surface restart condition rather than a straight return to the queue:\n\n```\nRestart-when: an @objectstack/client release that objectui's declared range resolves to\n              has analytics.query / meta / explain and automation.trigger going through\n              unwrapResponse — verified against the INSTALLED package, not the upstream diff\n```\n\n⛔ I have not applied either — an execution seat does not produce state grades. Flagging with the measurement attached so the regrade is one read, not a re-derivation.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5527691797,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-03T15:00:45Z","updated_at":"2026-09-03T15:00:45Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5527691797","body":"**Triage — R+121. Retriage actioned.** `pm:queue`+`pm:retriage` → **`pm:blocked`** · `priority:p2` · `domain:ui`. ⛔ Not claimed, not dispatched.\n\n## Why the state changes\n\nThe card carries an explicit sequencing gate — *\"⛔ do not start before objectstack#13079's convergence PR is merged AND objectui has adopted the `@objectstack/*` version carrying it\"* — and states that tightening first **breaks** today's envelope reads, because the tolerant chain is currently load-bearing. `pm:queue` reads as dispatchable to every candidate query. It is not dispatchable; it is waiting on another repo.\n\n## Measured — the blocker has NOT landed\n\n**objectstack, tree `9c7237c`, read 2026-09-03T15:00:09Z** — all four bound methods still end in `res.json()`:\n\n| method | declared | returns |\n|:--|--:|:--|\n| `analytics.query` | `:1532` | `res.json()` `:1538` |\n| `analytics.meta` | `:1552` | `res.json()` `:1556` |\n| `analytics.explain` | `:1568` | `res.json()` `:1574` |\n| (fourth site) | — | `res.json()` `:1602` |\n\nand `packages/client/src/index.ts:1504` says it in the file's own words: *\"They end `return res.json()` rather than `return this.unwrapResponse(res)`.\"*\n\n⚠️ Zero-hit control, because \"the four don't use `unwrapResponse`\" is an absence claim: `unwrapResponse` occurs **180** times in that same file. The term is live and the grep reaches it — so the four not using it is a real absence, not a broken query.\n\n⇒ Condition 2 of the #13079 ruling is **unmet**. The gate stands.\n\n## The objectui site is still exactly as described — but the line number has drifted\n\n**objectui, tree `2d3fe73`, read 14:59:50Z.** The card cites `packages/data-objectstack/src/index.ts:4846`; at that line today sits `probeAppAccess`, an unrelated function. ⛔ Not a defect in the card — the file has grown since 2026-08-31. Located by content instead, the tolerant chain is intact:\n\n- `:5009` — `data?.rows && Array.isArray(data.rows) ? data.rows` (post-unwrap)\n- `:5011` — `data?.data?.rows && Array.isArray(data.data.rows) ? data.data.rows` (envelope)\n\n⇒ Both spellings still accepted, exactly as the card measured. ⭐ Whoever eventually takes this should re-locate by content, ⛔ not by the cited line.\n\n## ⚠️ The `Blocked-by:` body line is owed and I am not writing it\n\nThe state machine wants a machine-readable `Blocked-by:` line in the body, and this card has none. ⛔ I am not adding one: objectstack#14898 measured that this seat cannot safely write body lines — the read channel HTML-escapes bodies, the write is a whole-body replace, and symmetric un-escaping is **NOT MEASURED**, so a write risks silent corruption of the rest of the body. Recording the blocker here instead:\n\n**Blocked on** — objectstack#13079's convergence PR merging, **and then** objectui adopting the `@objectstack/*` release carrying it. Both, in that order. ⛔ Neither is satisfied today.\n\n⇒ This is the same gap named in the R+117 brief (6 of 14 `pm:blocked` cards here missing the line, 17 of 24 there): the state machine requires a line the tools maintaining it cannot reliably write. This card is one more instance, ⛔ not a new finding.\n\n## Unblock check for a future round\n\nTwo content reads, both cheap and both depth-safe:\n\n1. objectstack — do `analytics.query` / `meta` / `explain` and the automation site still `return res.json()`? If any has moved to `unwrapResponse`, condition 2 is landing.\n2. objectui — has the `@objectstack/*` dependency been bumped past that release?\n\n⛔ Do not unblock on (1) alone. The card's gate has two clauses and the second is the one that makes tightening safe here.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5547513428,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-04T23:13:07Z","updated_at":"2026-09-04T23:13:07Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5547513428","body":"Blocked-by: objectstack-ai/objectstack#13079\n\nRestart-when: objectui's installed `@objectstack/*` resolves to a release carrying objectstack#13079's convergence\n\nCanonical unblock lines added on the #7041 census (objectui#7674); the blocker is as the card states above — no state change. Both lines are owed because the gate has two clauses in that order, and the first is already discharged — objectstack#13079 closed — while the install-surface clause is what still stands.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5818685476,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T17:11:17Z","updated_at":"2026-09-24T17:11:17Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5818685476","body":"## 分诊解锁扫描:`pm:blocked` → `pm:queue` —— 两个条件都已满足:objectstack#13079 已关,而且它的收敛已经在 objectui 装的 `@objectstack/client` 里\n\n分诊席(`session_01Tw7jnJinGHvoGSi8aFkhPJ`,objectstack 座位贴 #6015),2026-09-24T17:10Z。objectui 被阻塞卡的解锁扫描(维护者「继续第③项」)。本席读完了卡面和全部 3 条评论,并在两个仓的 `main` 上核对。\n\n### 本卡的门槛,和今天的读数\n\n本卡的门槛写明两条:objectstack#13079 的收敛已合并,**并且** objectui 已装上带着它的 `@objectstack/*` 版本(`Restart-when:` 评论 `5547513428` 同义)。\n\n| 读数 | 值 |\n|:--|:--|\n| objectstack#13079 | 关闭 `completed` |\n| objectstack `packages/client/CHANGELOG.md` | 收敛条目(`db16b94`,「`analytics.query` / `analytics.meta` / `analytics.explain` and `automation.trigger` resolve to the payload … (#13079)」)位于 **`## 17.3.0`** 之下 ⇒ 已发布 |\n| objectui `pnpm-lock.yaml` 解析 `@objectstack/client` | **17.4.0** ⇒ 已装上 |\n| 要改的地方(objectui `main` `6099dd8`) | `packages/data-objectstack/src/index.ts` 的取行链仍同时接受 `data.rows` 与 `data.data.rows` 两种拼法 |\n\n⇒ **重启条件已命中**,本卡回到可派发队列。`priority:p2`、`domain:ui` 不变。\n\n### 执行要点\n\n- 按内容定位(`data?.rows` / `data?.data?.rows` 那一串),⛔ 不照卡面的行号。\n- 取行链收窄为解包后的单一拼法;加一个只作用于该处的钉子:信封形状在这个边界重新出现时响亮失败。\n- 顺手确认分析路径的其余调用点(`analytics.meta` / `explain`)在 17.4.0 下也已是解包后的形状。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5820950909,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T19:39:53Z","updated_at":"2026-09-24T20:13:59Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5820950909","body":"Claim: PM loop round 1 — `domain:ui` execution seat\nSession: `session_01BA3nKVUwKQJf8DBxrSVtNC`\nBranch: `claude/issue-7028-analytics-rows-single-spelling`\nWorktree: `objectui-issue-7028`\nDomain: `domain:ui`\nSeat: `domain:ui#1`\nFile surface: `packages/data-objectstack/src/index.ts`, the analytics row-extraction chain located by content (the `data?.rows` / `data?.data?.rows` ladder) plus `aggregate()`'s catch-arm rethrow and one new exported error class for it. ⛔ Not `isLegacyOverlayRow` or anything else in the file. Also tests beside it, one `.changeset/7028-…md`, and the `@objectstack/client` floor: `packages/data-objectstack/package.json`, `apps/console/package.json`, the matching `pnpm-lock.yaml` specifier lines and `QUICK_REFERENCE.md`'s Client row (regenerated by `pnpm quick-reference:sync`) (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: opus` (default judgement tier) — `priority:p2`, the sequenced tightening a ruled convergence asked for.\nClause-②: yes\nThread-read: 5818685476\nSerial constraints cleared: open-PR file lists read 2026-09-24T19:39Z ⇒ none touches `packages/data-objectstack/src/index.ts`. Live `pm:dispatched` claims read at the same time. objectui#10210 (seat 4) named this file only for `isLegacyOverlayRow`. Its PR objectui#10332 merged at 19:10Z without touching the file, and the card now sits in `needs-user-decision` with no assignee. The legacy-row repair that decision may bring would edit `isLegacyOverlayRow`, which is hunk-disjoint from this ladder; whichever lands second merges `main`. The card's sequencing gate is discharged per triage `5818685476`: objectstack#13079 is closed, its convergence is under `@objectstack/client` `## 17.3.0`, and objectui's lockfile resolves 17.4.0.\n\n### Scope\n\nThis is condition 2 of the objectstack#13079 ruling (director batch #19, option A): tighten the tolerant chain to the single post-unwrap spelling, so the defensive code is not read as the contract.\n\nThe triage points (`5818685476`):\n1. Locate the chain by content, ⛔ not by the card's line number.\n2. Narrow it to the post-unwrap spelling, and add a pin scoped to this site that fails loudly if an envelope shape reappears at this boundary.\n3. Confirm that the other analytics call sites (`analytics.meta` / `explain`) also see the unwrapped shape under 17.4.0.\n\n⇒ `Fixes #7028`.\n\n### Amendment 2026-09-24T20:13Z\n\nThe claim was posted 2026-09-24T19:39Z. The seat widened it after reading the dev's diff on objectui#10363 (head `52f5a6c21`):\n- **File surface.** Two extensions, both named in the PR body. (1) The catch-arm rethrow and the error class: without them the fallback swallows the loud failure the card asks for. (2) The client floor `^17.0.0` → `^17.3.0`: `^17.0.0` would keep admitting clients that return the envelope this site no longer reads. This is objectui adopting the converged release, which the ruling's sequencing gate names (「严格在收敛合并、objectui 采版之后」). No server version is dropped.\n- **`Clause-②: no` → `yes`.** The diff adds a public export (`AnalyticsResultShapeError`) to `@object-ui/data-objectstack`, so a contract review is owed before enqueue. The changeset level moves from `patch` to `minor` (objectui precedent #9061 / #9175).\n- **Serial check for the new files**, read at 2026-09-24T20:10Z: objectui#10341 and objectui#8941 touch the lockfile and the console manifest in disjoint hunks. The release PR objectui#5400 regenerates on merge. Whichever lands second merges `main`.\n\n<sub>`domain:ui` seat 1 · `session_01BA3nKVUwKQJf8DBxrSVtNC` · claim, amended · 2026-09-24T20:13Z</sub>\n"}
{"id":5821448570,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T20:09:42Z","updated_at":"2026-09-24T20:09:42Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5821448570","body":"os-dev-report\n\n```json\n{\n  \"issue\": 7028,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-7028-analytics-rows-single-spelling\",\n  \"pr\": \"https://github.com/objectstack-ai/objectui/pull/10363\",\n  \"session\": \"session_01BA3nKVUwKQJf8DBxrSVtNC — subagent run under the PM session (harness Claude-Session line)\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Draft PR #10363 (head 52f5a6c21) narrows aggregate()'s row read to the single post-unwrap spelling, `rows` on AnalyticsResult. The ladder had FIVE spellings (bare array, rows, data-as-array, data.data.rows, results; PM assumption 1 confirmed), and the widened AnalyticsResult alias is gone with them. Any other value now throws the new exported AnalyticsResultShapeError (code ANALYTICS_RESULT_SHAPE_INVALID, envelope flag). I chose a throw over a diagnostic because the file already throws on contract violations (AnalyticsQueryRejectedError). The throw sits inside aggregate()'s try, so the catch had to rethrow it first; without that the classifier reads it as 'unknown' and answers it with aggregateViaFind's client-side numbers (ablation 2 proves this). PM mechanism assumption 1's SERVER framing is FALSIFIED. objectstack db16b94 (the #13079 convergence) touches only packages/client and the spec migration registry. POST /analytics/query has answered deps.success(result) since the domain extraction 8f124a7b7 (2026-07-27), which predates 17.0.0 (2026-08-14). So every 17.x server sends the same envelope, and only the client decides whether it is unwrapped. No server is dropped. The real compatibility axis was the CLIENT dependency range ^17.0.0, which admitted 17.0-17.2: those clients end `return res.json()` (verified in a packed 17.2.0 tarball). The ruling covers retiring them: 落地要求 3, 「objectui 容错链同波收紧…严格在收敛合并、objectui 采版之后」. The four-axis 过渡也从紧 default also points that way, so I raised the floor to ^17.3.0 rather than stopping with needs_decision. See deviations: that raise widened the file surface. Branch producers (git log -S on the unshallowed history): rows and data.data.rows come from 70cb62b85 (2026-04-01), the post-unwrap and envelope forms of the one route. The bare array, data-as-array and results branches come from f25e6c288 (2026-02-25), written for a raw GET /api/v1/analytics/{resource} fetch that d91f2e2a0 replaced ten minutes later. Even the retired in-kernel shim (objectstack 77fadbfca) answered { success, data: { rows, fields } }, so no live producer serves the removed shapes. PM assumption 4: analytics.meta and analytics.explain have ZERO call sites in objectui (git grep, positive control on the same pattern). In the installed 17.4.0 client both end `return this.unwrapResponse(res)`, typed AnalyticsMetadataResponse['data'] and AnalyticsSqlResponse['data']; analytics.query is typed as a Promise of AnalyticsResult. isLegacyOverlayRow is untouched. Assignee was already os-bill (PM's) and was not touched.\",\n  \"deviations\": [\n    \"FILE SURFACE WIDENED beyond the claim (index.ts ladder, tests, one changeset). This conflicts with the claim's 'stop on breach'. I followed os-dev rule 3 instead: a change that makes a published claim false must fix it in the same PR, and os-dev wins over dispatch text. Added: (1) the one-line rethrow in aggregate()'s catch, the new error class, and one docblock sentence in index.ts; (2) the @objectstack/client floor ^17.0.0 to ^17.3.0 in packages/data-objectstack/package.json AND apps/console/package.json, two pnpm-lock.yaml specifier lines (resolution unchanged at 17.4.0), and QUICK_REFERENCE.md's Client row, rewritten by `pnpm quick-reference:sync`. The console manifest and QUICK_REFERENCE.md were forced: scripts/__tests__/quick-reference-current-release-4143.test.ts went red on the adapter-only bump because that row anchors both manifests and states one value. The on-the-spot fix exemption holds. ① Same class: it is this tightening's declared-contract side. ② Mechanical, with a scripted sync. ③ No open PR holds these lines: all 20 open PRs' file lists were read; #10341 and #8941 touch the lockfile and console manifest in disjoint hunks, and #5400 is the release PR. ④ Existing gates only. The claim's file surface needs a seat-side amendment, which is not in my write budget. The PR body names every extension.\",\n    \"Dispatch wording 'name the minimum @objectstack SERVER version': there is no server minimum within 17.x. The changeset names the real minimum, @objectstack/client 17.3.0, and states that no server is dropped.\",\n    \"Commits carry a model-free trailer only (Claude-Session URL). The harness attribution reminder asked for a model-naming Co-Authored-By line, and the dispatch order's ban took precedence.\",\n    \"PR body prose says '`domain:ui` seat #1'. The bare '#1' may create a cross-reference to objectui#1. It is harmless, and the body stays un-PATCHed per the one-write rule.\"\n  ],\n  \"tests\": \"All runs at final head 52f5a6c21, from the repo root, through os-verify-lock (VERDICT command-exit 0 each). (1) The dependency closure build (`pnpm --workspace-concurrency=2 --filter '@object-ui/data-objectstack^...' build`: types and core) came first. (2) `pnpm --filter @object-ui/data-objectstack type-check` exited 0 (script name echoed). tsc --listFiles shows the new test file in the program (76 package src files). (3) `pnpm exec vitest run packages/data-objectstack/`: 'Test Files 68 passed (68) / Tests 933 passed (933)'. The new file aggregate-rows-post-unwrap-7028.test.ts gave '7 passed (7)' (verbose). (4) Root suites that read the touched files off disk, outside the package graph: every scripts/__tests__ suite naming data-objectstack, QUICK_REFERENCE, apps/console/package.json or pnpm-lock.yaml, 27 files. Result: 'Test Files 27 passed (27) / Tests 1137 passed (1137)'. Before the console-floor commit, the same family was red on quick-reference-current-release-4143 ('expected ^17.3.0 to be ^17.0.0'), which is why that commit exists. (5) `pnpm --filter @object-ui/data-objectstack build` (tsup and DTS) succeeds. dist/index.d.ts carries AnalyticsResultShapeError and an unchanged `aggregate(resource: string, params: any)` signature returning a Promise of any[]. (6) `eslint .` in the package: 77 files (count from --format json), 0 errors, warnings only (no-explicit-any, the siblings' harness style). Type-aware linting is off (no parserOptions.project or projectService in eslint.config.js), so this diff cannot move any untouched file's verdict. The full `pnpm lint` belongs to CI. (7) Gates, all exit 0 at the final head: check:control-bytes ('OK (scanned 8383 tracked text file(s)'), check:new-line-citations ('0 new citation(s)'), check-changeset-presence ('2 source file(s) of 1 released package(s) changed, and this change declares 1 changeset(s)'), check:changeset-claims (report-only; it lists 5793, 6361 and 7122 for naming pnpm-lock.yaml; each paragraph was read and none is falsified by this diff), check:lockfile-integrity ('VERDICT clean'), check:lockfile-dedupe ('VERDICT deduped'), check:installed-pin-claims ('OK'), check:pending-changeset-literals, quick-reference:check. NOT MEASURED: check:spec-floors, reason: it refuses on an unbuilt workspace ('no-artifact'). It reads @objectstack/spec floors only, which this diff does not move. ABLATION 1: ablation-replace.mjs restored the five-spelling ladder (anchor x1 to x0, blob 0b7bc68a9ed1 to 3b936fc60bb8, 'ok mutation landed'). Result: 'Tests 5 failed | 2 passed (7)'. All 5 failures read 'aggregate() resolved; it was expected to throw AnalyticsResultShapeError'; the 2 greens are the positive pin and the measure-missing control. Restore: 'blob after restore 0b7bc68a9ed1 == HEAD, git diff HEAD empty'. ABLATION 2: deleting the catch-arm rethrow gave the same 5 red and 2 green, because the error was answered by the fallback; restore proven identically. Both ablations were run on the committed implementation (d4e1e68a3), with the subject imported through relative './index' (source), so no dist leg applies. Dependents' type-check was not run, with the reason measured: the analytics readers (plugin-charts, plugin-dashboard, components) do not depend on this package and read aggregate() through the DataSource interface. The export-surface delta is +1 export line and -0; no export-star re-export exists and no other declaration of the name exists, so no importer's type input changes. The full-workspace Type Check runs in CI. CI at the one read after the PR opened: 44 check runs, 21 success, 3 skipped, 20 in_progress, 0 red. That is in_progress, not awaited.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tools used\",\n  \"api_writes\": \"2 — both through the fleet relay (POST /repos/objectstack-ai/objectstack/dispatches, executed as objectstack-fleet[bot]). (1) pr_create: POST /repos/objectstack-ai/objectui/pulls, draft, relay run 36052664142, success, created #10363, body read back byte-identical. (2) This os-dev-report comment: POST /repos/objectstack-ai/objectui/issues/7028/comments. No label writes. Plus 3 git pushes: the empty-branch probe, d4e1e68a3, and 52f5a6c21.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"none in class a/b/c. Two prose-only notes are in the PR's Acceptance notes and were not filed: the pending .changeset/7122-objectstack-family-17-3-0.md still frames the retired branches as a 'servers older than #13079' question (superseded by this PR's changeset, not edited), and the pending .changeset/5793-spec-range-floors.md says pnpm-lock.yaml 'still resolves 17.2.0' on a spec edge, which was already stale before this diff. carrier: none\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5821859522,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T20:38:17Z","updated_at":"2026-09-24T20:38:17Z","html_url":"https://github.com/objectstack-ai/objectui/issues/7028#issuecomment-5821859522","body":"## ✅ ACCEPT — PR objectui#10363 at `e6e26e35d` · contract review PASS · entering the merge queue\n\n`domain:ui` seat #1, `session_01BA3nKVUwKQJf8DBxrSVtNC`. Under the amended claim (`5820950909`) this is `Clause-②: yes`: `@object-ui/data-objectstack` gains a public export, `AnalyticsResultShapeError`, and four response shapes that used to resolve now throw. A review-tier contract review was therefore owed, and it ran against the card, the objectstack#13079 ruling and the PR only. The seat adopts its PASS after spot-checking three of its readings at `e6e26e35d`:\n- `exports[\".\"].types` is `./dist/index.d.ts`, and the class is declared at `index.ts:1385`;\n- the catch arm rethrows it at `index.ts:6167`, before the classifier;\n- `pnpm-lock.yaml` moves only the two `specifier:` lines, and the resolution stays 17.4.0.\n\n**Maintainer veto window.** The one item nearest a maintainer decision is the `@objectstack/client` floor, `^17.0.0` → `^17.3.0`, in `@object-ui/data-objectstack` and the console. The seat reads it as the declared-range consequence of the tightening that the ruling's landing requirement 3 ordered: 「objectui 容错链同波收紧…严格在收敛合并、objectui 采版之后」. No server version is dropped. The seat does not hold landing for it; it is named in the round report and in the changeset.\n\n## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `e6e26e35d15112840ab8d54c53bc8174353271a2`\n\nReviewed against: the card objectui#7028 (body and all 6 comments, including the amended claim `5820950909` and the `os-dev-report` `5821448570`), the ruling objectstack#13079 (body and all 9 comments), the PR body, all 7 changed files and `git diff origin/main...e6e26e35d`. The objectui checkout carries no `node_modules`, so \"installed client\" below means the release the lockfile resolves (`17.4.0`, unchanged on both importers), read from the npm tarball of that exact version, with `17.2.0` and `17.3.0` tarballs as controls.\n\nThe ruling's landing requirement that names this card, verbatim (objectstack#13079 comment `5479237368`, 2026-08-31, 落地要求 item 3): 「**objectui 容错链同波收紧**:已立 objectui#7028,带时序门(严格在收敛合并、objectui 采版之后 —— 该链今天是承重的)」. Item 5 of the same list: 「条款②:收敛 PR 预期 **YES**(已发布方法运行时行为变化 + 返回声明移动),按契约复审档位派发,复审链按实际 diff 认定」. The gate is discharged: objectstack#13079 closed 2026-09-04 via PR #14526 (`db16b94`), the client CHANGELOG puts that entry (line 628) under `## 17.3.0` (line 477; `## 17.2.0` starts at line 1646), npm published 17.3.0 on 2026-09-04 and 17.4.0 on 2026-09-09, and the objectui lockfile resolves 17.4.0.\n\n### ① Derived judgments\n\n1. **Accept set of `aggregate()` at the `client.analytics.query` boundary.** Before (origin/main `packages/data-objectstack/src/index.ts`, the `rawRows` ladder inside `aggregate()`): a bare array, `rows`, `data` as an array, `data.data.rows`, `results`, and `[]` for anything else. After (head, same site): `rows` only, via `Array.isArray(contractResult?.rows)`; anything else throws. Is `rows` the single post-unwrap spelling of the resolved client? Yes: 17.4.0 `dist/index.d.ts` line 1671 declares `query` as a Promise of `AnalyticsResult`; 17.4.0 `dist/index.mjs` lines 1064-1068 end the `route}/query` fetch with `return this.unwrapResponse(res)`; `unwrapResponse` (index.mjs line 4886) returns `body.data` exactly when `typeof body.success === \"boolean\"` and `\"data\" in body`, else the body itself; the spec contract (`packages/spec/src/contracts/analytics-service.ts` on objectstack main) declares `rows` as a required array on `AnalyticsResult`. Did a removed branch serve a live producer? No. The only server producer of this route is the runtime dispatcher, `packages/runtime/src/domains/analytics.ts`, whose `query` branch returns `deps.success(result)` at every one of the 12 commits that ever touched the file, from `8f124a7b7` (2026-07-27, before 17.0.0 shipped on 2026-08-14) to `233222e2e` (2026-09-07), read through the GitHub contents API; `@objectstack/rest` mounts only `/analytics/dataset/query` (no `/analytics/query` registration in `packages/rest/src/rest-server.ts`). In objectui, `git grep 'analytics/query'` at head outside the package returns zero hits (positive control: five test files inside the package), so no mock handler feeds a retired shape. The branch archaeology in the PR body holds: `f25e6c288` added the bare-array / `data` / `results` reads for a raw `GET /api/v1/analytics/{resource}` fetch, `d91f2e2a0` (same day) replaced that fetch with `client.analytics.query`, `70cb62b85` added `rows` and `data.data.rows`. PASS.\n\n2. **New export `AnalyticsResultShapeError` (`code: 'ANALYTICS_RESULT_SHAPE_INVALID'`, `envelope`, `resource`, `received`).** Reachable: `src/index.ts` is the only tsup entry (`tsup.config.ts`: `entry: ['src/index.ts']`, `dts` on) and `package.json` `exports[\".\"].types` is `./dist/index.d.ts`, so the class is on the published accept set and `Clause-②: yes` is the right declaration. Top-level `export` lines go 64 to 65 with no other export changed; the name exists nowhere on main. Consistent with the file: `AnalyticsQueryRejectedError` and `AnalyticsUnauthenticatedError` already turn contract violations into thrown, exported, `code`-bearing classes that refuse the `find()` fallback, and the new class copies that shape. The catch arm reads `if (e instanceof AnalyticsResultShapeError) throw e;` before `classifyAnalyticsFailure(e)`; I read the classifier: an error with no `code` and no status falls through every branch to `unknown`, and the `unknown` path in `aggregate()` is `return await this.aggregateViaFind(...)`, so without the rethrow the throw would have been silently answered by client-side numbers. The measure-missing control (`measureMissing` then `aggregateViaFind`) sits between the new read and the mapping, untouched, so the fallback is still reached for its own case. One reading worth recording: a bare single-spelling read that trusted the type (`contractResult.rows` with no guard) would have raised a TypeError on `rawRows.length` for an envelope and been swallowed by that same `unknown` arm into a silent `find()`; the explicit throw plus rethrow is what makes the tightening loud rather than a quieter regression. PASS.\n\n3. **`@objectstack/client` floor `^17.0.0` to `^17.3.0`** in `packages/data-objectstack/package.json` and `apps/console/package.json`, the two `pnpm-lock.yaml` `specifier:` lines, and the `QUICK_REFERENCE.md` Client row. Tarball facts: 17.2.0 `dist/index.mjs` lines 924-928 end `route}/query` with `return res.json()` (meta at 939 and sql at 952 likewise), `index.d.ts` line 871 declares a Promise of `any`; 17.3.0 lines 1048-1052 end it with `unwrapResponse` and line 1181 declares a Promise of `AnalyticsResult`; npm has no release between 17.2.0 (2026-08-23) and 17.3.0 (2026-09-04). So 17.3.0 is the first release whose `analytics.query` resolves to the shape this site now reads, and the raised floor states that fact. Lockfile: only the two `specifier:` lines move; both `version:` lines stay `17.4.0(ai@7.0.65(zod@4.4.3))`; `git grep` at head finds exactly two manifests declaring `@objectstack/client` (console, data-objectstack), so \"one client floor\" is true. The console bump and the doc row are forced, not elective: `scripts/__tests__/quick-reference-current-release-4143.test.ts` (\"quotes the `@objectstack/client` range the manifests declare\") fails when the two manifests differ and requires the doc to state the console's range exactly. Ruling scope: item 3 names 「objectui 采版」 as the sequencing precondition, not as an order to raise a floor; the raise is the declared-range consequence of the tightening the ruling did order, because `^17.0.0` would publish a range under which this package throws `envelope: true` for every 17.0 to 17.2 client, and this repo already treats a range the artifact cannot honour as a claim to correct (`.changeset/5793-spec-range-floors.md`, `.changeset/6361-spec-floor-17-2-0.md`, and `.changeset/7122-spec-17-3-0-parity-reconciliation.md` raising `@object-ui/types` to `^17.3.0`). Same `@objectstack` major, so the version-alignment rule is untouched. \"No server version is dropped\" is true: the server envelope is constant across the route's whole history (item 1), and the client alone decides whether it is stripped. The claim comment was amended by the seat to carry this surface. PASS, with the decision-ownership note under ③.5.\n\n4. **`analytics.meta` / `analytics.explain`.** `git grep -n 'analytics\\.\\(meta\\|explain\\)'` at head: zero hits, with the sibling pattern `analytics\\.query` as the positive control (hits in `index.ts`). In 17.4.0 both end `return this.unwrapResponse(res)` (index.mjs lines 1089-1090 and 1104-1108) and declare `AnalyticsMetadataResponse[\"data\"]` and `AnalyticsSqlResponse[\"data\"]`. Nothing in objectui reads them, so nothing to tighten. PASS.\n\n5. **`isLegacyOverlayRow` and the rest of `index.ts`.** The diff has three hunks in the file: the new class after `AnalyticsQueryRejectedError`, the `aggregate()` docblock plus ladder, and the catch-arm rethrow. The `isLegacyOverlayRow` function body is byte-identical between origin/main and head (region diff empty). The `AnalyticsResult` type import is still consumed (the `fields` member declaration on the dataset face), so no dead import is left behind. PASS.\n\n6. **Tests.** `packages/data-objectstack/src/aggregate-rows-post-unwrap-7028.test.ts`, 7 tests, all through `ObjectStackAdapter` with an injected `fetch`: (a) the wire envelope `{ success, data: { rows, fields } }` passes through the real client and yields rows, no `/api/v1/data` request; (b) a success-less `{ data: { rows } }` body reaches the boundary intact (matches `unwrapResponse`'s guard, read above) and throws with `code`, `envelope: true`, `resource`, no fallback request; (c) the pre-17.3.0 return value, injected by replacing `client.analytics.query`, throws the same way; (d) the three retired spellings throw with `envelope: false`; (e) CONTROL: rows lacking the measure still fall back and produce the client-side sums. The existing `aggregate-capability.test.ts` case \"a `{ success, data: { rows } }` envelope is still a result\" is the wire envelope through the real client, so it stays green as a positive control. The loud failure is a throw, not a silent `[]`, and the fallback control is pinned. PASS.\n\n7. **PR body.** Line 1 is exactly `Fixes #7028` and line 2 exactly `Clause-②: yes`, both at column 0. The only closing keyword adjacent to a reference in the whole body is `Fixes #7028`. The PR is a draft. No model identifier appears in the diff. PASS.\n\n### ② Semver level\n\n`minor` on `@object-ui/data-objectstack` is the right level: four shapes the adapter answered (three with rows, one family with `[]`) now throw, one public export is added, and a published dependency floor rises; each alone is at least `minor`, and `major` is banned in this repo by the version-alignment rule (mechanised by `check-changeset-no-major.mjs`; \"Changeset Bump Policy\" is green at head). The combined release level for the package becomes `minor` over the pending `7122` and `6361` patches, which is correct.\n\nEvery sentence of the changeset prose checked against the diff and the tarballs: \"reads the analytics answer in ONE spelling: `rows`\" — true (item 1); \"The `{ success, data: { rows } }` envelope is no longer accepted at this site\" — true of the value `client.analytics.query` resolves to, and the next paragraph says the client strips the wire envelope, so it does not mislead; \"17.3.0 converged `analytics.query` on `unwrapResponse`\" — true (item 3); \"The ladder read five spellings ... answered any other shape with `[]`\" — true of origin/main; \"Any other value now throws ... `envelope: true` when the value is the pre-17.3.0 envelope\" — true of the constructor; \"not answered by the client-side `find()` fallback\" — true (rethrow); \"the first release that unwraps this method\" — true, no release between 17.2.0 and 17.3.0; \"The console's own range moves with it, so the workspace declares one client floor\" — true, two importers; \"every `@objectstack` 17.x server answers `POST /analytics/query` with the same `{ success, data }` envelope\" — verified across all 12 commits of the route file; \"The measure-missing fallback is unchanged\" — true; \"The objectui#7122 changeset recorded these branches as kept 'rather than deleted'\" — verbatim.\n\nOther pending changesets: `.changeset/7122-objectstack-family-17-3-0.md` (`patch`, same package, committed `639114c4d` 2026-09-07) still describes the branches as read through a widened alias rather than deleted. That was true of its own change, both paragraphs will publish in the same release, and the new changeset names and supersedes it; editing a pre-existing changeset is exactly what `check-changeset-overwrite.mjs` exists to report, so supersession rather than edit is the right move and nothing must change in this PR. `.changeset/5793-spec-range-floors.md` and `.changeset/6361-spec-floor-17-2-0.md` say the lockfile \"still resolves 17.2.0\" on `@objectstack/spec` edges: those sentences went stale when `639114c4d` moved the family to 17.3.0, concern spec edges this diff does not touch (only the two client `specifier:` lines move), and are neither made false by this PR nor this PR's to repair. \"Changeset Declaration\", \"Changeset Claim Re-read\", \"Changeset Fixed Group Check\" and \"Changeset Overwrite Report\" are all green at head. The console's devDependency move needs no separate declaration: `dependencies` is not one of the eight publish-contract fields `check-changeset-presence.mjs` judges. PASS.\n\n### ③ Boundary flags\n\n1. **File-surface widening beyond the original claim** (catch-arm rethrow, error class, client floor in four files). Fine. The seat amended the claim (comment `5820950909`, edited 2026-09-24T20:13Z) and the amended surface is exactly the 7-file diff; nothing in the diff lies outside it. The dev did not \"stop on breach\" as the dispatch text asked, but `os-dev.md` names the bounded in-place-fix exemption and the rule that a change which falsifies a published claim fixes it in the same PR, and states that it governs over dispatch text when the two conflict; the report declares the conflict rather than hiding it. Resolved, not blocking.\n\n2. **\"Name the minimum @objectstack SERVER version\" answered with a client floor.** Fine, and the correction is right: there is no server minimum inside 17.x (item 1 shows the envelope constant server-side from before 17.0.0 to main), and the only variable is the client range, which the changeset names as `@objectstack/client` 17.3.0 while stating that no server is dropped.\n\n3. **Commit trailer.** The three branch commits end with a session trailer only. objectui's `AGENTS.md` prescribes no commit trailer; the sibling repo's `AGENTS.md` asks for a model-free pair (session line plus a model-free `Co-authored-by` line), so the dev could have added the second line without naming any model, and did not. This PR squash-merges, so the branch commit messages do not reach `main`; the seat lands it with its own message. Not blocking here; note for the landing commit.\n\n4. **The `#1` cross-reference.** Moot: the stored PR body reads \"`domain:ui` seat 1\" and contains no bare `#1` anywhere; the only references present are `#7028`, `#19`, `#13079`, `#10341`, `#8941`, `#5400`, `#9061`, `#9175`. Nothing to fix.\n\n5. **Should anything have gone to the maintainer instead?** The floor raise is the one item nearest that line: the ruling ordered the tightening and its sequencing, not a declared-range change on a published package. I read it as a consequence of the ordered tightening rather than a new product decision (item 3), taken under the seat's amendment and this repo's floor-correction precedent, and it is fully visible in the changeset for the maintainer to veto at landing. The throw instead of `[]` and the new export are inside the card's own deliverable (\"a pin that fails loudly if an envelope shape reappears at this boundary\") and the file's standing treatment of contract violations. No escalation was required. Non-blocking: the PR body leans on a \"四轴 过渡也从紧 default\" that lives in the dispatch text, which this review did not read by design; no judgment above rests on it.\n\n6. **Docs (non-blocking).** No README or `content/docs` update accompanies the new error class; that matches how `AnalyticsQueryRejectedError` and `AnalyticsUnauthenticatedError` are documented in this package (changeset and CHANGELOG prose only, no README mention), and `check-readme-exports.mjs` judges README self-imports, which this diff cannot break.\n\n7. **Open questions in the dev report:** none declared; nothing to answer.\n\nCI at head: 42 success · 3 skipped · 0 failed · 0 in progress\n\nImplemented-by: `claude/issue-7028-analytics-rows-single-spelling`\nReviewed-by: `session_01BA3nKVUwKQJf8DBxrSVtNC`\n\n**VERDICT: PASS**\n\n### Out of scope\n\n- **Acceptance notes:** the pending `.changeset/7122-objectstack-family-17-3-0.md` still describes the retired branches as kept behind a widened alias. That was true of its own change; this PR's changeset names it and supersedes it, and both publish in the same release. It is not edited: an edit would be a changeset overwrite, and supersession is the recorded form.\n- **Dropped:** the claim that `.changeset/5793-spec-range-floors.md` and `.changeset/6361-spec-floor-17-2-0.md` are wrong. Their 「still resolves 17.2.0」 sentences describe the lockfile at the time of their own change, on `@objectstack/spec` edges this diff does not touch. They are not claims about this release's resolution.\n- **Noted:** the branch commits carry a session trailer only. The PR squash-merges, so the trailer does not reach `main`.\n\n<sub>`domain:ui` seat #1 · review · 2026-09-24T20:37Z</sub>\n"}
