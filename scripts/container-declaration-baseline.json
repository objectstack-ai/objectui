{
  "note": [
    "Registrations that render an authored `children` list while their registry meta",
    "omits `isContainer`, so `validateTree` draws a FALSE `not-a-container` on a child",
    "list the renderer then puts on the page. Measured on main@d06059f24 by",
    "packages/components/src/renderers/__tests__/container-declaration-ratchet.test.tsx,",
    "over the WHOLE registry at runtime -- 131 bare authoring tags of 293 registry keys;",
    "58 render children, 13 declare the flag, 45 do not. 44 of those 45 are listed here",
    "and `button` is the excluded 45th (see `excluded` below).",
    "",
    "A RATCHET TO ZERO, NOT AN EXEMPTION LIST. objectui#6779's ruling admitted this",
    "stock so the class could be closed today; every entry is debt that still has to be",
    "paid. The pin is red in BOTH directions, which is what makes the list shrink-only:",
    "  - a tag that renders children, omits the flag and is NOT listed  -> red.",
    "    That is a NEW violation. Declare `isContainer` on the registration; do not",
    "    add a line here. Admitting an entry needs a ruling, the way these 44 got one.",
    "  - a listed tag that no longer violates                           -> red.",
    "    The defect is gone, so the entry is dead weight: delete the line. This is the",
    "    direction the list is supposed to move, and the red is how it gets noticed.",
    "",
    "TO PAY ONE OFF: add `isContainer: true` to that registration's meta and delete its",
    "line here. Do NOT instead stop the renderer from reading `schema.children` -- that",
    "shrinks the check rather than paying the debt, and it would break authored pages.",
    "Read the reasoning at packages/layout/src/index.ts (objectui#3900): `children` is a",
    "BASE property of every node in the JSON protocol (`BASE_PROPS` in",
    "sdui-parser/src/validate.ts), not a per-component authoring key, so the flag answers",
    "the protocol question \"does this node accept a child list?\" and widens no spec",
    "surface. A warning that LIES is worse than a missing one: it trains authors -- AI",
    "authors especially -- to discount the TRUE `not-a-container` reports.",
    "",
    "⚠️ CHECK THE PUBLIC TIER BEFORE PAYING ONE OFF. `renderers/layout/react-page.tsx`",
    "builds the JSX scope of every `kind:'react'` page with",
    "`if (!tag || cfg.isContainer) continue;` over `getPublicConfigs()`, so declaring the",
    "flag on a PUBLIC tag also DELETES its injected identifier from every react page.",
    "None of the 44 below is public (measured; the pin asserts it), so for them the",
    "declaration provably removes nothing. `button` is the one that is -- which is why it",
    "is excluded rather than listed.",
    "",
    "⚠️ AND THE PUBLIC TIER IS WIDER THAN THIS LIST'S ONE HIT -- do not read the paragraph",
    "above as `button` being the only public tag in the containment story. It is the only",
    "public tag among the 45 measured HERE. `PUBLIC_BLOCKS` (ADR-0080,",
    "packages/core/src/registry/public-blocks.ts) also carries `badge` and `alert` as bare",
    "keys, and both are registered bare. They are absent from this list only because they",
    "render `renderChildren(schema.body)` and never touch `schema.children`, so they do not",
    "violate -- yet. Measured 2026-08-30 over the live registry, and PINNED -- the ratchet",
    "test's `the public tier of the ruled 14 is THREE, not one` block asserts every fact in",
    "this paragraph, direction control included, so it cannot go quietly stale the way an",
    "unheld measurement in a durable ledger does: of the 14 tags objectui#6804 ruled on,",
    "THREE are public -- `button`, `badge`, `alert`. The 11 bare",
    "`sidebar-*` keys are NOT (the public sidebar is the namespaced `page:sidebar`, a",
    "different registration, and it already declares `isContainer: true`). ⇒ Whoever gives",
    "`badge` or `alert` a `children` read is moving PUBLISHED CONTRACT and inherits this",
    "whole trade-off; it is not the mechanical one-line fix the other 44 are.",
    "",
    "THE 2026-08-30 RULING (objectui#6804), AND WHY ONLY ONE OF ITS 14 TAGS IS WRITTEN",
    "DOWN HERE. That ruling answered the containment question for 14 tags at once --",
    "`button` plus the 13 `schema.body` readers (`badge`, `alert`, 11 `sidebar-*`) -- and",
    "the answer for all 14 is the same: do NOT declare `isContainer`, because reading",
    "children as a FALLBACK for `schema.label` / `schema.body` is not layout containment.",
    "Only `button` has an entry below, and that is NOT an oversight: it is the only one of",
    "the 14 that violates today. The other 13 put no authored child on the page, so the",
    "runtime predicate already scores them as non-containers -- and a line for them here",
    "would trip this file's OTHER red (`a listed tag that no longer violates`) on the day",
    "it was written. They are pinned as non-violators instead, by the `schema.body` readers",
    "block in the ratchet test. If objectui#6771's retirement of the `body` dialect gives",
    "any of them a `children` read, that tag earns its own reasoned entry THEN.",
    "",
    "`since` is the date the entry was admitted, so the list carries its own history: a",
    "line dated later than this file's first commit was added after the ruling and needs",
    "one of its own."
  ],
  "excluded": {
    "button": {
      "reason": "PERMANENT, RULED EXCEPTION -- not an oversight, not pending anything. Admitted 2026-08-29 as provisional by objectui#6779, which deferred the question to a separate card; that card is objectui#6804 and the maintainer ruled it 2026-08-30: `button` does NOT declare `isContainer`. Three grounds, all re-derived on this tree. (1) SEMANTICS -- `isContainer` means LAYOUT CONTAINMENT, not `this tag renders children`. `button` reads `schema.children` only as a fallback for `schema.label` (packages/components/src/renderers/form/button.tsx, `schema.label || renderChildren(...)`), so it is not a container; declaring it would make one predicate mean two different things. (2) COST -- `button` is the only public-tier member of the 45, and `renderers/layout/react-page.tsx` skips containers when it builds the JSX scope of every `kind:'react'` page, so declaring the flag DELETES the `Button` identifier from all of them. That is an immediate, user-visible capability loss. (3) PULL -- zero measured: nothing records anyone wanting to nest layout content inside a button, so the trade is a certain cost against a hypothetical gain. ⛔ Do NOT `fix` this entry by declaring the flag, and do NOT move `button` into `undeclared` -- both override the ruling. What keeps the exception honest is the pin, which asserts all three facts it rests on (still renders children, still undeclared, still public); if any one of them changes this goes RED and the exception must be RE-RULED, not quietly re-based. Scope note for the next hand: this card deliberately did not edit that `button.tsx` line -- the `schema.body || schema.children` expression on it belongs to objectui#6771 (retirement of the `body` dialect).",
      "issue": "objectui#6804",
      "since": "2026-08-29"
    },
    "alert": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a content fallback behind `description`, not a layout slot, so declaring it would make one predicate mean two things. ⚠️ PUBLIC TIER: this tag is in `PUBLIC_BLOCKS`, so declaring the flag would also delete its identifier from the JSX scope of every `kind:'react'` page — the same immediate, user-visible capability loss that made `button` an exclusion rather than a ratchet line. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "badge": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a label fallback behind `label`, the same shape `button` was excluded for, so declaring it would make one predicate mean two things. ⚠️ PUBLIC TIER: this tag is in `PUBLIC_BLOCKS`, so declaring the flag would also delete its identifier from the JSX scope of every `kind:'react'` page — the same immediate, user-visible capability loss that made `button` an exclusion rather than a ratchet line. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-content": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-footer": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-group": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-header": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-inset": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-menu": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-menu-item": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    },
    "sidebar-provider": {
      "reason": "RULED EXCEPTION, admitted by objectui#6771's retirement of the `body` child-list dialect. This registration rendered `renderChildren(schema.body)` and never touched `schema.children`, so it was one of the dozen for which `body` was the ONLY door; the maintainer ruling of 2026-09-01 retired that spelling and it now reads `children`, which is why the runtime predicate started calling it a container. ⛔ The answer is NOT `isContainer`: objectui#6804's ruling (2026-08-30) anticipated exactly this moment and wrote the cross-card instruction verbatim — when objectui#6771 gives these registrations a `children` read and objectui#6808's predicate marks them containers, the answer is a pin exception WITH ITS GROUND, not a declaration. The ground is the same one `button` rests on: `isContainer` means LAYOUT CONTAINMENT, and this read is a chrome slot on a shadcn sidebar part rather than an authored layout region, so declaring it would make one predicate mean two things. This tag is not in `PUBLIC_BLOCKS` (the public set carries the namespaced `page:sidebar`, a different type), so no injected react-page identifier is at stake either way. ⚠️ AND THERE IS A MEASURED COST TO THIS EXCEPTION, recorded here rather than left for rediscovery: `validateTree`'s containment branch reads `node.children?.length`, so an author writing `children` — now the ONLY key this registration reads — draws `not-a-container` on a page that renders correctly. That is the defect objectui#6771 was filed about (a false diagnostic on the one spelling that works), moved from `body` to `children` rather than removed. It is pinned as a refusal in `container-declaration-ratchet.test.tsx`'s `schema.body` readers block and reported on objectui#6771; ⛔ do not resolve it by quietly declaring the flag, which overrides objectui#6804.",
      "issue": "objectui#6804",
      "since": "2026-09-18"
    }
  },
  "undeclared": {
    "a": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "abbr": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "address": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "app": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "b": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "blockquote": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "cite": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "dd": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "del": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "div": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "dl": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "dt": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "em": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "figcaption": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "figure": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "form": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h1": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h2": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h3": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h4": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h5": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "h6": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "home": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "i": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "ins": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "li": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "mark": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "ol": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "p": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "page": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "pre": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "q": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "record": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "scroll-area": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "small": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "span": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "strong": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "sub": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "sup": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "time": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "toggle": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "u": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "ul": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    },
    "utility": {
      "since": "2026-08-29",
      "issue": "objectui#6779"
    }
  }
}
